<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>卖剩鸭</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://lsqstudy.top/"/>
  <updated>2020-07-16T08:50:38.598Z</updated>
  <id>http://lsqstudy.top/</id>
  
  <author>
    <name>卖剩鸭</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>04Java中的引用</title>
    <link href="http://lsqstudy.top/post/a85afe41.html"/>
    <id>http://lsqstudy.top/post/a85afe41.html</id>
    <published>2020-07-16T15:25:57.473Z</published>
    <updated>2020-07-16T08:50:38.598Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、提问"><a href="#01、提问" class="headerlink" title="01、提问"></a>01、提问</h1><blockquote><p>Java中的强引用_软引用_弱引用_虚引用分别是什么?</p></blockquote><h1 id="02、前言"><a href="#02、前言" class="headerlink" title="02、前言"></a>02、前言</h1><p>在原来的时候，我们谈到一个类的实例化</p><pre><code class="java">Person p = new Person()</code></pre><p>在等号的左边，就是一个对象的引用，存储在栈中</p><p>而等号右边，就是实例化的对象，存储在堆中</p><p>其实这样的一个引用关系，就被称为强引用</p><h1 id="03、整体架构"><a href="#03、整体架构" class="headerlink" title="03、整体架构"></a>03、整体架构</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716165028.png" alt=""></p><h1 id="04、强引用"><a href="#04、强引用" class="headerlink" title="04、强引用"></a>04、强引用</h1><p>当内存不足的时候，JVM开始垃圾回收，对于强引用的对象，就算是出现了OOM也不会对该对象进行回收，打死也不回收~！</p><p>强引用是我们最常见的普通对象引用，只要还有一个强引用指向一个对象，就能表明对象还“活着”，垃圾收集器不会碰这种对象。在Java中最常见的就是强引用，把一个对象赋给一个引用变量，这个引用变量就是一个强引用。当一个对象被强引用变量引用时，它处于可达状态，它是不可能被垃圾回收机制回收的，即使该对象以后永远都不会被用到，JVM也不会回收，因此强引用是造成Java内存泄漏的主要原因之一。</p><p>对于一个普通的对象，如果没有其它的引用关系，只要超过了引用的作用于或者显示地将相应（强）引用赋值为null，一般可以认为就是可以被垃圾收集的了（当然具体回收时机还是要看垃圾回收策略）</p><blockquote><p>强引用小例子：</p></blockquote><pre><code class="java">package com.lsq.jvm.Reference;/** * 强引用 * @Author: LSQ * Date: 2020/7/16 16:22 */public class StrongReferenceDemo {    public static void main(String[] args) {        // 这样定义的默认就是强应用        Object obj1 = new Object();        // 使用第二个引用，指向刚刚创建的Object对象        Object obj2 = obj1;        // 置空        obj1 = null;        // 垃圾回收        System.gc();        System.out.println(obj1);        System.out.println(obj2);    }}</code></pre><p>输出结果我们能够发现，即使 obj1 被设置成了null，然后调用gc进行回收，但是也没有回收实例出来的对象，obj2还是能够指向该地址，也就是说垃圾回收器，并没有将该对象进行垃圾回收</p><pre><code>nulljava.lang.Object@14ae5a5</code></pre><h1 id="05、软引用"><a href="#05、软引用" class="headerlink" title="05、软引用"></a>05、软引用</h1><p>软引用是一种相对弱化了一些的引用，需要用<code>Java.lang.ref.SoftReference</code>类来实现，可以让对象豁免一些垃圾收集，对于只有软引用的对象来讲：</p><ul><li>当系统内存充足时，它不会被回收</li><li>当系统内存不足时，它会被回收</li></ul><p>软引用通常在对内存敏感的程序中，比如高速缓存就用到了软引用，内存够用 的时候就保留，不够用就回收</p><p>具体使用</p><pre><code class="java">/** * 软引用 * * @author: 陌溪 * @create: 2020-03-23-16:39 */package com.lsq.jvm.Reference;import java.lang.ref.SoftReference;/** * @Description:软引用 * @Author: LSQ * Date: 2020/7/16 16:24 */public class SoftReferenceDemo {    /**     * 内存够用的时候     */    public static void softRefMemoryEnough() {        // 创建一个强应用        Object o1 = new Object();        // 创建一个软引用        SoftReference&lt;Object&gt; softReference = new SoftReference&lt;&gt;(o1);        System.out.println(o1);        System.out.println(softReference.get());        o1 = null;        // 手动GC        System.gc();        System.out.println(o1);        System.out.println(softReference.get());    }    /**     * JVM配置，故意产生大对象并配置小的内存，让它的内存不够用了导致OOM，看软引用的回收情况     * -Xms5m -Xmx5m -XX:+PrintGCDetails     */    public static void softRefMemoryNoEnough() {        System.out.println(&quot;========================&quot;);        // 创建一个强应用        Object o1 = new Object();        // 创建一个软引用        SoftReference&lt;Object&gt; softReference = new SoftReference&lt;&gt;(o1);        System.out.println(o1);        System.out.println(softReference.get());        o1 = null;        // 模拟OOM自动GC        try {            // 创建30M的大对象            byte[] bytes = new byte[30 * 1024 * 1024];        } catch (Exception e) {            e.printStackTrace();        } finally {            System.out.println(o1);            System.out.println(softReference.get());        }    }    public static void main(String[] args) {        softRefMemoryEnough();        softRefMemoryNoEnough();    }}</code></pre><p>我们写了两个方法，一个是内存够用的时候，一个是内存不够用的时候</p><p>我们首先查看内存够用的时候，首先输出的是 o1 和 软引用的 softReference，我们都能够看到值</p><p>然后我们把o1设置为null，执行手动GC后，我们发现softReference的值还存在，说明内存充足的时候，软引用的对象不会被回收</p><pre><code>java.lang.Object@14ae5a5java.lang.Object@14ae5a5[GC (System.gc()) [PSYoungGen: 1396K-&gt;504K(1536K)] 1504K-&gt;732K(5632K), 0.0007842 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [Full GC (System.gc()) [PSYoungGen: 504K-&gt;0K(1536K)] [ParOldGen: 228K-&gt;651K(4096K)] 732K-&gt;651K(5632K), [Metaspace: 3480K-&gt;3480K(1056768K)], 0.0058450 secs] [Times: user=0.00 sys=0.00, real=0.01 secs] nulljava.lang.Object@14ae5a5</code></pre><p>下面我们看当内存不够的时候，我们使用了JVM启动参数配置，给初始化堆内存为5M</p><pre><code>-Xms5m -Xmx5m -XX:+PrintGCDetails</code></pre><p>但是在创建对象的时候，我们创建了一个30M的大对象</p><pre><code>// 创建30M的大对象byte[] bytes = new byte[30 * 1024 * 1024];</code></pre><p>这就必然会触发垃圾回收机制，这也是中间出现的垃圾回收过程，最后看结果我们发现，o1 和 softReference都被回收了，因此说明，软引用在内存不足的时候，会自动回收</p><pre><code>java.lang.Object@7f31245ajava.lang.Object@7f31245a[GC (Allocation Failure) [PSYoungGen: 31K-&gt;160K(1536K)] 682K-&gt;811K(5632K), 0.0003603 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [GC (Allocation Failure) [PSYoungGen: 160K-&gt;96K(1536K)] 811K-&gt;747K(5632K), 0.0006385 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [Full GC (Allocation Failure) [PSYoungGen: 96K-&gt;0K(1536K)] [ParOldGen: 651K-&gt;646K(4096K)] 747K-&gt;646K(5632K), [Metaspace: 3488K-&gt;3488K(1056768K)], 0.0067976 secs] [Times: user=0.02 sys=0.00, real=0.01 secs] [GC (Allocation Failure) [PSYoungGen: 0K-&gt;0K(1536K)] 646K-&gt;646K(5632K), 0.0004024 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [Full GC (Allocation Failure) [PSYoungGen: 0K-&gt;0K(1536K)] [ParOldGen: 646K-&gt;627K(4096K)] 646K-&gt;627K(5632K), [Metaspace: 3488K-&gt;3488K(1056768K)], 0.0065506 secs] [Times: user=0.00 sys=0.00, real=0.01 secs] nullnull</code></pre><h1 id="06、弱引用"><a href="#06、弱引用" class="headerlink" title="06、弱引用"></a>06、弱引用</h1><p>不管内存是否够，只要有GC操作就会进行回收</p><p>弱引用需要用 <code>java.lang.ref.WeakReference</code> 类来实现，它比软引用生存期更短</p><p>对于只有弱引用的对象来说，只要垃圾回收机制一运行，不管JVM的内存空间是否足够，都会回收该对象占用的空间。</p><pre><code class="java">package com.lsq.jvm.Reference;import java.lang.ref.WeakReference;/** * @Description:弱引用 * @Author: LSQ * Date: 2020/7/16 16:30 */public class WeakReferenceDemo {    public static void main(String[] args) {        Object o1 = new Object();        WeakReference&lt;Object&gt; weakReference = new WeakReference&lt;&gt;(o1);        System.out.println(o1);        System.out.println(weakReference.get());        o1 = null;        System.gc();        System.out.println(o1);        System.out.println(weakReference.get());    }}</code></pre><p>我们看结果，能够发现，我们并没有制造出OOM内存溢出，而只是调用了一下GC操作，垃圾回收就把它给收集了</p><pre><code>java.lang.Object@14ae5a5java.lang.Object@14ae5a5[GC (System.gc()) [PSYoungGen: 5246K-&gt;808K(76288K)] 5246K-&gt;816K(251392K), 0.0008236 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [Full GC (System.gc()) [PSYoungGen: 808K-&gt;0K(76288K)] [ParOldGen: 8K-&gt;675K(175104K)] 816K-&gt;675K(251392K), [Metaspace: 3494K-&gt;3494K(1056768K)], 0.0035953 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] nullnull</code></pre><h1 id="07、软引用和弱引用的使用场景"><a href="#07、软引用和弱引用的使用场景" class="headerlink" title="07、软引用和弱引用的使用场景"></a>07、软引用和弱引用的使用场景</h1><p>场景：假如有一个应用需要读取大量的本地图片</p><ul><li>如果每次读取图片都从硬盘读取则会严重影响性能</li><li>如果一次性全部加载到内存中，又可能造成内存溢出</li></ul><p>此时使用软引用可以解决这个问题</p><p>设计思路：使用HashMap来保存图片的路径和相应图片对象关联的软引用之间的映射关系，在内存不足时，JVM会自动回收这些缓存图片对象所占的空间，从而有效地避免了OOM的问题</p><pre><code class="java">Map&lt;String, SoftReference&lt;String&gt;&gt; imageCache = new HashMap&lt;String, SoftReference&lt;Bitmap&gt;&gt;();</code></pre><h2 id="7-1、WeakHashMap是什么？"><a href="#7-1、WeakHashMap是什么？" class="headerlink" title="7.1、WeakHashMap是什么？"></a>7.1、WeakHashMap是什么？</h2><p>比如一些常常和底层打交道的，mybatis等，底层都应用到了WeakHashMap</p><p>WeakHashMap和HashMap类似，只不过它的Key是使用了弱引用的，也就是说，当执行GC的时候，HashMap中的key会进行回收，下面我们使用例子来测试一下</p><p>我们使用了两个方法，一个是普通的HashMap方法</p><blockquote><p>我们输入一个Key-Value键值对，然后让它的key置空，然后在查看结果</p></blockquote><pre><code class="java">    private static void myHashMap() {        Map&lt;Integer, String&gt; map = new HashMap&lt;&gt;();        Integer key = new Integer(1);        String value = &quot;HashMap&quot;;        map.put(key, value);        System.out.println(map);        key = null;        System.gc();        System.out.println(map);    }</code></pre><blockquote><p>第二个是使用了WeakHashMap，完整代码如下</p></blockquote><pre><code class="java">package com.lsq.jvm.Reference;import java.util.HashMap;import java.util.Map;import java.util.WeakHashMap;/** * WeakHashMap * @Author: LSQ * Date: 2020/7/16 16:38 */public class WeakHashMapDemo {    public static void main(String[] args) {        myHashMap();        System.out.println(&quot;==========&quot;);        myWeakHashMap();    }    private static void myHashMap() {        Map&lt;Integer, String&gt; map = new HashMap&lt;&gt;();        Integer key = new Integer(1);        String value = &quot;HashMap&quot;;        map.put(key, value);        System.out.println(map);        key = null;        System.gc();        System.out.println(map);    }    private static void myWeakHashMap() {        Map&lt;Integer, String&gt; map = new WeakHashMap&lt;&gt;();        Integer key = new Integer(1);        String value = &quot;WeakHashMap&quot;;        map.put(key, value);        System.out.println(map);        key = null;        System.gc();        System.out.println(map);    }}</code></pre><blockquote><p>最后输出结果为：</p></blockquote><pre><code>{1=HashMap}{1=HashMap}=========={1=WeakHashMap}{}</code></pre><p>从这里我们看到，对于普通的HashMap来说，key置空并不会影响，HashMap的键值对，因为这个属于强引用，不会被垃圾回收。</p><p>但是WeakHashMap，在进行GC操作后，弱引用的就会被回收</p><h1 id="08、虚引用"><a href="#08、虚引用" class="headerlink" title="08、虚引用"></a>08、虚引用</h1><h2 id="8-1、概念"><a href="#8-1、概念" class="headerlink" title="8.1、概念"></a>8.1、概念</h2><p>虚引用又称为幽灵引用，需要<code>java.lang.ref.PhantomReference</code> 类来实现</p><p>顾名思义，就是形同虚设，与其他几种引用都不同，虚引用并不会决定对象的生命周期。</p><p>如果一个对象持有虚引用，那么它就和没有任何引用一样，在任何时候都可能被垃圾回收器回收，它不能单独使用也不能通过它访问对象，虚引用必须和引用队列ReferenceQueue联合使用。</p><p>虚引用的主要作用和跟踪对象被垃圾回收的状态，仅仅是提供一种确保对象被finalize以后，做某些事情的机制。</p><p>PhantomReference的get方法总是返回null，因此无法访问对象的引用对象。其意义在于说明一个对象已经进入finalization阶段，可以被gc回收，用来实现比finalization机制更灵活的回收操作</p><p>换句话说，设置虚引用关联的唯一目的，就是在这个对象被收集器回收的时候，收到一个系统通知或者后续添加进一步的处理，Java技术允许使用finalize()方法在垃圾收集器将对象从内存中清除出去之前，做必要的清理工作</p><p>这个就相当于Spring AOP里面的后置通知</p><h2 id="8-2、场景"><a href="#8-2、场景" class="headerlink" title="8.2、场景"></a>8.2、场景</h2><p>一般用于在回收时候做通知相关操作</p><h1 id="09、引用队列-ReferenceQueue"><a href="#09、引用队列-ReferenceQueue" class="headerlink" title="09、引用队列 ReferenceQueue"></a>09、引用队列 ReferenceQueue</h1><p>软引用，弱引用，虚引用在回收之前，需要在引用队列保存一下</p><p>我们在初始化的弱引用或者虚引用的时候，可以传入一个引用队列</p><pre><code class="java">Object o1 = new Object();// 创建引用队列ReferenceQueue&lt;Object&gt; referenceQueue = new ReferenceQueue&lt;&gt;();// 创建一个弱引用WeakReference&lt;Object&gt; weakReference = new WeakReference&lt;&gt;(o1, referenceQueue);</code></pre><p>那么在进行GC回收的时候，弱引用和虚引用的对象都会被回收，但是在回收之前，它会被送至引用队列中</p><blockquote><p>完整代码如下：</p></blockquote><pre><code class="java">/** * 虚引用 * * @author: 陌溪 * @create: 2020-03-24-12:09 */public class PhantomReferenceDemo {    public static void main(String[] args) {        Object o1 = new Object();        // 创建引用队列        ReferenceQueue&lt;Object&gt; referenceQueue = new ReferenceQueue&lt;&gt;();        // 创建一个弱引用        WeakReference&lt;Object&gt; weakReference = new WeakReference&lt;&gt;(o1, referenceQueue);        // 创建一个弱引用//        PhantomReference&lt;Object&gt; weakReference = new PhantomReference&lt;&gt;(o1, referenceQueue);        System.out.println(o1);        System.out.println(weakReference.get());        // 取队列中的内容        System.out.println(referenceQueue.poll());        o1 = null;        System.gc();        System.out.println(&quot;执行GC操作&quot;);        try {            TimeUnit.SECONDS.sleep(2);        } catch (InterruptedException e) {            e.printStackTrace();        }        System.out.println(o1);        System.out.println(weakReference.get());        // 取队列中的内容        System.out.println(referenceQueue.poll());    }}</code></pre><p>运行结果</p><pre><code>java.lang.Object@14ae5a5java.lang.Object@14ae5a5null执行GC操作nullnulljava.lang.ref.WeakReference@7f3124</code></pre><p>从这里我们能看到，在进行垃圾回收后，我们弱引用对象，也被设置成null，但是在队列中还能够导出该引用的实例，这就说明在回收之前，该弱引用的实例被放置引用队列中了，我们可以通过引用队列进行一些后置操作</p><h2 id="GCRoots和四大引用小总结"><a href="#GCRoots和四大引用小总结" class="headerlink" title="GCRoots和四大引用小总结"></a>GCRoots和四大引用小总结</h2><ul><li><p>红色部分在垃圾回收之外，也就是强引用的</p></li><li><p>蓝色部分：属于软引用，在内存不够的时候，才回收</p></li><li><p>虚引用和弱引用：每次垃圾回收的时候，都会被干掉，但是它在干掉之前还会存在引用队列中，我们可以通过引用队列进行一些通知机制</p></li></ul><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716164931.png" style="zoom:80%;" />]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、提问&quot;&gt;&lt;a href=&quot;#01、提问&quot; class=&quot;headerlink&quot; title=&quot;01、提问&quot;&gt;&lt;/a&gt;01、提问&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;Java中的强引用_软引用_弱引用_虚引用分别是什么?&lt;/p&gt;
&lt;/blockquote&gt;
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JVM" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JVM/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>03JVM参数调优</title>
    <link href="http://lsqstudy.top/post/75c4b7e7.html"/>
    <id>http://lsqstudy.top/post/75c4b7e7.html</id>
    <published>2020-07-16T15:25:57.460Z</published>
    <updated>2020-07-16T08:08:13.825Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、提问"><a href="#01、提问" class="headerlink" title="01、提问"></a>01、提问</h1><p>你说你做过JVM调优和参数配置，请问如何盘点查看JVM系统默认值</p><blockquote><p>使用jps和jinfo进行查看</p></blockquote><pre><code>-Xms：初始堆空间-Xmx：堆最大值-Xss：栈空间</code></pre><blockquote><p><code>-Xms</code> 和<code>-Xmx</code>最好调整一致，防止JVM频繁进行收集和回收</p></blockquote><h1 id="02、JVM参数类型"><a href="#02、JVM参数类型" class="headerlink" title="02、JVM参数类型"></a>02、JVM参数类型</h1><ul><li>标配参数（从JDK1.0 - Java12都在，很稳定）<ul><li><code>-version</code></li><li><code>-help</code></li><li><code>java -showversion</code></li></ul></li><li>X参数（了解）<ul><li><code>-Xint</code>：解释执行</li><li><code>-Xcomp</code>：第一次使用就编译成本地代码</li><li><code>-Xmixed</code>：混合模式</li></ul></li><li>XX参数（重点）<ul><li>Boolean类型<ul><li>公式：<strong>-XX:+ 或者-某个属性</strong>：  + 表示开启，-表示关闭</li><li>Case：<code>-XX:-PrintGCDetails</code>：表示关闭了GC详情输出</li></ul></li><li>key-value类型<ul><li>公式：<strong>-XX:属性key=属性value</strong></li><li>不满意初始值，可以通过下列命令调整</li><li>case：如何：<code>-XX:MetaspaceSize=21807104</code>：查看Java元空间的值</li></ul></li></ul></li></ul><h1 id="03、查看信息"><a href="#03、查看信息" class="headerlink" title="03、查看信息"></a>03、查看信息</h1><blockquote><p>查看运行的Java程序，JVM参数是否开启，具体值为多少？</p></blockquote><p>首先我们运行一个HelloGC的java程序</p><pre><code class="java">package com.lsq.jvm.GCRoots;/** * @Description: * @Author: LSQ * Date: 2020/7/16 14:58 */public class HelloGC {    public static void main(String[] args) throws InterruptedException {        System.out.println(&quot;hello GC&quot;);        Thread.sleep(Integer.MAX_VALUE);    }}</code></pre><p>然后使用下列命令查看它的默认参数</p><pre><code class="bash">jps：查看java的后台进程jinfo：查看正在运行的java程序</code></pre><p>具体使用：</p><pre><code class="bash">jps -l得到进程号</code></pre><pre><code class="bash">1009610272 org.jetbrains.kotlin.daemon.KotlinCompileDaemon228 org.jetbrains.idea.maven.server.RemoteMavenServer5688 sun.tools.jps.Jps7260 org.jetbrains.jps.cmdline.Launcher9180 com.lsq.jvm.GCRoots.HelloGC</code></pre><p>查看到HelloGC的进程号为：12608</p><p>我们使用jinfo -flag 然后查看是否开启PrintGCDetails这个参数</p><pre><code class="bash">jinfo -flag PrintGCDetails 9180</code></pre><p>得到的内容为</p><pre><code class="bash">-XX:-PrintGCDetails</code></pre><p>上面提到了，-号表示关闭，即没有开启PrintGCDetails这个参数</p><p>下面我们需要在启动HelloGC的时候，增加 PrintGCDetails这个参数，需要在运行程序的时候配置JVM参数</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160630.png" alt=""></p><p>然后在VM Options中加入下面的代码，现在+号表示开启</p><pre><code class="bash">-XX:+PrintGCDetails</code></pre><p>然后在使用jinfo查看我们的配置</p><pre><code class="bash">jps -ljinfo -flag PrintGCDetails 6476</code></pre><p>得到的结果为</p><pre><code class="bash">-XX:+PrintGCDetails</code></pre><p>我们看到原来的-号变成了+号，说明我们通过 VM Options配置的JVM参数已经生效了</p><p>使用下列命令，会把jvm的全部默认参数输出</p><pre><code>jinfo -flags ***</code></pre><h1 id="04、题外话（坑题）"><a href="#04、题外话（坑题）" class="headerlink" title="04、题外话（坑题）"></a>04、题外话（坑题）</h1><p>两个经典参数：<code>-Xms</code> 和 <code>-Xmx</code>，这两个参数 如何解释</p><p>这两个参数，还是属于XX参数，因为取了别名</p><ul><li><code>-Xms</code>等价于<code>-XX:InitialHeapSize</code> ：初始化堆内存（默认只会用最大物理内存的64分1）</li><li><code>-Xmx</code> 等价于 -<code>XX:MaxHeapSize</code>   ：最大堆内存（默认只会用最大物理内存的4分1）</li></ul><h1 id="05、查看JVM默认参数"><a href="#05、查看JVM默认参数" class="headerlink" title="05、查看JVM默认参数"></a>05、查看JVM默认参数</h1><ul><li><p><code>-XX:+PrintFlagsInitial</code></p><ul><li>主要是查看初始默认值</li><li>公式<ul><li><code>java -XX:+PrintFlagsInitial -version</code></li><li><code>java -XX:+PrintFlagsInitial</code>（重要参数）</li></ul></li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160622.png" alt=""></p></li></ul><ul><li><code>-XX:+PrintFlagsFinal</code>：表示修改以后，最终的值</li></ul><p>​         会将JVM的各个结果都进行打印</p><p>​         如果有<code>:=</code> 表示修改过的， <code>=</code>表示没有修改过的</p><h1 id="06、工作中常用的JVM基本配置参数"><a href="#06、工作中常用的JVM基本配置参数" class="headerlink" title="06、工作中常用的JVM基本配置参数"></a>06、工作中常用的JVM基本配置参数</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160616.png" alt=""></p><h2 id="6-1、查看堆内存"><a href="#6-1、查看堆内存" class="headerlink" title="6.1、查看堆内存"></a>6.1、查看堆内存</h2><p>查看JVM的初始化堆内存 -Xms 和最大堆内存 Xmx</p><pre><code class="java">package com.lsq.jvm.GC;/** * @Description: * @Author: LSQ * Date: 2020/7/16 15:36 */public class HelloGC {    public static void main(String[] args) throws InterruptedException {        // 返回Java虚拟机中内存的总量        long totalMemory = Runtime.getRuntime().totalMemory();        // 返回Java虚拟机中试图使用的最大内存量        long maxMemory = Runtime.getRuntime().maxMemory();        System.out.println(&quot;TOTAL_MEMORY(-Xms) = &quot; + totalMemory + &quot;(字节)、&quot; + (totalMemory / (double)1024 / 1024) + &quot;MB&quot;);        System.out.println(&quot;MAX_MEMORY(-Xmx) = &quot; + maxMemory + &quot;(字节)、&quot; + (maxMemory / (double)1024 / 1024) + &quot;MB&quot;);    }}</code></pre><p>运行结果为：</p><pre><code class="bash">TOTAL_MEMORY(-Xms) = 128974848(字节)、123.0MBMAX_MEMORY(-Xmx) = 1883242496(字节)、1796.0MB</code></pre><p>-Xms 初始堆内存为：物理内存的1/64          -Xmx 最大堆内存为：系统物理内存的 1/4</p><h2 id="6-2、打印JVM默认参数"><a href="#6-2、打印JVM默认参数" class="headerlink" title="6.2、打印JVM默认参数"></a>6.2、打印JVM默认参数</h2><p>使用 <code>java -XX:+PrintCommandLineFlags</code> 打印出JVM的默认的简单初始化参数</p><p>比如我的机器输出为：</p><pre><code class="bash">-XX:InitialHeapSize=266376000 -XX:MaxHeapSize=4262016000 -XX:+PrintCommandLineFlags -XX:+UseCompressedClassPointers -XX:+UseCompressedOops -XX:-UseLargePagesIndividualAllocation -XX:+UseParallelGC </code></pre><h2 id="6-3、生活常用调优参数"><a href="#6-3、生活常用调优参数" class="headerlink" title="6.3、生活常用调优参数"></a>6.3、生活常用调优参数</h2><ul><li><p><code>-Xms</code>：初始化堆内存，默认为物理内存的1/64，等价于<code>-XX:initialHeapSize</code></p></li><li><p><code>-Xmx</code>：最大堆内存，默认为物理内存的1/4，等价于<code>-XX:MaxHeapSize</code></p></li><li><p><code>-Xss</code>：设计单个线程栈的大小，一般默认为512K~1024K，等价于<code>-XX:ThreadStackSize</code></p><ul><li>使用<code>jinfo -flag ThreadStackSize +线程PID</code>  会发现 <code>-XX:ThreadStackSize = 0</code></li><li>这个值的大小是取决于平台的</li><li>Linux/x64:1024KB</li><li>OS X：1024KB</li><li>Oracle Solaris：1024KB</li><li>Windows：取决于虚拟内存的大小</li></ul></li><li><p><code>-Xmn</code>：设置年轻代大小</p></li><li><p><code>-XX:MetaspaceSize</code>：设置元空间大小</p><ul><li>元空间的本质和永久代类似，都是对JVM规范中方法区的实现，不过元空间与永久代之间最大的区别在于：元空间并不在虚拟机中，而是使用本地内存，因此，默认情况下，元空间的大小仅受本地内存限制。</li><li><code>-Xms10m -Xmx10m -XX:MetaspaceSize=1024m  -XX:+PrintFlagsFinal</code></li><li>但是默认的元空间大小：只有20多M</li><li>为了防止在频繁的实例化对象的时候，让元空间出现OOM，因此可以把元空间设置的大一些</li></ul></li><li><p><code>-XX:PrintGCDetails</code>：输出详细GC收集日志信息</p><ul><li>GC</li><li>Full GC</li></ul></li></ul><blockquote><p>GC日志收集流程图</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160604.png" alt=""></p><p>我们使用一段代码，制造出垃圾回收的过程</p><p>首先我们设置一下程序的启动配置:  设置初始堆内存为10M，最大堆内存为10M</p><pre><code class="bash">-Xms10m -Xmx10m -XX:+PrintGCDetails</code></pre><p>然后用下列代码，创建一个 非常大空间的byte类型数组</p><pre><code class="java">byte [] byteArray = new byte[50 * 1024 * 1024];</code></pre><p>运行后，发现会出现下列错误，这就是OOM：java内存溢出，也就是堆空间不足</p><pre><code class="bash">Exception in thread &quot;main&quot; java.lang.OutOfMemoryError: Java heap space    at com.moxi.interview.study.GC.HelloGC.main(HelloGC.java:22)</code></pre><p>同时还打印出了GC垃圾回收时候的详情</p><pre><code class="bash">[GC (Allocation Failure) [PSYoungGen: 1972K-&gt;504K(2560K)] 1972K-&gt;740K(9728K), 0.0156109 secs] [Times: user=0.00 sys=0.00, real=0.03 secs] [GC (Allocation Failure) [PSYoungGen: 504K-&gt;480K(2560K)] 740K-&gt;772K(9728K), 0.0007815 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [Full GC (Allocation Failure) [PSYoungGen: 480K-&gt;0K(2560K)] [ParOldGen: 292K-&gt;648K(7168K)] 772K-&gt;648K(9728K), [Metaspace: 3467K-&gt;3467K(1056768K)], 0.0080505 secs] [Times: user=0.00 sys=0.00, real=0.01 secs] [GC (Allocation Failure) [PSYoungGen: 0K-&gt;0K(2560K)] 648K-&gt;648K(9728K), 0.0003035 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [Full GC (Allocation Failure) [PSYoungGen: 0K-&gt;0K(2560K)] [ParOldGen: 648K-&gt;630K(7168K)] 648K-&gt;630K(9728K), [Metaspace: 3467K-&gt;3467K(1056768K)], 0.0058502 secs] [Times: user=0.00 sys=0.00, real=0.01 secs] Heap PSYoungGen      total 2560K, used 80K [0x00000000ffd00000, 0x0000000100000000, 0x0000000100000000)  eden space 2048K, 3% used [0x00000000ffd00000,0x00000000ffd143d8,0x00000000fff00000)  from space 512K, 0% used [0x00000000fff00000,0x00000000fff00000,0x00000000fff80000)  to   space 512K, 0% used [0x00000000fff80000,0x00000000fff80000,0x0000000100000000) ParOldGen       total 7168K, used 630K [0x00000000ff600000, 0x00000000ffd00000, 0x00000000ffd00000)  object space 7168K, 8% used [0x00000000ff600000,0x00000000ff69dbd0,0x00000000ffd00000) Metaspace       used 3510K, capacity 4500K, committed 4864K, reserved 1056768K  class space    used 389K, capacity 392K, committed 512K, reserved 1048576K</code></pre><p>问题发生的原因：</p><p>因为们通过·<code>-Xms10m</code> 和 <code>-Xmx10m</code> 只给Java堆栈设置了10M的空间，但是创建了50M的对象，因此就会出现空间不足，而导致出错</p><p>同时在垃圾收集的时候，我们看到有两个对象：GC 和 Full GC</p><h3 id="6-3-1、GC垃圾收集"><a href="#6-3-1、GC垃圾收集" class="headerlink" title="6.3.1、GC垃圾收集"></a>6.3.1、GC垃圾收集</h3><p>GC在新生区</p><pre><code class="bash">[GC (Allocation Failure) [PSYoungGen: 1972K-&gt;504K(2560K)] 1972K-&gt;740K(9728K), 0.0156109 secs] [Times: user=0.00 sys=0.00, real=0.03 secs]</code></pre><p>GC (Allocation Failure)：表示分配失败，那么就需要触发年轻代空间中的内容被回收</p><pre><code class="bash">[PSYoungGen: 1972K-&gt;504K(2560K)] 1972K-&gt;740K(9728K), 0.0156109 secs]</code></pre><p>参数对应的图为：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160552.png" alt=""></p><h3 id="6-3-2、Full-GC垃圾回收"><a href="#6-3-2、Full-GC垃圾回收" class="headerlink" title="6.3.2、Full GC垃圾回收"></a>6.3.2、Full GC垃圾回收</h3><p>Full GC大部分发生在养老区</p><pre><code class="bash">[Full GC (Allocation Failure) [PSYoungGen: 0K-&gt;0K(2560K)] [ParOldGen: 648K-&gt;630K(7168K)] 648K-&gt;630K(9728K), [Metaspace: 3467K-&gt;3467K(1056768K)], 0.0058502 secs] [Times: user=0.00 sys=0.00, real=0.01 secs] </code></pre><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160544.png" alt=""></p><p>规律：</p><pre><code>[名称： GC前内存占用 -&gt; GC后内存占用 (该区内存总大小)]</code></pre><p>当我们出现了老年代都扛不住的时候，就会出现OOM异常</p><h2 id="6-4、-XX-SurvivorRatio"><a href="#6-4、-XX-SurvivorRatio" class="headerlink" title="6.4、-XX:SurvivorRatio"></a>6.4、<code>-XX:SurvivorRatio</code></h2><p>调节新生代中 eden 和 S0、S1的空间比例，默认为 -XX:SuriviorRatio=8，Eden:S0:S1 = 8:1:1</p><p>加入设置成 <code>-XX:SurvivorRatio=4</code>，则为 Eden:S0:S1 = 4:1:1</p><p>SurvivorRatio值就是设置eden区的比例占多少，S0和S1相同</p><p>Java堆从GC的角度还可以细分为：新生代（Eden区，From Survivor区合To Survivor区）和老年代</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160537.png" alt=""></p><blockquote><p>1、den、SurvivorFrom复制到SurvivorTo，年龄 + 1</p></blockquote><p>首先，当Eden区满的时候会触发第一次GC，把还活着的对象拷贝到SurvivorFrom去，当Eden区再次触发GC的时候会扫描Eden区合From区域，对这两个区域进行垃圾回收，经过这次回收后还存活的对象，则直接复制到To区域（如果对象的年龄已经到达老年的标准，则赋值到老年代区），通知把这些对象的年龄 + 1</p><blockquote><p>2、清空eden、SurvivorFrom</p></blockquote><p>然后，清空eden，SurvivorFrom中的对象，也即复制之后有交换，谁空谁是to</p><blockquote><p>3、SurvivorTo和SurvivorFrom互换</p></blockquote><p>最后，SurvivorTo和SurvivorFrom互换，原SurvivorTo成为下一次GC时的SurvivorFrom区，部分对象会在From和To区域中复制来复制去，如此交换15次（由JVM参数MaxTenuringThreshold决定，这个参数默认为15），最终如果还是存活，就存入老年代</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716160531.png" alt=""></p><h2 id="6-5、-XX-NewRatio（了解）"><a href="#6-5、-XX-NewRatio（了解）" class="headerlink" title="6.5、-XX:NewRatio（了解）"></a>6.5、<code>-XX:NewRatio</code>（了解）</h2><p>配置年轻代new 和老年代old 在堆结构的占比</p><p>默认： -XX:NewRatio=2 新生代占1，老年代2，年轻代占整个堆的1/3</p><p>-XX:NewRatio=4：新生代占1，老年代占4，年轻代占整个堆的1/5，NewRadio值就是设置老年代的占比，剩下的1个新生代</p><p>新生代特别小，会造成频繁的进行GC收集</p><h2 id="6-6、-XX-MaxTenuringThreshold"><a href="#6-6、-XX-MaxTenuringThreshold" class="headerlink" title="6.6、-XX:MaxTenuringThreshold"></a>6.6、<code>-XX:MaxTenuringThreshold</code></h2><p>设置垃圾最大年龄，SurvivorTo和SurvivorFrom互换，原SurvivorTo成为下一次GC时的SurvivorFrom区，部分对象会在From和To区域中复制来复制去，如此交换15次（由JVM参数MaxTenuringThreshold决定，这个参数默认为15），最终如果还是存活，就存入老年代</p><p>这里就是调整这个次数的，默认是15，并且设置的值 在 0~15之间</p><p>查看默认进入老年代年龄：·<code>info -flag MaxTenuringThreshold 17344</code></p><p><code>-XX:MaxTenuringThreshold=0</code>：设置垃圾最大年龄。如果设置为0的话，则年轻对象不经过Survivor区，直接进入老年代。对于年老代比较多的应用，可以提高效率。如果将此值设置为一个较大的值，则年轻代对象会在Survivor区进行多次复制，这样可以增加对象再年轻代的存活时间，增加在年轻代即被回收的概念</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、提问&quot;&gt;&lt;a href=&quot;#01、提问&quot; class=&quot;headerlink&quot; title=&quot;01、提问&quot;&gt;&lt;/a&gt;01、提问&lt;/h1&gt;&lt;p&gt;你说你做过JVM调优和参数配置，请问如何盘点查看JVM系统默认值&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;使用jps
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JVM" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JVM/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>01Github学习</title>
    <link href="http://lsqstudy.top/post/c46462d7.html"/>
    <id>http://lsqstudy.top/post/c46462d7.html</id>
    <published>2020-07-15T16:00:00.000Z</published>
    <updated>2020-07-16T15:23:53.360Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>使用Github优秀框架 + 源码 提升自己</p></blockquote><h1 id="01、常用词含义"><a href="#01、常用词含义" class="headerlink" title="01、常用词含义"></a>01、常用词含义</h1><ul><li>watch：会持续收到该项目的动态</li><li>fork：复制某个仓库到自己的Github仓库中</li><li>star：可以理解为点赞</li><li>clone：将项目下载至本地</li><li>follow：关注你感兴趣的作者，会收到他们的动态</li></ul><h1 id="02、in关键字限制搜索范围"><a href="#02、in关键字限制搜索范围" class="headerlink" title="02、in关键字限制搜索范围"></a>02、in关键字限制搜索范围</h1><ul><li>公式：<code>XXX关键字 in:name 或 description 或 readme</code></li><li>xxx in:name   项目名称含有XXX的</li><li>xxx in:description   项目描述含有XXX的</li><li>xxx in:readme   项目的readme文件中包含XXX的</li><li>组合使用<ul><li>xxx   in:name,readme    项目的名称和readme中包含xxx的</li></ul></li></ul><h1 id="03、stars或fork数量关键字查找"><a href="#03、stars或fork数量关键字查找" class="headerlink" title="03、stars或fork数量关键字查找"></a>03、stars或fork数量关键字查找</h1><ul><li>公式：<ul><li><code>xxx关键字  stars 通配符</code>  :&gt;  或者 :&gt;=</li><li>区间范围数字：  <code>stars:数字1..数字2</code></li></ul></li><li>案例<ul><li>查找stars数大于等于5000的Springboot项目：springboot  stars:&gt;=5000</li><li>查找forks数在1000~2000之间的springboot项目：springboot forks:1000..5000</li></ul></li><li>组合使用<ul><li>查找star大于1000，fork数在500到1000：<code>springboot stars:&gt;1000 forks:500..1000</code></li></ul></li></ul><h1 id="04、awesome加强搜索"><a href="#04、awesome加强搜索" class="headerlink" title="04、awesome加强搜索"></a>04、awesome加强搜索</h1><ul><li>公式：<code>awesome 关键字</code>：awesome系列，一般用来收集学习、工具、书籍类相关的项目</li><li>搜索优秀的redis相关的项目，包括框架，教程等：  awesome redis</li></ul><h1 id="05、高亮显示某行代码"><a href="#05、高亮显示某行代码" class="headerlink" title="05、高亮显示某行代码"></a>05、高亮显示某行代码</h1><ul><li>一行：地址后面紧跟  #L10<ul><li><code>https://github.com/moxi624/mogu_blog_v2/blob/master/mogu_admin/pom.xml#L13</code></li></ul></li><li>多行：地址后面紧跟 #Lx - #Ln<ul><li><code>https://github.com/moxi624/mogu_blog_v2/blob/master/mogu_admin/pom.xml#L13-L30</code></li></ul></li></ul><h1 id="06、项目内搜索"><a href="#06、项目内搜索" class="headerlink" title="06、项目内搜索"></a>06、项目内搜索</h1><blockquote><p>使用英文字母 <code>t</code> ,开启项目内搜索</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716232345.png" alt=""></p><h1 id="07、搜索某个地区内的大佬"><a href="#07、搜索某个地区内的大佬" class="headerlink" title="07、搜索某个地区内的大佬"></a>07、搜索某个地区内的大佬</h1><ul><li>location：地区</li><li>language：语言</li><li>例如：<code>location:beijing language:java</code></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;使用Github优秀框架 + 源码 提升自己&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h1 id=&quot;01、常用词含义&quot;&gt;&lt;a href=&quot;#01、常用词含义&quot; class=&quot;headerlink&quot; title=&quot;01、常用词含义&quot;&gt;&lt;/a&gt;01、常用
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="GitHub" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/GitHub/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>02什么是GCRoots？能做什么？</title>
    <link href="http://lsqstudy.top/post/198bb99c.html"/>
    <id>http://lsqstudy.top/post/198bb99c.html</id>
    <published>2020-07-15T16:00:00.000Z</published>
    <updated>2020-07-16T06:48:30.573Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、GCRoots"><a href="#01、GCRoots" class="headerlink" title="01、GCRoots"></a>01、GCRoots</h1><h2 id="1-1、提问"><a href="#1-1、提问" class="headerlink" title="1.1、提问"></a>1.1、提问</h2><p>问题：JVM垃圾回收的时候如何确定垃圾？是否知道什么是GC Roots？</p><h2 id="1-2、什么是垃圾"><a href="#1-2、什么是垃圾" class="headerlink" title="1.2、什么是垃圾"></a>1.2、什么是垃圾</h2><p>简单来说就是内存中已经不再被使用的空间就是垃圾</p><h2 id="1-3、如何判断一个对象是否可以被回收"><a href="#1-3、如何判断一个对象是否可以被回收" class="headerlink" title="1.3、如何判断一个对象是否可以被回收"></a>1.3、如何判断一个对象是否可以被回收</h2><h3 id="1-3-1、引用计数法"><a href="#1-3-1、引用计数法" class="headerlink" title="1.3.1、引用计数法"></a>1.3.1、引用计数法</h3><p>Java中，引用和对象是有关联的。如果要操作对象则必须用引用进行。</p><p>因此，很显然一个简单的办法就是通过引用计数来判断一个对象是否可以回收。简单说，给对象中添加一个引用计数器</p><p>每当有一个地方引用它，计数器值加1</p><p>每当有一个引用失效，计数器值减1</p><p>任何时刻计数器值为零的对象就是不可能再被使用的，那么这个对象就是可回收对象。</p><blockquote><p>那么为什么主流的Java虚拟机里面都没有选用这个方法呢？</p></blockquote><p>最主要的原因：它很难解决对象之间相互循环引用的问题。</p><p>该算法存在但目前无人用了，解决不了循环引用的问题，了解即可。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716144828.png" alt=""></p><h3 id="1-3-2、枚举根节点做可达性分析"><a href="#1-3-2、枚举根节点做可达性分析" class="headerlink" title="1.3.2、枚举根节点做可达性分析"></a>1.3.2、枚举根节点做可达性分析</h3><p>根搜索路径算法</p><p>为了解决引用计数法的循环引用个问题，Java使用了可达性分析的方法：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716144823.png" alt=""></p><p>所谓 GC Roots 或者说 Tracing Roots的“根集合” 就是一组必须活跃的引用</p><p>基本思路就是通过一系列名为 GC Roots的对象作为起始点，从这个被称为GC Roots的对象开始向下搜索，如果一个对象到GC Roots没有任何引用链相连，则说明此对象不可用。也即给定一个集合的引用作为根出发，通过引用关系遍历对象图，能被遍历到的（可到达的）对象就被判定为存活，没有被遍历到的对象就被判定为死亡</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716144814.png" alt=""></p><blockquote><p>说明</p></blockquote><ul><li>必须从GC Roots对象开始，这个类似于linux的 /  也就是根目录</li><li>蓝色部分是从GC Roots出发，能够循环可达</li><li>白色部分，从GC Roots出发，无法到达</li></ul><h2 id="1-4、一句话理解GC-Roots"><a href="#1-4、一句话理解GC-Roots" class="headerlink" title="1.4、一句话理解GC Roots"></a>1.4、一句话理解GC Roots</h2><p>假设我们现在有三个实体，分别是 人，狗，毛衣</p><p>然后他们之间的关系是：人 牵着 狗，狗穿着毛衣，他们之间是强连接的关系</p><p>有一天人消失了，只剩下狗狗 和 毛衣，这个时候，把人想象成 GC Roots，因为 人 和 狗之间失去了绳子连接，</p><p>那么狗可能被回收，也就是被警察抓起来，被送到流浪狗寄养所</p><p>假设狗和人有强连接的时候，狗狗就不会被当成是流浪狗</p><h2 id="1-5、那些对象可以当做GC-Roots"><a href="#1-5、那些对象可以当做GC-Roots" class="headerlink" title="1.5、那些对象可以当做GC Roots"></a>1.5、那些对象可以当做GC Roots</h2><ul><li>虚拟机栈（栈帧中的局部变量区，也叫做局部变量表）中的引用对象</li><li>方法区中的类静态属性引用的对象</li><li>方法区中常量引用的对象</li><li>本地方法栈中的JNI（Native方法）的引用对象</li></ul><h3 id="1-5-1、代码说明"><a href="#1-5-1、代码说明" class="headerlink" title="1.5.1、代码说明"></a>1.5.1、代码说明</h3><pre><code class="java">package com.lsq.jvm.GCRoots;/** * 在Java中，可以作为GC Roots的对象有： * - 虚拟机栈（栈帧中的局部变量区，也叫做局部变量表）中的引用对象 * - 方法区中的类静态属性引用的对象 * - 方法区中常量引用的对象 * - 本地方法栈中的JNI（Native方法）的引用对象 * @Author: LSQ * Date: 2020/7/16 14:41 */public class GCRootDemo {    // 方法区中的类静态属性引用的对象    // private static GCRootDemo2 t2;    // 方法区中的常量引用，GC Roots 也会以这个为起点，进行遍历    // private static final GCRootDemo3 t3 = new GCRootDemo3(8);    public static void m1() {        // 第一种，虚拟机栈中的引用对象        GCRootDemo t1 = new GCRootDemo();        System.gc();        System.out.println(&quot;第一次GC完成&quot;);    }    public static void main(String[] args) {        m1();    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、GCRoots&quot;&gt;&lt;a href=&quot;#01、GCRoots&quot; class=&quot;headerlink&quot; title=&quot;01、GCRoots&quot;&gt;&lt;/a&gt;01、GCRoots&lt;/h1&gt;&lt;h2 id=&quot;1-1、提问&quot;&gt;&lt;a href=&quot;#1-1、提问&quot; class=&quot;
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JVM" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JVM/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>01JVM体系结构</title>
    <link href="http://lsqstudy.top/post/9d66f7d2.html"/>
    <id>http://lsqstudy.top/post/9d66f7d2.html</id>
    <published>2020-07-15T16:00:00.000Z</published>
    <updated>2020-07-16T06:25:54.485Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、JVM体系结构"><a href="#01、JVM体系结构" class="headerlink" title="01、JVM体系结构"></a>01、JVM体系结构</h1><h2 id="1-1、概览"><a href="#1-1、概览" class="headerlink" title="1.1、概览"></a>1.1、概览</h2><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716142217.png" alt=""></p><blockquote><p>java gc 主要回收的是 <strong>方法区</strong> 和 <strong>堆</strong> 中的内容</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716142211.png" alt=""></p><h2 id="1-2、类加载器"><a href="#1-2、类加载器" class="headerlink" title="1.2、类加载器"></a>1.2、类加载器</h2><ul><li>类加载器是什么</li><li>双亲委派机制</li><li>Java类加载的沙箱安全机制</li></ul><h2 id="1-3、常见的垃圾回收算法"><a href="#1-3、常见的垃圾回收算法" class="headerlink" title="1.3、常见的垃圾回收算法"></a>1.3、常见的垃圾回收算法</h2><h3 id="1-3-1、引用计数"><a href="#1-3-1、引用计数" class="headerlink" title="1.3.1、引用计数"></a>1.3.1、引用计数</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716142202.png" alt=""></p><p>在双端循环，互相引用的时候，容易报错，目前很少使用这种方式了</p><h3 id="1-3-2、复制算法"><a href="#1-3-2、复制算法" class="headerlink" title="1.3.2、复制算法"></a>1.3.2、复制算法</h3><blockquote><p>MinorGC的过程（复制-&gt;清空-&gt;互换）</p></blockquote><p>复制算法在年轻代的时候，进行使用，复制时候有交换</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716142155.png" alt=""></p><blockquote><p>1：eden、SurvivorFrom 复制到 SurvivorTo，年龄+1 </p></blockquote><p>首先，当Eden区满的时候会触发第一次GC,把还活着的对象拷贝到SurvivorFrom区，当Eden区再次触发GC的时候会扫描Eden区和From区域,对这两个区域进行垃圾回收，经过这次回收后还存活的对象,则直接复制到To区域（如果有对象的年龄已经达到了老年的标准，则赋值到老年代区），同时把这些对象的年龄+1</p><blockquote><p>2：清空 eden、SurvivorFrom </p></blockquote><p>然后，清空Eden和SurvivorFrom中的对象，也即复制之后有交换，谁空谁是to</p><blockquote><p>3：SurvivorTo和 SurvivorFrom 互换 </p></blockquote><p>最后，SurvivorTo和SurvivorFrom互换，原SurvivorTo成为下一次GC时的SurvivorFrom区。部分对象会在From和To区域中复制来复制去,如此交换15次(由JVM参数MaxTenuringThreshold决定,这个参数默认是15),最终如果还是存活,就存入到老年代</p><blockquote><p>优点</p></blockquote><p>没有产生内存碎片</p><h3 id="1-3-3、标记清除"><a href="#1-3-3、标记清除" class="headerlink" title="1.3.3、标记清除"></a>1.3.3、标记清除</h3><p>先标记，后清除，缺点是会产生内存碎片，用于老年代多一些</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716142147.png" alt=""></p><h3 id="1-3-4、标记整理"><a href="#1-3-4、标记整理" class="headerlink" title="1.3.4、标记整理"></a>1.3.4、标记整理</h3><p>标记清除整理</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716142140.png" alt=""></p><p>但是需要付出代价，因为移动对象需要成本</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、JVM体系结构&quot;&gt;&lt;a href=&quot;#01、JVM体系结构&quot; class=&quot;headerlink&quot; title=&quot;01、JVM体系结构&quot;&gt;&lt;/a&gt;01、JVM体系结构&lt;/h1&gt;&lt;h2 id=&quot;1-1、概览&quot;&gt;&lt;a href=&quot;#1-1、概览&quot; class=&quot;
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JVM" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JVM/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>05Java内存溢出OOM</title>
    <link href="http://lsqstudy.top/post/c385b39.html"/>
    <id>http://lsqstudy.top/post/c385b39.html</id>
    <published>2020-07-15T16:00:00.000Z</published>
    <updated>2020-07-16T11:00:00.102Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、经典错误"><a href="#01、经典错误" class="headerlink" title="01、经典错误"></a>01、经典错误</h1><blockquote><p>JVM中常见的两个<strong>错误</strong></p></blockquote><p><code>StackoverFlowError</code>：栈溢出</p><p><code>OutofMemoryError: java heap space</code>：堆溢出</p><blockquote><p>除此之外，还有以下的错误</p></blockquote><ul><li>java.lang.StackOverflowError</li><li>java.lang.OutOfMemoryError：java heap space</li><li>java.lang.OutOfMemoryError：GC overhead limit exceeeded</li><li>java.lang.OutOfMemoryError：Direct buffer memory</li><li>java.lang.OutOfMemoryError：unable to create new native thread</li><li>java.lang.OutOfMemoryError：Metaspace</li></ul><h1 id="02、架构"><a href="#02、架构" class="headerlink" title="02、架构"></a>02、架构</h1><blockquote><p><code>OutOfMemoryError</code>和<code>StackOverflowError</code>是属于Error，不是Exception</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716171328.png" alt=""></p><h1 id="03、StackoverFlowError"><a href="#03、StackoverFlowError" class="headerlink" title="03、StackoverFlowError"></a>03、StackoverFlowError</h1><p>堆栈溢出，我们有最简单的一个递归调用，就会造成堆栈溢出，也就是深度的方法调用</p><p>栈一般是512K，不断的深度调用，直到栈被撑破</p><pre><code class="java">package com.lsq.jvm.OOM;/** * @Description: * @Author: LSQ * Date: 2020/7/16 17:08 */public class StackOverflowErrorDemo {    public static void main(String[] args) {        stackOverflowError();    }    /**     * 栈一般是512K，不断的深度调用，直到栈被撑破     * Exception in thread &quot;main&quot; java.lang.StackOverflowError     */    private static void stackOverflowError() {        //递归调用        stackOverflowError();    }}</code></pre><p>运行结果</p><pre><code>Exception in thread &quot;main&quot; java.lang.StackOverflowError    at com.moxi.interview.study.oom.StackOverflowErrorDemo.stackOverflowError(StackOverflowErrorDemo.java:17)</code></pre><h1 id="05、OutOfMemoryError"><a href="#05、OutOfMemoryError" class="headerlink" title="05、OutOfMemoryError"></a>05、OutOfMemoryError</h1><h2 id="5-1、java-heap-space"><a href="#5-1、java-heap-space" class="headerlink" title="5.1、java heap space"></a>5.1、java heap space</h2><p>创建了很多对象，导致堆空间不够存储</p><pre><code class="java">package com.lsq.jvm.OOM;/** * @Description: * @Author: LSQ * Date: 2020/7/16 17:11 */public class JavaHeapSpaceDemo {    public static void main(String[] args) {        // 堆空间的大小 -Xms10m -Xmx10m        // 创建一个 80M的字节数组        byte [] bytes = new byte[80 * 1024 * 1024];    }}</code></pre><p>我们创建一个80M的数组，会直接出现Java heap space</p><pre><code>Exception in thread &quot;main&quot; java.lang.OutOfMemoryError: Java heap space</code></pre><h2 id="5-2、GC-overhead-limit-exceeded"><a href="#5-2、GC-overhead-limit-exceeded" class="headerlink" title="5.2、GC overhead limit exceeded"></a>5.2、GC overhead limit exceeded</h2><p>GC回收时间过长时会抛出OutOfMemoryError，过长的定义是，超过了98%的时间用来做GC，并且回收了不到2%的堆内存</p><p>连续多次GC都只回收了不到2%的极端情况下，才会抛出。假设不抛出GC overhead limit 错误会造成什么情况呢？</p><p>那就是GC清理的这点内存很快会再次被填满，迫使GC再次执行，这样就形成了恶性循环，CPU的使用率一直都是100%，而GC却没有任何成果。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716171322.png" alt=""></p><blockquote><p>代码演示：</p></blockquote><p>为了更快的达到效果，我们首先需要设置JVM启动参数</p><pre><code>-Xms10m -Xmx10m -XX:+PrintGCDetails -XX:MaxDirectMemorySize=5m</code></pre><p>这个异常出现的步骤就是，我们不断的像list中插入String对象，直到启动GC回收</p><pre><code class="java">package com.lsq.jvm.OOM;import java.util.ArrayList;import java.util.List;/** * GC 回收超时 * JVM参数配置: -Xms10m -Xmx10m -XX:+PrintGCDetails -XX:MaxDirectMemorySize=5m * @Author: LSQ * Date: 2020/7/16 17:14 */public class GCOverheadLimitDemo {    public static void main(String[] args) {        int i = 0;        List&lt;String&gt; list = new ArrayList&lt;&gt;();        try {            while(true) {                list.add(String.valueOf(++i).intern());            }        } catch (Exception e) {            System.out.println(&quot;***************i:&quot; + i);            e.printStackTrace();            throw e;        } finally {        }    }}</code></pre><p>运行结果</p><pre><code>[Full GC (Ergonomics) [PSYoungGen: 2047K-&gt;0K(2560K)] [ParOldGen: 7130K-&gt;629K(7168K)] 9178K-&gt;629K(9728K), [Metaspace: 3295K-&gt;3295K(1056768K)], 0.0067829 secs] [Times: user=0.00 sys=0.00, real=0.01 secs] Exception in thread &quot;main&quot; java.lang.OutOfMemoryError: GC overhead limit exceeded    at java.lang.Integer.toString(Integer.java:403)    at java.lang.String.valueOf(String.java:3099)    at com.lsq.jvm.OOM.GCOverheadLimitDemo.main(GCOverheadLimitDemo.java:18)Heap PSYoungGen      total 2560K, used 123K [0x00000000ffd00000, 0x0000000100000000, 0x0000000100000000)  eden space 2048K, 6% used [0x00000000ffd00000,0x00000000ffd1ed30,0x00000000fff00000)  from space 512K, 0% used [0x00000000fff80000,0x00000000fff80000,0x0000000100000000)  to   space 512K, 0% used [0x00000000fff00000,0x00000000fff00000,0x00000000fff80000) ParOldGen       total 7168K, used 629K [0x00000000ff600000, 0x00000000ffd00000, 0x00000000ffd00000)  object space 7168K, 8% used [0x00000000ff600000,0x00000000ff69d7d0,0x00000000ffd00000) Metaspace       used 3368K, capacity 4500K, committed 4864K, reserved 1056768K  class space    used 361K, capacity 388K, committed 512K, reserved 1048576KProcess finished with exit code 1</code></pre><p>我们能够看到 多次Full GC，并没有清理出空间，在多次执行GC操作后，就抛出异常 <code>GC overhead limit</code></p><h2 id="5-2、Direct-buffer-memory"><a href="#5-2、Direct-buffer-memory" class="headerlink" title="5.2、Direct buffer memory"></a>5.2、Direct buffer memory</h2><p>Netty + NIO：这是由于NIO引起的</p><p>写NIO程序的时候经常会使用ByteBuffer来读取或写入数据，这是一种基于通道(Channel) 与 缓冲区(Buffer)的I/O方式，它可以使用Native 函数库直接分配堆外内存，然后通过一个存储在Java堆里面的DirectByteBuffer对象作为这块内存的引用进行操作。这样能在一些场景中显著提高性能，因为避免了在Java堆和Native堆中来回复制数据。</p><p>ByteBuffer.allocate(capability)：第一种方式是分配JVM堆内存，属于GC管辖范围，由于需要拷贝所以速度相对较慢</p><p>ByteBuffer.allocteDirect(capability)：第二种方式是分配OS本地内存，不属于GC管辖范围，由于不需要内存的拷贝，所以速度相对较快</p><p>但如果不断分配本地内存，堆内存很少使用，那么JVM就不需要执行GC，DirectByteBuffer对象就不会被回收，这时候怼内存充足，但本地内存可能已经使用光了，再次尝试分配本地内存就会出现OutOfMemoryError，那么程序就奔溃了。</p><p>一句话说：本地内存不足，但是堆内存充足的时候，就会出现这个问题</p><p>我们使用 -XX:MaxDirectMemorySize=5m 配置能使用的堆外物理内存为5M</p><pre><code class="java">-Xms10m -Xmx10m -XX:+PrintGCDetails -XX:MaxDirectMemorySize=5m</code></pre><p>然后我们申请一个6M的空间</p><pre><code class="java">// 只设置了5M的物理内存使用，但是却分配 6M的空间ByteBuffer bb = ByteBuffer.allocateDirect(6 * 1024 * 1024);</code></pre><blockquote><p>完整代码：</p></blockquote><pre><code class="java">package com.lsq.jvm.OOM;import java.nio.ByteBuffer;import java.util.concurrent.TimeUnit;/** * @Description:本地内存不足，但是堆内存充足的时候，就会出现这个问题 * @Author: LSQ * Date: 2020/7/16 17:45 */public class DirectbuffermemoryDemo {    public static void main(String[] args) {        System.out.println(&quot;配置的MaxDreMemory:&quot;+(sun.misc.VM.maxDirectMemory()/(double)1024/1024)+&quot;MB&quot;);        try { TimeUnit.SECONDS.sleep(3);} catch (InterruptedException e) { e.printStackTrace();}        //设置：-Xms10m -Xmx10m -XX:+PrintGCDetails -XX:MaxDirectMemorySize=5m        // 只设置了5M的物理内存使用，但是却分配 6M的空间        ByteBuffer bb = ByteBuffer.allocateDirect(6 * 1024 * 1024);    }}</code></pre><p>运行就会出现问题了</p><pre><code>配置的maxDirectMemory：5.0MB[GC (System.gc()) [PSYoungGen: 2030K-&gt;488K(2560K)] 2030K-&gt;796K(9728K), 0.0008326 secs] [Times: user=0.00 sys=0.00, real=0.00 secs] [Full GC (System.gc()) [PSYoungGen: 488K-&gt;0K(2560K)] [ParOldGen: 308K-&gt;712K(7168K)] 796K-&gt;712K(9728K), [Metaspace: 3512K-&gt;3512K(1056768K)], 0.0052052 secs] [Times: user=0.09 sys=0.00, real=0.00 secs] Exception in thread &quot;main&quot; java.lang.OutOfMemoryError: Direct buffer memory    at java.nio.Bits.reserveMemory(Bits.java:693)    at java.nio.DirectByteBuffer.&lt;init&gt;(DirectByteBuffer.java:123)    at java.nio.ByteBuffer.allocateDirect(ByteBuffer.java:311)    at com.moxi.interview.study.oom.DIrectBufferMemoryDemo.main(DIrectBufferMemoryDemo.java:19)</code></pre><h2 id="5-4、unable-to-create-new-native-thread"><a href="#5-4、unable-to-create-new-native-thread" class="headerlink" title="5.4、unable to create new native thread"></a>5.4、unable to create new native thread</h2><p>不能够创建更多的新的线程了，也就是说创建线程的上限达到了</p><p>在高并发场景的时候，会应用到</p><p>高并发请求服务器时，经常会出现如下异常<code>java.lang.OutOfMemoryError:unable to create new native thread</code>，准确说该native thread异常与对应的平台有关</p><p>导致原因：</p><ul><li>应用创建了太多线程，一个应用进程创建多个线程，超过系统承载极限</li><li>服务器并不允许你的应用程序创建这么多线程，linux系统默认运行单个进程可以创建的线程为1024个，如果应用创建超过这个数量，就会报 <code>java.lang.OutOfMemoryError:unable to create new native thread</code></li></ul><p>解决方法：</p><ol><li>想办法降低你应用程序创建线程的数量，分析应用是否真的需要创建这么多线程，如果不是，改代码将线程数降到最低</li><li>对于有的应用，确实需要创建很多线程，远超过linux系统默认1024个线程限制，可以通过修改linux服务器配置，扩大linux默认限制</li></ol><blockquote><p>完整代码</p></blockquote><pre><code class="java">package com.lsq.jvm.OOM;import java.util.concurrent.TimeUnit;/** * @Description:无法创建更多的线程 * @Author: LSQ * Date: 2020/7/16 17:22 */public class UnableCreateNewThreadDemo {    public static void main(String[] args) {        for (int i = 0; ; i++) {            System.out.println(&quot;************** i = &quot; + i);            new Thread(() -&gt; {                try {                    TimeUnit.SECONDS.sleep(Integer.MAX_VALUE);                } catch (InterruptedException e) {                    e.printStackTrace();                }            }, String.valueOf(i)).start();        }    }}</code></pre><p>这个时候，就会出现下列的错误，线程数大概在 900多个</p><pre><code>Exception in thread &quot;main&quot; java.lang.OutOfMemoryError: unable to cerate new native thread</code></pre><blockquote><p>linux如何查看线程数</p></blockquote><pre><code class="bash">[root@lsqstudy ~]# ulimit -u7270</code></pre><p>也可以查看文件查看文件，</p><pre><code class="bash">[root@lsqstudy ~]# cd /etc/security/limits.d/[root@lsqstudy limits.d]# lltotal 4-rw-r--r--. 1 root root 191 Apr 11  2018 20-nproc.conf[root@lsqstudy limits.d]# cat 20-nproc.conf *          soft    nproc     4096       #其他用户4096root       soft    nproc     unlimited  #其他用户4096无上限</code></pre><p>增加一个lsqstudy用户,允许创建1024个线程，就在这个文件后加入下面内容：</p><pre><code class="bash">lsqstudy         soft    nproc     1024       #其他用户4096</code></pre><h2 id="5-5、Metaspace"><a href="#5-5、Metaspace" class="headerlink" title="5.5、Metaspace"></a>5.5、Metaspace</h2><p>元空间内存不足，Matespace元空间应用的是本地内存。</p><p>元空间是方法区的实现。</p><p><code>-XX:MetaspaceSize</code> 的处理化大小为20M</p><h3 id="5-5-1、元空间是什么"><a href="#5-5-1、元空间是什么" class="headerlink" title="5.5.1、元空间是什么"></a>5.5.1、元空间是什么</h3><p>元空间就是我们的方法区，存放的是类模板，类信息，常量池等</p><p>Metaspace是方法区HotSpot中的实现，它与持久代最大的区别在于：Metaspace并不在虚拟内存中，而是使用本地内存，也即在java8中，class metadata（the virtual machines internal presentation of Java class），被存储在叫做Matespace的native memory</p><p>永久代（java8后背元空间Metaspace取代了）存放了以下信息：</p><ul><li>虚拟机加载的类信息</li><li>常量池</li><li>静态变量</li><li>即时编译后的代码</li></ul><p>模拟Metaspace空间溢出，我们不断生成类 往元空间里灌输，类占据的空间总会超过Metaspace指定的空间大小</p><h3 id="5-5-2、代码"><a href="#5-5-2、代码" class="headerlink" title="5.5.2、代码"></a>5.5.2、代码</h3><p>在模拟异常生成时候，因为初始化的元空间为20M，因此我们使用JVM参数调整元空间的大小，为了更好的效果</p><pre><code>-XX:MetaspaceSize=8m -XX:MaxMetaspaceSize=8m</code></pre><p>代码如下：</p><pre><code class="java">/** * 元空间溢出 */public class MetaspaceOutOfMemoryDemo {    // 静态类    static class OOMTest {    }    public static void main(final String[] args) {        // 模拟计数多少次以后发生异常        int i =0;        try {            while (true) {                i++;                // 使用Spring的动态字节码技术                Enhancer enhancer = new Enhancer();                enhancer.setSuperclass(OOMTest.class);                enhancer.setUseCache(false);                enhancer.setCallback(new MethodInterceptor() {                    @Override                    public Object intercept(Object o, Method method, Object[] objects, MethodProxy methodProxy) throws Throwable {                        return methodProxy.invokeSuper(o, args);                    }                });            }        } catch (Exception e) {            System.out.println(&quot;发生异常的次数:&quot; + i);            e.printStackTrace();        } finally {        }    }}</code></pre><p>会出现以下错误：</p><pre><code>发生异常的次数: 201java.lang.OutOfMemoryError:Metaspace</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、经典错误&quot;&gt;&lt;a href=&quot;#01、经典错误&quot; class=&quot;headerlink&quot; title=&quot;01、经典错误&quot;&gt;&lt;/a&gt;01、经典错误&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;JVM中常见的两个&lt;strong&gt;错误&lt;/strong&gt;&lt;/p&gt;
&lt;/bl
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JVM" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JVM/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>06垃圾收集器</title>
    <link href="http://lsqstudy.top/post/e41f9b46.html"/>
    <id>http://lsqstudy.top/post/e41f9b46.html</id>
    <published>2020-07-15T16:00:00.000Z</published>
    <updated>2020-07-16T13:33:58.431Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、GC垃圾回收算法和垃圾收集器关系"><a href="#01、GC垃圾回收算法和垃圾收集器关系" class="headerlink" title="01、GC垃圾回收算法和垃圾收集器关系"></a>01、GC垃圾回收算法和垃圾收集器关系</h1><blockquote><p>GC算法是内存回收的方法论，垃圾收集器就是GC垃圾回收算法的实现</p></blockquote><p>GC算法主要有以下几种</p><ul><li>引用计数（几乎不用，无法解决循环引用的问题）</li><li>复制拷贝（用于新生代）</li><li>标记清除（用于老年代）</li><li>标记整理（用于老年代）</li></ul><p>因为目前为止还没有完美的收集器出现，更没有万能的收集器，只是针对具体应用最合适的收集器，进行<strong>分代收集</strong>（那个代用什么收集器）</p><h1 id="02、四种主要的垃圾收集器"><a href="#02、四种主要的垃圾收集器" class="headerlink" title="02、四种主要的垃圾收集器"></a>02、四种主要的垃圾收集器</h1><ul><li>Serial：串行回收  <code>-XX:+UseSeriallGC</code></li><li>Parallel：并行回收  <code>-XX:+UseParallelGC</code></li><li>CMS：并发标记清除</li><li>G1</li><li>ZGC：（java 11 出现的）</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716193639.png" alt=""></p><h2 id="2-1、Serial"><a href="#2-1、Serial" class="headerlink" title="2.1、Serial"></a>2.1、Serial</h2><p>串行垃圾回收器，它为单线程环境设计且值使用一个线程进行垃圾收集，会暂停所有的用户线程，只有当垃圾回收完成时，才会重新唤醒主线程继续执行。所以不适合服务器环境</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716213039.png" alt=""></p><h2 id="2-2、Parallel"><a href="#2-2、Parallel" class="headerlink" title="2.2、Parallel"></a>2.2、Parallel</h2><p>并行垃圾收集器，多个垃圾收集线程并行工作，此时用户线程也是阻塞的，适用于科学计算 / 大数据处理等弱交互场景，也就是说Serial 和 Parallel其实是类似的，不过是多了几个线程进行垃圾收集，但是主线程都会被暂停，但是并行垃圾收集器处理时间，肯定比串行的垃圾收集器要更短</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716213033.png" alt="image-20200325085729428"></p><h2 id="2-3、CMS"><a href="#2-3、CMS" class="headerlink" title="2.3、CMS"></a>2.3、CMS</h2><p>并发标记清除，用户线程和垃圾收集线程同时执行（不一定是并行，可能是交替执行），不需要停顿用户线程，互联网公司都在使用，适用于响应时间有要求的场景。并发是可以有交互的，也就是说可以一边进行收集，一边执行应用程序。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716213030.png" alt=""></p><h2 id="2-4、G1"><a href="#2-4、G1" class="headerlink" title="2.4、G1"></a>2.4、G1</h2><p>G1垃圾回收器将堆内存分割成不同区域，然后并发的进行垃圾回收</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716213027.png" alt=""></p><h1 id="03、垃圾收集器总结"><a href="#03、垃圾收集器总结" class="headerlink" title="03、垃圾收集器总结"></a>03、垃圾收集器总结</h1><blockquote><p>注意：并行垃圾回收在单核CPU下可能会更慢</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716213018.png" alt=""></p><h1 id="04、查看默认垃圾收集器"><a href="#04、查看默认垃圾收集器" class="headerlink" title="04、查看默认垃圾收集器"></a>04、查看默认垃圾收集器</h1><p>使用下面JVM命令，查看配置的初始参数</p><pre><code>-XX:+PrintCommandLineFlags</code></pre><p>然后运行一个程序后，能够看到它的一些初始配置信息</p><pre><code>-XX:InitialHeapSize=266376000 -XX:MaxHeapSize=4262016000 -XX:+PrintCommandLineFlags -XX:+UseCompressedClassPointers -XX:+UseCompressedOops -XX:-UseLargePagesIndividualAllocation -XX:+UseParallelGC</code></pre><p>移动到最后一句，就能看到 <code>-XX:+UseParallelGC</code> 说明使用的是并行垃圾回收</p><pre><code>-XX:+UseParallelGC</code></pre><h1 id="05、默认垃圾收集器有哪些"><a href="#05、默认垃圾收集器有哪些" class="headerlink" title="05、默认垃圾收集器有哪些"></a>05、默认垃圾收集器有哪些</h1><p>Java中一共有7大垃圾收集器</p><ul><li>UserSerialGC：串行垃圾收集器</li><li>UserParallelGC：并行垃圾收集器</li><li>UseConcMarkSweepGC：（CMS）并发标记清除</li><li>UseParNewGC：年轻代的并行垃圾回收器</li><li>UseParallelOldGC：老年代的并行垃圾回收器</li><li>UseG1GC：G1垃圾收集器</li><li>UserSerialOldGC：串行老年代垃圾收集器（已经被移除）</li></ul><blockquote><p>底层源码</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716213010.png" alt=""></p><h1 id="06、各垃圾收集器的使用范围"><a href="#06、各垃圾收集器的使用范围" class="headerlink" title="06、各垃圾收集器的使用范围"></a>06、各垃圾收集器的使用范围</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716213002.png" alt=""></p><blockquote><p>1、新生代使用的：</p></blockquote><ul><li>Serial Copying： UserSerialGC，串行垃圾收集器</li><li>Parallel Scavenge：UserParallelGC，并行垃圾收集器</li><li>ParNew：UserParNewGC，新生代并行垃圾收集器</li></ul><blockquote><p>2、老年区使用的：</p></blockquote><ul><li>Serial Old：UseSerialOldGC，老年代串行垃圾收集器</li><li>Parallel Compacting（Parallel Old）：UseParallelOldGC，老年代并行垃圾收集器</li><li>CMS：UseConcMarkSwepp，并行标记清除垃圾收集器</li></ul><blockquote><p>3、各区都能使用的：</p></blockquote><p>G1：UseG1GC：G1垃圾收集器</p><p>垃圾收集器就来具体实现这些GC算法并实现内存回收，不同厂商，不同版本的虚拟机实现差别很大，HotSpot中包含的收集器如下图所示：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212947.png" alt=""></p><h1 id="07、部分参数预先说明"><a href="#07、部分参数预先说明" class="headerlink" title="07、部分参数预先说明"></a>07、部分参数预先说明</h1><ul><li>DefNew：Default New Generation</li><li>Tenured：Old</li><li>ParNew：Parallel New Generation</li><li>PSYoungGen：Parallel Scavenge</li><li>ParOldGen：Parallel Old Generation</li></ul><h1 id="08、Java中的Server和Client模式"><a href="#08、Java中的Server和Client模式" class="headerlink" title="08、Java中的Server和Client模式"></a>08、Java中的Server和Client模式</h1><p>使用范围：一般使用Server模式，Client模式基本不会使用</p><p>操作系统</p><ul><li>32位的Window操作系统，不论硬件如何都默认使用Client的JVM模式</li><li>32位的其它操作系统，2G内存同时有2个cpu以上用Server模式，低于该配置还是Client模式</li><li>64位只有Server模式</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212942.png" alt=""></p><h1 id="09、新生代下的垃圾收集器"><a href="#09、新生代下的垃圾收集器" class="headerlink" title="09、新生代下的垃圾收集器"></a>09、新生代下的垃圾收集器</h1><h3 id="9-1、串行GC-Serial"><a href="#9-1、串行GC-Serial" class="headerlink" title="9.1、串行GC(Serial)"></a>9.1、串行GC(Serial)</h3><p>串行GC（Serial）（Serial Copying）</p><p>是一个单线程单线程的收集器，在进行垃圾收集时候，必须暂停其他所有的工作线程直到它收集结束。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212933.png" alt=""></p><p>串行收集器是最古老，最稳定以及效率高的收集器，只使用一个线程去回收，但其在垃圾收集过程中可能会产生较长的停顿(Stop-The-World 状态)。 虽然在收集垃圾过程中需要暂停所有其它的工作线程，但是它简单高效，对于限定单个CPU环境来说，没有线程交互的开销可以获得最高的单线程垃圾收集效率，因此Serial垃圾收集器依然是Java虚拟机运行在Client模式下默认的新生代垃圾收集器</p><blockquote><p>JVM参数是：<code>-XX:+UseSerialGC</code></p></blockquote><p>开启后会使用：Serial(Young区用) + Serial Old(Old区用) 的收集器组合</p><p>表示：新生代、老年代都会使用串行回收收集器，新生代使用<strong>复制算法</strong>，老年代使用<strong>标记-整理算法</strong></p><pre><code>-Xms10m -Xmx10m -XX:PrintGCDetails -XX:+PrintConmandLineFlags -XX:+UseSerialGC</code></pre><h3 id="9-2、并行GC-ParNew"><a href="#9-2、并行GC-ParNew" class="headerlink" title="9.2、并行GC(ParNew)"></a>9.2、并行GC(ParNew)</h3><p>并行收集器，使用多线程进行垃圾回收，在垃圾收集，会Stop-the-World暂停其他所有的工作线程直到它收集结束</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212928.png" alt=""></p><p>ParNew收集器其实就是Serial收集器新生代的并行多线程版本，最常见的应用场景时配合老年代的CMS GC工作，其余的行为和Serial收集器完全一样，ParNew垃圾收集器在垃圾收集过程中同样也要暂停所有其他的工作线程。它是很多Java虚拟机运行在Server模式下新生代的默认垃圾收集器。</p><blockquote><p>常见对应JVM参数：<code>-XX:+UseParNewGC</code>   启动ParNew收集器，只影响新生代的收集，不影响老年代</p></blockquote><p>开启后会使用：<strong>ParNew（Young区用） + Serial Old的收集器组合</strong>，新生代使用<strong>复制算法</strong>，老年代采用<strong>标记-整理算法</strong></p><pre><code>-Xms10m -Xmx10m -XX:PrintGCDetails -XX:+PrintConmandLineFlags -XX:+UseParNewGC</code></pre><blockquote><p>但是会出现警告，即 ParNew 和 Serial Old 这样搭配，Java8已经不再被推荐</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212912.png" alt=""></p><p>备注： -XX:ParallelGCThreads   限制线程数量，默认开启和CPU数目相同的线程数</p><h3 id="9-3、并行回收GC（Parallel）-（Parallel-Scavenge）"><a href="#9-3、并行回收GC（Parallel）-（Parallel-Scavenge）" class="headerlink" title="9.3、并行回收GC（Parallel）/ （Parallel Scavenge）"></a>9.3、并行回收GC（Parallel）/ （Parallel Scavenge）</h3><p>因为Serial 和 ParNew都不推荐使用了，因此现在新生代默认使用的是Parallel Scavenge，也就是新生代和老年代都是使用并行</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212906.png" alt=""></p><p>Parallel Scavenge收集器类似ParNew也是一个新生代垃圾收集器，使用复制算法，也是一个并行的多线程的垃圾收集器，俗称吞吐量优先收集器。一句话：<strong>串行收集器在新生代和老年代的并行化</strong></p><p>它关注的重点是：</p><p>可控制的吞吐量（Thoughput = 运行用户代码时间 / (运行用户代码时间 + 垃圾收集时间) ），也即比如程序运行100分钟，垃圾收集时间1分钟，吞吐量就是99%。高吞吐量意味着高效利用CPU时间，它多用于在后台运算而不需要太多交互的任务。</p><p>自适应调节策略也是ParallelScavenge收集器与ParNew收集器的一个重要区别。（自适应调节策略：虚拟机会根据当前系统的运行情况收集性能监控信息，动态调整这些参数以提供最合适的停顿时间( -XX:MaxGCPauseMills)）或最大的吞吐量。</p><blockquote><p>常用JVM参数：<code>-XX:+UseParallelGC</code>或<code>-XX:+UseParallelOldGC</code>（可互相激活）使用Parallel Scanvenge收集器</p></blockquote><p>开启该参数后：新生代使用<strong>复制算法</strong>，老年代使用<strong>标记-整理算法</strong></p><pre><code>-Xms10m -Xmx10m -XX:PrintGCDetails -XX:+PrintConmandLineFlags -XX:+UseParallelGC</code></pre><h1 id="10、老年代下的垃圾收集器"><a href="#10、老年代下的垃圾收集器" class="headerlink" title="10、老年代下的垃圾收集器"></a>10、老年代下的垃圾收集器</h1><h2 id="10-1、串行GC（Serial-Old）-Serial-MSC"><a href="#10-1、串行GC（Serial-Old）-Serial-MSC" class="headerlink" title="10.1、串行GC（Serial Old） / (Serial MSC)"></a>10.1、串行GC（Serial Old） / (Serial MSC)</h2><p>Serial Old是Serial垃圾收集器老年代版本，它同样是一个单线程的收集器，使用标记-整理算法，这个收集器也主要是运行在Client默认的Java虚拟机中默认的老年代垃圾收集器</p><p>在Server模式下，主要有两个用途（了解，版本已经到8及以后）</p><ul><li>在JDK1.5之前版本中与新生代的Parallel Scavenge收集器搭配使用（Parallel Scavenge + Serial Old）</li><li>作为老年代版中使用CMS收集器的后备垃圾收集方案。</li></ul><blockquote><p>配置方法：</p></blockquote><pre><code>-Xms10m -Xmx10m -XX:PrintGCDetails -XX:+PrintConmandLineFlags -XX:+UseSerialOldlGC</code></pre><p><strong>该垃圾收集器，目前已经不推荐使用了</strong></p><h2 id="10-2、并行GC（Parallel-Old）-（Parallel-MSC）"><a href="#10-2、并行GC（Parallel-Old）-（Parallel-MSC）" class="headerlink" title="10.2、并行GC（Parallel Old）/ （Parallel MSC）"></a>10.2、并行GC（Parallel Old）/ （Parallel MSC）</h2><p>Parallel Old收集器是Parallel Scavenge的老年代版本，使用多线程的标记-整理算法，Parallel Old收集器在JDK1.6才开始提供。</p><p>在JDK1.6之前，新生代使用ParallelScavenge收集器只能搭配老年代的Serial Old收集器，只能保证新生代的吞吐量优先，无法保证整体的吞吐量。在JDK1.6以前(Parallel Scavenge + Serial Old)</p><p>Parallel Old正是为了在老年代同样提供吞吐量优先的垃圾收集器，如果系统对吞吐量要求比较高，JDK1.8后可以考虑新生代Parallel Scavenge和老年代Parallel Old 收集器的搭配策略。在JDK1.8及后（Parallel Scavenge + Parallel Old）</p><blockquote><p>JVM常用参数：</p></blockquote><pre><code>-XX +UseParallelOldGC：使用Parallel Old收集器，设置该参数后，新生代Parallel+老年代 Parallel Old</code></pre><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212854.png" alt=""></p><blockquote><p>使用老年代并行收集器：</p></blockquote><pre><code>-Xms10m -Xmx10m -XX:PrintGCDetails -XX:+PrintConmandLineFlags -XX:+UseParallelOldlGC</code></pre><h2 id="10-3、-并发标记清除GC（CMS）"><a href="#10-3、-并发标记清除GC（CMS）" class="headerlink" title="10.3、 并发标记清除GC（CMS）"></a>10.3、 并发标记清除GC（CMS）</h2><p>CMS收集器（Concurrent Mark Sweep：并发标记清除）是一种以最短回收停顿时间为目标的收集器</p><p>适合应用在互联网或者B/S系统的服务器上，这类应用尤其重视服务器的响应速度，希望系统停顿时间最短。</p><p>CMS非常适合堆内存大，CPU核数多的服务器端应用，也是G1出现之前大型应用的首选收集器。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212844.png" alt=""></p><p>Concurrent Mark Sweep：并发标记清除，并发收集低停顿，并发指的是与用户线程一起执行</p><blockquote><p>开启该收集器的JVM参数： -XX:+UseConcMarkSweepGC  开启该参数后，会自动将 -XX:+UseParNewGC打开</p></blockquote><p>开启该参数后，使用ParNew(young 区用）+ CMS（Old 区用） + Serial Old 的收集器组合，Serial Old将作为CMS出错的后备收集器</p><pre><code>-Xms10m -Xmx10m -XX:+PrintGCDetails -XX:+UseConcMarkSweepGC</code></pre><h3 id="10-3-1、四个步骤"><a href="#10-3-1、四个步骤" class="headerlink" title="10.3.1、四个步骤"></a>10.3.1、四个步骤</h3><ul><li>初始标记（CMS initial mark）<ul><li>只是标记一个GC Roots 能直接关联的对象，速度很快，仍然需要暂停所有的工作线程</li></ul></li><li>并发标记（CMS concurrent mark）和用户线程一起<ul><li>进行GC Roots跟踪过程，和用户线程一起工作，不需要暂停工作线程。主要标记过程，标记全部对象</li></ul></li><li>重新标记（CMS remark）<ul><li>为了修正在并发标记期间，因用户程序继续运行而导致标记产生变动的那一部分对象的标记记录，仍然需要暂停所有的工作线程，由于并发标记时，用户线程依然运行，因此在正式清理前，在做修正</li></ul></li><li>并发清除（CMS concurrent sweep）和用户线程一起<ul><li>清除GC Roots不可达对象，和用户线程一起工作，不需要暂停工作线程。基于标记结果，直接清理对象，由于耗时最长的并发标记和并发清除过程中，垃圾收集线程可以和用户现在一起并发工作，所以总体上来看CMS收集器的内存回收和用户线程是一起并发地执行。</li></ul></li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212826.png" alt=""></p><p>优点：并发收集低停顿</p><p>缺点：并发执行，对CPU资源压力大，采用的标记清除算法会导致大量碎片</p><p>由于并发进行，CMS在收集与应用线程会同时增加对堆内存的占用，也就是说，CMS必须在老年代堆内存用尽之前完成垃圾回收，否则CMS回收失败时，将触发担保机制，串行老年代收集器将会以STW方式进行一次GC，从而造成较大的停顿时间</p><p>标记清除算法无法整理空间碎片，老年代空间会随着应用时长被逐步耗尽，最后将不得不通过担保机制对堆内存进行压缩，CMS也提供了参数 -XX:CMSFullGCSBeForeCompaction（默认0，即每次都进行内存整理）来指定多少次CMS收集之后，进行一次压缩的Full GC</p><h1 id="11、提出问题"><a href="#11、提出问题" class="headerlink" title="11、提出问题"></a>11、提出问题</h1><blockquote><p>为什么新生代采用复制算法，老年代采用标整算法?</p></blockquote><h2 id="11-1、新生代使用复制算法"><a href="#11-1、新生代使用复制算法" class="headerlink" title="11.1、新生代使用复制算法"></a>11.1、新生代使用复制算法</h2><p>因为新生代对象的生存时间比较短，80%的都要回收的对象，采用标记-清除算法则内存碎片化比较严重，采用复制算法可以灵活高效，且便与整理空间。</p><h2 id="11-2、老年代采用标记整理"><a href="#11-2、老年代采用标记整理" class="headerlink" title="11.2、老年代采用标记整理"></a>11.2、老年代采用标记整理</h2><p>标记整理算法主要是为了解决标记清除算法存在内存碎片的问题，又解决了复制算法两个Survivor区的问题，因为老年代的空间比较大，不可能采用复制算法，特别占用内存空间</p><h1 id="12、垃圾收集器如何选择"><a href="#12、垃圾收集器如何选择" class="headerlink" title="12、垃圾收集器如何选择"></a>12、垃圾收集器如何选择</h1><h2 id="12-1、组合的选择"><a href="#12-1、组合的选择" class="headerlink" title="12.1、组合的选择"></a>12.1、组合的选择</h2><ul><li>单CPU或者小内存，单机程序<ul><li><code>-XX:+UseSerialGC</code></li></ul></li><li>多CPU，需要最大的吞吐量，如后台计算型应用<ul><li><code>-XX:+UseParallelGC</code>（这两个相互激活）</li><li><code>-XX:+UseParallelOldGC</code></li></ul></li><li>多CPU，追求低停顿时间，需要快速响应如互联网应用<ul><li><code>-XX:+UseConcMarkSweepGC</code></li><li><code>-XX:+ParNewGC</code></li></ul></li></ul><table><thead><tr><th align="center">参数</th><th align="center">新生代垃圾收集器</th><th align="center">新生代算法</th><th align="center">老年代垃圾收集器</th><th align="center">老年代算法</th></tr></thead><tbody><tr><td align="center">-XX:+UseSerialGC</td><td align="center">SerialGC</td><td align="center">复制</td><td align="center">SerialOldGC</td><td align="center">标记整理</td></tr><tr><td align="center">-XX:+UseParNewGC</td><td align="center">ParNew</td><td align="center">复制</td><td align="center">SerialOldGC</td><td align="center">标记整理</td></tr><tr><td align="center">-XX:+UseParallelGC</td><td align="center">Parallel [Scavenge]</td><td align="center">复制</td><td align="center">Parallel Old</td><td align="center">标记整理</td></tr><tr><td align="center">-XX:+UseConcMarkSweepGC</td><td align="center">ParNew</td><td align="center">复制</td><td align="center">CMS + Serial Old的收集器组合，Serial Old作为CMS出错的后备收集器</td><td align="center">标记清除</td></tr><tr><td align="center">-XX:+UseG1GC</td><td align="center">G1整体上采用标记整理算法</td><td align="center">局部复制</td><td align="center"></td><td align="center"></td></tr></tbody></table><h1 id="13、G1垃圾收集器"><a href="#13、G1垃圾收集器" class="headerlink" title="13、G1垃圾收集器"></a>13、G1垃圾收集器</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212623.png" alt=""></p><h2 id="13-1、开启G1垃圾收集器"><a href="#13-1、开启G1垃圾收集器" class="headerlink" title="13.1、开启G1垃圾收集器"></a>13.1、开启G1垃圾收集器</h2><pre><code>-XX:+UseG1GC</code></pre><h2 id="13-2、以前收集器的特点"><a href="#13-2、以前收集器的特点" class="headerlink" title="13.2、以前收集器的特点"></a>13.2、以前收集器的特点</h2><ul><li>年轻代和老年代是各自独立且连续的内存块</li><li>年轻代收集使用单eden + S0 + S1 进行复制算法</li><li>老年代收集必须扫描珍整个老年代区域</li><li>都是以尽可能少而快速地执行GC为设计原则</li></ul><h2 id="13-3、G1是什么"><a href="#13-3、G1是什么" class="headerlink" title="13.3、G1是什么"></a>13.3、G1是什么</h2><p>G1：Garbage-First 收集器，是一款面向服务端应用的收集器，应用在多处理器和大容量内存环境中，在实现高吞吐量的同时，尽可能满足垃圾收集暂停时间的要求。</p><blockquote><p>它还具有下面特征：</p></blockquote><ul><li>像CMS收集器一样，能与应用程序并发执行</li><li>整理空闲空间更快</li><li>需要更多的时间来预测GC停顿时间</li><li>不希望牺牲大量的吞吐量性能</li><li>不需要更大的Java Heap</li></ul><blockquote><p>G1收集器设计目标是取代CMS收集器，它同CMS相比，在以下方面表现的更出色</p></blockquote><ul><li>G1是一个有整理内存过程的垃圾收集器，不会产生很多内存碎片。</li><li>G1的Stop The World（STW）更可控，G1在停顿时间上添加了预测机制，用户可以指定期望停顿时间。</li></ul><blockquote><p>G1收集器简介</p></blockquote><p>CMS垃圾收集器虽然减少了暂停应用程序的运行时间，但是它还存在着内存碎片问题。于是，为了去除内存碎片问题，同时又保留CMS垃圾收集器低暂停时间的优点，JAVA7发布了一个新的垃圾收集器-G1垃圾收集器</p><p>G1是在2012在JDK1.7中可用，Oracle官方计划在JDK9中将G1变成默认的垃圾收集器以替代CMS，它是一款面向服务端应用的收集器，主要应用在多CPU和大内存服务器环境下，极大减少垃圾收集的停顿时间，全面提升服务器的性能，逐步替换Java8以前的CMS收集器</p><blockquote><p>主要改变：</p></blockquote><p>Eden，Survivor和Tenured等内存区域不再是连续了，而是变成一个个大小一样的region，每个region从1M到32M不等。一个region有可能属于Eden，Survivor或者Tenured内存区域。</p><h2 id="13-4、特点"><a href="#13-4、特点" class="headerlink" title="13.4、特点"></a>13.4、特点</h2><ul><li>G1能充分利用多CPU，多核环境硬件优势，尽量缩短STW</li><li>G1整体上采用标记-整理算法，局部是通过复制算法，不会产生内存碎片</li><li>宏观上看G1之中不再区分年轻代和老年代。把内存划分成多个独立的子区域（Region），可以近似理解为一个围棋的棋盘</li><li>G1收集器里面将整个内存区域都混合在一起了，但其本身依然在小范围内要进行年轻代和老年代的区分，保留了新生代和老年代，但他们不再是物理隔离的，而是通过一部分Region的集合且不需要Region是连续的，也就是说依然会采取不同的GC方式来处理不同的区域</li><li>G1虽然也是分代收集器，但整个内存分区不存在物理上的年轻代和老年代的区别，也不需要完全独立的Survivor（to space）堆做复制准备，G1只有逻辑上的分代概念，或者说每个分区都可能随G1的运行在不同代之间前后切换。</li></ul><h2 id="13-5、底层原理"><a href="#13-5、底层原理" class="headerlink" title="13.5、底层原理"></a>13.5、底层原理</h2><p>Region区域化垃圾收集器，化整为零，打破了原来新生区和老年区的壁垒，避免了全内存扫描，只需要按照区域来进行扫描即可。</p><p>区域化内存划片Region，整体遍为了一些列不连续的内存区域，避免了全内存区的GC操作。</p><p>核心思想是将整个堆内存区域分成大小相同的子区域（Region），在JVM启动时会自动设置子区域大小</p><p>在堆的使用上，G1并不要求对象的存储一定是物理上连续的，只要逻辑上连续即可，每个分区也不会固定地为某个代服务，可以按需在年轻代和老年代之间切换。启动时可以通过参数<code>-XX:G1HeapRegionSize=n</code> 可指定分区大小（1MB~32MB，且必须是2的幂），默认将整堆划分为2048个分区。</p><p>大小范围在1MB~32MB，最多能设置2048个区域，也即能够支持的最大内存为：32MB*2048 = 64G内存</p><p>Region区域化垃圾收集器</p><h2 id="13-6、Region区域化垃圾收集器"><a href="#13-6、Region区域化垃圾收集器" class="headerlink" title="13.6、Region区域化垃圾收集器"></a>13.6、Region区域化垃圾收集器</h2><blockquote><p>G1将新生代、老年代的物理空间划分取消了</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212614.png" alt=""></p><blockquote><p>同时对内存进行了区域划分</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212608.png" alt=""></p><p>G1算法将堆划分为若干个区域（Reign），它仍然属于分代收集器，这些Region的一部分包含新生代，新生代的垃圾收集依然采用暂停所有应用线程的方式，将存活对象拷贝到老年代或者Survivor空间</p><p>这些Region的一部分包含老年代，G1收集器通过将对象从一个区域复制到另外一个区域，完成了清理工作。这就意味着，在正常的处理过程中，G1完成了堆的压缩（至少是部分堆的压缩），这样也就不会有CMS内存碎片的问题存在了。</p><p>在G1中，还有一种特殊的区域，叫做Humongous（巨大的）区域，如果一个对象占用了空间超过了分区容量50%以上，G1收集器就认为这是一个巨型对象，这些巨型对象默认直接分配在老年代，但是如果他是一个短期存在的巨型对象，就会对垃圾收集器造成负面影响，为了解决这个问题，G1划分了一个Humongous区，它用来专门存放巨型对象。如果一个H区装不下一个巨型对象，那么G1会寻找连续的H区来存储，为了能找到连续的H区，有时候不得不启动Full GC。</p><h2 id="13-7、回收步骤"><a href="#13-7、回收步骤" class="headerlink" title="13.7、回收步骤"></a>13.7、回收步骤</h2><blockquote><p>针对Eden区进行收集，Eden区耗尽后会被触发，主要是<strong>小区域收集 + 形成连续的内存块</strong>，避免内碎片</p></blockquote><ul><li>Eden区的数据移动到Survivor区，加入出现Survivor区空间不够，Eden区数据会晋升到Old区</li><li>Survivor区的数据移动到新的Survivor区，部分数据晋升到Old区</li><li>最后Eden区收拾干净了，GC结束，用户的应用程序继续执行</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212600.png" alt=""></p><blockquote><p>回收完成后</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212552.png" alt=""></p><p>最后在收集完成后，就会形成连续的内存空间，这样就解决了内存碎片的问题</p><h2 id="13-8、四步过程"><a href="#13-8、四步过程" class="headerlink" title="13.8、四步过程"></a>13.8、四步过程</h2><ul><li>初始标记：只标记GC Roots能直接关联到的对象</li><li>并发标记：进行GC Roots Tracing（链路扫描）的过程</li><li>最终标记：修正并发标记期间，因为程序运行导致标记发生变化的那一部分对象</li><li>筛选回收：根据时间来进行价值最大化回收</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716212540.png" alt=""></p><h2 id="13-9、参数配置"><a href="#13-9、参数配置" class="headerlink" title="13.9、参数配置"></a>13.9、参数配置</h2><p>开发人员仅仅需要申明以下参数即可</p><p>三步归纳：<code>-XX:+UseG1GC  -Xmx32G  -XX:MaxGCPauseMillis=100</code></p><p>-XX:MaxGCPauseMillis=n：最大GC停顿时间单位毫秒，这是个软目标，JVM尽可能停顿小于这个时间</p><h2 id="13-10、G1和CMS比较"><a href="#13-10、G1和CMS比较" class="headerlink" title="13.10、G1和CMS比较"></a>13.10、G1和CMS比较</h2><ul><li>G1不会产生内碎片</li><li>G1可以精准控制停顿。</li><li>G1把整个堆（新生代、老年代）划分成多个固定大小的区域，</li><li>G1每次根据允许停顿的时间去收集垃圾最多的区域。</li></ul><h1 id="14、SpringBoot结合JVMGC"><a href="#14、SpringBoot结合JVMGC" class="headerlink" title="14、SpringBoot结合JVMGC"></a>14、SpringBoot结合JVMGC</h1><p>启动微服务时候，就可以带上JVM和GC的参数</p><ul><li><p>IDEA开发完微服务工程</p></li><li><p>maven进行clean package</p></li><li><p>要求微服务启动的时候，同时配置我们的JVM/GC的调优参数</p><ul><li><p>我们就可以根据具体的业务配置我们启动的JVM参数</p></li><li><p>例如：</p><pre><code class="bash">java -Xms1024m -Xmx1024 -XX:UseG1GC -jar   xxx.jar</code></pre></li></ul></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、GC垃圾回收算法和垃圾收集器关系&quot;&gt;&lt;a href=&quot;#01、GC垃圾回收算法和垃圾收集器关系&quot; class=&quot;headerlink&quot; title=&quot;01、GC垃圾回收算法和垃圾收集器关系&quot;&gt;&lt;/a&gt;01、GC垃圾回收算法和垃圾收集器关系&lt;/h1&gt;&lt;bloc
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JVM" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JVM/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>01Linux诊断原因</title>
    <link href="http://lsqstudy.top/post/63d9b5b8.html"/>
    <id>http://lsqstudy.top/post/63d9b5b8.html</id>
    <published>2020-07-15T16:00:00.000Z</published>
    <updated>2020-07-16T15:08:45.133Z</updated>
    
    <content type="html"><![CDATA[<h2 id="01、命令集合"><a href="#01、命令集合" class="headerlink" title="01、命令集合"></a>01、命令集合</h2><h2 id="1-1、整机：top，查看整机系统新能"><a href="#1-1、整机：top，查看整机系统新能" class="headerlink" title="1.1、整机：top，查看整机系统新能"></a>1.1、整机：top，查看整机系统新能</h2><pre><code class="bash">#q:退出[root@lsqstudy ~]# top</code></pre><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230719.png" alt=""></p><blockquote><p>重点指标： %CPU、%MEM 、load average</p></blockquote><p>按1的话，查看每个CPU的占用情况（多核CPU）</p><pre><code class="bash">#系统性能命令的精简版[root@lsqstudy ~]# uptime </code></pre><h2 id="1-2、CPU：vmstat"><a href="#1-2、CPU：vmstat" class="headerlink" title="1.2、CPU：vmstat"></a>1.2、CPU：vmstat</h2><ul><li>查看CPU（包含但是不限于）</li><li>查看额外<ul><li>查看所有CPU核信息：mpstat -p ALL 2</li><li>每个进程使用CPU的用量分解信息：pidstat -u 1 -p 进程编号</li></ul></li></ul><p>命令格式：<code>vmstat -n 2 3</code></p><p>说明：</p><ul><li>第一个参数2是时间间隔数（单位秒）</li><li>第二个参数3是采样的次数</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230728.png" alt=""></p><p>参数说明：</p><ul><li><strong>procs</strong><ul><li><strong>r</strong>：运行和等待的CPU时间片的进程数，原则上1核的CPU的运行队列不要超过2，整个系统的运行队列不超过总核数的2倍，否则代表系统压力过大</li><li><strong>b</strong>：等待资源的进程数，比如正在等待磁盘I/O、网络I/O等</li></ul></li><li><strong>cpu</strong><ul><li><strong>us</strong>：用户进程消耗CPU时间百分比，us值高，用户进程消耗CPU时间多，如果长期大于50%，优化程序</li><li><strong>sy</strong>：内核进程消耗的CPU时间百分比</li><li><strong>us + sy</strong> 参考值为80%，如果us + sy 大于80%，说明可能存在CPU不足</li><li><strong>id</strong>：处于空闲的CPU百分比</li><li><strong>wa</strong>：系统等待IO的CPU时间百分比</li><li><strong>st</strong>：来自于一个虚拟机偷取的CPU时间比</li></ul></li></ul><h2 id="1-3、内存：free"><a href="#1-3、内存：free" class="headerlink" title="1.3、内存：free"></a>1.3、内存：free</h2><ul><li><p>应用程序可用内存数：<code>free -m</code></p></li><li><p>应用程序可用内存/系统物理内存 &gt; 70% 内存充足</p></li><li><p>应用程序可用内存/系统物理内存 &lt; 20% 内存不足，需要增加内存</p></li><li><p>20% &lt;  应用程序可用内存/系统物理内存 &lt; 70%，表示内存基本够用</p></li></ul><p><code>free -h</code>：以人类能看懂的方式查看物理内存</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230732.png" alt=""></p><p><code>free -m</code>：以MB为单位，查看物理内存</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230736.png" alt=""></p><p><code>free -g</code>：不推荐，以GB为单位，查看物理内存</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230739.png" alt=""></p><h2 id="1-4、硬盘：df"><a href="#1-4、硬盘：df" class="headerlink" title="1.4、硬盘：df"></a>1.4、硬盘：df</h2><p>格式：<code>df -h  /</code>  ：-h：human，表示以人类能看到的方式换算</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230743.png" alt=""></p><blockquote><p>硬盘IO：iostat</p></blockquote><p>系统慢有两种原因引起的，一个是CPU高，一个是大量IO操作</p><p>格式：<code>iostat -xdk 2 3</code></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230746.png" alt=""></p><p>磁盘块设备分布：</p><ul><li><strong>rkB /s</strong>：每秒读取数据量kB；</li><li><strong>wkB/s</strong>：每秒写入数据量kB；</li><li><strong>svctm I/O</strong>：请求的平均服务时间，单位毫秒</li><li><strong>await I/O</strong>：请求的平均等待时间，单位毫秒，值越小，性能越好</li><li><strong>util</strong>：一秒钟有百分几的时间用于I/O操作。接近100%时，表示磁盘带宽跑满，需要优化程序或者增加磁盘；</li><li><strong>rkB/s</strong>，wkB/s根据系统应用不同会有不同的值，但有规律遵循：长期、超大数据读写，肯定不正常，需要优化程序读取。</li><li>svctm的值与await的值很接近，表示几乎没有I/O等待，磁盘性能好，如果await的值远高于svctm的值，则表示I/O队列等待太长，需要优化程序或更换更快磁盘</li></ul><h2 id="1-5、网络IO：ifstat"><a href="#1-5、网络IO：ifstat" class="headerlink" title="1.5、网络IO：ifstat"></a>1.5、网络IO：ifstat</h2><ul><li>默认本地没有，下载ifstat</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230750.png" alt=""></p><h1 id="02、生产环境服务器变慢，诊断思路和性能评估"><a href="#02、生产环境服务器变慢，诊断思路和性能评估" class="headerlink" title="02、生产环境服务器变慢，诊断思路和性能评估"></a>02、生产环境服务器变慢，诊断思路和性能评估</h1><p>记一次印象深刻的故障？</p><blockquote><p>结合Linux 和 JDK命令一起分析，步骤如下</p></blockquote><ol><li><p>使用<code>top</code>命令找出CPU占比最高的<br><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230754.png" alt=""></p></li><li><p><code>ps -ef</code> 或者 <code>jps</code> 进一步定位，得知是一个怎么样的后台程序出的问题<br><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230800.png" alt=""></p></li><li><p>定位到具体线程或者代码</p><ul><li><p><code>ps -mp 进程  -o THREAD，tid，time</code>：tid是线程id</p></li><li><p>参数解释</p><ul><li><code>-m</code>：显示所有的线程</li><li><code>-p</code>：pid进程使用CPU的时间</li><li><code>-o</code>：该参数后是用户自定义格式</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230804.png" alt=""></p></li></ul></li></ol><ul><li><p>将需要的线程ID转换为16进制格式（<strong>英文小写格式</strong>）</p><ul><li><code>printf “%x” 有问题的线程ID</code></li></ul></li><li><p><code>jstack 进程ID | grep tid（16进制线程ID小写英文） -A60</code>：精准定位到错误的地方<br><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/03/20200716230817.png" alt=""></p></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;01、命令集合&quot;&gt;&lt;a href=&quot;#01、命令集合&quot; class=&quot;headerlink&quot; title=&quot;01、命令集合&quot;&gt;&lt;/a&gt;01、命令集合&lt;/h2&gt;&lt;h2 id=&quot;1-1、整机：top，查看整机系统新能&quot;&gt;&lt;a href=&quot;#1-1、整机：top，查看整
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="Linux" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/Linux/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>02垃圾回收算法</title>
    <link href="http://lsqstudy.top/post/7ff67db7.html"/>
    <id>http://lsqstudy.top/post/7ff67db7.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.664Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、垃圾回收算法"><a href="#01、垃圾回收算法" class="headerlink" title="01、垃圾回收算法"></a>01、垃圾回收算法</h1><h2 id="1-1、引用计数法"><a href="#1-1、引用计数法" class="headerlink" title="1.1、引用计数法"></a>1.1、引用计数法</h2><blockquote><p>示例代码</p></blockquote><pre><code class="java">/**@Description:-verbose:gc*/public class RefCountGC{  private byte[] bigSize = new byte[2 * 1024 * 1024];//这个成员属性唯一的作用就是占用一点内存  Object instance = null;  public static void main(String[] args)  {    RefCountGC objectA = new RefCountGC();    RefCountGC objectB = new RefCountGC();    objectA.instance = objectB;    objectB.instance = objectA;    objectA = null;    objectB = null;    System.gc();  }}</code></pre><h2 id="1-2、复制算法-Copying"><a href="#1-2、复制算法-Copying" class="headerlink" title="1.2、复制算法(Copying)"></a>1.2、复制算法(Copying)</h2><blockquote><p>复制算法（Copying）一般用于新生代</p></blockquote><p>因为新生代的GC非常频繁，每次GC的对象较多，存活的对象较少。<br>所以采用复制算法效率更高，复制时只需要复制少量存活的对象。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714104128.png" alt=""></p><h3 id="1-2-1、原理"><a href="#1-2-1、原理" class="headerlink" title="1.2.1、原理"></a>1.2.1、原理</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122113.png" alt=""> </p><p>Minor GC会把Eden中的所有活的对象都移到Survivor区域中，如果Survivor区中放不下，那么剩下的活的对象就被移到Old  generation中，<strong>也即一旦收集后，Eden是就变成空的了</strong>。<br>  当对象在 Eden ( 包括一个 Survivor 区域，这里假设是 from 区域 ) 出生后，在经过一次 Minor GC 后，如果对象还存活，并且能够被另外一块 Survivor 区域所容纳( 上面已经假设为 from 区域，这里应为 to 区域，即 to 区域有足够的内存空间来存储 Eden 和 from 区域中存活的对象 )，则使用<strong>复制算法</strong>将这些仍然还存活的对象复制到另外一块 Survivor 区域 ( 即 to 区域 ) 中，然后清理所使用过的 Eden 以及 Survivor 区域 ( 即 from 区域 )，并且将这些对象的年龄设置为1，以后对象在 Survivor 区每熬过一次 Minor GC，就将对象的年龄 + 1，当对象的年龄达到某个值时 ( 默认是 15 岁，通过<code>-XX:MaxTenuringThreshold</code> 来设定参数)，这些对象就会成为老年代。</p><p><code>-XX:MaxTenuringThreshold</code> — 设置对象在新生代中存活的次数</p><blockquote><p>动态年龄阈值</p></blockquote><p>JVM并不要求对象年龄一定要达到 MaxTenuringThreshold 才会<br>晋升到老年代，晋升的年龄阈值是动态计算的。￼￼￼￼￼<br>如果在Survivor中，某个相同年龄阶段的所有对象大小的总和<br>大于Survivor区域的一半，则大于等于这个年龄的所有对象<br>可以直接进入老年代，无需等到MaxTenuringThreshold。</p><h3 id="1-2-2、解释"><a href="#1-2-2、解释" class="headerlink" title="1.2.2、解释"></a>1.2.2、解释</h3><p> HotSpot JVM把年轻代分为了三部分：1个Eden区和2个Survivor区（分别叫from和to）。默认比例为8:1:1,一般情况下，新创建的对象都会被分配到Eden区(一些大对象特殊处理),这些对象经过第一次Minor GC后，如果仍然存活，将会被移到Survivor区。对象在Survivor区中每熬过一次Minor GC，年龄就会增加1岁，当它的年龄增加到一定程度时，就会被移动到年老代中。因为年轻代中的对象基本都是朝生夕死的(90%以上)，所以<strong>在年轻代的垃圾回收算法使用的是复制算法</strong>，复制算法的基本思想就是将内存分为两块，每次只用其中一块，当这一块内存用完，就将还活着的对象复制到另外一块上面。<strong>复制算法不会产生内存碎片</strong>。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122108.png" alt=""></p><p>在GC开始的时候，对象只会存在于Eden区和名为“From”的Survivor区，Survivor区“To”是空的。紧接着进行GC，Eden区中所有存活的对象都会被复制到“To”，而在“From”区中，仍存活的对象会根据他们的年龄值来决定去向。年龄达到一定值(年龄阈值，可以通过-XX:MaxTenuringThreshold来设置)的对象会被移动到年老代中，没有达到阈值的对象会被复制到“To”区域。<strong>经过这次GC后，Eden区和From区已经被清空。这个时候，“From”和“To”会交换他们的角色，也就是新的“To”就是上次GC前的“From”，新的“From”就是上次GC前的“To”。</strong>不管怎样，都会保证名为To的Survivor区域是空的。Minor GC会一直重复这样的过程，直到“To”区被填满，“To”区被填满之后，会将所有对象移动到年老代中。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122105.png" alt=""></p><p>因为Eden区对象一般存活率较低，一般的，使用两块10%的内存作为空闲和活动区间，而另外80%的内存，则是用来给新建对象分配内存的。一旦发生GC，将10%的from活动区间与另外80%中存活的eden对象转移到10%的to空闲区间，接下来，将之前90%的内存全部释放，以此类推。 </p><h3 id="1-2-3、优缺点"><a href="#1-2-3、优缺点" class="headerlink" title="1.2.3、优缺点"></a>1.2.3、优缺点</h3><blockquote><p>1、优点</p></blockquote><p>复制算法的优点是不会产生连续的内存碎片，速度也很高效。</p><blockquote><p>2、缺点</p></blockquote><p>1、每次只使用内存的一半，就代表可使用的内存减少了1/2，代价很高昂。</p><p>2、如果对象的存活率很高，假设是100%存活，那么我们需要将所有对象都复制一遍，并将所有引用地址重置一遍。复制这一工作所花费的时间，在对象存活率达到一定程度时，将会变的不可忽视。 所以从以上描述不难看出，复制算法要想使用，<strong>最起码对象的存活率要非常低才行</strong>，而且最重要的是，我们必须要克服50%内存的浪费。</p><h2 id="1-3、标记清除-Mark-Sweep"><a href="#1-3、标记清除-Mark-Sweep" class="headerlink" title="1.3、标记清除(Mark-Sweep)"></a>1.3、标记清除(Mark-Sweep)</h2><blockquote><p>老年代一般是由标记清除或者是标记清除与标记整理的混合实现</p></blockquote><p>因为老年代中的对象存活率较高，几乎很少被回收，所以标记-清除和标记-整理算法GC的时间不会太长，<br>GC的对象相比新生代更少</p><h3 id="1-3-1、原理"><a href="#1-3-1、原理" class="headerlink" title="1.3.1、原理"></a>1.3.1、原理</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122101.png" alt=""></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122059.png" alt=""></p><p>当程序运行期间，若可以使用的内存被耗尽的时候，GC线程就会被触发并将程序暂停，随后将要回收的对象标记一遍，最终统一回收这些对象，完成标记清理工作接下来便让应用程序恢复运行。</p><blockquote><p>标记-清除算法分为2个步骤，标记、清除。</p></blockquote><p>  标记：从引用根节点开始标记遍历所有的GC Roots， 先标记出要回收的对象。<br>  清除：遍历整个堆，把标记的对象清除。 </p><h3 id="1-3-2、缺点"><a href="#1-3-2、缺点" class="headerlink" title="1.3.2、缺点"></a>1.3.2、缺点</h3><ol><li><strong>标记和清除2个阶段的耗时都比较长，可以总结为效率较低。</strong></li><li><strong>此算法需要暂停整个应用，会产生内存碎片。</strong>当内存碎片过多后，后续想要分配较大的对象时，无法找到足够大的内存碎片，可能又需要触发GC。 </li></ol><h2 id="1-4、标记压缩-Mark-Compact"><a href="#1-4、标记压缩-Mark-Compact" class="headerlink" title="1.4、标记压缩(Mark-Compact)"></a>1.4、标记压缩(Mark-Compact)</h2><p>标记压缩也叫标记-整理算法。是对标记-清除算法的一种改进。</p><blockquote><p>老年代一般是由标记清除或者是标记清除与标记整理的混合实现</p></blockquote><h3 id="1-4-1、原理"><a href="#1-4-1、原理" class="headerlink" title="1.4.1、原理"></a>1.4.1、原理</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122055.png" alt=""></p><p>标记-整理算法与标记-清除算法的在标记阶段是相同的，都是首先标记出所有可达(存活)的对象。<br>但<strong>标记之后并不直接清理未被标记(不可达)的对象，而是使被标记(存活)的对象向内存一端移动，然后清理掉这一端外的内存。</strong></p><h3 id="1-4-2、优缺点"><a href="#1-4-2、优缺点" class="headerlink" title="1.4.2、优缺点"></a>1.4.2、优缺点</h3><blockquote><p>优点</p></blockquote><p>几乎不会如标记-清除算法那样产生不连续的内存碎片。但所谓慢工出细活,标记-整理的效率是比标记-清除要低。</p><blockquote><p>缺点</p></blockquote><p>效率也不高，不仅要标记所有存活对象，还要整理所有存活对象的引用地址。</p><p>从效率上来说，标记/整理算法要低于复制算法。</p><h3 id="1-4-3、分代收集算法"><a href="#1-4-3、分代收集算法" class="headerlink" title="1.4.3、分代收集算法"></a>1.4.3、分代收集算法</h3><p><strong>分代收集算法并不是指某一种具体的垃圾收集算法，<br>而是将复制，标记-清除，标记-整理等算法合理运用到堆区的不同空间。</strong><br>比如新生代使用复制算法，老年代使用标记清除或标记整理算法。</p><p>现代的几乎所有的JVM都使用分代收集，毕竟每种算法都有优缺点，<br>结合它们的特点，对不同的环境采用不同的算法是非常明智的选择。</p><h2 id="1-5、内存分配与垃圾回收策略"><a href="#1-5、内存分配与垃圾回收策略" class="headerlink" title="1.5、内存分配与垃圾回收策略"></a>1.5、内存分配与垃圾回收策略</h2><ol><li>对象优先在eden区域被分配</li><li>大对象将直接进入老年代(大对象是指需要大量连续的内存空间的对象，如长字符串，大数组等。)</li><li>长期存活的对象将进入老年代</li></ol><h1 id="02、总结"><a href="#02、总结" class="headerlink" title="02、总结"></a>02、总结</h1><ul><li>内存效率：复制算法&gt;标记清除算法&gt;标记整理算法（此处的效率只是简单的对比时间复杂度，实际情况不一定如此）。 </li><li>内存整齐度：复制算法=标记整理算法&gt;标记清除算法。 </li><li>内存利用率：标记整理算法=标记清除算法&gt;复制算法。 </li></ul><p>可以看出，效率上来说，复制算法是当之无愧的老大，但是却浪费了太多内存，而为了尽量兼顾上面所提到的三个指标，标记/整理算法相对来说更平滑一些，但效率上依然不尽如人意，它比复制算法多了一个标记的阶段，又比标记/清除多了一个整理内存的过程</p><blockquote><p>难道就没有一种最优算法吗？ </p></blockquote><p> 回答：无，没有最好的算法，只有最合适的算法。==========&gt;分代收集算法。</p><blockquote><p>年轻代(Young Gen)  </p></blockquote><p>年轻代特点是区域相对老年代较小，对像存活率低。</p><p>这种情况复制算法的回收整理，速度是最快的。复制算法的效率只和当前存活对像大小有关，因而很适用于年轻代的回收。而复制算法内存利用率不高的问题，通过hotspot中的两个survivor的设计得到缓解。</p><blockquote><p>老年代(Tenure Gen)</p></blockquote><p>老年代的特点是区域较大，对像存活率高。</p><p>这种情况，存在大量存活率高的对像，复制算法明显变得不合适。一般是由标记清除或者是标记清除与标记整理的混合实现。</p><p>Mark阶段的开销与存活对像的数量成正比，这点上说来，对于老年代，标记清除或者标记整理有一些不符，但可以通过多核/线程利用，对并发、并行的形式提标记效率。</p><p>Sweep阶段的开销与所管理区域的大小形正相关，但Sweep“就地处决”的特点，回收的过程没有对像的移动。使其相对其它有对像移动步骤的回收算法，仍然是效率最好的。但是需要解决内存碎片问题。</p><p>Compact阶段的开销与存活对像的数据成开比，如上一条所描述，对于大量对像的移动是很大开销的，做为老年代的第一选择并不合适。</p><p>基于上面的考虑，老年代一般是由标记清除或者是标记清除与标记整理的混合实现。以hotspot中的CMS回收器为例，CMS是基于Mark-Sweep实现的，对于对像的回收效率很高，而对于碎片问题，CMS采用基于Mark-Compact算法的Serial Old回收器做为补偿措施：当内存回收不佳（碎片导致的Concurrent Mode Failure时），将采用Serial Old执行Full GC以达到对老年代内存的整理。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、垃圾回收算法&quot;&gt;&lt;a href=&quot;#01、垃圾回收算法&quot; class=&quot;headerlink&quot; title=&quot;01、垃圾回收算法&quot;&gt;&lt;/a&gt;01、垃圾回收算法&lt;/h1&gt;&lt;h2 id=&quot;1-1、引用计数法&quot;&gt;&lt;a href=&quot;#1-1、引用计数法&quot; class
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="GC" scheme="http://lsqstudy.top/categories/Java/GC/"/>
    
    
      <category term="GC" scheme="http://lsqstudy.top/tags/GC/"/>
    
  </entry>
  
  <entry>
    <title>03垃圾回收器</title>
    <link href="http://lsqstudy.top/post/194c267.html"/>
    <id>http://lsqstudy.top/post/194c267.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.664Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、垃圾回收器"><a href="#01、垃圾回收器" class="headerlink" title="01、垃圾回收器"></a>01、垃圾回收器</h1><p><strong>如果说垃圾回收算法是JVM对GC算法的方法论，那么垃圾回收器就是对GC算法的实现。</strong></p><blockquote><p>垃圾回收器主要分为以下几种收集器:</p></blockquote><ul><li><p>Serial收集器</p></li><li><p>Parallel Scanvel收集器</p></li><li><p>ParNew收集器</p></li><li><p>CMS收集器</p></li><li><p>G1收集器</p></li></ul><h2 id="1-1、Serial串行收集器"><a href="#1-1、Serial串行收集器" class="headerlink" title="1.1、Serial串行收集器"></a>1.1、Serial串行收集器</h2><blockquote><p>Serial收集器采用复制算法</p></blockquote><p>Serial收集器为单线程环境设计,并只使用一个线程进行垃圾回收。<br>在回收时，会暂停用户线程,并不适用于并发环境。</p><p>Serial收集器在单线程环境中是很高效的,它没有多线程切换的消耗。     </p><h4 id="1-1-1、Serial-Old-串行收集器-老年代版本"><a href="#1-1-1、Serial-Old-串行收集器-老年代版本" class="headerlink" title="1.1.1、Serial Old 串行收集器(老年代版本)"></a>1.1.1、Serial Old 串行收集器(老年代版本)</h4><blockquote><p>Serial Old收集器采用的是：标记-整理算法</p></blockquote><p>它是 Serial收集器的老年代使用的GC收集器，同样是一个单线程的垃圾收集器。 </p><pre><code class="text">/** 开启串行收集器使用 -XX:+UseSerialGC ,   * 这样默认新生代使用 Serial 收集器,  * 老年代使用 Serial Old 收集器.   *  * 设置VM参数:  * -XX:+Xlogs:gc* 打印gc信息  * -XX:+PrintCommandLineFlags  打印java版本信息  * -XX:+UseSerialGC 使用串行GC  */                      //如果程序正常运行,日志会显示 :// 新生代的信息为:  def new generation.....// 老年代的信息为:  tenured generation.....</code></pre><h2 id="1-2、Parallel-Scavenge-并行多线程收集器"><a href="#1-2、Parallel-Scavenge-并行多线程收集器" class="headerlink" title="1.2、Parallel Scavenge 并行多线程收集器"></a>1.2、Parallel Scavenge 并行多线程收集器</h2><blockquote><p>Parallel Scavenge收集器采用：复制算法</p></blockquote><p>Parallel Scavenge是并行收集器，它使用多个垃圾回收线程一起工作,但是仍然会暂停用户线程。</p><p>Parallel Scavenge与其它垃圾回收器不同的是它<strong>更关注于达到可控制的吞吐量。</strong></p><p>吞吐量是CPU运行用户应用程序代码的时间与CPU总消耗的时间的比值:</p><pre><code class="text">吞吐量 = 应用程序代码运行时间 / (应用程序代码运行时间 + GC时间)</code></pre><h3 id="1-2-1、Parallel-Old-并行收集器-老年代版本"><a href="#1-2-1、Parallel-Old-并行收集器-老年代版本" class="headerlink" title="1.2.1、Parallel Old 并行收集器(老年代版本)"></a>1.2.1、Parallel Old 并行收集器(老年代版本)</h3><p>它是 Parallel Scavenge 的老年代版本,同样是一个并行多线程的收集器。</p><p><strong>Parallel Old收集器采用标记-整理算法。</strong></p><pre><code class="text">    /**     *      * 设置 Parallel Scavenge 收集器的参数:     * -XX:+UseParallelGC     *      * ParallelGC老年代默认使用的 Parallel Old GC 回收器     *      * 并行收集器打印的新生代的信息为:     *  PSYoungGen ....     *  老年代的信息为:     *  ParOldGen ....     *      */</code></pre><h2 id="1-3、ParNew-多线程收集器"><a href="#1-3、ParNew-多线程收集器" class="headerlink" title="1.3、ParNew 多线程收集器"></a>1.3、ParNew 多线程收集器</h2><blockquote><p>ParNew收集器采用：复制算法</p></blockquote><p>它可以看做是多线程版的Serial收集器。<br>除了多线程外，ParNew收集器与Serial收集器几乎没啥区别。</p><p><strong>PS:目前只有Serial和ParNew能作为CMS收集器的新生代收集器。</strong></p><pre><code class="text">     /**       *        * 设置ParNewGC回收器的参数为:       * -XX:+UseConcMarkSweepGC       *        */</code></pre><h2 id="1-4、CMS-并发标记清除收集器"><a href="#1-4、CMS-并发标记清除收集器" class="headerlink" title="1.4、CMS 并发标记清除收集器"></a>1.4、CMS 并发标记清除收集器</h2><blockquote><p>见名知意,CMS收集器使用的是：标记-清除算法</p></blockquote><p>Concurrent Mark Sweep,并发标记-清除垃圾回收器。<br>它是一款老年代的收集器，是<strong>以达到最短回收停顿时间目标的收集器。</strong></p><p><strong>CMS在垃圾回收过程中，用户线程可以同时工作，无需暂停。</strong></p><p><strong>因为CMS收集器采用的是标记-清除算法，所以回收时可能会产生不连续的内存碎片。</strong></p><p><strong>PS: CMS收集器在jdk14中被删除了。</strong></p><h3 id="1-4-1、CMS收集器回收过程"><a href="#1-4-1、CMS收集器回收过程" class="headerlink" title="1.4.1、CMS收集器回收过程"></a>1.4.1、CMS收集器回收过程</h3><ul><li><p>初始标记(Stop The World，此阶段会暂停用户线程): 只标记与GC ROOT直接关联的对象。</p></li><li><p>并发标记: 对第一个阶段已经标记的对象进行Tracing，标记所有可达的对象。</p></li><li><p>重新标记(Stop The World,此阶段会暂停用户线程): 在第二个阶段，由于用户程序的运行，<br>可能有些对象之间的引用关系受到了影响，所以需要对这部分对象进行重新标记调整。</p></li><li><p>并发清除: 清除所有未被标记的对象。</p></li></ul><pre><code class="text">    /**     *     * 设置 CMS 收集器参数:     * -XX:+UseConcMarkSweepGC     *     * 使用ConcMarkSweepGC收集器后,它的新生代使用的是:     * ParNew收集器.     *     * 当ConcMarkSweepGC收集器出现异常时,会将CMS替换成Serial Old收集器     *     * CMS回收分为4个阶段:     *     * 初始标记:    (Stop the world 暂停用户线程)     * 标记与GC Root直接可达的对象.           *     * 并发标记:       * 从第一步标记的可达的对象开始,并发的标记所有可达的对象      *     * 重新标记:    (Stop the world 暂停用户线程)     * 在第二部的并发标记阶段,由于程序运行导致对象间引用的关系发生变化,     * 就需要重新标记     *     * 并发清除:          * 这个阶段不暂停用户线程,并且并发的去清除未被标记的对象     *      */</code></pre><h2 id="1-5、G1-收集器"><a href="#1-5、G1-收集器" class="headerlink" title="1.5、G1 收集器"></a>1.5、G1 收集器</h2><p>G1收集器可以说是目前最前沿的一款收集器，它是一款面向服务端的收集器。<br>G1收集器<strong>无需配合其他收集器就可以管理整个堆内存。</strong><br>jdk9开始，G1成为jdk默认使用的垃圾回收器。</p><h3 id="1-5-1、G1回收器的特点"><a href="#1-5-1、G1回收器的特点" class="headerlink" title="1.5.1、G1回收器的特点"></a>1.5.1、G1回收器的特点</h3><ul><li><p>并行和并发: G1能够充分利用多核cpu的优势，使垃圾回收与用户线程同时运行。</p></li><li><p>可预测的停顿: 降低GC停顿时间是CMS与G1收集器的共同目标。但是除了降低GC停顿时间，G1收集器还可以建立可预测的停顿时间模型。(…太np了 =_=)</p></li><li><p>空间整合: 这是G1收集器不同于其他收集器的最大亮点了。在其他收集器中，堆区基本都分为新生代和老年代。而在G1收集器中虽然仍然保留了新生代和老年代的概念，但已经不再是物理上的分隔了。<strong>在G1收集器的堆内存模型中，内存被分割成了一块一块大小相等的Region，在这些Region中，Region的类型也不同，有eden，survivor，old，humongous之分。当有大对象时，对象会被分配到Humongous Region之中。</strong></p></li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714121946.png" alt="G1收集器Region"></p><h3 id="1-5-2、G1收集器回收过程"><a href="#1-5-2、G1收集器回收过程" class="headerlink" title="1.5.2、G1收集器回收过程"></a>1.5.2、G1收集器回收过程</h3><pre><code class="text">G1收集器与CMS收集器的回收过程相似</code></pre><ul><li><p>初始标记(Stop The World,此阶段会暂停用户线程): 只标记与GC ROOT直接关联的对象。</p></li><li><p>并发标记: 对第一个阶段标记的对象Tracing，标记所有可达的对象。</p></li><li><p>最终标记(Stop The World,此阶段会暂停用户线程): 在并发标记阶段，由于用户线程执行，<br>可能导致被标记对象之间的引用关系发生影响，需要对这些对象进行重新标记调整。</p></li><li><p>筛选回收: 不同于CMS的并发清除，G1收集器首先会对所有Region的回收价值和回收成本进行排序,<br>然后再进行回收。这样可以在有限的时间内获得最大的回收率。</p></li></ul><pre><code> /**     *     * 指定G1回收器的参数是: -XX:+UseG1GC     *     * 1:初始标记:(Stop the world 暂停用户线程)     *   标记所有与GC Root直接可达的对象     *     * 2:并发标记     *  从第一个阶段标记的对象开始,trace标记     *     * 4:最终标记:(Stop the world 暂停用户线程)     *  在第二步并发标记的阶段,由于程序执行,     *  导致被标记对象之间的引用关系发生变化,所以需要重新调整标记     *     * 5:筛选回收:     *  和CMS的并发回收不一样,     *  G1收集器首先会对所有Region的回收价值和回收成本进行排序,     *  然后再进行回收。     *  这样可以保证在有限的时间内获得最大的回收率.     *     */</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、垃圾回收器&quot;&gt;&lt;a href=&quot;#01、垃圾回收器&quot; class=&quot;headerlink&quot; title=&quot;01、垃圾回收器&quot;&gt;&lt;/a&gt;01、垃圾回收器&lt;/h1&gt;&lt;p&gt;&lt;strong&gt;如果说垃圾回收算法是JVM对GC算法的方法论，那么垃圾回收器就是对GC算法的
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="GC" scheme="http://lsqstudy.top/categories/Java/GC/"/>
    
    
      <category term="GC" scheme="http://lsqstudy.top/tags/GC/"/>
    
  </entry>
  
  <entry>
    <title>01GC总体概述</title>
    <link href="http://lsqstudy.top/post/697d2448.html"/>
    <id>http://lsqstudy.top/post/697d2448.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.661Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、判断对象存活的方法"><a href="#01、判断对象存活的方法" class="headerlink" title="01、判断对象存活的方法"></a>01、判断对象存活的方法</h1><blockquote><p>在垃圾回收器对堆内存回收前，需要判断对象是否存活。</p></blockquote><ul><li><p><strong>引用计数算法</strong>: 给每个对象添加一个引用计数器,每当对象被引用,对象的引用计数器就加1,当引用失效时,引用计数器就减1。直到引用计数器为0,就代表对象不再被引用。</p></li><li><p><strong>可达性算法</strong>: 通过GC ROOT的对象节点往下搜索,节点走过的路径被称为引用链。如果一个对象不处于任何引用链,那么就可以判断此对象是不可达的。</p></li></ul><blockquote><p>引用计数法缺点</p></blockquote><p>引用计数的主要缺陷是很难解决<strong>循环引用</strong>的问题：就是当<strong>2个对象互相引用</strong>的时候,除了彼此,没有其它对象再引用这2个对象,那么他们的引用计数都为1,就无法被回收。                </p><h2 id="1-1、什么是GC-Root"><a href="#1-1、什么是GC-Root" class="headerlink" title="1.1、什么是GC Root ?"></a>1.1、什么是GC Root ?</h2><p>上面说通过GC Root对象搜索引用链,那么GC Root对象是什么对象,或者说什么样的对象是GC Root对象。可以作为GC Root对象的有: </p><ol><li>虚拟机栈和本地方法栈区中的引用对象(stack)</li><li>方法区中类的静态属性引用的对象(static)</li><li>方法区中的常量引用的对象(final)   </li></ol><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714103131.png" alt="可达性算法">      </p><h1 id="02、JVM复习"><a href="#02、JVM复习" class="headerlink" title="02、JVM复习"></a>02、JVM复习</h1><blockquote><p>JVM结构</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122202.png" alt=""></p><blockquote><p>GC作用的地方</p></blockquote><p><img src="F:%5Clsqstudy%5C%E4%B8%AA%E4%BA%BA%E9%A1%B9%E7%9B%AE%5Cblog%5Cblog%5Csource_posts%5Cdocs%5Cjava%5Cgc%5Cupload%5Cimage-20200714085318493.png" alt=""></p><h1 id="03、GC是什么-分代收集算法"><a href="#03、GC是什么-分代收集算法" class="headerlink" title="03、GC是什么(分代收集算法)"></a>03、GC是什么(分代收集算法)</h1><ul><li>次数上频繁收集Young区</li><li>次数上较少收集Old区</li><li>基本不动元空间</li></ul><h1 id="04、GC算法总体概述"><a href="#04、GC算法总体概述" class="headerlink" title="04、GC算法总体概述"></a>04、GC算法总体概述</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122221.png" alt=""></p><p>JVM在进行GC时，并非每次都对上面三个内存区域一起回收的，大部分时候回收的都是指新生代。<br>因此GC按照回收的区域又分了两种类型，一种是普通GC（minor GC），一种是全局GC（major GC or Full GC）</p><blockquote><p>Minor GC和Full GC的区别</p></blockquote><ul><li><strong>普通GC（minor GC）</strong>：只针对新生代区域的GC,指发生在新生代的垃圾收集动作，因为大多数Java对象存活率都不高，所以Minor GC非常频繁，一般回收速度也比较快。 </li><li><strong>全局GC（major GC or Full GC</strong>）：指发生在老年代的垃圾收集动作，出现了Major GC，经常会伴随至少一次的Minor GC（但并不是绝对的）。Major GC的速度一般要比Minor GC慢上10倍以上</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、判断对象存活的方法&quot;&gt;&lt;a href=&quot;#01、判断对象存活的方法&quot; class=&quot;headerlink&quot; title=&quot;01、判断对象存活的方法&quot;&gt;&lt;/a&gt;01、判断对象存活的方法&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;在垃圾回收器对堆内存回收前，需要判
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="GC" scheme="http://lsqstudy.top/categories/Java/GC/"/>
    
    
      <category term="GC" scheme="http://lsqstudy.top/tags/GC/"/>
    
  </entry>
  
  <entry>
    <title>01谈谈你对volatile的理解</title>
    <link href="http://lsqstudy.top/post/34524172.html"/>
    <id>http://lsqstudy.top/post/34524172.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T23:34:02.979Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、Volatile和JMM内存模型的可见性"><a href="#01、Volatile和JMM内存模型的可见性" class="headerlink" title="01、Volatile和JMM内存模型的可见性"></a>01、Volatile和JMM内存模型的可见性</h1><h2 id="1-1、Volatile"><a href="#1-1、Volatile" class="headerlink" title="1.1、Volatile"></a>1.1、Volatile</h2><p>Volatile在日常的单线程环境是应用不到的。</p><blockquote><p>Volatile是Java虚拟机提供的<code>轻量级</code>的同步机制</p></blockquote><p>Volatile具有三大特征：</p><ul><li>保证可见性</li><li>不保证原子性</li><li>禁止指令重排（保证有序性）</li></ul><h2 id="1-2、JMM你谈谈"><a href="#1-2、JMM你谈谈" class="headerlink" title="1.2、JMM你谈谈"></a>1.2、JMM你谈谈</h2><h3 id="1-2-1、JMM是什么？"><a href="#1-2-1、JMM是什么？" class="headerlink" title="1.2.1、JMM是什么？"></a>1.2.1、JMM是什么？</h3><p>JMM是<strong>Java内存模型</strong>，也就是Java Memory Model，简称JMM，<strong>本身是一种抽象的概念，实际上并不存在</strong>，它描述的是一组规则或规范，通过这组规范定义了程序中各个变量（包括实例字段，静态字段和构成数组对象的元素）的访问方式</p><p>JMM关于同步的规定：</p><ul><li>线程解锁前，必须把共享变量的值刷新回主内存</li><li>线程解锁前，必须读取主内存的最新值，到自己的工作内存</li><li>加锁和解锁是同一把锁</li></ul><p>由于JVM运行程序的实体是线程，而每个线程创建时JVM都会为其创建一个工作内存（有些地方称为栈空间），工作内存是每个线程的私有数据区域，而Java内存模型中规定所有变量都存储在主内存，主内存是共享内存区域，所有线程都可以访问，<strong>但线程对变量的操作（读取赋值等）必须在工作内存中进行，首先要将变量从主内存拷贝到自己的工作内存空间，然后对变量进行操作，操作完成后再将变量写回主内存</strong>，不能直接操作主内存中的变量，各个线程中的工作内存中存储着主内存中的变量副本拷贝，因此不同的线程间无法访问对方的工作内存，线程间的通信（传值）必须通过主内存来完成，其简要访问过程：</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200715170317.png" style="zoom:80%;" /><p>数据传输速率：硬盘 &lt; 内存 &lt; &lt; cache &lt; CPU   </p><p>上面提到了两个概念：主内存  和 工作内存</p><ul><li><p>主内存：就是计算机的内存，也就是经常提到的8G内存，16G内存</p></li><li><p>工作内存：但我们实例化 new student，那么 age = 25 也是存储在主内存中</p><ul><li>当同时有三个线程同时访问 student中的age变量时，<br>那么每个线程都会拷贝一份，到各自的工作内存，从而实现了变量的拷贝</li></ul><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715170445.png" alt="image-20200309154435933" style="zoom:80%;" /></li></ul><p>即：JMM内存模型的可见性，指的是当主内存区域中的值被某个线程写入更改后，其它线程会马上知晓更改后的值，并重新得到更改后的值。</p><h3 id="1-2-2、缓存一致性"><a href="#1-2-2、缓存一致性" class="headerlink" title="1.2.2、缓存一致性"></a>1.2.2、缓存一致性</h3><p>为什么这里主线程中某个值被更改后，其它线程能马上知晓呢？其实这里是用到了总线嗅探技术</p><p>在说嗅探技术之前，首先谈谈缓存一致性的问题，就是当多个处理器运算任务都涉及到同一块主内存区域的时候，将可能导致各自的缓存数据不一。</p><p>为了解决缓存一致性的问题，需要各个处理器访问缓存时都遵循一些协议，在读写时要根据协议进行操作，这类协议主要有MSI、MESI等等。</p><blockquote><p>1、MESI</p></blockquote><p>当CPU写数据时，如果发现操作的变量是共享变量，即在其它CPU中也存在该变量的副本，会发出信号通知其它CPU将该内存变量的缓存行设置为无效，因此当其它CPU读取这个变量的时，发现自己缓存该变量的缓存行是无效的，那么它就会从内存中重新读取。</p><blockquote><p>2、总线嗅探</p></blockquote><p>那么是如何发现数据是否失效呢？</p><p>这里是用到了总线嗅探技术，就是每个处理器通过嗅探在总线上传播的数据来检查自己缓存值是否过期了，当处理器发现自己的缓存行对应的内存地址被修改，就会将当前处理器的缓存行设置为无效状态，当处理器对这个数据进行修改操作的时候，会重新从内存中把数据读取到处理器缓存中。</p><blockquote><p>3、总线风暴</p></blockquote><p>总线嗅探技术有哪些缺点？</p><p>由于Volatile的MESI缓存一致性协议，需要不断的从主内存嗅探和CAS循环，无效的交互会导致总线带宽达到峰值。因此不要大量使用volatile关键字，至于什么时候使用volatile、什么时候用锁以及Syschonized都是需要根据实际场景的。</p><h3 id="1-2-3、JMM的特性"><a href="#1-2-3、JMM的特性" class="headerlink" title="1.2.3、JMM的特性"></a>1.2.3、JMM的特性</h3><blockquote><p>JMM的三大特性，volatile只保证了两个，即可见性和有序性，不满足原子性</p></blockquote><ul><li>可见性</li><li>原子性</li><li>有序性</li></ul><h3 id="1-2-4、可见性代码验证"><a href="#1-2-4、可见性代码验证" class="headerlink" title="1.2.4、可见性代码验证"></a>1.2.4、可见性代码验证</h3><blockquote><p>1、但我们对于成员变量没有添加任何修饰时，是无法感知其它线程修改后的值</p></blockquote><pre><code class="java">package com.lsq.juc.Volatile;import java.util.concurrent.TimeUnit;/** * @Description * @Author LSQ * Date 2020/7/15 16:48 * * Volatile Java虚拟机提供的轻量级同步机制 * * 可见性（及时通知） * 不保证原子性 * 禁止指令重排 * *//** * 假设是主物理内存 */class MyData {    int number = 0;    public void addTo60() {        this.number = 60;    }}/** * 验证volatile的可见性 * 1. 假设int number = 0， number变量之前没有添加volatile关键字修饰 */public class VolatileVisibleDemo {    public static void main(String args []) {        // 资源类        MyData myData = new MyData();        // AAA线程 实现了Runnable接口的，lambda表达式        new Thread(() -&gt; {            System.out.println(Thread.currentThread().getName() + &quot;\t come in&quot;);            // 线程睡眠3秒，假设在进行运算            try {                TimeUnit.SECONDS.sleep(3);            } catch (InterruptedException e) {                e.printStackTrace();            }            // 修改number的值            myData.addTo60();            // 输出修改后的值            System.out.println(Thread.currentThread().getName() + &quot;\t update number value:&quot; + myData.number);        }, &quot;AAA&quot;).start();        while(myData.number == 0) {            // main线程就一直在这里等待循环，直到number的值不等于零        }        // 按道理这个值是不可能打印出来的，因为主线程运行的时候，number的值为0，所以一直在循环        // 如果能输出这句话，说明AAA线程在睡眠3秒后，更新的number的值，重新写入到主内存，并被main线程感知到了        System.out.println(Thread.currentThread().getName() + &quot;\t mission is over&quot;);        /**         * 最后输出结果：         * AAA     come in         * AAA     update number value:60         * 最后线程没有停止，并行没有输出  mission is over 这句话，说明没有用volatile修饰的变量，是没有可见性         */    }}</code></pre><p>输出结果为</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715170557.png" alt="">    </p><p>最后线程没有停止，并行没有输出  mission is over 这句话，说明没有用volatile修饰的变量，是没有可见性</p><blockquote><p>2、当我们修改MyData类中的成员变量时，并且添加volatile关键字修饰</p></blockquote><pre><code class="java">/** * 假设是主物理内存 */class MyData {    /**     * volatile 修饰的关键字，是为了增加 主线程和线程之间的可见性，只要有一个线程修改了内存中的值，其它线程也能马上感知     */    volatile int number = 0;    public void addTo60() {        this.number = 60;    }}</code></pre><p>最后输出的结果为：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715170631.png" alt=""></p><p>主线程也执行完毕了，说明volatile修饰的变量，是具备JVM轻量级同步机制的，能够感知其它线程的修改后的值。</p><h2 id="1-3、参考"><a href="#1-3、参考" class="headerlink" title="1.3、参考"></a>1.3、参考</h2><p><a href="https://mp.weixin.qq.com/s/Oa3tcfAFO9IgsbE22C5TEg" target="_blank" rel="noopener">https://mp.weixin.qq.com/s/Oa3tcfAFO9IgsbE22C5TEg</a></p><h1 id="02、Volatile不保证原子性"><a href="#02、Volatile不保证原子性" class="headerlink" title="02、Volatile不保证原子性"></a>02、Volatile不保证原子性</h1><h2 id="2-1、前言"><a href="#2-1、前言" class="headerlink" title="2.1、前言"></a>2.1、前言</h2><p>各个线程对主内存中共享变量的操作都是各个线程各自拷贝到自己的工作内存进行操作，然后在写回到主内存中的。</p><h2 id="2-2、原子性"><a href="#2-2、原子性" class="headerlink" title="2.2、原子性"></a>2.2、原子性</h2><p>不可分割，完整性，也就是说某个线程正在做某个具体业务时，中间不可以被加塞或者被分割，需要具体完成，要么同时成功，要么同时失败。</p><p>数据库也经常提到事务具备原子性</p><h2 id="2-3、代码测试"><a href="#2-3、代码测试" class="headerlink" title="2.3、代码测试"></a>2.3、代码测试</h2><p>为了测试volatile是否保证原子性，我们创建了20个线程，然后每个线程分别循环1000次，来调用number++的方法</p><pre><code class="java">        MyData myData = new MyData();        // 创建10个线程，线程里面进行1000次循环        for (int i = 0; i &lt; 20; i++) {            new Thread(() -&gt; {                // 里面                for (int j = 0; j &lt; 1000; j++) {                    myData.addPlusPlus();                }            }, String.valueOf(i)).start();        }</code></pre><p>最后通过 Thread.activeCount()，来感知20个线程是否执行完毕，这里判断线程数是否大于2，为什么是2？因为默认是有两个线程的，一个main线程，一个gc线程</p><pre><code class="java">// 需要等待上面20个线程都计算完成后，在用main线程取得最终的结果值while(Thread.activeCount() &gt; 2) {    // yield表示不执行    Thread.yield();}</code></pre><p>然后在线程执行完毕后，我们在查看number的值，假设volatile保证原子性的话，那么最后输出的值应该是</p><p>20 * 1000 = 20000,</p><p>完整代码如下所示：</p><pre><code class="java">package com.lsq.juc.Volatile;/** * @Description * @Author LSQ * Date 2020/7/15 17:14 *//** * Volatile Java虚拟机提供的轻量级同步机制 * * 可见性（及时通知） * 不保证原子性 * 禁止指令重排 *//** * 假设是主物理内存 */class MyAtomicData {    /**     * volatile 修饰的关键字，是为了增加 主线程和线程之间的可见性，只要有一个线程修改了内存中的值，其它线程也能马上感知     */    volatile int number = 0;    public void addTo60() {        this.number = 60;    }    /**     * 注意，此时number 前面是加了volatile修饰     */    public void addPlusPlus() {        number ++;    }}/** * 验证volatile的可见性 * 1、 假设int number = 0， number变量之前没有添加volatile关键字修饰 * 2、添加了volatile，可以解决可见性问题 * * 验证volatile不保证原子性 * 1、原子性指的是什么意思？ */public class VolatileAtomicDemo {    public static void main(String args []) {        MyAtomicData myData = new MyAtomicData();        // 创建10个线程，线程里面进行1000次循环        for (int i = 0; i &lt; 20; i++) {            new Thread(() -&gt; {                // 里面                for (int j = 0; j &lt; 1000; j++) {                    myData.addPlusPlus();                }            }, String.valueOf(i)).start();        }        // 需要等待上面20个线程都计算完成后，在用main线程取得最终的结果值        // 这里判断线程数是否大于2，为什么是2？因为默认是有两个线程的，一个main线程，一个gc线程        while(Thread.activeCount() &gt; 2) {            // yield表示不执行            Thread.yield();        }        // 查看最终的值        // 假设volatile保证原子性，那么输出的值应该为：  20 * 1000 = 20000        System.out.println(Thread.currentThread().getName() + &quot;\t finally number value: &quot; + myData.number);    }}</code></pre><p>最终结果我们会发现，number输出的值并没有20000，而且是每次运行的结果都不一致的，这说明了volatile修饰的变量不保证原子性</p><p>第一次：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173205.png" alt=""></p><p>第二次：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173234.png" alt=""></p><p>第三次：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173254.png" alt=""></p><h2 id="2-4、为什么出现数值丢失"><a href="#2-4、为什么出现数值丢失" class="headerlink" title="2.4、为什么出现数值丢失"></a>2.4、为什么出现数值丢失</h2><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173410.png" style="zoom:80%;" /><p>各自线程在写入主内存的时候，出现了数据的丢失，而引起的数值缺失的问题</p><p>下面我们将一个简单的number++操作，转换为字节码文件一探究竟</p><pre><code class="java">public class T1 {    volatile int n = 0;    public void add() {        n++;    }}</code></pre><p>转换后的字节码文件</p><pre><code class="java">public class com.moxi.interview.study.thread.T1 {  volatile int n;  public com.moxi.interview.study.thread.T1();    Code:       0: aload_0       1: invokespecial #1                  // Method java/lang/Object.&quot;&lt;init&gt;&quot;:()V       4: aload_0       5: iconst_0       6: putfield      #2                  // Field n:I       9: return  public void add();    Code:       0: aload_0       1: dup       2: getfield      #2                  // Field n:I       5: iconst_1       6: iadd       7: putfield      #2                  // Field n:I      10: return}</code></pre><p>这里查看字节码的操作，是用到了IDEA的javap命令</p><p>我们首先，使用IDEA提供的External Tools，来扩展<code>javap</code>命令</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715174349.png" alt=""></p><p>完成上述操作后，我们在需要查看字节码的文件下，右键选择 External Tools即可</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173618.png" alt=""></p><blockquote><p>如果出现了找不到指定类，先将代码运行一次，将其编译成class文件</p></blockquote><p>下面我们就针对 add() 这个方法的字节码文件进行分析</p><p>字节码指令对照表：<a href="https://segmentfault.com/a/1190000008722128" target="_blank" rel="noopener">https://segmentfault.com/a/1190000008722128</a></p><pre><code class="java">  public void add();    Code:       0: aload_0       1: dup       2: getfield      #2    // Field n:I       5: iconst_1       6: iadd       7: putfield      #2    // Field n:I      10: return</code></pre><p>我们能够发现 n++这条命令，被拆分成了3个指令</p><ul><li>执行<code>getfield</code> 从主内存拿到原始n</li><li>执行<code>iadd</code> 进行加1操作</li><li>执行<code>putfileld</code> 把累加后的值写回主内存 </li></ul><p>假设我们没有加 <code>synchronized</code>那么第一步就可能存在着，三个线程同时通过getfield命令，拿到主存中的 n值，然后三个线程，各自在自己的工作内存中进行加1操作，但他们并发进行  <code>iadd</code> 命令的时候，因为只能一个进行写，所以其它操作会被挂起，假设1线程，先进行了写操作，在写完后，volatile的可见性，应该需要告诉其它两个线程，主内存的值已经被修改了，但是因为太快了，其它两个线程，陆续执行 <code>iadd</code>命令，进行写入操作，这就造成了其他线程没有接受到主内存n的改变，从而覆盖了原来的值，出现写丢失，这样也就让最终的结果少于20000</p><h2 id="2-5、如何解决"><a href="#2-5、如何解决" class="headerlink" title="2.5、如何解决"></a>2.5、如何解决</h2><p>因此这也说明，在多线程环境下 number ++ 在多线程环境下是非线程安全的，解决的方法有哪些呢？</p><blockquote><p>在方法上加入 synchronized</p></blockquote><pre><code class="java">    public synchronized void addPlusPlus() {        number ++;    }</code></pre><p>运行结果：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715174923.png" alt=""></p><p>我们能够发现引入synchronized关键字后，保证了该方法每次只能够一个线程进行访问和操作，最终输出的结果也就为20000</p><h2 id="2-6、其它解决方法"><a href="#2-6、其它解决方法" class="headerlink" title="2.6、其它解决方法"></a>2.6、其它解决方法</h2><p>上面的方法引入synchronized，虽然能够保证原子性，但是为了解决number++，而引入重量级的同步机制，有种 杀鸡用牛刀</p><p>除了引用synchronized关键字外，还可以使用JUC下面的原子包装类，即刚刚的int类型的number，可以使用AtomicInteger来代替</p><pre><code class="java">    /**     *  创建一个原子Integer包装类，默认为0      */    AtomicInteger atomicInteger = new AtomicInteger();    public void addAtomic() {        // 相当于 atomicInter ++        atomicInteger.getAndIncrement();    }</code></pre><p>然后同理，继续刚刚的操作</p><pre><code class="java">        // 创建10个线程，线程里面进行1000次循环        for (int i = 0; i &lt; 20; i++) {            new Thread(() -&gt; {                // 里面                for (int j = 0; j &lt; 1000; j++) {                    myData.addPlusPlus();                    myData.addAtomic();                }            }, String.valueOf(i)).start();        }</code></pre><p>最后输出</p><pre><code class="java">        // 假设volatile保证原子性，那么输出的值应该为：  20 * 1000 = 20000        System.out.println(Thread.currentThread().getName() + &quot;\t finally number value: &quot; + myData.number);        System.out.println(Thread.currentThread().getName() + &quot;\t finally atomicNumber value: &quot; + myData.atomicInteger);</code></pre><p>下面的结果，一个是引入synchronized，一个是使用了原子包装类AtomicInteger</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715175233.png" alt=""></p><h1 id="03、Volatile禁止指令重排"><a href="#03、Volatile禁止指令重排" class="headerlink" title="03、Volatile禁止指令重排"></a>03、Volatile禁止指令重排</h1><p>计算机在执行程序时，为了提高性能，编译器和处理器常常会对指令重排，一般分为以下三种：</p><pre><code>源代码 -&gt; 编译器优化的重排 -&gt; 指令并行的重排 -&gt; 内存系统的重排 -&gt; 最终执行指令</code></pre><p>单线程环境里面确保最终执行结果和代码顺序的结果一致</p><p>处理器在进行重排序时，必须要考虑指令之间的<code>数据依赖性</code></p><p>多线程环境中线程交替执行，由于编译器优化重排的存在，两个线程中使用的变量能否保证一致性是无法确定的，结果无法预测。</p><h2 id="3-1、指令重排-example-1"><a href="#3-1、指令重排-example-1" class="headerlink" title="3.1、指令重排 - example 1"></a>3.1、指令重排 - example 1</h2><pre><code class="java">public void mySort() {    int x = 11;    int y = 12;    x = x + 5;    y = x * x;}</code></pre><p>按照正常单线程环境，执行顺序是 1 2 3 4</p><p>但是在多线程环境下，可能出现以下的顺序：</p><pre><code>- 2 1 3 4- 1 3 2 4 </code></pre><p>上述的过程就可以当做是指令的重排，即内部执行顺序，和我们的代码顺序不一样</p><p>但是指令重排也是有限制的，即不会出现下面的顺序</p><pre><code>4 3 2 1</code></pre><p>因为处理器在进行重排时候，必须考虑到指令之间的数据依赖性</p><p>因为步骤 4：需要依赖于 y的申明，以及x的申明，故因为存在数据依赖，无法首先执行</p><h3 id="3-1-1、例子"><a href="#3-1-1、例子" class="headerlink" title="3.1.1、例子"></a>3.1.1、例子</h3><pre><code class="java">int a,b,x,y = 0</code></pre><table><thead><tr><th>线程1</th><th>线程2</th></tr></thead><tbody><tr><td>x = a;</td><td>y = b;</td></tr><tr><td>b = 1;</td><td>a = 2;</td></tr><tr><td></td><td></td></tr><tr><td>x = 0;  y = 0</td><td></td></tr></tbody></table><p>因为上面的代码，不存在数据的依赖性，因此编译器可能对数据进行重排</p><table><thead><tr><th>线程1</th><th>线程2</th></tr></thead><tbody><tr><td>b = 1;</td><td>a = 2;</td></tr><tr><td>x = a;</td><td>y = b;</td></tr><tr><td></td><td></td></tr><tr><td>x = 2;  y = 1</td><td></td></tr></tbody></table><p>这样造成的结果，和最开始的就不一致了，这就是导致重排后，结果和最开始的不一样，因此为了防止这种结果出现，<code>volatile</code>就规定禁止指令重排，为了保证数据的一致性</p><h2 id="3-2、指令重排-example-2"><a href="#3-2、指令重排-example-2" class="headerlink" title="3.2、指令重排 - example 2"></a>3.2、指令重排 - example 2</h2><p>比如下面这段代码</p><pre><code class="java">public class ResortSeqDemo {    int a= 0;    boolean flag = false;    public void method01() {        a = 1;        flag = true;    }    public void method02() {        if(flag) {            a = a + 5;            System.out.println(&quot;reValue:&quot; + a);        }    }}</code></pre><p>我们按照正常的顺序，分别调用method01()  和 method02() 那么，最终输出就是 a = 6</p><p>但是如果在多线程环境下，因为方法1 和 方法2，他们之间不存在数据依赖的问题，因此原先的顺序可能是</p><pre><code>a = 1;flag = true;a = a + 5;System.out.println(&quot;reValue:&quot; + a);</code></pre><p>但是在经过编译器，指令，或者内存的重排后，可能会出现这样的情况</p><pre><code>flag = true;a = a + 5;System.out.println(&quot;reValue:&quot; + a);a = 1;</code></pre><p>也就是先执行 flag = true后，另外一个线程马上调用方法2，满足 flag的判断，最终让a + 5，结果为5，这样同样出现了数据不一致的问题</p><p>为什么会出现这个结果：多线程环境中线程交替执行，由于编译器优化重排的存在，两个线程中使用的变量能否保证一致性是无法确定的，结果无法预测。</p><p>这样就需要通过<code>volatile</code>来修饰，来保证线程安全性</p><h2 id="3-3、Volatile针对指令重排做了啥"><a href="#3-3、Volatile针对指令重排做了啥" class="headerlink" title="3.3、Volatile针对指令重排做了啥"></a>3.3、Volatile针对指令重排做了啥</h2><p>Volatile实现禁止指令重排优化，从而避免了多线程环境下程序出现乱序执行的现象</p><p>首先了解一个概念，内存屏障（Memory Barrier）又称内存栅栏，是一个CPU指令，它的作用有两个：</p><ul><li>保证特定操作的顺序</li><li>保证某些变量的内存可见性（利用该特性实现volatile的内存可见性）</li></ul><p>由于编译器和处理器都能执行指令重排的优化，如果在指令间插入一条Memory Barrier则会告诉编译器和CPU，不管什么指令都不能对这条Memory Barrier指令重排序，也就是说 <code>通过插入内存屏障禁止在内存屏障前后的指令执行重排序优化</code>。 内存屏障另外一个作用是刷新出各种CPU的缓存数，因此任何CPU上的线程都能读取到这些数据的最新版本。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715181015.png" alt=""></p><p>也就是过在Volatile的写 和 读的时候，加入屏障，防止出现指令重排的</p><h2 id="3-4、线程安全获得保证"><a href="#3-4、线程安全获得保证" class="headerlink" title="3.4、线程安全获得保证"></a>3.4、线程安全获得保证</h2><p>工作内存与主内存同步延迟现象导致的可见性问题</p><ul><li><p>可通过synchronized或volatile关键字解决，他们都可以使一个线程修改后的变量立即对其它线程可见对于指令重排导致的可见性问题和有序性问题</p></li><li><p>可以使用volatile关键字解决，因为volatile关键字的另一个作用就是禁止重排序优化</p></li></ul><h1 id="04、你在哪些地方用到过volatile"><a href="#04、你在哪些地方用到过volatile" class="headerlink" title="04、你在哪些地方用到过volatile?"></a>04、你在哪些地方用到过volatile?</h1><h2 id="4-1、单例模式DCL代码"><a href="#4-1、单例模式DCL代码" class="headerlink" title="4.1、单例模式DCL代码"></a>4.1、单例模式DCL代码</h2><p>首先回顾一下，单线程下的单例模式代码</p><pre><code class="java">package com.lsq.juc.Volatile;/** * @Description: SingletonDemo（单例模式） * @Author: LSQ * Date: 2020/7/15 18:13 */public class SingletonDemo {    private static SingletonDemo instance = null;    private SingletonDemo () {        System.out.println(Thread.currentThread().getName() + &quot;\t 我是构造方法SingletonDemo&quot;);    }    public static SingletonDemo getInstance() {        if(instance == null) {            instance = new SingletonDemo();        }        return instance;    }    public static void main(String[] args) {        // 这里的 == 是比较内存地址        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());    }}</code></pre><p>最后输出的结果</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715182111.png" alt=""></p><p>但是在多线程的环境下，我们的单例模式是否还是同一个对象了</p><pre><code class="java">/** * SingletonDemo（单例模式） * * @author: 陌溪 * @create: 2020-03-10-16:40 */public class SingletonDemo {    private static SingletonDemo instance = null;    private SingletonDemo () {        System.out.println(Thread.currentThread().getName() + &quot;\t 我是构造方法SingletonDemo&quot;);    }    public static SingletonDemo getInstance() {        if(instance == null) {            instance = new SingletonDemo();        }        return instance;    }    public static void main(String[] args) {        for (int i = 0; i &lt; 10; i++) {            new Thread(() -&gt; {                SingletonDemo.getInstance();            }, String.valueOf(i)).start();        }    }}</code></pre><p>从下面的结果我们可以看出，我们通过SingletonDemo.getInstance() 获取到的对象，并不是同一个，而是被下面几个线程都进行了创建，那么在多线程环境下，单例模式如何保证呢？</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715182217.png" alt=""></p><h3 id="4-1-1、解决方法1"><a href="#4-1-1、解决方法1" class="headerlink" title="4.1.1、解决方法1"></a>4.1.1、解决方法1</h3><p>引入synchronized关键字</p><pre><code class="java">    public synchronized static SingletonDemo getInstance() {        if(instance == null) {            instance = new SingletonDemo();        }        return instance;    }</code></pre><p>输出结果</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715182519.png" alt=""></p><p>我们能够发现，通过引入Synchronized关键字，能够解决高并发环境下的单例模式问题</p><p>但是synchronized属于重量级的同步机制，它只允许一个线程同时访问获取实例的方法，但是为了保证数据一致性，而减低了并发性，因此采用的比较少</p><h3 id="4-1-2、解决方法2"><a href="#4-1-2、解决方法2" class="headerlink" title="4.1.2、解决方法2"></a>4.1.2、解决方法2</h3><p>通过引入DCL   Double Check Lock   双端检锁机制</p><p>就是在进来和出去的时候，进行检测</p><pre><code class="java">    public static SingletonDemo getInstance() {        if(instance == null) {            // 同步代码段的时候，进行检测            synchronized (SingletonDemo.class) {                if(instance == null) {                    instance = new SingletonDemo();                }            }        }        return instance;    }</code></pre><p>最后输出的结果为：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715183444.png" alt="image-20200310165703190"></p><p>从输出结果来看，确实能够保证单例模式的正确性，但是上面的方法还是存在问题的</p><p>DCL（双端检锁）机制不一定是线程安全的，原因是有指令重排的存在，加入volatile可以禁止指令重排</p><p>原因是在某一个线程执行到第一次检测的时候，读取到 instance 不为null，instance的引用对象可能没有完成实例化。因为 instance = new SingletonDemo()；可以分为以下三步进行完成：</p><ul><li>memory = allocate();   // 1、分配对象内存空间</li><li>instance(memory);   // 2、初始化对象</li><li>instance = memory;  // 3、设置instance指向刚刚分配的内存地址，此时instance != null</li></ul><p>但是我们通过上面的三个步骤，能够发现，步骤2 和 步骤3之间不存在 数据依赖关系，而且无论重排前 还是重排后，程序的执行结果在单线程中并没有改变，因此这种重排优化是允许的。</p><ul><li>memory = allocate();   // 1、分配对象内存空间</li><li>instance = memory;  // 3、设置instance指向刚刚分配的内存地址，此时instance != null，但是对象还没有初始化完成</li><li>instance(memory);   // 2、初始化对象</li></ul><p>这样就会造成什么问题呢？</p><p>也就是当我们执行到重排后的步骤2，试图获取instance的时候，会得到null，因为对象的初始化还没有完成，而是在重排后的步骤3才完成，因此执行单例模式的代码时候，就会重新在创建一个instance实例</p><p><code>指令重排只会保证串行语义的执行一致性（单线程），但并不会关系多线程间的语义一致性</code></p><p>所以当一条线程访问instance不为null时，由于instance实例未必已初始化完成，这就造成了线程安全的问题</p><p>所以需要引入volatile，来保证出现指令重排的问题，从而保证单例模式的线程安全性</p><pre><code>private static volatile SingletonDemo instance = null;</code></pre><h3 id="4-1-3、最终代码"><a href="#4-1-3、最终代码" class="headerlink" title="4.1.3、最终代码"></a>4.1.3、最终代码</h3><pre><code class="java">package com.lsq.juc.Volatile;/** * @Description: SingletonDemo（单例模式） * @Author: LSQ * Date: 2020/7/15 18:13 */public class SingletonDemo {    private static volatile SingletonDemo instance = null;    private SingletonDemo () {        System.out.println(Thread.currentThread().getName() + &quot;\t 我是构造方法SingletonDemo&quot;);    }    public static SingletonDemo getInstance() {        if(instance == null) {            // a 双重检查加锁多线程情况下会出现某个线程虽然这里已经为空，但是另外一个线程已经执行到d处            synchronized (SingletonDemo.class) //b            {                //不加volitale关键字的话有可能会出现尚未完全初始化就获取到的情况。原因是内存模型允许无序写入                if(instance == null) {                    // d 此时才开始初始化                    instance = new SingletonDemo();                }            }        }        return instance;    }    public static void main(String[] args) {//        // 这里的 == 是比较内存地址//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        for (int i = 0; i &lt; 10; i++) {            new Thread(() -&gt; {                SingletonDemo.getInstance();            }, String.valueOf(i)).start();        }    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、Volatile和JMM内存模型的可见性&quot;&gt;&lt;a href=&quot;#01、Volatile和JMM内存模型的可见性&quot; class=&quot;headerlink&quot; title=&quot;01、Volatile和JMM内存模型的可见性&quot;&gt;&lt;/a&gt;01、Volatile和JMM内存
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>02CAS底层原理</title>
    <link href="http://lsqstudy.top/post/7e7a8188.html"/>
    <id>http://lsqstudy.top/post/7e7a8188.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T13:51:28.252Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、CAS你知道吗？"><a href="#01、CAS你知道吗？" class="headerlink" title="01、CAS你知道吗？"></a>01、CAS你知道吗？</h1><h2 id="1-1、概念"><a href="#1-1、概念" class="headerlink" title="1.1、概念"></a>1.1、概念</h2><p>CAS的全称是Compare-And-Swap，它是CPU并发原语</p><p>它的功能是判断内存某个位置的值是否为预期值，如果是则更改为新的值，这个过程是原子的</p><p>CAS并发原语体现在Java语言中就是<code>sun.misc.Unsafe</code>类的各个方法。调用UnSafe类中的CAS方法，JVM会帮我们实现出CAS汇编指令，这是一种完全依赖于硬件的功能，通过它实现了原子操作，再次强调，由于CAS是一种系统原语，原语属于操作系统用于范畴，是由若干条指令组成，用于完成某个功能的一个过程，并且原语的执行必须是连续的，在执行过程中不允许被中断，也就是说CAS是一条CPU的原子指令，不会造成所谓的数据不一致的问题，也就是说CAS是线程安全的。</p><h2 id="1-2、代码使用"><a href="#1-2、代码使用" class="headerlink" title="1.2、代码使用"></a>1.2、代码使用</h2><p>首先调用AtomicInteger创建了一个实例， 并初始化为5</p><pre><code class="java">        // 创建一个原子类        AtomicInteger atomicInteger = new AtomicInteger(5);</code></pre><p>然后调用CAS方法，企图更新成2019，这里有两个参数，一个是5，表示期望值，第二个就是我们要更新的值</p><pre><code class="java">atomicInteger.compareAndSet(5, 2019)</code></pre><p>然后再次使用了一个方法，同样将值改成1024</p><pre><code class="java">atomicInteger.compareAndSet(5, 1024)</code></pre><p>完整代码如下：</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.atomic.AtomicInteger;/** * @Description: 比较并交换：compareAndSet * @Author: LSQ * Date: 2020/7/15 19:50 */public class CASDemo {    public static void main(String[] args) {        // 创建一个原子类        AtomicInteger atomicInteger = new AtomicInteger(5);        /**         * 一个是期望值，一个是更新值，但期望值和原来的值相同时，才能够更改         * 假设三秒前，我拿的是5，也就是expect为5，然后我需要更新成 2019         */        System.out.println(atomicInteger.compareAndSet(5, 2019) + &quot;\t current data: &quot; + atomicInteger.get());        System.out.println(atomicInteger.compareAndSet(5, 1024) + &quot;\t current data: &quot; + atomicInteger.get());    }}</code></pre><p>上面代码的执行结果为</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715195203.png" style="zoom:80%;" /><p>这是因为我们执行第一个的时候，期望值和原本值是满足的，因此修改成功，但是第二次后，主内存的值已经修改成了2019，不满足期望值，因此返回了false，本次写入失败</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715195237.png" style="zoom:80%;" /><p>这个就类似于SVN或者Git的版本号，如果没有人更改过，就能够正常提交，否者需要先将代码pull下来，合并代码后，然后提交</p><h2 id="1-3、CAS底层原理"><a href="#1-3、CAS底层原理" class="headerlink" title="1.3、CAS底层原理"></a>1.3、CAS底层原理</h2><p>首先我们先看看 atomicInteger.getAndIncrement()方法的源码</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715195451.png" alt=""></p><p>从这里能够看到，底层又调用了一个unsafe类的getAndAddInt方法</p><h3 id="1-3-1、unsafe类"><a href="#1-3-1、unsafe类" class="headerlink" title="1.3.1、unsafe类"></a>1.3.1、unsafe类</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215035.png" alt=""></p><p>Unsafe是CAS的核心类，由于Java方法无法直接访问底层系统，需要通过本地（Native）方法来访问，Unsafe相当于一个后门，基于该类可以直接操作特定的内存数据。Unsafe类存在<code>sun.misc</code>包中，其内部方法操作可以像C的指针一样直接操作内存，因为Java中的CAS操作的执行依赖于Unsafe类的方法。</p><p><code>注意Unsafe类的所有方法都是native修饰的，也就是说unsafe类中的方法都直接调用操作系统底层资源执行相应的任务</code></p><blockquote><p>为什么Atomic修饰的包装类，能够保证原子性，依靠的就是底层的unsafe类</p></blockquote><h3 id="1-3-2、变量valueOffset"><a href="#1-3-2、变量valueOffset" class="headerlink" title="1.3.2、变量valueOffset"></a>1.3.2、变量valueOffset</h3><p>表示该变量值在内存中的偏移地址，因为Unsafe就是根据内存偏移地址获取数据的。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215043.png" alt=""></p><p>从这里我们能够看到，通过valueOffset，直接通过内存地址，获取到值，然后进行加1的操作</p><h3 id="1-3-3、变量value用volatile修饰"><a href="#1-3-3、变量value用volatile修饰" class="headerlink" title="1.3.3、变量value用volatile修饰"></a>1.3.3、变量value用volatile修饰</h3><p>保证了多线程之间的内存可见性</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215051.png" alt=""></p><p>var5：就是我们从主内存中拷贝到工作内存中的值</p><p>那么操作的时候，需要比较工作内存中的值，和主内存中的值进行比较</p><p>假设执行 compareAndSwapInt返回false，那么就一直执行 while方法，直到期望的值和真实值一样</p><ul><li>val1：AtomicInteger对象本身</li><li>var2：该对象值得引用地址</li><li>var4：需要变动的数量</li><li>var5：用var1和var2找到的内存中的真实值<ul><li>用该对象当前的值与var5比较</li><li>如果相同，更新var5 + var4 并返回true</li><li>如果不同，继续取值然后再比较，直到更新完成</li></ul></li></ul><p>这里没有用synchronized，而用CAS，这样提高了并发性，也能够实现一致性，是因为每个线程进来后，进入的do while循环，然后不断的获取内存中的值，判断是否为最新，然后在进行更新操作。</p><p>假设线程A和线程B同时执行getAndInt操作（分别跑在不同的CPU上）</p><ol><li>AtomicInteger里面的value原始值为3，即主内存中AtomicInteger的 value 为3，根据JMM模型，线程A和线程B各自持有一份价值为3的副本，分别存储在各自的工作内存</li><li>线程A通过getIntVolatile(var1 , var2) 拿到value值3，这是线程A被挂起（该线程失去CPU执行权）</li><li>线程B也通过getIntVolatile(var1, var2)方法获取到value值也是3，此时刚好线程B没有被挂起，并执行了compareAndSwapInt方法，比较内存的值也是3，成功修改内存值为4，线程B打完收工，一切OK</li><li>这是线程A恢复，执行CAS方法，比较发现自己手里的数字3和主内存中的数字4不一致，说明该值已经被其它线程抢先一步修改过了，那么A线程本次修改失败，只能够重新读取后在来一遍了，也就是在执行do while</li><li>线程A重新获取value值，因为变量value被volatile修饰，所以其它线程对它的修改，线程A总能够看到，线程A继续执行compareAndSwapInt进行比较替换，直到成功。</li></ol><p>Unsafe类 + CAS思想： 也就是自旋，自我旋转</p><h2 id="1-4、底层汇编"><a href="#1-4、底层汇编" class="headerlink" title="1.4、底层汇编"></a>1.4、底层汇编</h2><p>Unsafe类中的compareAndSwapInt是一个本地方法，该方法的实现位于unsafe.cpp中</p><ul><li>先想办法拿到变量value在内存中的地址</li><li>通过Atomic::cmpxchg实现比较替换，其中参数X是即将更新的值，参数e是原内存的值</li></ul><h2 id="1-5、CAS缺点"><a href="#1-5、CAS缺点" class="headerlink" title="1.5、CAS缺点"></a>1.5、CAS缺点</h2><p>CAS不加锁，保证一次性，但是需要多次比较</p><ul><li>循环时间长，开销大（因为执行的是do while，如果比较不成功一直在循环，最差的情况，就是某个线程一直取到的值和预期值都不一样，这样就会无限循环）</li><li>只能保证一个共享变量的原子操作<ul><li>当对一个共享变量执行操作时，我们可以通过循环CAS的方式来保证原子操作</li><li>但是对于多个共享变量操作时，循环CAS就无法保证操作的原子性，这个时候只能用锁来保证原子性</li></ul></li><li>引出来ABA问题？</li></ul><h2 id="1-6、ABA问题"><a href="#1-6、ABA问题" class="headerlink" title="1.6、ABA问题"></a>1.6、ABA问题</h2><p>下面讲解</p><h2 id="1-7、总结"><a href="#1-7、总结" class="headerlink" title="1.7、总结"></a>1.7、总结</h2><h3 id="1-7-1、CAS"><a href="#1-7-1、CAS" class="headerlink" title="1.7.1、CAS"></a>1.7.1、CAS</h3><p>CAS是compareAndSwap，比较当前工作内存中的值和主物理内存中的值，如果相同则执行规定操作，否者继续比较直到主内存和工作内存的值一致为止</p><h3 id="1-7-2、CAS应用"><a href="#1-7-2、CAS应用" class="headerlink" title="1.7.2、CAS应用"></a>1.7.2、CAS应用</h3><p>CAS有3个操作数，内存值V，旧的预期值A，要修改的更新值B。当且仅当预期值A和内存值V相同时，将内存值V修改为B，否者什么都不做</p><h1 id="02、原子类AtomicInteger的ABA问题"><a href="#02、原子类AtomicInteger的ABA问题" class="headerlink" title="02、原子类AtomicInteger的ABA问题"></a>02、原子类AtomicInteger的ABA问题</h1><h2 id="2-1、连环套路"><a href="#2-1、连环套路" class="headerlink" title="2.1、连环套路"></a>2.1、连环套路</h2><p>从AtomicInteger引出下面的问题</p><p>CAS -&gt; Unsafe -&gt; CAS底层思想 -&gt; ABA -&gt; 原子引用更新 -&gt; 如何规避ABA问题</p><h2 id="2-2、ABA问题是什么"><a href="#2-2、ABA问题是什么" class="headerlink" title="2.2、ABA问题是什么"></a>2.2、ABA问题是什么</h2><p>狸猫换太子</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215104.png" style="zoom:80%;" /><p>假设现在有两个线程，分别是T1 和 T2，然后T1执行某个操作的时间为10秒，T2执行某个时间的操作是2秒，<br>最开始AB两个线程，分别从主内存中获取A值，<br>但是因为B的执行速度更快，他先把A的值改成B，然后再修改成A，然后执行完毕，<br>T1线程在10秒后，执行完毕，判断内存中的值为A，并且和自己预期的值一样，它就认为没有人更改了主内存中的值，就快乐的修改成B，<br>但是实际上 可能中间经历了 ABCDEFA 这个变换，也就是中间的值经历了狸猫换太子。</p><p>所以ABA问题就是，在进行获取主内存值的时候，该内存值在我们写入主内存的时候，已经被修改了N次，<br>但是最终又改成原来的值了</p><h2 id="2-3、CAS导致ABA问题"><a href="#2-3、CAS导致ABA问题" class="headerlink" title="2.3、CAS导致ABA问题"></a>2.3、CAS导致ABA问题</h2><p>CAS算法实现了一个重要的前提，需要取出内存中某时刻的数据，并在当下时刻比较并替换，那么这个时间差会导致数据的变化。</p><p>CAS只管开头和结尾，也就是头和尾是一样，那就修改成功，中间的这个过程，可能会被人修改过。</p><p>ABA问题就像一个人被拖下车打了一顿，其后又被送回到车上。</p><h2 id="2-4、原子引用"><a href="#2-4、原子引用" class="headerlink" title="2.4、原子引用"></a>2.4、原子引用</h2><p>原子引用其实和原子包装类是差不多的概念，就是将一个java类，用原子引用类进行包装起来，那么这个类就具备了原子性</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.atomic.AtomicReference;/** * @Description: 原子引用 * @Author: LSQ * Date: 2020/7/15 20:57 */class User {    String userName;    int age;    public User(String userName, int age) {        this.userName = userName;        this.age = age;    }    public String getUserName() {        return userName;    }    public void setUserName(String userName) {        this.userName = userName;    }    public int getAge() {        return age;    }    public void setAge(int age) {        this.age = age;    }    @Override    public String toString() {        return &quot;User{&quot; +                &quot;userName=&#39;&quot; + userName + &#39;\&#39;&#39; +                &quot;, age=&quot; + age +                &#39;}&#39;;    }}public class AtomicReferenceDemo {    public static void main(String[] args) {        User z3 = new User(&quot;z3&quot;, 22);        User l4 = new User(&quot;l4&quot;, 25);        // 创建原子引用包装类        AtomicReference&lt;User&gt; atomicReference = new AtomicReference&lt;&gt;();        // 现在主物理内存的共享变量，为z3        atomicReference.set(z3);        // 比较并交换，如果现在主物理内存的值为z3，那么交换成l4        System.out.println(atomicReference.compareAndSet(z3, l4) + &quot;\t &quot; + atomicReference.get().toString());        // 比较并交换，现在主物理内存的值是l4了，但是预期为z3，因此交换失败        System.out.println(atomicReference.compareAndSet(z3, l4) + &quot;\t &quot; + atomicReference.get().toString());    }}</code></pre><h3 id="2-4-1、基于原子引用的ABA问题"><a href="#2-4-1、基于原子引用的ABA问题" class="headerlink" title="2.4.1、基于原子引用的ABA问题"></a>2.4.1、基于原子引用的ABA问题</h3><p>我们首先创建了两个线程，然后T1线程，执行一次ABA的操作，T2线程在一秒后修改主内存的值</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.TimeUnit;import java.util.concurrent.atomic.AtomicReference;/** * @Description:ABA问题的解决，AtomicStampedReference * @Author: LSQ * Date: 2020/7/15 21:00 */public class ABADemo {    /**     * 普通的原子引用包装类     */    static AtomicReference&lt;Integer&gt; atomicReference = new AtomicReference&lt;&gt;(100);    public static void main(String[] args) {        new Thread(() -&gt; {            // 把100 改成 101 然后在改成100，也就是ABA            atomicReference.compareAndSet(100, 101);            atomicReference.compareAndSet(101, 100);        }, &quot;t1&quot;).start();        new Thread(() -&gt; {            try {                // 睡眠一秒，保证t1线程，完成了ABA操作                TimeUnit.SECONDS.sleep(1);            } catch (InterruptedException e) {                e.printStackTrace();            }            // 把100 改成 101 然后在改成100，也就是ABA            System.out.println(atomicReference.compareAndSet(100, 2019) + &quot;\t&quot; + atomicReference.get());        }, &quot;t2&quot;).start();    }}</code></pre><p>我们发现，它能够成功的修改，这就是ABA问题</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215116.png" style="zoom:80%;" /><h2 id="2-5、解决ABA问题"><a href="#2-5、解决ABA问题" class="headerlink" title="2.5、解决ABA问题"></a>2.5、解决ABA问题</h2><p>新增修改版本号，类似于时间戳的概念</p><pre><code>T1：  100 1                 2019 2T2：  100 1     101 2       100  3</code></pre><p>如果T1修改的时候，版本号为2，落后于现在的版本号3，所以要重新获取最新值，这里就提出了一个使用时间戳版本号，来解决ABA问题的思路</p><h2 id="2-6、AtomicStampedReference"><a href="#2-6、AtomicStampedReference" class="headerlink" title="2.6、AtomicStampedReference"></a>2.6、AtomicStampedReference</h2><p>时间戳原子引用，来这里应用于版本号的更新，也就是每次更新的时候，需要比较期望值和当前值，以及期望版本号和当前版本号</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.TimeUnit;import java.util.concurrent.atomic.AtomicReference;import java.util.concurrent.atomic.AtomicStampedReference;/** * @Description:ABA问题的解决，AtomicStampedReference * @Author: LSQ * Date: 2020/7/15 21:00 */public class ABADemo {    /**     * 普通的原子引用包装类     */    static AtomicReference&lt;Integer&gt; atomicReference = new AtomicReference&lt;&gt;(100);    // 传递两个值，一个是初始值，一个是初始版本号    static AtomicStampedReference&lt;Integer&gt; atomicStampedReference = new AtomicStampedReference&lt;&gt;(100, 1);    public static void main(String[] args) {        System.out.println(&quot;============以下是ABA问题的产生==========&quot;);        new Thread(() -&gt; {            // 把100 改成 101 然后在改成100，也就是ABA            atomicReference.compareAndSet(100, 101);            atomicReference.compareAndSet(101, 100);        }, &quot;t1&quot;).start();        new Thread(() -&gt; {            // 睡眠一秒，保证t1线程，完成了ABA操作          try {TimeUnit.SECONDS.sleep(1);} catch (InterruptedException e) { e.printStackTrace();}            // 把100 改成 101 然后在改成100，也就是ABA            System.out.println(atomicReference.compareAndSet(100, 2019) + &quot;\t&quot; + atomicReference.get());        }, &quot;t2&quot;).start();        try {TimeUnit.SECONDS.sleep(2);} catch (InterruptedException e) { e.printStackTrace();}        System.out.println(&quot;============以下是ABA问题的解决==========&quot;);        new Thread(() -&gt; {            // 获取版本号            int stamp = atomicStampedReference.getStamp();            System.out.println(Thread.currentThread().getName() + &quot;\t 第一次版本号&quot; + stamp);            // 暂停t3一秒钟          try {TimeUnit.SECONDS.sleep(1);} catch (InterruptedException e) { e.printStackTrace();}            // 传入4个值，期望值，更新值，期望版本号，更新版本号            atomicStampedReference.compareAndSet(100, 101, atomicStampedReference.getStamp(), atomicStampedReference.getStamp() + 1);            System.out.println(Thread.currentThread().getName() + &quot;\t 第二次版本号&quot; + atomicStampedReference.getStamp());            atomicStampedReference.compareAndSet(101, 100, atomicStampedReference.getStamp(), atomicStampedReference.getStamp() + 1);            System.out.println(Thread.currentThread().getName() + &quot;\t 第三次版本号&quot; + atomicStampedReference.getStamp());        }, &quot;t3&quot;).start();        new Thread(() -&gt; {            // 获取版本号            int stamp = atomicStampedReference.getStamp();            System.out.println(Thread.currentThread().getName() + &quot;\t 第一次版本号&quot; + stamp);            // 暂停t4 3秒钟，保证t3线程也进行一次ABA问题           try {TimeUnit.SECONDS.sleep(3);} catch (InterruptedException e) { e.printStackTrace();}            boolean result = atomicStampedReference.compareAndSet(100, 2019, stamp, stamp + 1);            System.out.println(Thread.currentThread().getName() + &quot;\t 修改成功否：&quot; + result + &quot;\t 当前最新实际版本号：&quot; + atomicStampedReference.getStamp());            System.out.println(Thread.currentThread().getName() + &quot;\t 当前实际最新值&quot; + atomicStampedReference.getReference());        }, &quot;t4&quot;).start();    }}</code></pre><p>运行结果为：</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215123.png" style="zoom:80%;" /><p>我们能够发现，线程t3，在进行ABA操作后，版本号变更成了3，而线程t4在进行操作的时候，就出现操作失败了，因为版本号和当初拿到的不一样</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、CAS你知道吗？&quot;&gt;&lt;a href=&quot;#01、CAS你知道吗？&quot; class=&quot;headerlink&quot; title=&quot;01、CAS你知道吗？&quot;&gt;&lt;/a&gt;01、CAS你知道吗？&lt;/h1&gt;&lt;h2 id=&quot;1-1、概念&quot;&gt;&lt;a href=&quot;#1-1、概念&quot; cla
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>03ArrayList为什么线程不安全</title>
    <link href="http://lsqstudy.top/post/32574176.html"/>
    <id>http://lsqstudy.top/post/32574176.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T14:27:01.627Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、Collection线程不安全的举例"><a href="#01、Collection线程不安全的举例" class="headerlink" title="01、Collection线程不安全的举例"></a>01、Collection线程不安全的举例</h1><h2 id="1-1、前言"><a href="#1-1、前言" class="headerlink" title="1.1、前言"></a>1.1、前言</h2><p>1、当我们执行下面语句的时候，底层进行了什么操作</p><pre><code class="java">new ArrayList&lt;Integer&gt;();</code></pre><p>底层创建了一个空的数组，伴随着初始值为10</p><p>当执行add方法后，如果超过了10，那么会进行扩容，扩容的大小为原值的一半，也就是5个，使用下列方法扩容</p><pre><code class="java">Arrays.copyOf(elementData, netCapacity)</code></pre><h2 id="1-2、单线程环境下"><a href="#1-2、单线程环境下" class="headerlink" title="1.2、单线程环境下"></a>1.2、单线程环境下</h2><p>单线程环境的ArrayList是不会有问题的</p><pre><code class="java">public class ArrayListNotSafeDemo {    public static void main(String[] args) {        List&lt;String&gt; list = new ArrayList&lt;&gt;();        list.add(&quot;a&quot;);        list.add(&quot;b&quot;);        list.add(&quot;c&quot;);        for(String element : list) {            System.out.println(element);        }    }}</code></pre><h2 id="1-3、多线程环境"><a href="#1-3、多线程环境" class="headerlink" title="1.3、多线程环境"></a>1.3、多线程环境</h2><p>为什么ArrayList是线程不安全的？因为在进行写操作的时候，方法上为了保证并发性，是没有添加synchronized修饰，所以并发写的时候，就会出现问题</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221328.png" alt=""></p><p>当我们同时启动30个线程去操作List的时候</p><pre><code class="java">/** * 集合类线程不安全举例 * @author: 陌溪 * @create: 2020-03-12-20:15 */public class ArrayListNotSafeDemo {    public static void main(String[] args) {        List&lt;String&gt; list = new ArrayList&lt;&gt;();        for (int i = 0; i &lt; 30; i++) {            new Thread(() -&gt; {                list.add(UUID.randomUUID().toString().substring(0, 8));                System.out.println(list);            }, String.valueOf(i)).start();        }    }}</code></pre><p>这个时候出现了错误，也就是<code>java.util.ConcurrentModificationException</code></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221324.png" alt=""></p><p>这个异常是 并发修改的异常</p><h2 id="1-4、解决方案"><a href="#1-4、解决方案" class="headerlink" title="1.4、解决方案"></a>1.4、解决方案</h2><h3 id="1-4-1、方案一：Vector【不推荐】"><a href="#1-4-1、方案一：Vector【不推荐】" class="headerlink" title="1.4.1、方案一：Vector【不推荐】"></a>1.4.1、方案一：Vector【不推荐】</h3><p>不用ArrayList这种不安全的List实现类，而采用Vector，线程安全的</p><p>关于Vector如何实现线程安全的，而是在方法上加了锁，即synchronized</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221320.png" alt=""></p><p>这样就每次只能够一个线程进行操作，所以不会出现线程不安全的问题，但是因为加锁了，导致并发性基于下降</p><h3 id="1-4-2、方案二：Collections-synchronized-【不推荐】"><a href="#1-4-2、方案二：Collections-synchronized-【不推荐】" class="headerlink" title="1.4.2、方案二：Collections.synchronized()【不推荐】"></a>1.4.2、方案二：Collections.synchronized()【不推荐】</h3><pre><code class="java">List&lt;String&gt; list = Collections.synchronizedList(new ArrayList&lt;&gt;());</code></pre><p>采用Collections集合工具类，在ArrayList外面包装一层 同步 机制</p><h3 id="1-4-3、方案三：JUC的方法-写时复制-【推荐】"><a href="#1-4-3、方案三：JUC的方法-写时复制-【推荐】" class="headerlink" title="1.4.3、方案三：JUC的方法[写时复制]【推荐】"></a>1.4.3、方案三：JUC的方法[写时复制]【推荐】</h3><blockquote><p><code>CopyOnWriteArrayList</code>：写时复制，主要是一种读写分离的思想</p></blockquote><p>写时复制，CopyOnWrite容器即写时复制的容器，往一个容器中添加元素的时候，不直接往当前容器Object[]添加，而是先将Object[]进行copy，复制出一个新的容器object[] newElements，然后新的容器Object[] newElements里添加原始，添加元素完后，在将原容器的引用指向新的容器 setArray(newElements)；这样做的好处是可以对copyOnWrite容器进行并发的度，而不需要加锁，因为当前容器不需要添加任何元素。所以CopyOnWrite容器也是一种读写分离的思想，读和写不同的容器。</p><p>就是写的时候，把ArrayList扩容一个出来，然后把值填写上去，在通知其他的线程，ArrayList的引用指向扩容后的</p><blockquote><p>查看底层add方法源码</p></blockquote><pre><code class="java">    public boolean add(E e) {        final ReentrantLock lock = this.lock;        lock.lock();        try {            Object[] elements = getArray();            int len = elements.length;            Object[] newElements = Arrays.copyOf(elements, len + 1);            newElements[len] = e;            setArray(newElements);            return true;        } finally {            lock.unlock();        }    }</code></pre><blockquote><p>首先需要加锁</p></blockquote><pre><code class="java">final ReentrantLock lock = this.lock;lock.lock();</code></pre><blockquote><p>然后在末尾扩容一个单位</p></blockquote><pre><code class="java">Object[] elements = getArray();int len = elements.length;Object[] newElements = Arrays.copyOf(elements, len + 1);</code></pre><blockquote><p>然后在把扩容后的空间，填写上需要add的内容</p></blockquote><pre><code class="java">newElements[len] = e;</code></pre><p>最后把内容set到Array中</p><h2 id="1-5、HashSet线程不安全"><a href="#1-5、HashSet线程不安全" class="headerlink" title="1.5、HashSet线程不安全"></a>1.5、HashSet线程不安全</h2><h3 id="1-5-1、CopyOnWriteArraySet"><a href="#1-5-1、CopyOnWriteArraySet" class="headerlink" title="1.5.1、CopyOnWriteArraySet"></a>1.5.1、CopyOnWriteArraySet</h3><p>底层还是使用CopyOnWriteArrayList进行实例化</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221315.png" alt=""></p><h3 id="1-5-2、HashSet底层结构"><a href="#1-5-2、HashSet底层结构" class="headerlink" title="1.5.2、HashSet底层结构"></a>1.5.2、HashSet底层结构</h3><p>同理HashSet的底层结构就是HashMap</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221311.png" alt=""></p><blockquote><p>但是为什么我调用 HashSet.add()的方法，只需要传递一个元素，而HashMap是需要传递key-value键值对？</p></blockquote><p>首先我们查看hashSet的add方法</p><pre><code class="java">public boolean add(E e) {    return map.put(e, PRESENT)==null;}</code></pre><p>我们能发现但我们调用add的时候，存储一个值进入map中，只是作为key进行存储，而value存储的是一个Object类型的常量，也就是说HashSet只关心key，而不关心value</p><h2 id="1-6、HashMap线程不安全"><a href="#1-6、HashMap线程不安全" class="headerlink" title="1.6、HashMap线程不安全"></a>1.6、HashMap线程不安全</h2><p>同理HashMap在多线程环境下，也是不安全的</p><pre><code class="java">public static void main(String[] args) {    Map&lt;String, String&gt; map = new HashMap&lt;&gt;();    for (int i = 0; i &lt; 30; i++) {        new Thread(() -&gt; {            map.put(Thread.currentThread().getName(), UUID.randomUUID().toString().substring(0, 8));            System.out.println(map);        }, String.valueOf(i)).start();    }}</code></pre><h3 id="1-6-1、解决方法"><a href="#1-6-1、解决方法" class="headerlink" title="1.6.1、解决方法"></a>1.6.1、解决方法</h3><p>1、使用<code>Collections.synchronizedMap(new HashMap&lt;&gt;())</code>;【不推荐】</p><p>2、使用<code>ConcurrentHashMap</code>【推荐】</p><pre><code class="java">Map&lt;String, String&gt; map = new ConcurrentHashMap&lt;&gt;();</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、Collection线程不安全的举例&quot;&gt;&lt;a href=&quot;#01、Collection线程不安全的举例&quot; class=&quot;headerlink&quot; title=&quot;01、Collection线程不安全的举例&quot;&gt;&lt;/a&gt;01、Collection线程不安全的举例&lt;/
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>04TransferValue是什么</title>
    <link href="http://lsqstudy.top/post/9f38e304.html"/>
    <id>http://lsqstudy.top/post/9f38e304.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T23:55:53.175Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、值传递和引用传递"><a href="#01、值传递和引用传递" class="headerlink" title="01、值传递和引用传递"></a>01、值传递和引用传递</h1><h2 id="1-1、举例"><a href="#1-1、举例" class="headerlink" title="1.1、举例"></a>1.1、举例</h2><pre><code class="java">package com.lsq.juc.ChangeValue;/** * @Description:值传递和引用传递 * @Author: LSQ * Date: 2020/7/16 7:47 */class Person {    private Integer id;    private String personName;    public Person(String personName) {        this.personName = personName;    }    public void setPersonName(String personName) {        this.personName = personName;    }    public String getPersonName() {        return personName;    }}public class TransferValueDemo {    public void changeValue1(int age) {        age = 30;    }    public void changeValue2(Person person) {        person.setPersonName(&quot;XXXX&quot;);    }    public void changeValue3(String str) {        str = &quot;XXX&quot;;    }    public static void main(String[] args) {        TransferValueDemo test = new TransferValueDemo();        // 定义基本数据类型        int age = 20;        test.changeValue1(age);        System.out.println(&quot;age ----&quot; + age);        // 实例化person类        Person person = new Person(&quot;abc&quot;);        test.changeValue2(person);        System.out.println(&quot;personName-----&quot; + person.getPersonName());        // String        String str = &quot;abc&quot;;        test.changeValue3(str);        System.out.println(&quot;string-----&quot; + str);    }}</code></pre><p>最后输出结果</p><pre><code>age ----20personName-----XXXXstring-----abc</code></pre><h2 id="1-2、changeValue1的执行过程"><a href="#1-2、changeValue1的执行过程" class="headerlink" title="1.2、changeValue1的执行过程"></a>1.2、changeValue1的执行过程</h2><blockquote><p>栈管运行，堆管存储</p></blockquote><p>八种基本数据类型，在栈里面分配内存，属于值传递</p><p>当们执行 changeValue1的时候，因为int是基本数据类型，所以传递的是int = 20这个值，相当于传递的是一个副本，main方法里面的age并没有改变，因此输出的结果 age还是20，属于值传递</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716075154.png" style="zoom:80%;" /><h2 id="1-3、changeValue2的执行过程"><a href="#1-3、changeValue2的执行过程" class="headerlink" title="1.3、changeValue2的执行过程"></a>1.3、changeValue2的执行过程</h2><p>因为Person是属于对象，传递的是内存地址，当执行changeValue2的时候，会改变内存中的Person的值，属于引用传递，两个指针都是指向同一个地址</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716075309.png" style="zoom:80%;" /><h2 id="1-4、changeValue3的执行过程"><a href="#1-4、changeValue3的执行过程" class="headerlink" title="1.4、changeValue3的执行过程"></a>1.4、changeValue3的执行过程</h2><p>String不属于基本数据类型，但是为什么执行完成后，还是abc呢？</p><p>这是因为String的特殊性，当我们执行String str = “abc”的时候，它会把 <code>abc</code> 放入常量池中<br>当我们执行changeValue3的时候，会重新新建一个xxx，并没有销毁abc，然后指向xxx，然后最后我们输出的是main中的引用，还是指向的abc，因此最后输出结果还是abc</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716075334.png" style="zoom:80%;" />]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、值传递和引用传递&quot;&gt;&lt;a href=&quot;#01、值传递和引用传递&quot; class=&quot;headerlink&quot; title=&quot;01、值传递和引用传递&quot;&gt;&lt;/a&gt;01、值传递和引用传递&lt;/h1&gt;&lt;h2 id=&quot;1-1、举例&quot;&gt;&lt;a href=&quot;#1-1、举例&quot; cla
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>05Java的锁</title>
    <link href="http://lsqstudy.top/post/68d2e89d.html"/>
    <id>http://lsqstudy.top/post/68d2e89d.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-16T01:46:37.519Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、独占锁（写锁）-共享锁（读锁）-互斥锁"><a href="#01、独占锁（写锁）-共享锁（读锁）-互斥锁" class="headerlink" title="01、独占锁（写锁） / 共享锁（读锁） / 互斥锁"></a>01、独占锁（写锁） / 共享锁（读锁） / 互斥锁</h1><h2 id="1-1、概念"><a href="#1-1、概念" class="headerlink" title="1.1、概念"></a>1.1、概念</h2><p>独占锁：指该锁一次只能被一个线程所持有。对<strong><code>ReentrantLock</code>和<code>Synchronized</code>而言都是独占锁</strong></p><p>共享锁：指该锁可以被多个线程锁持有</p><p>对<strong><code>ReentrantReadWriteLock</code>其读锁是共享，其写锁是独占</strong></p><p>写的时候只能一个人写，但是读的时候，可以多个人同时读</p><h2 id="1-2、为什么会有写锁和读锁"><a href="#1-2、为什么会有写锁和读锁" class="headerlink" title="1.2、为什么会有写锁和读锁"></a>1.2、为什么会有写锁和读锁</h2><p>原来我们使用ReentrantLock创建锁的时候，是独占锁，也就是说一次只能一个线程访问，但是有一个读写分离场景，读的时候想同时进行写，因此原来独占锁的并发性就没这么好了，因为读锁并不会造成数据不一致的问题，因此可以多个人共享读</p><blockquote><p>多个线程 同时读一个资源类没有任何问题，所以为了满足并发量，读取共享资源应该可以同时进行，但是如果一个线程想去写共享资源，就不应该再有其它线程可以对该资源进行读或写</p></blockquote><p>读-读：能共存</p><p>读-写：不能共存</p><p>写-写：不能共存</p><h2 id="1-3、代码实现"><a href="#1-3、代码实现" class="headerlink" title="1.3、代码实现"></a>1.3、代码实现</h2><p>实现一个读写缓存的操作，假设开始没有加锁的时候，会出现什么情况</p><pre><code class="java">package com.lsq.juc.lock;/** * 读写锁: *  多个线程 同时读一个资源类没有任何问题，所以为了满足并发量，读取共享资源应该可以同时进行 *  但是，如果一个线程想去写共享资源，就不应该再有其它线程可以对该资源进行读或写 * @Author: LSQ * Date: 2020/7/16 8:03 */import java.util.HashMap;import java.util.Map;import java.util.concurrent.TimeUnit;/** * 资源类 */class MyCache {    private volatile Map&lt;String, Object&gt; map = new HashMap&lt;&gt;();    // private Lock lock = null;    /**     * 定义写操作     * 满足：原子 + 独占     * @param key     * @param value     */    public void put(String key, Object value) {        System.out.println(Thread.currentThread().getName() + &quot;\t 正在写入：&quot; + key);        try {            // 模拟网络拥堵，延迟0.3秒            TimeUnit.MILLISECONDS.sleep(300);        } catch (InterruptedException e) {            e.printStackTrace();        }        map.put(key, value);        System.out.println(Thread.currentThread().getName() + &quot;\t 写入完成&quot;);    }    public void get(String key) {        System.out.println(Thread.currentThread().getName() + &quot;\t 正在读取:&quot;);        try {            // 模拟网络拥堵，延迟0.3秒            TimeUnit.MILLISECONDS.sleep(300);        } catch (InterruptedException e) {            e.printStackTrace();        }        Object value = map.get(key);        System.out.println(Thread.currentThread().getName() + &quot;\t 读取完成：&quot; + value);    }}public class ReadWriteLockDemo {    public static void main(String[] args) {        MyCache myCache = new MyCache();        // 线程操作资源类，5个线程写        for (int i = 0; i &lt; 5; i++) {            // lambda表达式内部必须是final            final int tempInt = i;            new Thread(() -&gt; {                myCache.put(tempInt + &quot;&quot;, tempInt +  &quot;&quot;);            }, String.valueOf(i)).start();        }        // 线程操作资源类， 5个线程读        for (int i = 0; i &lt; 5; i++) {            // lambda表达式内部必须是final            final int tempInt = i;            new Thread(() -&gt; {                myCache.get(tempInt + &quot;&quot;);            }, String.valueOf(i)).start();        }    }}</code></pre><p>我们分别创建5个线程写入缓存</p><pre><code class="java">        // 线程操作资源类，5个线程写        for (int i = 0; i &lt; 5; i++) {            // lambda表达式内部必须是final            final int tempInt = i;            new Thread(() -&gt; {                myCache.put(tempInt + &quot;&quot;, tempInt +  &quot;&quot;);            }, String.valueOf(i)).start();        }</code></pre><p>5个线程读取缓存，</p><pre><code class="java">        // 线程操作资源类， 5个线程读        for (int i = 0; i &lt; 5; i++) {            // lambda表达式内部必须是final            final int tempInt = i;            new Thread(() -&gt; {                myCache.get(tempInt + &quot;&quot;);            }, String.valueOf(i)).start();        }</code></pre><p>最后运行结果：</p><pre><code>0     正在写入：04     正在写入：43     正在写入：31     正在写入：12     正在写入：20     正在读取:1     正在读取:2     正在读取:3     正在读取:4     正在读取:2     写入完成4     写入完成4     读取完成：null0     写入完成3     读取完成：null0     读取完成：null1     写入完成3     写入完成1     读取完成：null2     读取完成：null</code></pre><p>我们可以看到，在写入的时候，被其它线程频繁打断，这就造成了，还没写完，其它线程又开始写，这样就造成数据不一致</p><h2 id="1-4、解决方法"><a href="#1-4、解决方法" class="headerlink" title="1.4、解决方法"></a>1.4、解决方法</h2><p>上面的代码是没有加锁的，这样就会造成线程在进行写入操作的时候，被其它线程频繁打断，从而不具备原子性，这个时候，我们就需要用到读写锁来解决了</p><pre><code class="java">/*** 创建一个读写锁* 它是一个读写融为一体的锁，在使用的时候，需要转换*/private ReentrantReadWriteLock rwLock = new ReentrantReadWriteLock();</code></pre><p>当我们在进行<strong>写操作</strong>的时候，就需要转换成写锁</p><pre><code class="java">// 创建一个写锁rwLock.writeLock().lock();// 写锁 释放rwLock.writeLock().unlock();</code></pre><p>当们在进行<strong>读操作</strong>的时候，在转换成读锁</p><pre><code class="java">// 创建一个读锁rwLock.readLock().lock();// 读锁 释放rwLock.readLock().unlock();</code></pre><p>这里的读锁和写锁的区别在于，写锁一次只能一个线程进入，执行写操作，而读锁是多个线程能够同时进入，进行读取的操作</p><p>完整代码：</p><pre><code class="java">package com.lsq.juc.lock;/** * 读写锁 * 多个线程 同时读一个资源类没有任何问题，所以为了满足并发量，读取共享资源应该可以同时进行 * 但是，如果一个线程想去写共享资源，就不应该再有其它线程可以对该资源进行读或写 * * @Author: LSQ * Date: 2020/7/16 8:03 */import java.util.HashMap;import java.util.Map;import java.util.concurrent.TimeUnit;import java.util.concurrent.locks.ReentrantReadWriteLock;class MyCache {    /**     * 缓存中的东西，必须保持可见性，因此使用volatile修饰     */    private volatile Map&lt;String, Object&gt; map = new HashMap&lt;&gt;();    /**     * 创建一个读写锁     * 它是一个读写融为一体的锁，在使用的时候，需要转换     */    private ReentrantReadWriteLock rwLock = new ReentrantReadWriteLock();    /**     * 定义写操作     * 满足：原子 + 独占     * @param key     * @param value     */    public void put(String key, Object value) {        // 创建一个写锁        rwLock.writeLock().lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t 正在写入：&quot; + key);            try {                // 模拟网络拥堵，延迟0.3秒                TimeUnit.MILLISECONDS.sleep(300);            } catch (InterruptedException e) {                e.printStackTrace();            }            map.put(key, value);            System.out.println(Thread.currentThread().getName() + &quot;\t 写入完成&quot;);        } catch (Exception e) {            e.printStackTrace();        } finally {            // 写锁 释放            rwLock.writeLock().unlock();        }    }    /**     * 获取     * @param key     */    public void get(String key) {        // 读锁        rwLock.readLock().lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t 正在读取:&quot;);            try {                // 模拟网络拥堵，延迟0.3秒                TimeUnit.MILLISECONDS.sleep(300);            } catch (InterruptedException e) {                e.printStackTrace();            }            Object value = map.get(key);            System.out.println(Thread.currentThread().getName() + &quot;\t 读取完成：&quot; + value);        } catch (Exception e) {            e.printStackTrace();        } finally {            // 读锁释放            rwLock.readLock().unlock();        }    }    /**     * 清空缓存     */    public void clean() {        map.clear();    }}public class ReadWriteLockDemo {    public static void main(String[] args) {        MyCache myCache = new MyCache();        // 线程操作资源类，5个线程写        for (int i = 1; i &lt;= 5; i++) {            // lambda表达式内部必须是final            final int tempInt = i;            new Thread(() -&gt; {                myCache.put(tempInt + &quot;&quot;, tempInt + &quot;&quot;);            }, String.valueOf(i)).start();        }        // 线程操作资源类， 5个线程读        for (int i = 1; i &lt;= 5; i++) {            // lambda表达式内部必须是final            final int tempInt = i;            new Thread(() -&gt; {                myCache.get(tempInt + &quot;&quot;);            }, String.valueOf(i)).start();        }    }}</code></pre><p>运行结果：</p><pre><code>1     正在写入：11     写入完成2     正在写入：22     写入完成3     正在写入：33     写入完成4     正在写入：44     写入完成5     正在写入：55     写入完成2     正在读取:3     正在读取:1     正在读取:4     正在读取:5     正在读取:2     读取完成：21     读取完成：14     读取完成：43     读取完成：35     读取完成：5</code></pre><p>从运行结果我们可以看出，写入操作是一个一个线程进行执行的，并且中间不会被打断，而读操作的时候，是同时5个线程进入，然后并发读取操作</p><h1 id="02、公平锁和非公平锁"><a href="#02、公平锁和非公平锁" class="headerlink" title="02、公平锁和非公平锁"></a>02、公平锁和非公平锁</h1><h2 id="2-1、概念"><a href="#2-1、概念" class="headerlink" title="2.1、概念"></a>2.1、概念</h2><h3 id="2-1-1、公平锁"><a href="#2-1-1、公平锁" class="headerlink" title="2.1.1、公平锁"></a>2.1.1、公平锁</h3><p>是指多个线程按照申请锁的顺序来获取锁，类似于排队买饭，先来后到，先来先服务，就是公平的，也就是队列</p><h3 id="2-1-2、非公平锁"><a href="#2-1-2、非公平锁" class="headerlink" title="2.1.2、非公平锁"></a>2.1.2、非公平锁</h3><p>是指多个线程获取锁的顺序，并不是按照申请锁的顺序，有可能后申请的线程比先申请的线程优先获取锁，在高并发环境下，有可能造成优先级翻转，或者饥饿的线程（也就是某个线程一直得不到锁）</p><h2 id="2-2、如何创建"><a href="#2-2、如何创建" class="headerlink" title="2.2、如何创建"></a>2.2、如何创建</h2><p>并发包中<code>ReentrantLock</code>的创建可以指定析构函数的boolean类型来得到公平锁或者非公平锁，默认是非公平锁</p><pre><code class="java">/*** 创建一个可重入锁，true 表示公平锁，false 表示非公平锁。默认非公平锁*/Lock lock = new ReentrantLock(true);</code></pre><h2 id="2-3、两者区别"><a href="#2-3、两者区别" class="headerlink" title="2.3、两者区别"></a>2.3、两者区别</h2><ul><li><strong>公平锁</strong>：就是很公平，并发环境中，每个线程在获取锁时会先查看此锁维护的等待队列，如果为空，或者当前线程是等待队列中的第一个，就占用锁，否者就会加入到等待队列中，以后安装FIFO的规则从队列中取到自己</li><li><strong>非公平锁：</strong> 非公平锁比较粗鲁，上来就直接尝试占有锁，如果尝试失败，就再采用类似公平锁那种方式。</li></ul><h2 id="2-4、题外话"><a href="#2-4、题外话" class="headerlink" title="2.4、题外话"></a>2.4、题外话</h2><p>Java ReenttrantLock通过构造函数指定该锁是否公平，默认是非公平锁，因为非公平锁的优点在于吞吐量比公平锁大，<strong>对于synchronized而言，也是一种非公平锁</strong></p><h1 id="03、可重入锁和递归锁ReentrantLock"><a href="#03、可重入锁和递归锁ReentrantLock" class="headerlink" title="03、可重入锁和递归锁ReentrantLock"></a>03、可重入锁和递归锁ReentrantLock</h1><h2 id="3-1、概念"><a href="#3-1、概念" class="headerlink" title="3.1、概念"></a>3.1、概念</h2><blockquote><p><strong>可重入锁</strong> 也称 <strong>递归锁</strong></p></blockquote><p>指的是同一线程外层函数获得锁之后，内层递归函数仍然能获取到该锁的代码，在同一线程在外层方法获取锁的时候，在进入内层方法会自动获取锁</p><p>也就是说：<strong>线程可以进入任何一个它已经拥有的锁所同步的代码块</strong></p><p><code>ReentrantLock</code> / <code>Synchronized</code>就是一个典型的可重入锁</p><h2 id="3-2、代码"><a href="#3-2、代码" class="headerlink" title="3.2、代码"></a>3.2、代码</h2><p>可重入锁就是，在一个method1方法中加入一把锁，方法2也加锁了，那么他们拥有的是同一把锁</p><pre><code class="java">public synchronized void method1() {    method2();}public synchronized void method2() {}</code></pre><p>也就是说我们只需要进入method1后，那么它也能直接进入method2方法，因为他们所拥有的锁，是同一把。</p><h2 id="3-3、作用"><a href="#3-3、作用" class="headerlink" title="3.3、作用"></a>3.3、作用</h2><p>可重入锁的最大作用就是避免死锁</p><h2 id="3-4、可重入锁验证"><a href="#3-4、可重入锁验证" class="headerlink" title="3.4、可重入锁验证"></a>3.4、可重入锁验证</h2><h3 id="3-4-1、证明Synchronized"><a href="#3-4-1、证明Synchronized" class="headerlink" title="3.4.1、证明Synchronized"></a>3.4.1、证明Synchronized</h3><pre><code class="java">package com.lsq.juc.lock;/** * 可重入锁（也叫递归锁） * 指的是同一线程外层函数获得锁之后，内层递归函数仍然能获取到该锁的代码，在同一线程在外层方法获取锁的时候，在进入内层方法会自动获取锁 * * 也就是说：线程可以进入任何一个它已经拥有的锁所同步的代码块 * * @Author: LSQ * Date: 2020/7/16 8:44 *//** * 资源类 */class Phone {    /**     * 发送短信     * @throws Exception     */    public synchronized void sendSMS() throws Exception{        System.out.println(Thread.currentThread().getName() + &quot;\t invoked sendSMS()&quot;);        // 在同步方法中，调用另外一个同步方法        sendEmail();    }    /**     * 发邮件     * @throws Exception     */    public synchronized void sendEmail() throws Exception{        System.out.println(Thread.currentThread().getId() + &quot;\t invoked sendEmail()&quot;);    }}public class ReenterLockDemo {    public static void main(String[] args) {        Phone phone = new Phone();        // 两个线程操作资源列        new Thread(() -&gt; {            try {                phone.sendSMS();            } catch (Exception e) {                e.printStackTrace();            }        }, &quot;t1&quot;).start();        new Thread(() -&gt; {            try {                phone.sendSMS();            } catch (Exception e) {                e.printStackTrace();            }        }, &quot;t2&quot;).start();    }}</code></pre><p>在这里，我们编写了一个资源类phone，拥有两个加了synchronized的同步方法，分别是sendSMS 和 sendEmail，我们在sendSMS方法中，调用sendEmail。最后在主线程同时开启了两个线程进行测试，最后得到的结果为：</p><pre><code class="java">t1     invoked sendSMS()t1     invoked sendEmail()t2     invoked sendSMS()t2     invoked sendEmail()</code></pre><p>这就说明当 t1 线程进入sendSMS的时候，拥有了一把锁，同时t2线程无法进入，直到t1线程拿着锁，执行了sendEmail 方法后，才释放锁，这样t2才能够进入</p><pre><code class="java">t1     invoked sendSMS()      t1线程在外层方法获取锁的时候t1     invoked sendEmail()    t1在进入内层方法会自动获取锁t2     invoked sendSMS()      t2线程在外层方法获取锁的时候t2     invoked sendEmail()    t2在进入内层方法会自动获取锁</code></pre><h3 id="3-4-2、证明ReentrantLock"><a href="#3-4-2、证明ReentrantLock" class="headerlink" title="3.4.2、证明ReentrantLock"></a>3.4.2、证明ReentrantLock</h3><pre><code class="java">import java.util.concurrent.TimeUnit;import java.util.concurrent.locks.Lock;import java.util.concurrent.locks.ReentrantLock;/** * 资源类 */class Phone implements Runnable{    Lock lock = new ReentrantLock();    /**     * set进去的时候，就加锁，调用set方法的时候，能否访问另外一个加锁的set方法     */    public void getLock() {        lock.lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t get Lock&quot;);            setLock();        } finally {            lock.unlock();        }    }    public void setLock() {        lock.lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t set Lock&quot;);        } finally {            lock.unlock();        }    }    @Override    public void run() {        getLock();    }}public class ReenterLockDemo {    public static void main(String[] args) {        Phone phone = new Phone();        /**         * 因为Phone实现了Runnable接口         */        Thread t3 = new Thread(phone, &quot;t3&quot;);        Thread t4 = new Thread(phone, &quot;t4&quot;);        t3.start();        t4.start();    }}</code></pre><p>现在我们使用ReentrantLock进行验证，首先资源类实现了Runnable接口，重写Run方法，里面调用get方法，get方法在进入的时候，就加了锁</p><pre><code class="java">    public void getLock() {        lock.lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t get Lock&quot;);            setLock();        } finally {            lock.unlock();        }    }</code></pre><p>然后在方法里面，又调用另外一个加了锁的setLock方法</p><pre><code class="java">    public void setLock() {        lock.lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t set Lock&quot;);        } finally {            lock.unlock();        }    }</code></pre><p>最后输出结果我们能发现，结果和加synchronized方法是一致的，都是在外层的方法获取锁之后，线程能够直接进入里层</p><pre><code class="java">t3     get Lockt3     set Lockt4     get Lockt4     set Lock</code></pre><p><strong>当我们在getLock方法加两把锁会是什么情况呢？</strong>  (阿里面试)</p><pre><code class="java">    public void getLock() {        lock.lock();        lock.lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t get Lock&quot;);            setLock();        } finally {            lock.unlock();            lock.unlock();        }    }</code></pre><p>最后得到的结果也是一样的，因为里面不管有几把锁，其它他们都是同一把锁，也就是说用同一个钥匙都能够打开</p><p><strong>当我们在getLock方法加两把锁，但是只解一把锁会出现什么情况呢？</strong></p><pre><code class="java">public void getLock() {    lock.lock();    lock.lock();    try {        System.out.println(Thread.currentThread().getName() + &quot;\t get Lock&quot;);        setLock();    } finally {        lock.unlock();        lock.unlock();    }}</code></pre><p>得到结果</p><pre><code>t3     get Lockt3     set Lock</code></pre><p>也就是说程序直接卡死，线程不能出来，也就说明我们申请几把锁，最后需要解除几把锁</p><p><strong>当我们只加一把锁，但是用两把锁来解锁的时候，又会出现什么情况呢？</strong></p><pre><code class="java">    public void getLock() {        lock.lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t get Lock&quot;);            setLock();        } finally {            lock.unlock();            lock.unlock();        }    }</code></pre><p>这个时候，运行程序会直接报错</p><pre><code class="java">t3     get Lockt3     set Lockt4     get Lockt4     set LockException in thread &quot;t3&quot; Exception in thread &quot;t4&quot; java.lang.IllegalMonitorStateException    at java.util.concurrent.locks.ReentrantLock$Sync.tryRelease(ReentrantLock.java:151)    at java.util.concurrent.locks.AbstractQueuedSynchronizer.release(AbstractQueuedSynchronizer.java:1261)    at java.util.concurrent.locks.ReentrantLock.unlock(ReentrantLock.java:457)    at com.moxi.interview.study.thread.Phone.getLock(ReenterLockDemo.java:52)    at com.moxi.interview.study.thread.Phone.run(ReenterLockDemo.java:67)    at java.lang.Thread.run(Thread.java:745)java.lang.IllegalMonitorStateException    at java.util.concurrent.locks.ReentrantLock$Sync.tryRelease(ReentrantLock.java:151)    at java.util.concurrent.locks.AbstractQueuedSynchronizer.release(AbstractQueuedSynchronizer.java:1261)    at java.util.concurrent.locks.ReentrantLock.unlock(ReentrantLock.java:457)    at com.moxi.interview.study.thread.Phone.getLock(ReenterLockDemo.java:52)    at com.moxi.interview.study.thread.Phone.run(ReenterLockDemo.java:67)    at java.lang.Thread.run(Thread.java:745)</code></pre><h1 id="04、自旋锁"><a href="#04、自旋锁" class="headerlink" title="04、自旋锁"></a>04、自旋锁</h1><p>自旋锁：spinlock，是指尝试获取锁的线程不会立即阻塞，而是采用循环的方式去尝试获取锁，这样的好处是减少线程上下文切换的消耗，缺点是循环会消耗CPU</p><p>比较并交换，底层使用的就是自旋，自旋就是多次尝试，多次访问，不会阻塞的状态就是自旋。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716094612.png" alt=""></p><h2 id="4-1、优缺点"><a href="#4-1、优缺点" class="headerlink" title="4.1、优缺点"></a>4.1、优缺点</h2><p>优点：循环比较获取直到成功为止，没有类似于wait的阻塞</p><p>缺点：当不断自旋的线程越来越多的时候，会因为执行while循环不断的消耗CPU资源</p><h2 id="4-2、手写自旋锁"><a href="#4-2、手写自旋锁" class="headerlink" title="4.2、手写自旋锁"></a>4.2、手写自旋锁</h2><p>通过CAS操作完成自旋锁，A线程先进来调用myLock方法自己持有锁5秒，B随后进来发现当前有线程持有锁，不是null，所以只能通过自旋等待，直到A释放锁后B随后抢到</p><pre><code class="java">package com.lsq.juc.lock;import java.util.concurrent.TimeUnit;import java.util.concurrent.atomic.AtomicReference;/** * 手写一个自旋锁 * * 循环比较获取直到成功为止，没有类似于wait的阻塞 * * 通过CAS操作完成自旋锁，A线程先进来调用myLock方法自己持有锁5秒，B随后进来发现当前有线程持有锁，不是null，所以只能通过自旋等待，直到A释放锁后B随后抢到 * * @Author: LSQ * Date: 2020/7/16 8:59 */public class SpinLockDemo {    // 现在的泛型装的是Thread，原子引用线程    AtomicReference&lt;Thread&gt; atomicReference = new AtomicReference&lt;&gt;();    public void myLock() {        // 获取当前进来的线程        Thread thread = Thread.currentThread();        System.out.println(Thread.currentThread().getName() + &quot;\t come in &quot;);        // 开始自旋，期望值是null，更新值是当前线程，如果是null，则更新为当前线程，否者自旋        while(!atomicReference.compareAndSet(null, thread)) {        }    }    /**     * 解锁     */    public void myUnLock() {        // 获取当前进来的线程        Thread thread = Thread.currentThread();        // 自己用完了后，把atomicReference变成null        atomicReference.compareAndSet(thread, null);        System.out.println(Thread.currentThread().getName() + &quot;\t invoked myUnlock()&quot;);    }    public static void main(String[] args) {        SpinLockDemo spinLockDemo = new SpinLockDemo();        // 启动t1线程，开始操作        new Thread(() -&gt; {            // 开始占有锁            spinLockDemo.myLock();            try {                TimeUnit.SECONDS.sleep(5);            } catch (InterruptedException e) {                e.printStackTrace();            }            // 开始释放锁            spinLockDemo.myUnLock();        }, &quot;t1&quot;).start();        // 让main线程暂停1秒，使得t1线程，先执行        try {            TimeUnit.SECONDS.sleep(5);        } catch (InterruptedException e) {            e.printStackTrace();        }        // 1秒后，启动t2线程，开始占用这个锁        new Thread(() -&gt; {            // 开始占有锁            spinLockDemo.myLock();            // 开始释放锁            spinLockDemo.myUnLock();        }, &quot;t2&quot;).start();    }}</code></pre><p>最后输出结果</p><pre><code>t1     come in .....五秒后.....t1     invoked myUnlock()t2     come in t2     invoked myUnlock()</code></pre><p>首先输出的是 t1     come in </p><p>然后1秒后，t2线程启动，发现锁被t1占有，所有不断的执行 compareAndSet方法，来进行比较，直到t1释放锁后，也就是5秒后，t2成功获取到锁，然后释放</p><h1 id="05、扩展Synchronized无法禁止指令重排"><a href="#05、扩展Synchronized无法禁止指令重排" class="headerlink" title="05、扩展Synchronized无法禁止指令重排"></a>05、扩展Synchronized无法禁止指令重排</h1><blockquote><p>问题：为什么Synchronized无法禁止指令重排，却能保证有序性？</p></blockquote><h2 id="5-1、前言"><a href="#5-1、前言" class="headerlink" title="5.1、前言"></a>5.1、前言</h2><p>首先我们要分析下这道题，这简单的一个问题，其实里面还是包含了很多信息的，要想回答好这个问题，面试者至少要知道一下概念：</p><ul><li>Java内存模型</li><li>并发编程有序性问题</li><li>指令重排</li><li>synchronized锁</li><li>可重入锁</li><li>排它锁</li><li>as-if-serial语义</li><li>单线程&amp;多线程</li></ul><h2 id="5-2、发标准解答"><a href="#5-2、发标准解答" class="headerlink" title="5.2、发标准解答"></a>5.2、发标准解答</h2><p>为了进一步提升计算机各方面能力，在硬件层面做了很多优化，如处理器优化和指令重排等，但是这些技术的引入就会导致有序性问题。</p><blockquote><p>先解释什么是有序性问题，也知道是什么原因导致的有序性问题</p></blockquote><p>我们也知道，最好的解决有序性问题的办法，就是禁止处理器优化和指令重排，就像volatile中使用内存屏障一样。</p><blockquote><p>表明你知道啥是指令重排，也知道他的实现原理</p></blockquote><p>但是，虽然很多硬件都会为了优化做一些重排，但是在Java中，不管怎么排序，都不能影响单线程程序的执行结果。这就是as-if-serial语义，所有硬件优化的前提都是必须遵守as-if-serial语义。</p><p>as-if-serial语义把<strong>单线程</strong>程序保护了起来，遵守as-if-serial语义的编译器，runtime 和处理器共同为编写单线程程序的程序员创建了一个幻觉：单线程程序是按程序的顺序来执行的。as-if-serial语义使单线程程序员无需担心重排序会 干扰他们，也无需担心内存可见性问题。</p><blockquote><p>重点！解释下什么是as-if-serial语义，因为这是这道题的第一个关键词，答上来就对了一半了</p></blockquote><p>再说下synchronized，他是Java提供的锁，可以通过他对Java中的对象加锁，并且他是一种排他的、可重入的锁。</p><p>所以，当某个线程执行到一段被synchronized修饰的代码之前，会先进行加锁，执行完之后再进行解锁。在加锁之后，解锁之前，其他线程是无法再次获得锁的，只有这条加锁线程可以重复获得该锁。</p><blockquote><p>介绍synchronized的原理，这是本题的第二个关键点，到这里基本就可以拿满分了。</p></blockquote><p>synchronized通过排他锁的方式就保证了同一时间内，被synchronized修饰的代码是单线程执行的。所以呢，这就满足了as-if-serial语义的一个关键前提，那就是<strong>单线程</strong>，因为有as-if-serial语义保证，单线程的有序性就天然存在了。</p><p>tw</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、独占锁（写锁）-共享锁（读锁）-互斥锁&quot;&gt;&lt;a href=&quot;#01、独占锁（写锁）-共享锁（读锁）-互斥锁&quot; class=&quot;headerlink&quot; title=&quot;01、独占锁（写锁） / 共享锁（读锁） / 互斥锁&quot;&gt;&lt;/a&gt;01、独占锁（写锁） / 共享锁
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>06CountDownLatch、CyclicBarrier、Semaphore使用</title>
    <link href="http://lsqstudy.top/post/84f8e3c5.html"/>
    <id>http://lsqstudy.top/post/84f8e3c5.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-16T01:45:43.641Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、CountDownLatch"><a href="#01、CountDownLatch" class="headerlink" title="01、CountDownLatch"></a>01、CountDownLatch</h1><h2 id="1-1、概念"><a href="#1-1、概念" class="headerlink" title="1.1、概念"></a>1.1、概念</h2><blockquote><p>让一些线程阻塞直到另一些线程完成一系列操作才被唤醒</p></blockquote><p>CountDownLatch主要有两个方法，当一个或多个线程调用await方法时，调用线程就会被阻塞。其它线程调用CountDown方法会将计数器减1（调用CountDown方法的线程不会被阻塞），当计数器的值变成零时，因调用await方法被阻塞的线程会被唤醒，继续执行</p><h2 id="1-2、场景"><a href="#1-2、场景" class="headerlink" title="1.2、场景"></a>1.2、场景</h2><p>现在有这样一个场景，假设一个自习室里有7个人，其中有一个是班长，班长的主要职责就是在其它6个同学走了后，关灯，锁教室门，然后走人，因此班长是需要最后一个走的，那么有什么方法能够控制班长这个线程是最后一个执行，而其它线程是随机执行的</p><h2 id="1-3、解决方案"><a href="#1-3、解决方案" class="headerlink" title="1.3、解决方案"></a>1.3、解决方案</h2><blockquote><p>这个时候就用到了CountDownLatch，计数器了。我们一共创建6个线程，然后计数器的值也设置成6</p></blockquote><pre><code class="java">// 计数器CountDownLatch countDownLatch = new CountDownLatch(6);</code></pre><blockquote><p>然后每次学生线程执行完，就让计数器的值减1</p></blockquote><pre><code class="java">for (int i = 0; i &lt;= 6; i++) {    new Thread(() -&gt; {        System.out.println(Thread.currentThread().getName() + &quot;\t 上完自习，离开教室&quot;);        countDownLatch.countDown();    }, String.valueOf(i)).start();}</code></pre><blockquote><p>最后我们需要通过CountDownLatch的await方法来控制班长主线程的执行，这里 countDownLatch.await()可以想成是一道墙，只有当计数器的值为0的时候，墙才会消失，主线程才能继续往下执行</p></blockquote><pre><code class="java">countDownLatch.await();System.out.println(Thread.currentThread().getName() + &quot;\t 班长最后关门&quot;);</code></pre><blockquote><p>不加CountDownLatch的执行结果，我们发现main线程提前已经执行完成了</p></blockquote><pre><code>1     上完自习，离开教室0     上完自习，离开教室main     班长最后关门2     上完自习，离开教室3     上完自习，离开教室4     上完自习，离开教室5     上完自习，离开教室6     上完自习，离开教室</code></pre><blockquote><p>引入CountDownLatch后的执行结果，我们能够控制住main方法的执行，这样能够保证前提任务的执行</p></blockquote><pre><code>0     上完自习，离开教室2     上完自习，离开教室4     上完自习，离开教室1     上完自习，离开教室5     上完自习，离开教室6     上完自习，离开教室3     上完自习，离开教室main     班长最后关门</code></pre><h2 id="1-4、完整代码"><a href="#1-4、完整代码" class="headerlink" title="1.4、完整代码"></a>1.4、完整代码</h2><pre><code class="java">package com.lsq.juc.lock;import java.util.concurrent.CountDownLatch;/** * @Description: * @Author: LSQ * Date: 2020/7/16 9:31 */public class CountDownLatchDemo {    public static void main(String[] args) throws InterruptedException {        // 计数器        CountDownLatch countDownLatch = new CountDownLatch(6);        for (int i = 0; i &lt;= 6; i++) {            new Thread(() -&gt; {                System.out.println(Thread.currentThread().getName() + &quot;\t 上完自习，离开教室&quot;);                countDownLatch.countDown();            }, String.valueOf(i)).start();        }        countDownLatch.await();        System.out.println(Thread.currentThread().getName() + &quot;\t 班长最后关门&quot;);    }}</code></pre><h1 id="02、CyclicBarrier"><a href="#02、CyclicBarrier" class="headerlink" title="02、CyclicBarrier"></a>02、CyclicBarrier</h1><h2 id="2-1、概念"><a href="#2-1、概念" class="headerlink" title="2.1、概念"></a>2.1、概念</h2><blockquote><p>和CountDownLatch相反，需要集齐七颗龙珠，召唤神龙。也就是做加法，开始是0，加到某个值的时候就执行</p></blockquote><p>CyclicBarrier的字面意思就是可循环（cyclic）使用的屏障（Barrier）。它要求做的事情是，让一组线程到达一个屏障（也可以叫同步点）时被阻塞，直到最后一个线程到达屏障时，屏障才会开门，所有被屏障拦截的线程才会继续干活，线程进入屏障通过CyclicBarrier的await方法。</p><h2 id="2-2、案例"><a href="#2-2、案例" class="headerlink" title="2.2、案例"></a>2.2、案例</h2><p>集齐7个龙珠，召唤神龙的Demo，我们需要首先创建CyclicBarrier</p><pre><code class="java">/*** 定义一个循环屏障，参数1：需要累加的值，参数2 需要执行的方法*/CyclicBarrier cyclicBarrier = new CyclicBarrier(7, () -&gt; {    System.out.println(&quot;召唤神龙&quot;);});</code></pre><p>然后同时编写七个线程，进行龙珠收集，但一个线程收集到了的时候，我们需要让他执行await方法，等待到7个线程全部执行完毕后，我们就执行原来定义好的方法</p><pre><code class="java">        for (int i = 0; i &lt; 7; i++) {            final Integer tempInt = i;            new Thread(() -&gt; {                System.out.println(Thread.currentThread().getName() + &quot;\t 收集到 第&quot; + tempInt + &quot;颗龙珠&quot;);                try {                    // 先到的被阻塞，等全部线程完成后，才能执行方法                    cyclicBarrier.await();                } catch (InterruptedException e) {                    e.printStackTrace();                } catch (BrokenBarrierException e) {                    e.printStackTrace();                }            }, String.valueOf(i)).start();        }</code></pre><h2 id="2-3、完整代码"><a href="#2-3、完整代码" class="headerlink" title="2.3、完整代码"></a>2.3、完整代码</h2><pre><code class="java">package com.lsq.juc.lock;import java.util.concurrent.BrokenBarrierException;import java.util.concurrent.CyclicBarrier;/** * @Description:CyclicBarrier循环屏障 * @Author: LSQ * Date: 2020/7/16 9:36 */public class CyclicBarrierDemo {    public static void main(String[] args) {        /**         * 定义一个循环屏障，参数1：需要累加的值，参数2 需要执行的方法         */        CyclicBarrier cyclicBarrier = new CyclicBarrier(7, () -&gt; {            System.out.println(&quot;召唤神龙&quot;);        });        for (int i = 0; i &lt; 7; i++) {            final Integer tempInt = i;            new Thread(() -&gt; {                System.out.println(Thread.currentThread().getName() + &quot;\t 收集到 第&quot; + tempInt + &quot;颗龙珠&quot;);                try {                    // 先到的被阻塞，等全部线程完成后，才能执行方法                    cyclicBarrier.await();                } catch (InterruptedException e) {                    e.printStackTrace();                } catch (BrokenBarrierException e) {                    e.printStackTrace();                }            }, String.valueOf(i)).start();        }    }}</code></pre><h1 id="03、Semaphore：信号量"><a href="#03、Semaphore：信号量" class="headerlink" title="03、Semaphore：信号量"></a>03、Semaphore：信号量</h1><h2 id="3-1、概念"><a href="#3-1、概念" class="headerlink" title="3.1、概念"></a>3.1、概念</h2><p>信号量主要用于两个目的</p><ul><li>一个是用于共享资源的互斥使用</li><li>另一个用于并发线程数的控制</li></ul><h2 id="3-2、代码"><a href="#3-2、代码" class="headerlink" title="3.2、代码"></a>3.2、代码</h2><p>我们模拟一个抢车位的场景，假设一共有6个车，3个停车位</p><p>那么我们首先需要定义信号量为3，也就是3个停车位</p><pre><code class="java">/*** 初始化一个信号量为3，默认是false 非公平锁， 模拟3个停车位*/Semaphore semaphore = new Semaphore(3, false);</code></pre><p>然后我们模拟6辆车同时并发抢占停车位，但第一个车辆抢占到停车位后，信号量需要减1</p><pre><code class="java">// 代表一辆车，已经占用了该车位semaphore.acquire(); // 抢占</code></pre><p>同时车辆假设需要等待3秒后，释放信号量</p><pre><code class="java">// 每个车停3秒try {    TimeUnit.SECONDS.sleep(3);} catch (InterruptedException e) {    e.printStackTrace();}</code></pre><p>最后车辆离开，释放信号量</p><pre><code class="java">// 释放停车位semaphore.release();</code></pre><h2 id="3-3、完整代码"><a href="#3-3、完整代码" class="headerlink" title="3.3、完整代码"></a>3.3、完整代码</h2><pre><code class="java">package com.lsq.juc.lock;import java.util.concurrent.Semaphore;import java.util.concurrent.TimeUnit;/** * @Description:信号量Demo * @Author: LSQ * Date: 2020/7/16 9:44 */public class SemaphoreDemo {    public static void main(String[] args) {        /**         * 初始化一个信号量为3，默认是false 非公平锁， 模拟3个停车位         */        Semaphore semaphore = new Semaphore(3, false);        // 模拟6部车        for (int i = 0; i &lt; 6; i++) {            new Thread(() -&gt; {                try {                    // 代表一辆车，已经占用了该车位                    semaphore.acquire(); // 抢占                    System.out.println(Thread.currentThread().getName() + &quot;\t 抢到车位&quot;);                    // 每个车停3秒                    try {                        TimeUnit.SECONDS.sleep(3);                    } catch (InterruptedException e) {                        e.printStackTrace();                    }                    System.out.println(Thread.currentThread().getName() + &quot;\t 离开车位&quot;);                } catch (InterruptedException e) {                    e.printStackTrace();                } finally {                    // 释放停车位                    semaphore.release();                }            }, String.valueOf(i)).start();        }    }}</code></pre><p>运行结果</p><pre><code>0     抢到车位2     抢到车位1     抢到车位2     离开车位1     离开车位3     抢到车位0     离开车位4     抢到车位5     抢到车位4     离开车位3     离开车位5     离开车位</code></pre><p>看运行结果能够发现，0 2 1 车辆首先抢占到了停车位，然后等待3秒后，离开，然后后面 3 4 5 又抢到了车位</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、CountDownLatch&quot;&gt;&lt;a href=&quot;#01、CountDownLatch&quot; class=&quot;headerlink&quot; title=&quot;01、CountDownLatch&quot;&gt;&lt;/a&gt;01、CountDownLatch&lt;/h1&gt;&lt;h2 id=&quot;1-1、概
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>07阻塞队列</title>
    <link href="http://lsqstudy.top/post/6e2e70f3.html"/>
    <id>http://lsqstudy.top/post/6e2e70f3.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-16T03:09:50.549Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、概念"><a href="#01、概念" class="headerlink" title="01、概念"></a>01、概念</h1><h2 id="1-1、队列"><a href="#1-1、队列" class="headerlink" title="1.1、队列"></a>1.1、队列</h2><p>队列就可以想成是一个数组，从一头进入，一头出去，排队买饭</p><h2 id="1-2、阻塞队列"><a href="#1-2、阻塞队列" class="headerlink" title="1.2、阻塞队列"></a>1.2、阻塞队列</h2><p>BlockingQueue   阻塞队列，排队拥堵，首先它是一个队列，而一个阻塞队列在数据结构中所起的作用大致如下图所示：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716095119.png" alt=""></p><p>线程1往阻塞队列中添加元素，而线程2从阻塞队列中移除元素</p><ul><li><p><strong>当阻塞队列是空时，从队列中获取元素的操作将会被阻塞</strong></p><ul><li>当蛋糕店的柜子空的时候，无法从柜子里面获取蛋糕</li></ul></li><li><p><strong>当阻塞队列是满时，从队列中添加元素的操作将会被阻塞</strong></p><ul><li>当蛋糕店的柜子满的时候，无法继续向柜子里面添加蛋糕了</li></ul></li></ul><p>也就是说 试图从空的阻塞队列中获取元素的线程将会被阻塞，直到其它线程往空的队列插入新的元素</p><p>同理，试图往已经满的阻塞队列中添加新元素的线程，直到其它线程往满的队列中移除一个或多个元素，或者完全清空队列后，使队列重新变得空闲起来，并后续新增</p><h1 id="02、为什么要用？"><a href="#02、为什么要用？" class="headerlink" title="02、为什么要用？"></a>02、为什么要用？</h1><p>去海底捞吃饭，大厅满了，需要进候厅等待，但是这些等待的客户能够对商家带来利润，因此我们非常欢迎他们阻塞</p><p>在多线程领域：所谓的阻塞，在某些清空下会挂起线程（即阻塞），一旦条件满足，被挂起的线程又会自动唤醒</p><h2 id="2-1、为什么需要BlockingQueue"><a href="#2-1、为什么需要BlockingQueue" class="headerlink" title="2.1、为什么需要BlockingQueue"></a>2.1、为什么需要BlockingQueue</h2><p>好处是我们不需要关心什么时候需要阻塞线程，什么时候需要唤醒线程，因为这一切BlockingQueue都帮你一手包办了</p><p>在concurrent包发布以前，在多线程环境下，我们每个程序员都必须自己取控制这些细节，尤其还要兼顾效率和线程安全，而这会给我们的程序带来不小的复杂度。</p><h1 id="03、架构"><a href="#03、架构" class="headerlink" title="03、架构"></a>03、架构</h1><blockquote><p>你用过List集合类?<br>ArrayList集合类熟悉么？<br>还用过 CopyOnWriteList  和 BlockingQueue?</p></blockquote><p>BlockingQueue阻塞队列是属于一个接口，底下有七个实现类</p><ul><li><code>ArrayBlockQueue</code>：由数组结构组成的有界阻塞队列</li><li><code>LinkedBlockingQueue</code>：由链表结构组成的有界（但是默认大小 Integer.MAX_VALUE）的阻塞队列<ul><li>有界，但是界限非常大，相当于无界，可以当成无界</li></ul></li><li>PriorityBlockQueue：支持优先级排序的无界阻塞队列</li><li>DelayQueue：使用优先级队列实现的延迟无界阻塞队列</li><li><code>SynchronousQueue</code>：不存储元素的阻塞队列，也即单个元素的队列<ul><li>生产一个，消费一个，不存储元素，不消费不生产</li></ul></li><li>LinkedTransferQueue：由链表结构组成的无界阻塞队列</li><li>LinkedBlockingDeque：由链表结构组成的双向阻塞队列</li></ul><blockquote><p>这里需要掌握的是：<code>ArrayBlockQueue</code>、<code>LinkedBlockingQueue</code>、<code>SynchronousQueue</code></p></blockquote><h1 id="04、BlockingQueue核心方法"><a href="#04、BlockingQueue核心方法" class="headerlink" title="04、BlockingQueue核心方法"></a>04、BlockingQueue核心方法</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716103520.png" alt=""></p><table><thead><tr><th>抛出异常</th><th>当阻塞队列满时：在往队列中add插入元素会抛出 <code>IIIegalStateException：Queue full</code>                     当阻塞队列空时：再往队列中remove移除元素，会抛出<code>NoSuchException</code></th></tr></thead><tbody><tr><td>特殊性</td><td>插入方法，成功true，失败false       移除方法：成功返回出队列元素，队列没有就返回空</td></tr><tr><td>一直阻塞</td><td>当阻塞队列满时，生产者继续往队列里put元素，队列会一直阻塞生产线程直到put数据or响应中断退出， 当阻塞队列空时，消费者线程试图从队列里take元素，队列会一直阻塞消费者线程直到队列可用。</td></tr><tr><td>超时退出</td><td>当阻塞队列满时，队里会阻塞生产者线程一定时间，超过限时后生产者线程会退出</td></tr></tbody></table><h2 id="4-1、抛出异常组"><a href="#4-1、抛出异常组" class="headerlink" title="4.1、抛出异常组"></a>4.1、抛出异常组</h2><p>但执行add方法，向已经满的ArrayBlockingQueue中添加元素时候，会抛出异常</p><pre><code class="java">// 阻塞队列，需要填入默认值BlockingQueue&lt;String&gt; blockingQueue = new ArrayBlockingQueue&lt;&gt;(3);System.out.println(blockingQueue.add(&quot;a&quot;));System.out.println(blockingQueue.add(&quot;b&quot;));System.out.println(blockingQueue.add(&quot;c&quot;));System.out.println(blockingQueue.add(&quot;XXX&quot;));</code></pre><p>运行后：</p><pre><code class="java">truetruetrueException in thread &quot;main&quot; java.lang.IllegalStateException: Queue full    at java.util.AbstractQueue.add(AbstractQueue.java:98)    at java.util.concurrent.ArrayBlockingQueue.add(ArrayBlockingQueue.java:312)    at com.moxi.interview.study.queue.BlockingQueueDemo.main(BlockingQueueDemo.java:25)</code></pre><p>同时如果我们多取出元素的时候，也会抛出异常，我们假设只存储了3个值，但是取的时候，取了四次</p><pre><code class="java">// 阻塞队列，需要填入默认值BlockingQueue&lt;String&gt; blockingQueue = new ArrayBlockingQueue&lt;&gt;(3);System.out.println(blockingQueue.add(&quot;a&quot;));System.out.println(blockingQueue.add(&quot;b&quot;));System.out.println(blockingQueue.add(&quot;c&quot;));System.out.println(blockingQueue.remove());System.out.println(blockingQueue.remove());System.out.println(blockingQueue.remove());System.out.println(blockingQueue.remove());</code></pre><p>那么出现异常</p><pre><code class="java">truetruetrueabcException in thread &quot;main&quot; java.util.NoSuchElementException    at java.util.AbstractQueue.remove(AbstractQueue.java:117)    at com.moxi.interview.study.queue.BlockingQueueDemo.main(BlockingQueueDemo.java:30)</code></pre><h2 id="4-2、布尔类型组"><a href="#4-2、布尔类型组" class="headerlink" title="4.2、布尔类型组"></a>4.2、布尔类型组</h2><p>我们使用 offer的方法，添加元素时候，如果阻塞队列满了后，会返回false，否者返回true</p><p>同时在取的时候，如果队列已空，那么会返回null</p><pre><code class="java">BlockingQueue blockingQueue = new ArrayBlockingQueue(3);System.out.println(blockingQueue.offer(&quot;a&quot;));System.out.println(blockingQueue.offer(&quot;b&quot;));System.out.println(blockingQueue.offer(&quot;c&quot;));System.out.println(blockingQueue.offer(&quot;d&quot;));System.out.println(blockingQueue.poll());System.out.println(blockingQueue.poll());System.out.println(blockingQueue.poll());System.out.println(blockingQueue.poll());</code></pre><p>运行结果</p><pre><code class="java">truetruetruefalseabcnull</code></pre><h2 id="4-3、阻塞队列组"><a href="#4-3、阻塞队列组" class="headerlink" title="4.3、阻塞队列组"></a>4.3、阻塞队列组</h2><p>我们使用 put的方法，添加元素时候，如果阻塞队列满了后，添加消息的线程，会一直阻塞，直到队列元素减少，会被清空，才会唤醒</p><p>一般在消息中间件，比如RabbitMQ中会使用到，因为需要保证消息百分百不丢失，因此只有让它阻塞</p><pre><code class="java">BlockingQueue&lt;String&gt; blockingQueue = new ArrayBlockingQueue&lt;&gt;(3);blockingQueue.put(&quot;a&quot;);blockingQueue.put(&quot;b&quot;);blockingQueue.put(&quot;c&quot;);System.out.println(&quot;================&quot;);blockingQueue.take();blockingQueue.take();blockingQueue.take();blockingQueue.take();</code></pre><p>同时使用take取消息的时候，如果内容不存在的时候，也会被阻塞</p><h2 id="4-4、不见不散组"><a href="#4-4、不见不散组" class="headerlink" title="4.4、不见不散组"></a>4.4、不见不散组</h2><p>offer( )  ， poll 加时间</p><p>使用offer插入的时候，需要指定时间，如果2秒还没有插入，那么就放弃插入</p><pre><code class="java">BlockingQueue&lt;String&gt; blockingQueue = new ArrayBlockingQueue&lt;&gt;(3);System.out.println(blockingQueue.offer(&quot;a&quot;, 2L, TimeUnit.SECONDS));System.out.println(blockingQueue.offer(&quot;b&quot;, 2L, TimeUnit.SECONDS));System.out.println(blockingQueue.offer(&quot;c&quot;, 2L, TimeUnit.SECONDS));System.out.println(blockingQueue.offer(&quot;d&quot;, 2L, TimeUnit.SECONDS));</code></pre><p>同时取的时候也进行判断</p><pre><code class="java">System.out.println(blockingQueue.poll(2L, TimeUnit.SECONDS));System.out.println(blockingQueue.poll(2L, TimeUnit.SECONDS));System.out.println(blockingQueue.poll(2L, TimeUnit.SECONDS));System.out.println(blockingQueue.poll(2L, TimeUnit.SECONDS));</code></pre><p>如果2秒内取不出来，那么就返回null</p><h1 id="05、SynchronousQueue"><a href="#05、SynchronousQueue" class="headerlink" title="05、SynchronousQueue"></a>05、SynchronousQueue</h1><p><code>SynchronousQueue</code>没有容量，与其他BlockingQueue不同，SynchronousQueue是一个不存储的BlockingQueue，每一个put操作必须等待一个take操作，否者不能继续添加元素</p><p>下面我们测试SynchronousQueue添加元素的过程</p><p>首先我们创建了两个线程，一个线程用于生产，一个线程用于消费</p><p>生产的线程分别put了 A、B、C这三个字段</p><pre><code class="java">BlockingQueue&lt;String&gt; blockingQueue = new SynchronousQueue&lt;&gt;();new Thread(() -&gt; {    try {               System.out.println(Thread.currentThread().getName() + &quot;\t put A &quot;);        blockingQueue.put(&quot;A&quot;);        System.out.println(Thread.currentThread().getName() + &quot;\t put B &quot;);        blockingQueue.put(&quot;B&quot;);                System.out.println(Thread.currentThread().getName() + &quot;\t put C &quot;);        blockingQueue.put(&quot;C&quot;);            } catch (InterruptedException e) {        e.printStackTrace();    }}, &quot;t1&quot;).start();</code></pre><p>消费线程使用take，消费阻塞队列中的内容，并且每次消费前，都等待5秒</p><pre><code class="java">        new Thread(() -&gt; {            try {                try {                    TimeUnit.SECONDS.sleep(5);                } catch (InterruptedException e) {                    e.printStackTrace();                }                blockingQueue.take();                System.out.println(Thread.currentThread().getName() + &quot;\t take A &quot;);                try {                    TimeUnit.SECONDS.sleep(5);                } catch (InterruptedException e) {                    e.printStackTrace();                }                blockingQueue.take();                System.out.println(Thread.currentThread().getName() + &quot;\t take B &quot;);                try {                    TimeUnit.SECONDS.sleep(5);                } catch (InterruptedException e) {                    e.printStackTrace();                }                blockingQueue.take();                System.out.println(Thread.currentThread().getName() + &quot;\t take C &quot;);            } catch (InterruptedException e) {                e.printStackTrace();            }        }, &quot;t2&quot;).start();</code></pre><p>最后结果输出为：</p><pre><code>t1     put A t2     take A 5秒后...t1     put B t2     take B 5秒后...t1     put C t2     take C </code></pre><p>我们从最后的运行结果可以看出，每次t1线程向队列中添加阻塞队列添加元素后，t1输入线程就会等待 t2消费线程，t2消费后，t2处于挂起状态，等待t1在存入，从而周而复始，形成 一存一取的状态</p><h1 id="06、阻塞队列的用处"><a href="#06、阻塞队列的用处" class="headerlink" title="06、阻塞队列的用处"></a>06、阻塞队列的用处</h1><h2 id="6-1、生产者消费者模式"><a href="#6-1、生产者消费者模式" class="headerlink" title="6.1、生产者消费者模式"></a>6.1、生产者消费者模式</h2><p>一个初始值为0的变量，两个线程对其交替操作，一个加1，一个减1，来5轮</p><p>关于多线程的操作，我们需要记住下面几句</p><ul><li>线程 操作 资源类</li><li>判断 干活 通知</li><li>防止虚假唤醒机制</li></ul><p>我们下面实现一个简单的生产者消费者模式，首先有资源类ShareData</p><pre><code class="java">/** * 资源类 */class ShareData {    private int number = 0;    private Lock lock = new ReentrantLock();    private Condition condition = lock.newCondition();    public void increment() throws Exception{        // 同步代码块，加锁        lock.lock();        try {            // 判断            while(number != 0) {                // 等待不能生产                condition.await();            }            // 干活            number++;            System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number);            // 通知 唤醒            condition.signalAll();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }    }    public void decrement() throws Exception{        // 同步代码块，加锁        lock.lock();        try {            // 判断            while(number == 0) {                // 等待不能消费                condition.await();            }            // 干活            number--;            System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number);            // 通知 唤醒            condition.signalAll();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }    }}</code></pre><p>里面有一个number变量，同时提供了increment 和 decrement的方法，分别让number 加1和减1</p><blockquote><p>但是我们在进行判断的时候，<strong>为了防止出现虚假唤醒机制，不能使用if来进行判断，而应该使用while</strong></p></blockquote><pre><code class="java">// 判断while(number != 0) {    // 等待不能生产    condition.await();}</code></pre><p>不能使用 if判断</p><pre><code class="java">// 判断if(number != 0) {    // 等待不能生产    condition.await();}</code></pre><h2 id="6-2、完整代码"><a href="#6-2、完整代码" class="headerlink" title="6.2、完整代码"></a>6.2、完整代码</h2><pre><code class="java">package com.lsq.juc.Queue;import java.util.concurrent.locks.Condition;import java.util.concurrent.locks.Lock;import java.util.concurrent.locks.ReentrantLock;/** * 生产者消费者 传统版 * 题目：一个初始值为0的变量，两个线程对其交替操作，一个加1，一个减1，来5轮 * @Author: LSQ * Date: 2020/7/16 10:48 *//** * 线程 操作 资源类 * 判断 干活 通知 * 防止虚假唤醒机制 *//** * 资源类 */class ShareData {    private int number = 0;    private Lock lock = new ReentrantLock();    private Condition condition = lock.newCondition();    public void increment() throws Exception{        // 同步代码块，加锁        lock.lock();        try {            // 判断            while(number != 0) {                // 等待不能生产                condition.await();            }            // 干活            number++;            System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number);            // 通知 唤醒            condition.signalAll();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }    }    public void decrement() throws Exception{        // 同步代码块，加锁        lock.lock();        try {            // 判断            while(number == 0) {                // 等待不能消费                condition.await();            }            // 干活            number--;            System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number);            // 通知 唤醒            condition.signalAll();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }    }}public class ProdConsumerTraditionDemo {    public static void main(String[] args) {        // 高内聚，低耦合    内聚指的是，一个空调，自身带有调节温度高低的方法        ShareData shareData = new ShareData();        // t1线程，生产        new Thread(() -&gt; {            for (int i = 0; i &lt; 5; i++) {                try {                    shareData.increment();                } catch (Exception e) {                    e.printStackTrace();                }            }        }, &quot;t1&quot;).start();        // t2线程，消费        new Thread(() -&gt; {            for (int i = 0; i &lt; 5; i++) {                try {                    shareData.decrement();                } catch (Exception e) {                    e.printStackTrace();                }            }        }, &quot;t2&quot;).start();    }}</code></pre><p>最后运行成功后，我们一个进行生产，一个进行消费</p><pre><code>t1     1t2     0t1     1t2     0t1     1t2     0t1     1t2     0t1     1t2     0</code></pre><h1 id="07、【重点】生成者和消费者3-0"><a href="#07、【重点】生成者和消费者3-0" class="headerlink" title="07、【重点】生成者和消费者3.0"></a>07、【重点】生成者和消费者3.0</h1><p>在concurrent包发布以前，在多线程环境下，我们每个程序员都必须自己去控制这些细节，尤其还要兼顾效率和线程安全，则这会给我们的程序带来不小的时间复杂度</p><p>现在我们使用新版的阻塞队列版生产者和消费者，使用：volatile、CAS、atomicInteger、BlockQueue、线程交互、原子引用</p><pre><code class="java">package com.lsq.juc.Queue;import java.util.concurrent.ArrayBlockingQueue;import java.util.concurrent.BlockingQueue;import java.util.concurrent.TimeUnit;import java.util.concurrent.atomic.AtomicInteger;/** * 生产者消费者  阻塞队列版 * 使用：volatile、CAS、atomicInteger、BlockQueue、线程交互、原子引用 * @Author: LSQ * Date: 2020/7/16 10:50 *//** * 源源类 */class MyResource {    // 默认开启，进行生产消费    // 这里用到了volatile是为了保持数据的可见性，也就是当FLAG修改时，要马上通知其它线程进行修改    private volatile boolean FLAG = true;    // 使用原子包装类，而不用number++    private AtomicInteger atomicInteger = new AtomicInteger();    // 这里不能为了满足条件，而实例化一个具体的SynchronousBlockingQueue    BlockingQueue&lt;String&gt; blockingQueue = null;    // 而应该采用依赖注入里面的，构造注入方法传入    public MyResource(BlockingQueue&lt;String&gt; blockingQueue) {        this.blockingQueue = blockingQueue;        // 查询出传入的class是什么        System.out.println(blockingQueue.getClass().getName());    }    /**     * 生产     * @throws Exception     */    public void myProd() throws Exception{        String data = null;        boolean retValue;        // 多线程环境的判断，一定要使用while进行，防止出现虚假唤醒        // 当FLAG为true的时候，开始生产        while(FLAG) {            data = atomicInteger.incrementAndGet() + &quot;&quot;;            // 2秒存入1个data            retValue = blockingQueue.offer(data, 2L, TimeUnit.SECONDS);            if(retValue) {                System.out.println(Thread.currentThread().getName() + &quot;\t 插入队列:&quot; + data  + &quot;成功&quot; );            } else {                System.out.println(Thread.currentThread().getName() + &quot;\t 插入队列:&quot; + data  + &quot;失败&quot; );            }            try {                TimeUnit.SECONDS.sleep(1);            } catch (InterruptedException e) {                e.printStackTrace();            }        }        System.out.println(Thread.currentThread().getName() + &quot;\t 停止生产，表示FLAG=false，生产介绍&quot;);    }    /**     * 消费     * @throws Exception     */    public void myConsumer() throws Exception{        String retValue;        // 多线程环境的判断，一定要使用while进行，防止出现虚假唤醒        // 当FLAG为true的时候，开始生产        while(FLAG) {            // 2秒存入1个data            retValue = blockingQueue.poll(2L, TimeUnit.SECONDS);            if(retValue != null &amp;&amp; retValue != &quot;&quot;) {                System.out.println(Thread.currentThread().getName() + &quot;\t 消费队列:&quot; + retValue  + &quot;成功&quot; );            } else {                FLAG = false;                System.out.println(Thread.currentThread().getName() + &quot;\t 消费失败，队列中已为空，退出&quot; );                // 退出消费队列                return;            }        }    }    /**     * 停止生产的判断     */    public void stop() {        this.FLAG = false;    }}public class ProdConsumerBlockingQueueDemo {    public static void main(String[] args) {        // 传入具体的实现类， ArrayBlockingQueue        MyResource myResource = new MyResource(new ArrayBlockingQueue&lt;String&gt;(10));        new Thread(() -&gt; {            System.out.println(Thread.currentThread().getName() + &quot;\t 生产线程启动&quot;);            System.out.println(&quot;&quot;);            System.out.println(&quot;&quot;);            try {                myResource.myProd();                System.out.println(&quot;&quot;);                System.out.println(&quot;&quot;);            } catch (Exception e) {                e.printStackTrace();            }        }, &quot;prod&quot;).start();        new Thread(() -&gt; {            System.out.println(Thread.currentThread().getName() + &quot;\t 消费线程启动&quot;);            try {                myResource.myConsumer();            } catch (Exception e) {                e.printStackTrace();            }        }, &quot;consumer&quot;).start();        // 5秒后，停止生产和消费        try {            TimeUnit.SECONDS.sleep(5);        } catch (InterruptedException e) {            e.printStackTrace();        }        System.out.println(&quot;&quot;);        System.out.println(&quot;&quot;);        System.out.println(&quot;5秒中后，生产和消费线程停止，线程结束&quot;);        myResource.stop();    }}</code></pre><p>最后运行结果</p><pre><code>java.util.concurrent.ArrayBlockingQueueprod     生产线程启动consumer     消费线程启动prod     插入队列:1成功consumer     消费队列:1成功prod     插入队列:2成功consumer     消费队列:2成功prod     插入队列:3成功consumer     消费队列:3成功prod     插入队列:4成功consumer     消费队列:4成功prod     插入队列:5成功consumer     消费队列:5成功5秒中后，生产和消费线程停止，线程结束prod     停止生产，表示FLAG=false，生产介绍</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、概念&quot;&gt;&lt;a href=&quot;#01、概念&quot; class=&quot;headerlink&quot; title=&quot;01、概念&quot;&gt;&lt;/a&gt;01、概念&lt;/h1&gt;&lt;h2 id=&quot;1-1、队列&quot;&gt;&lt;a href=&quot;#1-1、队列&quot; class=&quot;headerlink&quot; title=&quot;1
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>08Synchronized和Lock的区别与好处</title>
    <link href="http://lsqstudy.top/post/980a6200.html"/>
    <id>http://lsqstudy.top/post/980a6200.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-16T03:34:37.304Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、Synchronized和Lock的区别"><a href="#01、Synchronized和Lock的区别" class="headerlink" title="01、Synchronized和Lock的区别"></a>01、Synchronized和Lock的区别</h1><h2 id="1-1、前言"><a href="#1-1、前言" class="headerlink" title="1.1、前言"></a>1.1、前言</h2><blockquote><p>早期的时候我们对线程的主要操作为：</p></blockquote><p>synchronized ：wati  notify</p><blockquote><p>然后后面出现了替代方案</p></blockquote><p>lock ：await  singal</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716113428.png" alt=""></p><h2 id="1-2、问题"><a href="#1-2、问题" class="headerlink" title="1.2、问题"></a>1.2、问题</h2><blockquote><p>synchronized 和 lock 有什么区别？用新的lock有什么好处？举例说明</p></blockquote><ol><li><code>synchronized</code>属于JVM层面，属于java的关键字<ul><li>monitorenter（底层是通过monitor对象来完成，其实<code>wait/notify</code>等方法也依赖于monitor对象 只能在同步块或者方法中才能调用<code>wait/ notify</code>等方法）</li><li>Lock是具体类（<code>java.util.concurrent.locks.Lock</code>）是api层面的锁</li></ul></li><li>使用方法：<ul><li><code>synchronized</code>：不需要用户去手动释放锁，当synchronized代码执行后，系统会自动让线程释放对锁的占用</li><li><code>ReentrantLock</code>：则需要用户去手动释放锁，若没有主动释放锁，就有可能出现死锁的现象，需要<code>lock()</code>和 <code>unlock()</code>配置<code>try-catch</code>语句来完成</li></ul></li><li>等待是否中断<ul><li><code>synchronized</code>：不可中断，除非抛出异常或者正常运行完成</li><li><code>ReentrantLock</code>：可中断，可以设置超时方法<ul><li>设置超时方法，<code>trylock(long timeout, TimeUnit unit)</code></li><li><code>lockInterrupible()</code>放代码块中，调用<code>interrupt()</code>方法可以中断</li></ul></li></ul></li><li>加锁是否公平<ul><li><code>synchronized</code>：非公平锁</li><li><code>ReentrantLock</code>：默认非公平锁，构造函数可以传递boolean值，true为公平锁，false为非公平锁</li></ul></li><li>锁绑定多个条件Condition<ul><li><code>synchronized</code>：没有，要么随机，要么全部唤醒</li><li><code>ReentrantLock</code>：用来实现分组唤醒需要唤醒的线程，可以精确唤醒，而不是像synchronized那样，要么随机，要么全部唤醒</li></ul></li></ol><h2 id="1-3、-举例"><a href="#1-3、-举例" class="headerlink" title="1.3、 举例"></a>1.3、 举例</h2><p>针对刚刚提到的区别的第5条，我们有下面这样的一个场景</p><pre><code>题目：多线程之间按顺序调用，实现 A-&gt; B -&gt; C 三个线程启动，要求如下：AA打印5次，BB打印10次，CC打印15次紧接着AA打印5次，BB打印10次，CC打印15次..来10轮</code></pre><p>我们会发现，这样的场景在使用synchronized来完成的话，会非常的困难，但是使用lock就非常方便了</p><p>也就是我们需要实现一个链式唤醒的操作</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716112800.png" style="zoom:80%;" /><p>当A线程执行完后，B线程才能执行，然后B线程执行完成后，C线程才执行</p><blockquote><p>1、首先我们需要创建一个重入锁</p></blockquote><pre><code class="java">// 创建一个重入锁private Lock lock = new ReentrantLock();</code></pre><blockquote><p>2、然后定义三个条件，也可以称为锁的钥匙，通过它就可以获取到锁，进入到方法里面</p></blockquote><pre><code class="java">// 这三个相当于备用钥匙private Condition condition1 = lock.newCondition();private Condition condition2 = lock.newCondition();private Condition condition3 = lock.newCondition();</code></pre><blockquote><p>3、然后开始记住锁的三部曲： 判断   干活  唤醒</p></blockquote><p>这里的判断，为了避免虚假唤醒，一定要采用  while</p><p>干活就是把需要的内容，打印出来</p><p>唤醒的话，就是修改资源类的值，然后精准唤醒线程进行干活：这里A 唤醒B， B唤醒C，C又唤醒A</p><pre><code class="java">    public void print5() {        lock.lock();        try {            // 判断            while(number != 1) {                // 不等于1，需要等待                condition1.await();            }            // 干活            for (int i = 0; i &lt; 5; i++) {                System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number + &quot;\t&quot; + i);            }            // 唤醒 （干完活后，需要通知B线程执行）            number = 2;            // 通知2号去干活了            condition2.signal();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }</code></pre><h2 id="1-4、完整代码"><a href="#1-4、完整代码" class="headerlink" title="1.4、完整代码"></a>1.4、完整代码</h2><pre><code class="java">package com.lsq.juc.lock;import java.util.concurrent.locks.Condition;import java.util.concurrent.locks.Lock;import java.util.concurrent.locks.ReentrantLock;/** * @Description:Synchronized 和 Lock的区别 * @Author: LSQ * Date: 2020/7/16 11:30 */class ShareResource {    // A 1   B 2   c 3    private int number = 1;    // 创建一个重入锁    private Lock lock = new ReentrantLock();    // 这三个相当于备用钥匙    private Condition condition1 = lock.newCondition();    private Condition condition2 = lock.newCondition();    private Condition condition3 = lock.newCondition();    public void print5() {        lock.lock();        try {            // 判断            while(number != 1) {                // 不等于1，需要等待                condition1.await();            }            // 干活            for (int i = 0; i &lt; 5; i++) {                System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number + &quot;\t&quot; + i);            }            // 唤醒 （干完活后，需要通知B线程执行）            number = 2;            // 通知2号去干活了            condition2.signal();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }    }    public void print10() {        lock.lock();        try {            // 判断            while(number != 2) {                // 不等于1，需要等待                condition2.await();            }            // 干活            for (int i = 0; i &lt; 10; i++) {                System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number + &quot;\t&quot; + i);            }            // 唤醒 （干完活后，需要通知C线程执行）            number = 3;            // 通知2号去干活了            condition3.signal();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }    }    public void print15() {        lock.lock();        try {            // 判断            while(number != 3) {                // 不等于1，需要等待                condition3.await();            }            // 干活            for (int i = 0; i &lt; 15; i++) {                System.out.println(Thread.currentThread().getName() + &quot;\t &quot; + number + &quot;\t&quot; + i);            }            // 唤醒 （干完活后，需要通知C线程执行）            number = 1;            // 通知1号去干活了            condition1.signal();        } catch (Exception e) {            e.printStackTrace();        } finally {            lock.unlock();        }    }}public class SyncAndReentrantLockDemo {    public static void main(String[] args) {        ShareResource shareResource = new ShareResource();        new Thread(() -&gt; {            for (int i = 0; i &lt; 10; i++) {                shareResource.print5();            }        }, &quot;A&quot;).start();        new Thread(() -&gt; {            for (int i = 0; i &lt; 10; i++) {                shareResource.print10();            }        }, &quot;B&quot;).start();        new Thread(() -&gt; {            for (int i = 0; i &lt; 10; i++) {                shareResource.print15();            }        }, &quot;C&quot;).start();    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、Synchronized和Lock的区别&quot;&gt;&lt;a href=&quot;#01、Synchronized和Lock的区别&quot; class=&quot;headerlink&quot; title=&quot;01、Synchronized和Lock的区别&quot;&gt;&lt;/a&gt;01、Synchronized和L
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>09线程池</title>
    <link href="http://lsqstudy.top/post/ef7e1452.html"/>
    <id>http://lsqstudy.top/post/ef7e1452.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-16T04:28:13.401Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、线程池"><a href="#01、线程池" class="headerlink" title="01、线程池"></a>01、线程池</h1><h2 id="1-1、Java中有哪些方法获取多线程"><a href="#1-1、Java中有哪些方法获取多线程" class="headerlink" title="1.1、Java中有哪些方法获取多线程"></a>1.1、Java中有哪些方法获取多线程</h2><ul><li>实现Runnable接口</li><li>实现Callable接口</li><li>实例化Thread类</li><li>使用线程池获取</li></ul><h2 id="1-2、Callable接口"><a href="#1-2、Callable接口" class="headerlink" title="1.2、Callable接口"></a>1.2、Callable接口</h2><p>Callable接口，是一种让线程执行完成后，能够返回结果的</p><p>在说到Callable接口的时候，我们不得不提到Runnable接口</p><pre><code class="java">/** * 实现Runnable接口 */class MyThread implements Runnable {    @Override    public void run() {    }}</code></pre><p>我们知道，实现Runnable接口的时候，需要重写run方法，也就是线程在启动的时候，会自动调用的方法</p><p>同理，我们实现Callable接口，也需要<strong>实现call方法，但是这个时候我们还需要有返回值</strong>，这个Callable接口的应用场景一般就在于批处理业务，比如转账的时候，需要给一会返回结果的状态码回来，代表本次操作成功还是失败</p><pre><code class="java">/** * Callable有返回值 * 批量处理的时候，需要带返回值的接口（例如支付失败的时候，需要返回错误状态） * */class MyThread2 implements Callable&lt;Integer&gt; {    @Override    public Integer call() throws Exception {        System.out.println(&quot;come in Callable&quot;);        return 1024;    }}</code></pre><p>最后我们需要做的就是通过Thread线程， 将MyThread2实现Callable接口的类包装起来</p><p>这里<strong>需要用到的是FutureTask类，他实现了Runnable接口</strong>，并且还需要传递一个实现Callable接口的类作为构造函数</p><pre><code class="java">// FutureTask：实现了Runnable接口，构造函数又需要传入 Callable接口// 这里通过了FutureTask接触了Callable接口FutureTask&lt;Integer&gt; futureTask = new FutureTask&lt;&gt;(new MyThread2());</code></pre><p>然后在用Thread进行实例化，传入实现Runnabnle接口的FutureTask的类</p><pre><code class="java">Thread t1 = new Thread(futureTask, &quot;aaa&quot;);t1.start();</code></pre><p>最后通过 <code>futureTask.get()</code>获取到返回值</p><pre><code class="java">// 输出FutureTask的返回值System.out.println(&quot;result FutureTask &quot; + futureTask.get());</code></pre><p>这就相当于原来我们的方式是main方法一条龙之心，后面在引入Callable后，对于执行比较久的线程，可以单独新开一个线程进行执行，最后在进行汇总输出</p><p>最后需要注意的是 要求获得Callable线程的计算结果，如果没有计算完成就要去强求，会导致阻塞，直到计算完成</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716122732.png" alt=""></p><p>也就是说 <code>futureTask.get()</code> 需要放在最后执行，这样不会导致主线程阻塞</p><p>也可以使用下面算法，使用类似于自旋锁的方式来进行判断是否运行完毕</p><pre><code class="java">// 判断futureTask是否计算完成while(!futureTask.isDone()) {}</code></pre><h3 id="1-2-1、注意"><a href="#1-2-1、注意" class="headerlink" title="1.2.1、注意"></a>1.2.1、注意</h3><blockquote><p>多个线程执行 一个FutureTask的时候，只会计算一次</p></blockquote><pre><code class="java">FutureTask&lt;Integer&gt; futureTask = new FutureTask&lt;&gt;(new MyThread2());// 开启两个线程计算futureTasknew Thread(futureTask, &quot;AAA&quot;).start();new Thread(futureTask, &quot;BBB&quot;).start();</code></pre><blockquote><p>如果我们要两个线程同时计算任务的话，那么需要这样写，需要定义两个futureTask</p></blockquote><pre><code class="java">FutureTask&lt;Integer&gt; futureTask = new FutureTask&lt;&gt;(new MyThread2());FutureTask&lt;Integer&gt; futureTask2 = new FutureTask&lt;&gt;(new MyThread2());// 开启两个线程计算futureTasknew Thread(futureTask, &quot;AAA&quot;).start();new Thread(futureTask2, &quot;BBB&quot;).start();</code></pre><h2 id="1-3、ThreadPoolExecutor"><a href="#1-3、ThreadPoolExecutor" class="headerlink" title="1.3、ThreadPoolExecutor"></a>1.3、ThreadPoolExecutor</h2><h3 id="1-3-1、为什么用线程池"><a href="#1-3-1、为什么用线程池" class="headerlink" title="1.3.1、为什么用线程池"></a>1.3.1、为什么用线程池</h3><p>线程池做的主要工作就是控制运行的线程的数量，处理过程中，将任务放入到队列中，然后线程创建后，启动这些任务，如果线程数量超过了最大数量的线程排队等候，等其它线程执行完毕，再从队列中取出任务来执行。</p><p>线程池中的任务是放入到阻塞队列中的</p><blockquote><p>它的主要特点为：<strong>线程复用、控制最大并发数、管理线程</strong></p></blockquote><h3 id="1-3-2、线程池的好处"><a href="#1-3-2、线程池的好处" class="headerlink" title="1.3.2、线程池的好处"></a>1.3.2、线程池的好处</h3><blockquote><p>多核处理的好处是：省略的上下文的切换开销</p></blockquote><p>原来我们实例化对象的时候，是使用 new关键字进行创建，到了Spring后，我们学了IOC依赖注入，发现Spring帮我们将对象已经加载到了Spring容器中，只需要通过<code>@Autowrite</code>注解，就能够自动注入，从而使用</p><blockquote><p>使用线程池的好处</p></blockquote><ul><li>降低资源消耗。通过重复利用已创建的线程，降低线程创建和销毁造成的消耗</li><li>提高响应速度。当任务到达时，任务可以不需要等到线程创建就立即执行</li><li>提高线程的可管理性。线程是稀缺资源，如果无线创建，不仅会消耗系统资源，还会降低系统的稳定性，使用线程池可以进行统一的分配，调优和监控</li></ul><h3 id="1-3-3、架构说明"><a href="#1-3-3、架构说明" class="headerlink" title="1.3.3、架构说明"></a>1.3.3、架构说明</h3><p>Java中线程池是通过Executor框架实现的，该框架中用到了Executor，Executors（代表工具类），ExecutorService，<code>ThreadPoolExecutor</code>这几个类。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716115546.png" alt=""></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716122717.png" alt=""></p><h3 id="1-3-4、创建线程池"><a href="#1-3-4、创建线程池" class="headerlink" title="1.3.4、创建线程池"></a>1.3.4、创建线程池</h3><ul><li><code>Executors.newFixedThreadPool(int i)</code>：创建一个拥有 i 个线程的线程池<ul><li>执行长期的任务，性能好很多</li><li>创建一个定长线程池，可控制线程数最大并发数，超出的线程会在队列中等待</li></ul></li><li><code>Executors.newSingleThreadExecutor</code>：创建一个只有1个线程的 单线程池<ul><li>一个任务一个任务执行的场景</li><li>创建一个单线程化的线程池，它只会用唯一的工作线程来执行任务，保证所有任务按照指定顺序执行</li></ul></li><li><code>Executors.newCacheThreadPool()</code>;  创建一个可扩容的线程池<ul><li>执行很多短期异步的小程序或者负载教轻的服务器</li><li>创建一个可缓存线程池，如果线程长度超过处理需要，可灵活回收空闲线程，如无可回收，则新建新线程</li></ul></li></ul><p>具体使用，首先我们需要使用Executors工具类，进行创建线程池，这里创建了一个拥有5个线程的线程池</p><pre><code class="java">// 一池5个处理线程（用池化技术，一定要记得关闭）ExecutorService threadPool = Executors.newFixedThreadPool(5);// 创建一个只有一个线程的线程池ExecutorService threadPool = Executors.newSingleThreadExecutor();// 创建一个拥有N个线程的线程池，根据调度创建合适的线程ExecutorService threadPool = Executors.newCacheThreadPool();</code></pre><p>然后我们执行下面的的应用场景</p><pre><code>模拟10个用户来办理业务，每个用户就是一个来自外部请求线程</code></pre><p>我们需要使用 threadPool.execute执行业务，execute需要传入一个实现了Runnable接口的线程</p><pre><code class="java">threadPool.execute(() -&gt; {    System.out.println(Thread.currentThread().getName() + &quot;\t 给用户办理业务&quot;);});</code></pre><p>然后我们使用完毕后关闭线程池</p><pre><code class="java">threadPool.shutdown();</code></pre><p>完整代码为：</p><pre><code class="java">package com.lsq.juc.ThreadPool;import java.util.concurrent.ExecutorService;import java.util.concurrent.Executors;/** * @Description:第四种获取 / 使用 Java多线程的方式，通过线程池 * @Author: LSQ * Date: 2020/7/16 11:59 */public class MyThreadPoolDemo {    public static void main(String[] args) {        // Array  Arrays(辅助工具类)        // Collection Collections(辅助工具类)        // Executor Executors(辅助工具类)        // 一池5个处理线程（用池化技术，一定要记得关闭）        ExecutorService threadPool = Executors.newFixedThreadPool(5);        // 模拟10个用户来办理业务，每个用户就是一个来自外部请求线程        try {            // 循环十次，模拟业务办理，让5个线程处理这10个请求            for (int i = 0; i &lt; 10; i++) {                final int tempInt = i;                threadPool.execute(() -&gt; {                    System.out.println(Thread.currentThread().getName() + &quot;\t 给用户:&quot; + tempInt + &quot; 办理业务&quot;);                });            }        } catch (Exception e) {            e.printStackTrace();        } finally {            threadPool.shutdown();        }    }}</code></pre><p>最后结果：</p><pre><code>pool-1-thread-1     给用户:0 办理业务pool-1-thread-5     给用户:4 办理业务pool-1-thread-1     给用户:5 办理业务pool-1-thread-4     给用户:3 办理业务pool-1-thread-2     给用户:1 办理业务pool-1-thread-3     给用户:2 办理业务pool-1-thread-2     给用户:9 办理业务pool-1-thread-4     给用户:8 办理业务pool-1-thread-1     给用户:7 办理业务pool-1-thread-5     给用户:6 办理业务</code></pre><p>我们能够看到，一共有5个线程，在给10个用户办理业务</p><h2 id="1-4、线程池：底层实现"><a href="#1-4、线程池：底层实现" class="headerlink" title="1.4、线程池：底层实现"></a>1.4、线程池：底层实现</h2><p>查看源码，点击了<code>Executors.newSingleThreadExecutor</code>和 <code>Executors.newFixedThreadPool</code>能够发现底层都是使用了<code>ThreadPoolExecutor</code></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716120240.png" alt=""></p><p>我们可以看到线程池的内部，还使用到了<code>LinkedBlockingQueue</code>链表阻塞队列</p><p>同时在查看<code>Executors.newCacheThreadPool</code> 看到底层用的是<code>SynchronousBlockingQueue</code>阻塞队列</p><p>最后查看一下，完整的三个创建线程的方法</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716120355.png" alt=""></p><h2 id="1-5、线程池：重要参数"><a href="#1-5、线程池：重要参数" class="headerlink" title="1.5、线程池：重要参数"></a>1.5、线程池：重要参数</h2><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716120458.png" alt=""></p><blockquote><p>7大参数：</p></blockquote><ul><li><code>corePoolSize</code>：核心线程数，线程池中的常驻核心线程数<ul><li>在创建线程池后，当有请求任务来之后，就会安排池中的线程去执行请求任务，近似理解为今日当值线程</li><li>当线程池中的线程数目达到corePoolSize后，就会把到达的队列放到缓存队列中</li></ul></li><li><code>maximumPoolSize</code>：线程池能够容纳同时执行的最大线程数，此值必须大于等于1、<ul><li>相当有扩容后的线程数，这个线程池能容纳的最多线程数</li></ul></li><li><code>keepAliveTime</code>：多余的空闲线程存活时间<ul><li>当线程池数量超过corePoolSize时，当空闲时间达到keepAliveTime值时，多余的空闲线程会被销毁，直到只剩下corePoolSize个线程为止</li><li>默认情况下，只有当线程池中的线程数大于corePoolSize时，keepAliveTime才会起作用</li></ul></li><li><code>unit</code>：keepAliveTime的单位</li><li><code>workQueue</code>：任务队列，被提交的但未被执行的任务（类似于银行里面的候客区）<ul><li><code>LinkedBlockingQueue</code>：链表阻塞队列</li><li><code>SynchronousBlockingQueue</code>：同步阻塞队列</li></ul></li><li><code>threadFactory</code>：表示生成线程池中工作线程的线程工厂，用于创建线程池 一般用默认即可</li><li><code>handler</code>：拒绝策略，表示当队列满了并且工作线程大于线程池的最大线程数（maximumPoolSize3）时，如何来拒绝请求执行的Runnable的策略</li></ul><blockquote><p>当营业窗口和阻塞队列中都满了时候，就需要设置拒绝策略</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716122801.png" alt=""></p><h2 id="1-6、线程池：拒绝策略"><a href="#1-6、线程池：拒绝策略" class="headerlink" title="1.6、线程池：拒绝策略"></a>1.6、线程池：拒绝策略</h2><p>以下所有拒绝策略都实现了<code>RejectedExecutionHandler</code>接口</p><ul><li><code>AbortPolicy</code>：默认，直接抛出RejectedExcutionException异常，阻止系统正常运行</li><li><code>DiscardPolicy</code>：直接丢弃任务，不予任何处理也不抛出异常，如果运行任务丢失，这是一种好方案</li><li><code>CallerRunsPolicy</code>：该策略既不会抛弃任务，也不会抛出异常，而是将某些任务回退到调用者</li><li><code>DiscardOldestPolicy</code>：抛弃队列中等待最久的任务，然后把当前任务加入队列中尝试再次提交当前任务</li></ul><h2 id="1-7、线程池：底层工作原理"><a href="#1-7、线程池：底层工作原理" class="headerlink" title="1.7、线程池：底层工作原理"></a>1.7、线程池：底层工作原理</h2><h3 id="线程池运行架构图"><a href="#线程池运行架构图" class="headerlink" title="线程池运行架构图"></a>线程池运行架构图</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200716120959.png" alt=""></p><blockquote><p>说明</p></blockquote><ol><li><p>在创建了线程池后，等待提交过来的任务请求</p></li><li><p>当调用<code>execute()</code>方法添加一个请求任务时，线程池会做出如下判断</p><ol><li>如果正在运行的线程池数量小于<code>corePoolSize</code>，那么马上创建线程运行这个任务</li><li>如果正在运行的线程数量大于或等于<code>corePoolSize</code>，那么将这个任务放入队列</li><li>如果这时候队列满了，并且正在运行的线程数量还小于<code>maximumPoolSize，</code>那么还是创建非核心线程like运行这个任务；</li><li>如果队列满了并且正在运行的线程数量大于或等于<code>maximumPoolSize</code>，那么线程池会启动饱和拒绝策略来执行</li></ol></li><li><p>当一个线程完成任务时，它会从队列中取下一个任务来执行</p><ol><li>当一个线程无事可做操作一定的时间<code>keepAliveTime</code>时，线程池会判断：</li><li>如果当前运行的线程数大于<code>corePoolSize</code>，那么这个线程就被停掉</li><li>所以线程池的所有任务完成后，它会最终收缩到<code>corePoolSize</code>的大小</li></ol></li></ol><blockquote><p>以顾客去银行办理业务为例，谈谈线程池的底层工作原理</p></blockquote><ol><li>最开始假设来了两个顾客，因为<code>corePoolSize</code>为2，因此这两个顾客直接能够去窗口办理</li><li>后面又来了三个顾客，因为<code>corePool</code>已经被顾客占用了，因此只有去候客区，也就是阻塞队列中等待</li><li>后面的人又陆陆续续来了，候客区可能不够用了，因此需要申请增加处理请求的窗口，这里的窗口指的是线程池中的线程数，以此来解决线程不够用的问题</li><li>假设受理窗口已经达到最大数，并且请求数还是不断递增，此时候客区和线程池都已经满了，为了防止大量请求冲垮线程池，已经需要开启拒绝策略</li><li>临时增加的线程会因为超过了最大存活时间，就会销毁，最后从最大数削减到核心数</li></ol><h2 id="1-8、为什么不用默认创建的线程池？"><a href="#1-8、为什么不用默认创建的线程池？" class="headerlink" title="1.8、为什么不用默认创建的线程池？"></a>1.8、为什么不用默认创建的线程池？</h2><blockquote><p>线程池创建的方法有：固定数的，单一的，可变的，那么在实际开发中，应该使用哪个？</p></blockquote><p>答案：一个都不用，使用自己自定义的</p><blockquote><p>为什么不用Executors中JDK提供的？</p></blockquote><p>根据阿里巴巴手册：并发控制这章</p><ul><li>线程资源必须通过线程池提供，不允许在应用中自行显式创建线程<ul><li>使用线程池的好处是减少在创建和销毁线程上所消耗的时间以及系统资源的开销，解决资源不足的问题，如果不使用线程池，有可能造成系统创建大量同类线程而导致消耗完内存或者“过度切换”的问题</li></ul></li><li>线程池不允许使用Executors去创建，而是通过<code>ThreadToolExecutors</code>的方式，这样的处理方式让写的同学更加明确线程池的运行规则，规避资源耗尽的风险<ul><li><code>Executors</code>返回的线程池对象弊端如下：<ul><li><code>FixedThreadPool</code>和<code>SingleThreadPool</code>：<ul><li>运行的请求队列长度为：<code>Integer.MAX_VALUE</code>，可能会堆积大量的请求，从而导致<code>OOM</code></li></ul></li><li><code>CacheThreadPool</code>和<code>ScheduledThreadPool</code>:<ul><li>运行的请求队列长度为：<code>Integer.MAX_VALUE</code>，可能会堆积大量的请求，从而导致<code>OOM</code></li></ul></li></ul></li></ul></li></ul><h2 id="1-9、手写线程池"><a href="#1-9、手写线程池" class="headerlink" title="1.9、手写线程池"></a>1.9、手写线程池</h2><h3 id="1-9-1、采用默认拒绝策略"><a href="#1-9-1、采用默认拒绝策略" class="headerlink" title="1.9.1、采用默认拒绝策略"></a>1.9.1、采用默认拒绝策略</h3><p>从上面我们知道，因为默认的<code>Executors</code>创建的线程池，底层都是使用<code>LinkBlockingQueue</code>作为阻塞队列的，而LinkBlockingQueue虽然是有界的，但是它的界限是<code>Integer.MAX_VALUE</code>大概有20多亿，可以相当是无界的了，因此我们要使用<code>ThreadPoolExecutor</code>自己手动创建线程池，然后指定阻塞队列的大小</p><blockquote><p>1、创建了一个 核心线程数为2，最大线程数为5，并且阻塞队列数为3的线程池</p></blockquote><pre><code class="java">        // 手写线程池        final Integer corePoolSize = 2;        final Integer maximumPoolSize = 5;        final Long keepAliveTime = 1L;        // 自定义线程池，只改变了LinkBlockingQueue的队列大小        ExecutorService executorService = new ThreadPoolExecutor(                corePoolSize,                maximumPoolSize,                keepAliveTime,                TimeUnit.SECONDS,                new LinkedBlockingQueue&lt;&gt;(3),                Executors.defaultThreadFactory(),                new ThreadPoolExecutor.AbortPolicy());</code></pre><blockquote><p>2、然后使用for循环，模拟10个用户来进行请求</p></blockquote><pre><code class="java">      // 模拟10个用户来办理业务，每个用户就是一个来自外部请求线程        try {            // 循环十次，模拟业务办理，让5个线程处理这10个请求            for (int i = 0; i &lt; 10; i++) {                final int tempInt = i;                executorService.execute(() -&gt; {                    System.out.println(Thread.currentThread().getName() + &quot;\t 给用户:&quot; + tempInt + &quot; 办理业务&quot;);                });            }        } catch (Exception e) {            e.printStackTrace();        } finally {            executorService.shutdown();        }</code></pre><blockquote><p>运行结果：用户执行到第9个的时候，触发了异常，程序中断</p></blockquote><pre><code>pool-1-thread-1     给用户:0 办理业务pool-1-thread-4     给用户:6 办理业务pool-1-thread-3     给用户:5 办理业务pool-1-thread-2     给用户:1 办理业务pool-1-thread-2     给用户:4 办理业务pool-1-thread-5     给用户:7 办理业务pool-1-thread-4     给用户:2 办理业务pool-1-thread-3     给用户:3 办理业务java.util.concurrent.RejectedExecutionException: Task com.moxi.interview.study.thread.MyThreadPoolDemo$$Lambda$1/1747585824@4dd8dc3 rejected from java.util.concurrent.ThreadPoolExecutor@6d03e736[Running, pool size = 5, active threads = 3, queued tasks = 0, completed tasks = 5]    at java.util.concurrent.ThreadPoolExecutor$AbortPolicy.rejectedExecution(ThreadPoolExecutor.java:2047)    at java.util.concurrent.ThreadPoolExecutor.reject(ThreadPoolExecutor.java:823)    at java.util.concurrent.ThreadPoolExecutor.execute(ThreadPoolExecutor.java:1369)    at com.moxi.interview.study.thread.MyThreadPoolDemo.main(MyThreadPoolDemo.java:34)</code></pre><p>这是因为触发了拒绝策略，而我们设置的拒绝策略是默认的AbortPolicy，也就是抛异常的</p><p>触发条件是，请求的线程大于 阻塞队列大小 + 最大线程数 = 8 的时候，也就是说第9个线程来获取线程池中的线程时，就会抛出异常从而报错退出。</p><h3 id="1-9-2、采用allerRunsPolicy拒绝策略"><a href="#1-9-2、采用allerRunsPolicy拒绝策略" class="headerlink" title="1.9.2、采用allerRunsPolicy拒绝策略"></a>1.9.2、采用<code>allerRunsPolicy</code>拒绝策略</h3><p>当我们更好其它的拒绝策略时，采用<code>CallerRunsPolicy</code>拒绝策略，也称为回退策略，就是把任务丢回原来的请求开启线程着，我们看运行结果</p><pre><code>pool-1-thread-1     给用户:0 办理业务pool-1-thread-5     给用户:7 办理业务pool-1-thread-4     给用户:6 办理业务main     给用户:8 办理业务pool-1-thread-3     给用户:5 办理业务pool-1-thread-2     给用户:1 办理业务pool-1-thread-3     给用户:9 办理业务pool-1-thread-4     给用户:4 办理业务pool-1-thread-5     给用户:3 办理业务pool-1-thread-1     给用户:2 办理业务</code></pre><p>我们发现，输出的结果里面出现了main线程，因为线程池出发了拒绝策略，把任务回退到main线程，然后main线程对任务进行处理</p><h3 id="1-9-3、采用-DiscardPolicy拒绝策略"><a href="#1-9-3、采用-DiscardPolicy拒绝策略" class="headerlink" title="1.9.3、采用 DiscardPolicy拒绝策略"></a>1.9.3、采用 <code>DiscardPolicy</code>拒绝策略</h3><pre><code>pool-1-thread-1     给用户:0 办理业务pool-1-thread-3     给用户:5 办理业务pool-1-thread-1     给用户:2 办理业务pool-1-thread-2     给用户:1 办理业务pool-1-thread-1     给用户:4 办理业务pool-1-thread-5     给用户:7 办理业务pool-1-thread-4     给用户:6 办理业务pool-1-thread-3     给用户:3 办理业务</code></pre><p>采用DiscardPolicy拒绝策略会，线程池会自动把后面的任务都直接丢弃，也不报异常，当任务无关紧要的时候，可以采用这个方式</p><h3 id="1-9-3、采用DiscardOldestPolicy拒绝策略"><a href="#1-9-3、采用DiscardOldestPolicy拒绝策略" class="headerlink" title="1.9.3、采用DiscardOldestPolicy拒绝策略"></a>1.9.3、采用DiscardOldestPolicy拒绝策略</h3><pre><code>pool-1-thread-1     给用户:0 办理业务pool-1-thread-4     给用户:6 办理业务pool-1-thread-1     给用户:4 办理业务pool-1-thread-3     给用户:5 办理业务pool-1-thread-2     给用户:1 办理业务pool-1-thread-1     给用户:9 办理业务pool-1-thread-4     给用户:8 办理业务pool-1-thread-5     给用户:7 办理业务</code></pre><p>这个策略和刚刚差不多，会把最久的队列中的任务替换掉</p><h2 id="1-10、线程池的合理参数"><a href="#1-10、线程池的合理参数" class="headerlink" title="1.10、线程池的合理参数"></a>1.10、线程池的合理参数</h2><blockquote><p>生产环境中如何配置<code>corePoolSize</code>和<code>maximumPoolSize</code>?</p></blockquote><p>这个是根据具体业务来配置的，分为CPU密集型和IO密集型</p><blockquote><p>CPU密集型</p></blockquote><p>CPU密集的意思是该任务需要大量的运算，而没有阻塞，CPU一直全速运行</p><p>CPU密集任务只有在真正的多核CPU上才可能得到加速（通过多线程）</p><p>而在单核CPU上，无论你开几个模拟的多线程该任务都不可能得到加速，因为CPU总的运算能力就那些</p><p>CPU密集型任务配置尽可能少的线程数量：</p><p>一般公式：<strong>CPU核数 + 1个线程数</strong></p><blockquote><p>IO密集型</p></blockquote><p>由于IO密集型任务线程并不是一直在执行任务，则可能多的线程，如 CPU核数 * 2</p><p>IO密集型，即该任务需要大量的IO操作，即大量的阻塞</p><p>在单线程上运行IO密集型的任务会导致浪费大量的CPU运算能力花费在等待上</p><p>所以IO密集型任务中使用多线程可以大大的加速程序的运行，即使在单核CPU上，这种加速主要就是利用了被浪费掉的阻塞时间。</p><p>IO密集时，大部分线程都被阻塞，故需要多配置线程数：</p><p>参考公式：C<strong>PU核数 / (1 - 阻塞系数)      阻塞系数在0.8 ~ 0.9左右</strong></p><p><strong>例如：8核CPU：8/ (1 - 0.9) = 80个线程数</strong></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、线程池&quot;&gt;&lt;a href=&quot;#01、线程池&quot; class=&quot;headerlink&quot; title=&quot;01、线程池&quot;&gt;&lt;/a&gt;01、线程池&lt;/h1&gt;&lt;h2 id=&quot;1-1、Java中有哪些方法获取多线程&quot;&gt;&lt;a href=&quot;#1-1、Java中有哪些方法获取多线
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
</feed>
