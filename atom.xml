<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>卖剩鸭</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://lsqstudy.top/"/>
  <updated>2020-07-14T13:28:37.664Z</updated>
  <id>http://lsqstudy.top/</id>
  
  <author>
    <name>卖剩鸭</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>02垃圾回收算法</title>
    <link href="http://lsqstudy.top/post/7ff67db7.html"/>
    <id>http://lsqstudy.top/post/7ff67db7.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.664Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、垃圾回收算法"><a href="#01、垃圾回收算法" class="headerlink" title="01、垃圾回收算法"></a>01、垃圾回收算法</h1><h2 id="1-1、引用计数法"><a href="#1-1、引用计数法" class="headerlink" title="1.1、引用计数法"></a>1.1、引用计数法</h2><blockquote><p>示例代码</p></blockquote><pre><code class="java">/**@Description:-verbose:gc*/public class RefCountGC{  private byte[] bigSize = new byte[2 * 1024 * 1024];//这个成员属性唯一的作用就是占用一点内存  Object instance = null;  public static void main(String[] args)  {    RefCountGC objectA = new RefCountGC();    RefCountGC objectB = new RefCountGC();    objectA.instance = objectB;    objectB.instance = objectA;    objectA = null;    objectB = null;    System.gc();  }}</code></pre><h2 id="1-2、复制算法-Copying"><a href="#1-2、复制算法-Copying" class="headerlink" title="1.2、复制算法(Copying)"></a>1.2、复制算法(Copying)</h2><blockquote><p>复制算法（Copying）一般用于新生代</p></blockquote><p>因为新生代的GC非常频繁，每次GC的对象较多，存活的对象较少。<br>所以采用复制算法效率更高，复制时只需要复制少量存活的对象。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714104128.png" alt=""></p><h3 id="1-2-1、原理"><a href="#1-2-1、原理" class="headerlink" title="1.2.1、原理"></a>1.2.1、原理</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122113.png" alt=""> </p><p>Minor GC会把Eden中的所有活的对象都移到Survivor区域中，如果Survivor区中放不下，那么剩下的活的对象就被移到Old  generation中，<strong>也即一旦收集后，Eden是就变成空的了</strong>。<br>  当对象在 Eden ( 包括一个 Survivor 区域，这里假设是 from 区域 ) 出生后，在经过一次 Minor GC 后，如果对象还存活，并且能够被另外一块 Survivor 区域所容纳( 上面已经假设为 from 区域，这里应为 to 区域，即 to 区域有足够的内存空间来存储 Eden 和 from 区域中存活的对象 )，则使用<strong>复制算法</strong>将这些仍然还存活的对象复制到另外一块 Survivor 区域 ( 即 to 区域 ) 中，然后清理所使用过的 Eden 以及 Survivor 区域 ( 即 from 区域 )，并且将这些对象的年龄设置为1，以后对象在 Survivor 区每熬过一次 Minor GC，就将对象的年龄 + 1，当对象的年龄达到某个值时 ( 默认是 15 岁，通过<code>-XX:MaxTenuringThreshold</code> 来设定参数)，这些对象就会成为老年代。</p><p><code>-XX:MaxTenuringThreshold</code> — 设置对象在新生代中存活的次数</p><blockquote><p>动态年龄阈值</p></blockquote><p>JVM并不要求对象年龄一定要达到 MaxTenuringThreshold 才会<br>晋升到老年代，晋升的年龄阈值是动态计算的。￼￼￼￼￼<br>如果在Survivor中，某个相同年龄阶段的所有对象大小的总和<br>大于Survivor区域的一半，则大于等于这个年龄的所有对象<br>可以直接进入老年代，无需等到MaxTenuringThreshold。</p><h3 id="1-2-2、解释"><a href="#1-2-2、解释" class="headerlink" title="1.2.2、解释"></a>1.2.2、解释</h3><p> HotSpot JVM把年轻代分为了三部分：1个Eden区和2个Survivor区（分别叫from和to）。默认比例为8:1:1,一般情况下，新创建的对象都会被分配到Eden区(一些大对象特殊处理),这些对象经过第一次Minor GC后，如果仍然存活，将会被移到Survivor区。对象在Survivor区中每熬过一次Minor GC，年龄就会增加1岁，当它的年龄增加到一定程度时，就会被移动到年老代中。因为年轻代中的对象基本都是朝生夕死的(90%以上)，所以<strong>在年轻代的垃圾回收算法使用的是复制算法</strong>，复制算法的基本思想就是将内存分为两块，每次只用其中一块，当这一块内存用完，就将还活着的对象复制到另外一块上面。<strong>复制算法不会产生内存碎片</strong>。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122108.png" alt=""></p><p>在GC开始的时候，对象只会存在于Eden区和名为“From”的Survivor区，Survivor区“To”是空的。紧接着进行GC，Eden区中所有存活的对象都会被复制到“To”，而在“From”区中，仍存活的对象会根据他们的年龄值来决定去向。年龄达到一定值(年龄阈值，可以通过-XX:MaxTenuringThreshold来设置)的对象会被移动到年老代中，没有达到阈值的对象会被复制到“To”区域。<strong>经过这次GC后，Eden区和From区已经被清空。这个时候，“From”和“To”会交换他们的角色，也就是新的“To”就是上次GC前的“From”，新的“From”就是上次GC前的“To”。</strong>不管怎样，都会保证名为To的Survivor区域是空的。Minor GC会一直重复这样的过程，直到“To”区被填满，“To”区被填满之后，会将所有对象移动到年老代中。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122105.png" alt=""></p><p>因为Eden区对象一般存活率较低，一般的，使用两块10%的内存作为空闲和活动区间，而另外80%的内存，则是用来给新建对象分配内存的。一旦发生GC，将10%的from活动区间与另外80%中存活的eden对象转移到10%的to空闲区间，接下来，将之前90%的内存全部释放，以此类推。 </p><h3 id="1-2-3、优缺点"><a href="#1-2-3、优缺点" class="headerlink" title="1.2.3、优缺点"></a>1.2.3、优缺点</h3><blockquote><p>1、优点</p></blockquote><p>复制算法的优点是不会产生连续的内存碎片，速度也很高效。</p><blockquote><p>2、缺点</p></blockquote><p>1、每次只使用内存的一半，就代表可使用的内存减少了1/2，代价很高昂。</p><p>2、如果对象的存活率很高，假设是100%存活，那么我们需要将所有对象都复制一遍，并将所有引用地址重置一遍。复制这一工作所花费的时间，在对象存活率达到一定程度时，将会变的不可忽视。 所以从以上描述不难看出，复制算法要想使用，<strong>最起码对象的存活率要非常低才行</strong>，而且最重要的是，我们必须要克服50%内存的浪费。</p><h2 id="1-3、标记清除-Mark-Sweep"><a href="#1-3、标记清除-Mark-Sweep" class="headerlink" title="1.3、标记清除(Mark-Sweep)"></a>1.3、标记清除(Mark-Sweep)</h2><blockquote><p>老年代一般是由标记清除或者是标记清除与标记整理的混合实现</p></blockquote><p>因为老年代中的对象存活率较高，几乎很少被回收，所以标记-清除和标记-整理算法GC的时间不会太长，<br>GC的对象相比新生代更少</p><h3 id="1-3-1、原理"><a href="#1-3-1、原理" class="headerlink" title="1.3.1、原理"></a>1.3.1、原理</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122101.png" alt=""></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122059.png" alt=""></p><p>当程序运行期间，若可以使用的内存被耗尽的时候，GC线程就会被触发并将程序暂停，随后将要回收的对象标记一遍，最终统一回收这些对象，完成标记清理工作接下来便让应用程序恢复运行。</p><blockquote><p>标记-清除算法分为2个步骤，标记、清除。</p></blockquote><p>  标记：从引用根节点开始标记遍历所有的GC Roots， 先标记出要回收的对象。<br>  清除：遍历整个堆，把标记的对象清除。 </p><h3 id="1-3-2、缺点"><a href="#1-3-2、缺点" class="headerlink" title="1.3.2、缺点"></a>1.3.2、缺点</h3><ol><li><strong>标记和清除2个阶段的耗时都比较长，可以总结为效率较低。</strong></li><li><strong>此算法需要暂停整个应用，会产生内存碎片。</strong>当内存碎片过多后，后续想要分配较大的对象时，无法找到足够大的内存碎片，可能又需要触发GC。 </li></ol><h2 id="1-4、标记压缩-Mark-Compact"><a href="#1-4、标记压缩-Mark-Compact" class="headerlink" title="1.4、标记压缩(Mark-Compact)"></a>1.4、标记压缩(Mark-Compact)</h2><p>标记压缩也叫标记-整理算法。是对标记-清除算法的一种改进。</p><blockquote><p>老年代一般是由标记清除或者是标记清除与标记整理的混合实现</p></blockquote><h3 id="1-4-1、原理"><a href="#1-4-1、原理" class="headerlink" title="1.4.1、原理"></a>1.4.1、原理</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122055.png" alt=""></p><p>标记-整理算法与标记-清除算法的在标记阶段是相同的，都是首先标记出所有可达(存活)的对象。<br>但<strong>标记之后并不直接清理未被标记(不可达)的对象，而是使被标记(存活)的对象向内存一端移动，然后清理掉这一端外的内存。</strong></p><h3 id="1-4-2、优缺点"><a href="#1-4-2、优缺点" class="headerlink" title="1.4.2、优缺点"></a>1.4.2、优缺点</h3><blockquote><p>优点</p></blockquote><p>几乎不会如标记-清除算法那样产生不连续的内存碎片。但所谓慢工出细活,标记-整理的效率是比标记-清除要低。</p><blockquote><p>缺点</p></blockquote><p>效率也不高，不仅要标记所有存活对象，还要整理所有存活对象的引用地址。</p><p>从效率上来说，标记/整理算法要低于复制算法。</p><h3 id="1-4-3、分代收集算法"><a href="#1-4-3、分代收集算法" class="headerlink" title="1.4.3、分代收集算法"></a>1.4.3、分代收集算法</h3><p><strong>分代收集算法并不是指某一种具体的垃圾收集算法，<br>而是将复制，标记-清除，标记-整理等算法合理运用到堆区的不同空间。</strong><br>比如新生代使用复制算法，老年代使用标记清除或标记整理算法。</p><p>现代的几乎所有的JVM都使用分代收集，毕竟每种算法都有优缺点，<br>结合它们的特点，对不同的环境采用不同的算法是非常明智的选择。</p><h2 id="1-5、内存分配与垃圾回收策略"><a href="#1-5、内存分配与垃圾回收策略" class="headerlink" title="1.5、内存分配与垃圾回收策略"></a>1.5、内存分配与垃圾回收策略</h2><ol><li>对象优先在eden区域被分配</li><li>大对象将直接进入老年代(大对象是指需要大量连续的内存空间的对象，如长字符串，大数组等。)</li><li>长期存活的对象将进入老年代</li></ol><h1 id="02、总结"><a href="#02、总结" class="headerlink" title="02、总结"></a>02、总结</h1><ul><li>内存效率：复制算法&gt;标记清除算法&gt;标记整理算法（此处的效率只是简单的对比时间复杂度，实际情况不一定如此）。 </li><li>内存整齐度：复制算法=标记整理算法&gt;标记清除算法。 </li><li>内存利用率：标记整理算法=标记清除算法&gt;复制算法。 </li></ul><p>可以看出，效率上来说，复制算法是当之无愧的老大，但是却浪费了太多内存，而为了尽量兼顾上面所提到的三个指标，标记/整理算法相对来说更平滑一些，但效率上依然不尽如人意，它比复制算法多了一个标记的阶段，又比标记/清除多了一个整理内存的过程</p><blockquote><p>难道就没有一种最优算法吗？ </p></blockquote><p> 回答：无，没有最好的算法，只有最合适的算法。==========&gt;分代收集算法。</p><blockquote><p>年轻代(Young Gen)  </p></blockquote><p>年轻代特点是区域相对老年代较小，对像存活率低。</p><p>这种情况复制算法的回收整理，速度是最快的。复制算法的效率只和当前存活对像大小有关，因而很适用于年轻代的回收。而复制算法内存利用率不高的问题，通过hotspot中的两个survivor的设计得到缓解。</p><blockquote><p>老年代(Tenure Gen)</p></blockquote><p>老年代的特点是区域较大，对像存活率高。</p><p>这种情况，存在大量存活率高的对像，复制算法明显变得不合适。一般是由标记清除或者是标记清除与标记整理的混合实现。</p><p>Mark阶段的开销与存活对像的数量成正比，这点上说来，对于老年代，标记清除或者标记整理有一些不符，但可以通过多核/线程利用，对并发、并行的形式提标记效率。</p><p>Sweep阶段的开销与所管理区域的大小形正相关，但Sweep“就地处决”的特点，回收的过程没有对像的移动。使其相对其它有对像移动步骤的回收算法，仍然是效率最好的。但是需要解决内存碎片问题。</p><p>Compact阶段的开销与存活对像的数据成开比，如上一条所描述，对于大量对像的移动是很大开销的，做为老年代的第一选择并不合适。</p><p>基于上面的考虑，老年代一般是由标记清除或者是标记清除与标记整理的混合实现。以hotspot中的CMS回收器为例，CMS是基于Mark-Sweep实现的，对于对像的回收效率很高，而对于碎片问题，CMS采用基于Mark-Compact算法的Serial Old回收器做为补偿措施：当内存回收不佳（碎片导致的Concurrent Mode Failure时），将采用Serial Old执行Full GC以达到对老年代内存的整理。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、垃圾回收算法&quot;&gt;&lt;a href=&quot;#01、垃圾回收算法&quot; class=&quot;headerlink&quot; title=&quot;01、垃圾回收算法&quot;&gt;&lt;/a&gt;01、垃圾回收算法&lt;/h1&gt;&lt;h2 id=&quot;1-1、引用计数法&quot;&gt;&lt;a href=&quot;#1-1、引用计数法&quot; class
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="GC" scheme="http://lsqstudy.top/categories/Java/GC/"/>
    
    
      <category term="GC" scheme="http://lsqstudy.top/tags/GC/"/>
    
  </entry>
  
  <entry>
    <title>03垃圾回收器</title>
    <link href="http://lsqstudy.top/post/194c267.html"/>
    <id>http://lsqstudy.top/post/194c267.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.664Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、垃圾回收器"><a href="#01、垃圾回收器" class="headerlink" title="01、垃圾回收器"></a>01、垃圾回收器</h1><p><strong>如果说垃圾回收算法是JVM对GC算法的方法论，那么垃圾回收器就是对GC算法的实现。</strong></p><blockquote><p>垃圾回收器主要分为以下几种收集器:</p></blockquote><ul><li><p>Serial收集器</p></li><li><p>Parallel Scanvel收集器</p></li><li><p>ParNew收集器</p></li><li><p>CMS收集器</p></li><li><p>G1收集器</p></li></ul><h2 id="1-1、Serial串行收集器"><a href="#1-1、Serial串行收集器" class="headerlink" title="1.1、Serial串行收集器"></a>1.1、Serial串行收集器</h2><blockquote><p>Serial收集器采用复制算法</p></blockquote><p>Serial收集器为单线程环境设计,并只使用一个线程进行垃圾回收。<br>在回收时，会暂停用户线程,并不适用于并发环境。</p><p>Serial收集器在单线程环境中是很高效的,它没有多线程切换的消耗。     </p><h4 id="1-1-1、Serial-Old-串行收集器-老年代版本"><a href="#1-1-1、Serial-Old-串行收集器-老年代版本" class="headerlink" title="1.1.1、Serial Old 串行收集器(老年代版本)"></a>1.1.1、Serial Old 串行收集器(老年代版本)</h4><blockquote><p>Serial Old收集器采用的是：标记-整理算法</p></blockquote><p>它是 Serial收集器的老年代使用的GC收集器，同样是一个单线程的垃圾收集器。 </p><pre><code class="text">/** 开启串行收集器使用 -XX:+UseSerialGC ,   * 这样默认新生代使用 Serial 收集器,  * 老年代使用 Serial Old 收集器.   *  * 设置VM参数:  * -XX:+Xlogs:gc* 打印gc信息  * -XX:+PrintCommandLineFlags  打印java版本信息  * -XX:+UseSerialGC 使用串行GC  */                      //如果程序正常运行,日志会显示 :// 新生代的信息为:  def new generation.....// 老年代的信息为:  tenured generation.....</code></pre><h2 id="1-2、Parallel-Scavenge-并行多线程收集器"><a href="#1-2、Parallel-Scavenge-并行多线程收集器" class="headerlink" title="1.2、Parallel Scavenge 并行多线程收集器"></a>1.2、Parallel Scavenge 并行多线程收集器</h2><blockquote><p>Parallel Scavenge收集器采用：复制算法</p></blockquote><p>Parallel Scavenge是并行收集器，它使用多个垃圾回收线程一起工作,但是仍然会暂停用户线程。</p><p>Parallel Scavenge与其它垃圾回收器不同的是它<strong>更关注于达到可控制的吞吐量。</strong></p><p>吞吐量是CPU运行用户应用程序代码的时间与CPU总消耗的时间的比值:</p><pre><code class="text">吞吐量 = 应用程序代码运行时间 / (应用程序代码运行时间 + GC时间)</code></pre><h3 id="1-2-1、Parallel-Old-并行收集器-老年代版本"><a href="#1-2-1、Parallel-Old-并行收集器-老年代版本" class="headerlink" title="1.2.1、Parallel Old 并行收集器(老年代版本)"></a>1.2.1、Parallel Old 并行收集器(老年代版本)</h3><p>它是 Parallel Scavenge 的老年代版本,同样是一个并行多线程的收集器。</p><p><strong>Parallel Old收集器采用标记-整理算法。</strong></p><pre><code class="text">    /**     *      * 设置 Parallel Scavenge 收集器的参数:     * -XX:+UseParallelGC     *      * ParallelGC老年代默认使用的 Parallel Old GC 回收器     *      * 并行收集器打印的新生代的信息为:     *  PSYoungGen ....     *  老年代的信息为:     *  ParOldGen ....     *      */</code></pre><h2 id="1-3、ParNew-多线程收集器"><a href="#1-3、ParNew-多线程收集器" class="headerlink" title="1.3、ParNew 多线程收集器"></a>1.3、ParNew 多线程收集器</h2><blockquote><p>ParNew收集器采用：复制算法</p></blockquote><p>它可以看做是多线程版的Serial收集器。<br>除了多线程外，ParNew收集器与Serial收集器几乎没啥区别。</p><p><strong>PS:目前只有Serial和ParNew能作为CMS收集器的新生代收集器。</strong></p><pre><code class="text">     /**       *        * 设置ParNewGC回收器的参数为:       * -XX:+UseConcMarkSweepGC       *        */</code></pre><h2 id="1-4、CMS-并发标记清除收集器"><a href="#1-4、CMS-并发标记清除收集器" class="headerlink" title="1.4、CMS 并发标记清除收集器"></a>1.4、CMS 并发标记清除收集器</h2><blockquote><p>见名知意,CMS收集器使用的是：标记-清除算法</p></blockquote><p>Concurrent Mark Sweep,并发标记-清除垃圾回收器。<br>它是一款老年代的收集器，是<strong>以达到最短回收停顿时间目标的收集器。</strong></p><p><strong>CMS在垃圾回收过程中，用户线程可以同时工作，无需暂停。</strong></p><p><strong>因为CMS收集器采用的是标记-清除算法，所以回收时可能会产生不连续的内存碎片。</strong></p><p><strong>PS: CMS收集器在jdk14中被删除了。</strong></p><h3 id="1-4-1、CMS收集器回收过程"><a href="#1-4-1、CMS收集器回收过程" class="headerlink" title="1.4.1、CMS收集器回收过程"></a>1.4.1、CMS收集器回收过程</h3><ul><li><p>初始标记(Stop The World，此阶段会暂停用户线程): 只标记与GC ROOT直接关联的对象。</p></li><li><p>并发标记: 对第一个阶段已经标记的对象进行Tracing，标记所有可达的对象。</p></li><li><p>重新标记(Stop The World,此阶段会暂停用户线程): 在第二个阶段，由于用户程序的运行，<br>可能有些对象之间的引用关系受到了影响，所以需要对这部分对象进行重新标记调整。</p></li><li><p>并发清除: 清除所有未被标记的对象。</p></li></ul><pre><code class="text">    /**     *     * 设置 CMS 收集器参数:     * -XX:+UseConcMarkSweepGC     *     * 使用ConcMarkSweepGC收集器后,它的新生代使用的是:     * ParNew收集器.     *     * 当ConcMarkSweepGC收集器出现异常时,会将CMS替换成Serial Old收集器     *     * CMS回收分为4个阶段:     *     * 初始标记:    (Stop the world 暂停用户线程)     * 标记与GC Root直接可达的对象.           *     * 并发标记:       * 从第一步标记的可达的对象开始,并发的标记所有可达的对象      *     * 重新标记:    (Stop the world 暂停用户线程)     * 在第二部的并发标记阶段,由于程序运行导致对象间引用的关系发生变化,     * 就需要重新标记     *     * 并发清除:          * 这个阶段不暂停用户线程,并且并发的去清除未被标记的对象     *      */</code></pre><h2 id="1-5、G1-收集器"><a href="#1-5、G1-收集器" class="headerlink" title="1.5、G1 收集器"></a>1.5、G1 收集器</h2><p>G1收集器可以说是目前最前沿的一款收集器，它是一款面向服务端的收集器。<br>G1收集器<strong>无需配合其他收集器就可以管理整个堆内存。</strong><br>jdk9开始，G1成为jdk默认使用的垃圾回收器。</p><h3 id="1-5-1、G1回收器的特点"><a href="#1-5-1、G1回收器的特点" class="headerlink" title="1.5.1、G1回收器的特点"></a>1.5.1、G1回收器的特点</h3><ul><li><p>并行和并发: G1能够充分利用多核cpu的优势，使垃圾回收与用户线程同时运行。</p></li><li><p>可预测的停顿: 降低GC停顿时间是CMS与G1收集器的共同目标。但是除了降低GC停顿时间，G1收集器还可以建立可预测的停顿时间模型。(…太np了 =_=)</p></li><li><p>空间整合: 这是G1收集器不同于其他收集器的最大亮点了。在其他收集器中，堆区基本都分为新生代和老年代。而在G1收集器中虽然仍然保留了新生代和老年代的概念，但已经不再是物理上的分隔了。<strong>在G1收集器的堆内存模型中，内存被分割成了一块一块大小相等的Region，在这些Region中，Region的类型也不同，有eden，survivor，old，humongous之分。当有大对象时，对象会被分配到Humongous Region之中。</strong></p></li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714121946.png" alt="G1收集器Region"></p><h3 id="1-5-2、G1收集器回收过程"><a href="#1-5-2、G1收集器回收过程" class="headerlink" title="1.5.2、G1收集器回收过程"></a>1.5.2、G1收集器回收过程</h3><pre><code class="text">G1收集器与CMS收集器的回收过程相似</code></pre><ul><li><p>初始标记(Stop The World,此阶段会暂停用户线程): 只标记与GC ROOT直接关联的对象。</p></li><li><p>并发标记: 对第一个阶段标记的对象Tracing，标记所有可达的对象。</p></li><li><p>最终标记(Stop The World,此阶段会暂停用户线程): 在并发标记阶段，由于用户线程执行，<br>可能导致被标记对象之间的引用关系发生影响，需要对这些对象进行重新标记调整。</p></li><li><p>筛选回收: 不同于CMS的并发清除，G1收集器首先会对所有Region的回收价值和回收成本进行排序,<br>然后再进行回收。这样可以在有限的时间内获得最大的回收率。</p></li></ul><pre><code> /**     *     * 指定G1回收器的参数是: -XX:+UseG1GC     *     * 1:初始标记:(Stop the world 暂停用户线程)     *   标记所有与GC Root直接可达的对象     *     * 2:并发标记     *  从第一个阶段标记的对象开始,trace标记     *     * 4:最终标记:(Stop the world 暂停用户线程)     *  在第二步并发标记的阶段,由于程序执行,     *  导致被标记对象之间的引用关系发生变化,所以需要重新调整标记     *     * 5:筛选回收:     *  和CMS的并发回收不一样,     *  G1收集器首先会对所有Region的回收价值和回收成本进行排序,     *  然后再进行回收。     *  这样可以保证在有限的时间内获得最大的回收率.     *     */</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、垃圾回收器&quot;&gt;&lt;a href=&quot;#01、垃圾回收器&quot; class=&quot;headerlink&quot; title=&quot;01、垃圾回收器&quot;&gt;&lt;/a&gt;01、垃圾回收器&lt;/h1&gt;&lt;p&gt;&lt;strong&gt;如果说垃圾回收算法是JVM对GC算法的方法论，那么垃圾回收器就是对GC算法的
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="GC" scheme="http://lsqstudy.top/categories/Java/GC/"/>
    
    
      <category term="GC" scheme="http://lsqstudy.top/tags/GC/"/>
    
  </entry>
  
  <entry>
    <title>01GC总体概述</title>
    <link href="http://lsqstudy.top/post/697d2448.html"/>
    <id>http://lsqstudy.top/post/697d2448.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.661Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、判断对象存活的方法"><a href="#01、判断对象存活的方法" class="headerlink" title="01、判断对象存活的方法"></a>01、判断对象存活的方法</h1><blockquote><p>在垃圾回收器对堆内存回收前，需要判断对象是否存活。</p></blockquote><ul><li><p><strong>引用计数算法</strong>: 给每个对象添加一个引用计数器,每当对象被引用,对象的引用计数器就加1,当引用失效时,引用计数器就减1。直到引用计数器为0,就代表对象不再被引用。</p></li><li><p><strong>可达性算法</strong>: 通过GC ROOT的对象节点往下搜索,节点走过的路径被称为引用链。如果一个对象不处于任何引用链,那么就可以判断此对象是不可达的。</p></li></ul><blockquote><p>引用计数法缺点</p></blockquote><p>引用计数的主要缺陷是很难解决<strong>循环引用</strong>的问题：就是当<strong>2个对象互相引用</strong>的时候,除了彼此,没有其它对象再引用这2个对象,那么他们的引用计数都为1,就无法被回收。                </p><h2 id="1-1、什么是GC-Root"><a href="#1-1、什么是GC-Root" class="headerlink" title="1.1、什么是GC Root ?"></a>1.1、什么是GC Root ?</h2><p>上面说通过GC Root对象搜索引用链,那么GC Root对象是什么对象,或者说什么样的对象是GC Root对象。可以作为GC Root对象的有: </p><ol><li>虚拟机栈和本地方法栈区中的引用对象(stack)</li><li>方法区中类的静态属性引用的对象(static)</li><li>方法区中的常量引用的对象(final)   </li></ol><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714103131.png" alt="可达性算法">      </p><h1 id="02、JVM复习"><a href="#02、JVM复习" class="headerlink" title="02、JVM复习"></a>02、JVM复习</h1><blockquote><p>JVM结构</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122202.png" alt=""></p><blockquote><p>GC作用的地方</p></blockquote><p><img src="F:%5Clsqstudy%5C%E4%B8%AA%E4%BA%BA%E9%A1%B9%E7%9B%AE%5Cblog%5Cblog%5Csource_posts%5Cdocs%5Cjava%5Cgc%5Cupload%5Cimage-20200714085318493.png" alt=""></p><h1 id="03、GC是什么-分代收集算法"><a href="#03、GC是什么-分代收集算法" class="headerlink" title="03、GC是什么(分代收集算法)"></a>03、GC是什么(分代收集算法)</h1><ul><li>次数上频繁收集Young区</li><li>次数上较少收集Old区</li><li>基本不动元空间</li></ul><h1 id="04、GC算法总体概述"><a href="#04、GC算法总体概述" class="headerlink" title="04、GC算法总体概述"></a>04、GC算法总体概述</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200714122221.png" alt=""></p><p>JVM在进行GC时，并非每次都对上面三个内存区域一起回收的，大部分时候回收的都是指新生代。<br>因此GC按照回收的区域又分了两种类型，一种是普通GC（minor GC），一种是全局GC（major GC or Full GC）</p><blockquote><p>Minor GC和Full GC的区别</p></blockquote><ul><li><strong>普通GC（minor GC）</strong>：只针对新生代区域的GC,指发生在新生代的垃圾收集动作，因为大多数Java对象存活率都不高，所以Minor GC非常频繁，一般回收速度也比较快。 </li><li><strong>全局GC（major GC or Full GC</strong>）：指发生在老年代的垃圾收集动作，出现了Major GC，经常会伴随至少一次的Minor GC（但并不是绝对的）。Major GC的速度一般要比Minor GC慢上10倍以上</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、判断对象存活的方法&quot;&gt;&lt;a href=&quot;#01、判断对象存活的方法&quot; class=&quot;headerlink&quot; title=&quot;01、判断对象存活的方法&quot;&gt;&lt;/a&gt;01、判断对象存活的方法&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;在垃圾回收器对堆内存回收前，需要判
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="GC" scheme="http://lsqstudy.top/categories/Java/GC/"/>
    
    
      <category term="GC" scheme="http://lsqstudy.top/tags/GC/"/>
    
  </entry>
  
  <entry>
    <title>docs/interview/juc/01谈谈你对volatile的理解</title>
    <link href="http://lsqstudy.top/post/e77fd8ff.html"/>
    <id>http://lsqstudy.top/post/e77fd8ff.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T10:42:09.015Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、Volatile和JMM内存模型的可见性"><a href="#01、Volatile和JMM内存模型的可见性" class="headerlink" title="01、Volatile和JMM内存模型的可见性"></a>01、Volatile和JMM内存模型的可见性</h1><h2 id="1-1、Volatile"><a href="#1-1、Volatile" class="headerlink" title="1.1、Volatile"></a>1.1、Volatile</h2><p>Volatile在日常的单线程环境是应用不到的。</p><blockquote><p>Volatile是Java虚拟机提供的<code>轻量级</code>的同步机制</p></blockquote><p>Volatile具有三大特征：</p><ul><li>保证可见性</li><li>不保证原子性</li><li>禁止指令重排（保证有序性）</li></ul><h2 id="1-2、JMM你谈谈"><a href="#1-2、JMM你谈谈" class="headerlink" title="1.2、JMM你谈谈"></a>1.2、JMM你谈谈</h2><h3 id="1-2-1、JMM是什么？"><a href="#1-2-1、JMM是什么？" class="headerlink" title="1.2.1、JMM是什么？"></a>1.2.1、JMM是什么？</h3><p>JMM是<strong>Java内存模型</strong>，也就是Java Memory Model，简称JMM，<strong>本身是一种抽象的概念，实际上并不存在</strong>，它描述的是一组规则或规范，通过这组规范定义了程序中各个变量（包括实例字段，静态字段和构成数组对象的元素）的访问方式</p><p>JMM关于同步的规定：</p><ul><li>线程解锁前，必须把共享变量的值刷新回主内存</li><li>线程解锁前，必须读取主内存的最新值，到自己的工作内存</li><li>加锁和解锁是同一把锁</li></ul><p>由于JVM运行程序的实体是线程，而每个线程创建时JVM都会为其创建一个工作内存（有些地方称为栈空间），工作内存是每个线程的私有数据区域，而Java内存模型中规定所有变量都存储在主内存，主内存是共享内存区域，所有线程都可以访问，<strong>但线程对变量的操作（读取赋值等）必须在工作内存中进行，首先要将变量从主内存拷贝到自己的工作内存空间，然后对变量进行操作，操作完成后再将变量写回主内存</strong>，不能直接操作主内存中的变量，各个线程中的工作内存中存储着主内存中的变量副本拷贝，因此不同的线程间无法访问对方的工作内存，线程间的通信（传值）必须通过主内存来完成，其简要访问过程：</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200715170317.png" style="zoom:80%;" /><p>数据传输速率：硬盘 &lt; 内存 &lt; &lt; cache &lt; CPU   </p><p>上面提到了两个概念：主内存  和 工作内存</p><ul><li><p>主内存：就是计算机的内存，也就是经常提到的8G内存，16G内存</p></li><li><p>工作内存：但我们实例化 new student，那么 age = 25 也是存储在主内存中</p><ul><li>当同时有三个线程同时访问 student中的age变量时，<br>那么每个线程都会拷贝一份，到各自的工作内存，从而实现了变量的拷贝</li></ul><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715170445.png" alt="image-20200309154435933" style="zoom:80%;" /></li></ul><p>即：JMM内存模型的可见性，指的是当主内存区域中的值被某个线程写入更改后，其它线程会马上知晓更改后的值，并重新得到更改后的值。</p><h3 id="1-2-2、缓存一致性"><a href="#1-2-2、缓存一致性" class="headerlink" title="1.2.2、缓存一致性"></a>1.2.2、缓存一致性</h3><p>为什么这里主线程中某个值被更改后，其它线程能马上知晓呢？其实这里是用到了总线嗅探技术</p><p>在说嗅探技术之前，首先谈谈缓存一致性的问题，就是当多个处理器运算任务都涉及到同一块主内存区域的时候，将可能导致各自的缓存数据不一。</p><p>为了解决缓存一致性的问题，需要各个处理器访问缓存时都遵循一些协议，在读写时要根据协议进行操作，这类协议主要有MSI、MESI等等。</p><blockquote><p>1、MESI</p></blockquote><p>当CPU写数据时，如果发现操作的变量是共享变量，即在其它CPU中也存在该变量的副本，会发出信号通知其它CPU将该内存变量的缓存行设置为无效，因此当其它CPU读取这个变量的时，发现自己缓存该变量的缓存行是无效的，那么它就会从内存中重新读取。</p><blockquote><p>2、总线嗅探</p></blockquote><p>那么是如何发现数据是否失效呢？</p><p>这里是用到了总线嗅探技术，就是每个处理器通过嗅探在总线上传播的数据来检查自己缓存值是否过期了，当处理器发现自己的缓存行对应的内存地址被修改，就会将当前处理器的缓存行设置为无效状态，当处理器对这个数据进行修改操作的时候，会重新从内存中把数据读取到处理器缓存中。</p><blockquote><p>3、总线风暴</p></blockquote><p>总线嗅探技术有哪些缺点？</p><p>由于Volatile的MESI缓存一致性协议，需要不断的从主内存嗅探和CAS循环，无效的交互会导致总线带宽达到峰值。因此不要大量使用volatile关键字，至于什么时候使用volatile、什么时候用锁以及Syschonized都是需要根据实际场景的。</p><h3 id="1-2-3、JMM的特性"><a href="#1-2-3、JMM的特性" class="headerlink" title="1.2.3、JMM的特性"></a>1.2.3、JMM的特性</h3><blockquote><p>JMM的三大特性，volatile只保证了两个，即可见性和有序性，不满足原子性</p></blockquote><ul><li>可见性</li><li>原子性</li><li>有序性</li></ul><h3 id="1-2-4、可见性代码验证"><a href="#1-2-4、可见性代码验证" class="headerlink" title="1.2.4、可见性代码验证"></a>1.2.4、可见性代码验证</h3><blockquote><p>1、但我们对于成员变量没有添加任何修饰时，是无法感知其它线程修改后的值</p></blockquote><pre><code class="java">package com.lsq.juc.Volatile;import java.util.concurrent.TimeUnit;/** * @Description * @Author LSQ * Date 2020/7/15 16:48 * * Volatile Java虚拟机提供的轻量级同步机制 * * 可见性（及时通知） * 不保证原子性 * 禁止指令重排 * *//** * 假设是主物理内存 */class MyData {    int number = 0;    public void addTo60() {        this.number = 60;    }}/** * 验证volatile的可见性 * 1. 假设int number = 0， number变量之前没有添加volatile关键字修饰 */public class VolatileVisibleDemo {    public static void main(String args []) {        // 资源类        MyData myData = new MyData();        // AAA线程 实现了Runnable接口的，lambda表达式        new Thread(() -&gt; {            System.out.println(Thread.currentThread().getName() + &quot;\t come in&quot;);            // 线程睡眠3秒，假设在进行运算            try {                TimeUnit.SECONDS.sleep(3);            } catch (InterruptedException e) {                e.printStackTrace();            }            // 修改number的值            myData.addTo60();            // 输出修改后的值            System.out.println(Thread.currentThread().getName() + &quot;\t update number value:&quot; + myData.number);        }, &quot;AAA&quot;).start();        while(myData.number == 0) {            // main线程就一直在这里等待循环，直到number的值不等于零        }        // 按道理这个值是不可能打印出来的，因为主线程运行的时候，number的值为0，所以一直在循环        // 如果能输出这句话，说明AAA线程在睡眠3秒后，更新的number的值，重新写入到主内存，并被main线程感知到了        System.out.println(Thread.currentThread().getName() + &quot;\t mission is over&quot;);        /**         * 最后输出结果：         * AAA     come in         * AAA     update number value:60         * 最后线程没有停止，并行没有输出  mission is over 这句话，说明没有用volatile修饰的变量，是没有可见性         */    }}</code></pre><p>输出结果为</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715170557.png" alt="">    </p><p>最后线程没有停止，并行没有输出  mission is over 这句话，说明没有用volatile修饰的变量，是没有可见性</p><blockquote><p>2、当我们修改MyData类中的成员变量时，并且添加volatile关键字修饰</p></blockquote><pre><code class="java">/** * 假设是主物理内存 */class MyData {    /**     * volatile 修饰的关键字，是为了增加 主线程和线程之间的可见性，只要有一个线程修改了内存中的值，其它线程也能马上感知     */    volatile int number = 0;    public void addTo60() {        this.number = 60;    }}</code></pre><p>最后输出的结果为：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715170631.png" alt=""></p><p>主线程也执行完毕了，说明volatile修饰的变量，是具备JVM轻量级同步机制的，能够感知其它线程的修改后的值。</p><h2 id="1-3、参考"><a href="#1-3、参考" class="headerlink" title="1.3、参考"></a>1.3、参考</h2><p><a href="https://mp.weixin.qq.com/s/Oa3tcfAFO9IgsbE22C5TEg" target="_blank" rel="noopener">https://mp.weixin.qq.com/s/Oa3tcfAFO9IgsbE22C5TEg</a></p><h1 id="02、Volatile不保证原子性"><a href="#02、Volatile不保证原子性" class="headerlink" title="02、Volatile不保证原子性"></a>02、Volatile不保证原子性</h1><h2 id="2-1、前言"><a href="#2-1、前言" class="headerlink" title="2.1、前言"></a>2.1、前言</h2><p>各个线程对主内存中共享变量的操作都是各个线程各自拷贝到自己的工作内存进行操作，然后在写回到主内存中的。</p><h2 id="2-2、原子性"><a href="#2-2、原子性" class="headerlink" title="2.2、原子性"></a>2.2、原子性</h2><p>不可分割，完整性，也就是说某个线程正在做某个具体业务时，中间不可以被加塞或者被分割，需要具体完成，要么同时成功，要么同时失败。</p><p>数据库也经常提到事务具备原子性</p><h2 id="2-3、代码测试"><a href="#2-3、代码测试" class="headerlink" title="2.3、代码测试"></a>2.3、代码测试</h2><p>为了测试volatile是否保证原子性，我们创建了20个线程，然后每个线程分别循环1000次，来调用number++的方法</p><pre><code class="java">        MyData myData = new MyData();        // 创建10个线程，线程里面进行1000次循环        for (int i = 0; i &lt; 20; i++) {            new Thread(() -&gt; {                // 里面                for (int j = 0; j &lt; 1000; j++) {                    myData.addPlusPlus();                }            }, String.valueOf(i)).start();        }</code></pre><p>最后通过 Thread.activeCount()，来感知20个线程是否执行完毕，这里判断线程数是否大于2，为什么是2？因为默认是有两个线程的，一个main线程，一个gc线程</p><pre><code class="java">// 需要等待上面20个线程都计算完成后，在用main线程取得最终的结果值while(Thread.activeCount() &gt; 2) {    // yield表示不执行    Thread.yield();}</code></pre><p>然后在线程执行完毕后，我们在查看number的值，假设volatile保证原子性的话，那么最后输出的值应该是</p><p>20 * 1000 = 20000,</p><p>完整代码如下所示：</p><pre><code class="java">package com.lsq.juc.Volatile;/** * @Description * @Author LSQ * Date 2020/7/15 17:14 *//** * Volatile Java虚拟机提供的轻量级同步机制 * * 可见性（及时通知） * 不保证原子性 * 禁止指令重排 *//** * 假设是主物理内存 */class MyAtomicData {    /**     * volatile 修饰的关键字，是为了增加 主线程和线程之间的可见性，只要有一个线程修改了内存中的值，其它线程也能马上感知     */    volatile int number = 0;    public void addTo60() {        this.number = 60;    }    /**     * 注意，此时number 前面是加了volatile修饰     */    public void addPlusPlus() {        number ++;    }}/** * 验证volatile的可见性 * 1、 假设int number = 0， number变量之前没有添加volatile关键字修饰 * 2、添加了volatile，可以解决可见性问题 * * 验证volatile不保证原子性 * 1、原子性指的是什么意思？ */public class VolatileAtomicDemo {    public static void main(String args []) {        MyAtomicData myData = new MyAtomicData();        // 创建10个线程，线程里面进行1000次循环        for (int i = 0; i &lt; 20; i++) {            new Thread(() -&gt; {                // 里面                for (int j = 0; j &lt; 1000; j++) {                    myData.addPlusPlus();                }            }, String.valueOf(i)).start();        }        // 需要等待上面20个线程都计算完成后，在用main线程取得最终的结果值        // 这里判断线程数是否大于2，为什么是2？因为默认是有两个线程的，一个main线程，一个gc线程        while(Thread.activeCount() &gt; 2) {            // yield表示不执行            Thread.yield();        }        // 查看最终的值        // 假设volatile保证原子性，那么输出的值应该为：  20 * 1000 = 20000        System.out.println(Thread.currentThread().getName() + &quot;\t finally number value: &quot; + myData.number);    }}</code></pre><p>最终结果我们会发现，number输出的值并没有20000，而且是每次运行的结果都不一致的，这说明了volatile修饰的变量不保证原子性</p><p>第一次：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173205.png" alt=""></p><p>第二次：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173234.png" alt=""></p><p>第三次：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173254.png" alt=""></p><h2 id="2-4、为什么出现数值丢失"><a href="#2-4、为什么出现数值丢失" class="headerlink" title="2.4、为什么出现数值丢失"></a>2.4、为什么出现数值丢失</h2><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173410.png" style="zoom:80%;" /><p>各自线程在写入主内存的时候，出现了数据的丢失，而引起的数值缺失的问题</p><p>下面我们将一个简单的number++操作，转换为字节码文件一探究竟</p><pre><code class="java">public class T1 {    volatile int n = 0;    public void add() {        n++;    }}</code></pre><p>转换后的字节码文件</p><pre><code class="java">public class com.moxi.interview.study.thread.T1 {  volatile int n;  public com.moxi.interview.study.thread.T1();    Code:       0: aload_0       1: invokespecial #1                  // Method java/lang/Object.&quot;&lt;init&gt;&quot;:()V       4: aload_0       5: iconst_0       6: putfield      #2                  // Field n:I       9: return  public void add();    Code:       0: aload_0       1: dup       2: getfield      #2                  // Field n:I       5: iconst_1       6: iadd       7: putfield      #2                  // Field n:I      10: return}</code></pre><p>这里查看字节码的操作，是用到了IDEA的javap命令</p><p>我们首先，使用IDEA提供的External Tools，来扩展<code>javap</code>命令</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715174349.png" alt=""></p><p>完成上述操作后，我们在需要查看字节码的文件下，右键选择 External Tools即可</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715173618.png" alt=""></p><blockquote><p>如果出现了找不到指定类，先将代码运行一次，将其编译成class文件</p></blockquote><p>下面我们就针对 add() 这个方法的字节码文件进行分析</p><p>字节码指令对照表：<a href="https://segmentfault.com/a/1190000008722128" target="_blank" rel="noopener">https://segmentfault.com/a/1190000008722128</a></p><pre><code class="java">  public void add();    Code:       0: aload_0       1: dup       2: getfield      #2    // Field n:I       5: iconst_1       6: iadd       7: putfield      #2    // Field n:I      10: return</code></pre><p>我们能够发现 n++这条命令，被拆分成了3个指令</p><ul><li>执行<code>getfield</code> 从主内存拿到原始n</li><li>执行<code>iadd</code> 进行加1操作</li><li>执行<code>putfileld</code> 把累加后的值写回主内存 </li></ul><p>假设我们没有加 <code>synchronized</code>那么第一步就可能存在着，三个线程同时通过getfield命令，拿到主存中的 n值，然后三个线程，各自在自己的工作内存中进行加1操作，但他们并发进行  <code>iadd</code> 命令的时候，因为只能一个进行写，所以其它操作会被挂起，假设1线程，先进行了写操作，在写完后，volatile的可见性，应该需要告诉其它两个线程，主内存的值已经被修改了，但是因为太快了，其它两个线程，陆续执行 <code>iadd</code>命令，进行写入操作，这就造成了其他线程没有接受到主内存n的改变，从而覆盖了原来的值，出现写丢失，这样也就让最终的结果少于20000</p><h2 id="2-5、如何解决"><a href="#2-5、如何解决" class="headerlink" title="2.5、如何解决"></a>2.5、如何解决</h2><p>因此这也说明，在多线程环境下 number ++ 在多线程环境下是非线程安全的，解决的方法有哪些呢？</p><blockquote><p>在方法上加入 synchronized</p></blockquote><pre><code class="java">    public synchronized void addPlusPlus() {        number ++;    }</code></pre><p>运行结果：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715174923.png" alt=""></p><p>我们能够发现引入synchronized关键字后，保证了该方法每次只能够一个线程进行访问和操作，最终输出的结果也就为20000</p><h2 id="2-6、其它解决方法"><a href="#2-6、其它解决方法" class="headerlink" title="2.6、其它解决方法"></a>2.6、其它解决方法</h2><p>上面的方法引入synchronized，虽然能够保证原子性，但是为了解决number++，而引入重量级的同步机制，有种 杀鸡用牛刀</p><p>除了引用synchronized关键字外，还可以使用JUC下面的原子包装类，即刚刚的int类型的number，可以使用AtomicInteger来代替</p><pre><code class="java">    /**     *  创建一个原子Integer包装类，默认为0      */    AtomicInteger atomicInteger = new AtomicInteger();    public void addAtomic() {        // 相当于 atomicInter ++        atomicInteger.getAndIncrement();    }</code></pre><p>然后同理，继续刚刚的操作</p><pre><code class="java">        // 创建10个线程，线程里面进行1000次循环        for (int i = 0; i &lt; 20; i++) {            new Thread(() -&gt; {                // 里面                for (int j = 0; j &lt; 1000; j++) {                    myData.addPlusPlus();                    myData.addAtomic();                }            }, String.valueOf(i)).start();        }</code></pre><p>最后输出</p><pre><code class="java">        // 假设volatile保证原子性，那么输出的值应该为：  20 * 1000 = 20000        System.out.println(Thread.currentThread().getName() + &quot;\t finally number value: &quot; + myData.number);        System.out.println(Thread.currentThread().getName() + &quot;\t finally atomicNumber value: &quot; + myData.atomicInteger);</code></pre><p>下面的结果，一个是引入synchronized，一个是使用了原子包装类AtomicInteger</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715175233.png" alt=""></p><h1 id="03、Volatile禁止指令重排"><a href="#03、Volatile禁止指令重排" class="headerlink" title="03、Volatile禁止指令重排"></a>03、Volatile禁止指令重排</h1><p>计算机在执行程序时，为了提高性能，编译器和处理器常常会对指令重排，一般分为以下三种：</p><pre><code>源代码 -&gt; 编译器优化的重排 -&gt; 指令并行的重排 -&gt; 内存系统的重排 -&gt; 最终执行指令</code></pre><p>单线程环境里面确保最终执行结果和代码顺序的结果一致</p><p>处理器在进行重排序时，必须要考虑指令之间的<code>数据依赖性</code></p><p>多线程环境中线程交替执行，由于编译器优化重排的存在，两个线程中使用的变量能否保证一致性是无法确定的，结果无法预测。</p><h2 id="3-1、指令重排-example-1"><a href="#3-1、指令重排-example-1" class="headerlink" title="3.1、指令重排 - example 1"></a>3.1、指令重排 - example 1</h2><pre><code class="java">public void mySort() {    int x = 11;    int y = 12;    x = x + 5;    y = x * x;}</code></pre><p>按照正常单线程环境，执行顺序是 1 2 3 4</p><p>但是在多线程环境下，可能出现以下的顺序：</p><pre><code>- 2 1 3 4- 1 3 2 4 </code></pre><p>上述的过程就可以当做是指令的重排，即内部执行顺序，和我们的代码顺序不一样</p><p>但是指令重排也是有限制的，即不会出现下面的顺序</p><pre><code>4 3 2 1</code></pre><p>因为处理器在进行重排时候，必须考虑到指令之间的数据依赖性</p><p>因为步骤 4：需要依赖于 y的申明，以及x的申明，故因为存在数据依赖，无法首先执行</p><h3 id="3-1-1、例子"><a href="#3-1-1、例子" class="headerlink" title="3.1.1、例子"></a>3.1.1、例子</h3><pre><code class="java">int a,b,x,y = 0</code></pre><table><thead><tr><th>线程1</th><th>线程2</th></tr></thead><tbody><tr><td>x = a;</td><td>y = b;</td></tr><tr><td>b = 1;</td><td>a = 2;</td></tr><tr><td></td><td></td></tr><tr><td>x = 0;  y = 0</td><td></td></tr></tbody></table><p>因为上面的代码，不存在数据的依赖性，因此编译器可能对数据进行重排</p><table><thead><tr><th>线程1</th><th>线程2</th></tr></thead><tbody><tr><td>b = 1;</td><td>a = 2;</td></tr><tr><td>x = a;</td><td>y = b;</td></tr><tr><td></td><td></td></tr><tr><td>x = 2;  y = 1</td><td></td></tr></tbody></table><p>这样造成的结果，和最开始的就不一致了，这就是导致重排后，结果和最开始的不一样，因此为了防止这种结果出现，<code>volatile</code>就规定禁止指令重排，为了保证数据的一致性</p><h2 id="3-2、指令重排-example-2"><a href="#3-2、指令重排-example-2" class="headerlink" title="3.2、指令重排 - example 2"></a>3.2、指令重排 - example 2</h2><p>比如下面这段代码</p><pre><code class="java">public class ResortSeqDemo {    int a= 0;    boolean flag = false;    public void method01() {        a = 1;        flag = true;    }    public void method02() {        if(flag) {            a = a + 5;            System.out.println(&quot;reValue:&quot; + a);        }    }}</code></pre><p>我们按照正常的顺序，分别调用method01()  和 method02() 那么，最终输出就是 a = 6</p><p>但是如果在多线程环境下，因为方法1 和 方法2，他们之间不存在数据依赖的问题，因此原先的顺序可能是</p><pre><code>a = 1;flag = true;a = a + 5;System.out.println(&quot;reValue:&quot; + a);</code></pre><p>但是在经过编译器，指令，或者内存的重排后，可能会出现这样的情况</p><pre><code>flag = true;a = a + 5;System.out.println(&quot;reValue:&quot; + a);a = 1;</code></pre><p>也就是先执行 flag = true后，另外一个线程马上调用方法2，满足 flag的判断，最终让a + 5，结果为5，这样同样出现了数据不一致的问题</p><p>为什么会出现这个结果：多线程环境中线程交替执行，由于编译器优化重排的存在，两个线程中使用的变量能否保证一致性是无法确定的，结果无法预测。</p><p>这样就需要通过<code>volatile</code>来修饰，来保证线程安全性</p><h2 id="3-3、Volatile针对指令重排做了啥"><a href="#3-3、Volatile针对指令重排做了啥" class="headerlink" title="3.3、Volatile针对指令重排做了啥"></a>3.3、Volatile针对指令重排做了啥</h2><p>Volatile实现禁止指令重排优化，从而避免了多线程环境下程序出现乱序执行的现象</p><p>首先了解一个概念，内存屏障（Memory Barrier）又称内存栅栏，是一个CPU指令，它的作用有两个：</p><ul><li>保证特定操作的顺序</li><li>保证某些变量的内存可见性（利用该特性实现volatile的内存可见性）</li></ul><p>由于编译器和处理器都能执行指令重排的优化，如果在指令间插入一条Memory Barrier则会告诉编译器和CPU，不管什么指令都不能对这条Memory Barrier指令重排序，也就是说 <code>通过插入内存屏障禁止在内存屏障前后的指令执行重排序优化</code>。 内存屏障另外一个作用是刷新出各种CPU的缓存数，因此任何CPU上的线程都能读取到这些数据的最新版本。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715181015.png" alt=""></p><p>也就是过在Volatile的写 和 读的时候，加入屏障，防止出现指令重排的</p><h2 id="3-4、线程安全获得保证"><a href="#3-4、线程安全获得保证" class="headerlink" title="3.4、线程安全获得保证"></a>3.4、线程安全获得保证</h2><p>工作内存与主内存同步延迟现象导致的可见性问题</p><ul><li><p>可通过synchronized或volatile关键字解决，他们都可以使一个线程修改后的变量立即对其它线程可见对于指令重排导致的可见性问题和有序性问题</p></li><li><p>可以使用volatile关键字解决，因为volatile关键字的另一个作用就是禁止重排序优化</p></li></ul><h1 id="04、你在哪些地方用到过volatile"><a href="#04、你在哪些地方用到过volatile" class="headerlink" title="04、你在哪些地方用到过volatile?"></a>04、你在哪些地方用到过volatile?</h1><h2 id="4-1、单例模式DCL代码"><a href="#4-1、单例模式DCL代码" class="headerlink" title="4.1、单例模式DCL代码"></a>4.1、单例模式DCL代码</h2><p>首先回顾一下，单线程下的单例模式代码</p><pre><code class="java">package com.lsq.juc.Volatile;/** * @Description: SingletonDemo（单例模式） * @Author: LSQ * Date: 2020/7/15 18:13 */public class SingletonDemo {    private static SingletonDemo instance = null;    private SingletonDemo () {        System.out.println(Thread.currentThread().getName() + &quot;\t 我是构造方法SingletonDemo&quot;);    }    public static SingletonDemo getInstance() {        if(instance == null) {            instance = new SingletonDemo();        }        return instance;    }    public static void main(String[] args) {        // 这里的 == 是比较内存地址        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());    }}</code></pre><p>最后输出的结果</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715182111.png" alt=""></p><p>但是在多线程的环境下，我们的单例模式是否还是同一个对象了</p><pre><code class="java">/** * SingletonDemo（单例模式） * * @author: 陌溪 * @create: 2020-03-10-16:40 */public class SingletonDemo {    private static SingletonDemo instance = null;    private SingletonDemo () {        System.out.println(Thread.currentThread().getName() + &quot;\t 我是构造方法SingletonDemo&quot;);    }    public static SingletonDemo getInstance() {        if(instance == null) {            instance = new SingletonDemo();        }        return instance;    }    public static void main(String[] args) {        for (int i = 0; i &lt; 10; i++) {            new Thread(() -&gt; {                SingletonDemo.getInstance();            }, String.valueOf(i)).start();        }    }}</code></pre><p>从下面的结果我们可以看出，我们通过SingletonDemo.getInstance() 获取到的对象，并不是同一个，而是被下面几个线程都进行了创建，那么在多线程环境下，单例模式如何保证呢？</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715182217.png" alt=""></p><h3 id="4-1-1、解决方法1"><a href="#4-1-1、解决方法1" class="headerlink" title="4.1.1、解决方法1"></a>4.1.1、解决方法1</h3><p>引入synchronized关键字</p><pre><code class="java">    public synchronized static SingletonDemo getInstance() {        if(instance == null) {            instance = new SingletonDemo();        }        return instance;    }</code></pre><p>输出结果</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715182519.png" alt=""></p><p>我们能够发现，通过引入Synchronized关键字，能够解决高并发环境下的单例模式问题</p><p>但是synchronized属于重量级的同步机制，它只允许一个线程同时访问获取实例的方法，但是为了保证数据一致性，而减低了并发性，因此采用的比较少</p><h3 id="4-1-2、解决方法2"><a href="#4-1-2、解决方法2" class="headerlink" title="4.1.2、解决方法2"></a>4.1.2、解决方法2</h3><p>通过引入DCL   Double Check Lock   双端检锁机制</p><p>就是在进来和出去的时候，进行检测</p><pre><code class="java">    public static SingletonDemo getInstance() {        if(instance == null) {            // 同步代码段的时候，进行检测            synchronized (SingletonDemo.class) {                if(instance == null) {                    instance = new SingletonDemo();                }            }        }        return instance;    }</code></pre><p>最后输出的结果为：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715183444.png" alt="image-20200310165703190"></p><p>从输出结果来看，确实能够保证单例模式的正确性，但是上面的方法还是存在问题的</p><p>DCL（双端检锁）机制不一定是线程安全的，原因是有指令重排的存在，加入volatile可以禁止指令重排</p><p>原因是在某一个线程执行到第一次检测的时候，读取到 instance 不为null，instance的引用对象可能没有完成实例化。因为 instance = new SingletonDemo()；可以分为以下三步进行完成：</p><ul><li>memory = allocate();   // 1、分配对象内存空间</li><li>instance(memory);   // 2、初始化对象</li><li>instance = memory;  // 3、设置instance指向刚刚分配的内存地址，此时instance != null</li></ul><p>但是我们通过上面的三个步骤，能够发现，步骤2 和 步骤3之间不存在 数据依赖关系，而且无论重排前 还是重排后，程序的执行结果在单线程中并没有改变，因此这种重排优化是允许的。</p><ul><li>memory = allocate();   // 1、分配对象内存空间</li><li>instance = memory;  // 3、设置instance指向刚刚分配的内存地址，此时instance != null，但是对象还没有初始化完成</li><li>instance(memory);   // 2、初始化对象</li></ul><p>这样就会造成什么问题呢？</p><p>也就是当我们执行到重排后的步骤2，试图获取instance的时候，会得到null，因为对象的初始化还没有完成，而是在重排后的步骤3才完成，因此执行单例模式的代码时候，就会重新在创建一个instance实例</p><p><code>指令重排只会保证串行语义的执行一致性（单线程），但并不会关系多线程间的语义一致性</code></p><p>所以当一条线程访问instance不为null时，由于instance实例未必已初始化完成，这就造成了线程安全的问题</p><p>所以需要引入volatile，来保证出现指令重排的问题，从而保证单例模式的线程安全性</p><pre><code>private static volatile SingletonDemo instance = null;</code></pre><h3 id="4-1-3、最终代码"><a href="#4-1-3、最终代码" class="headerlink" title="4.1.3、最终代码"></a>4.1.3、最终代码</h3><pre><code class="java">package com.lsq.juc.Volatile;/** * @Description: SingletonDemo（单例模式） * @Author: LSQ * Date: 2020/7/15 18:13 */public class SingletonDemo {    private static volatile SingletonDemo instance = null;    private SingletonDemo () {        System.out.println(Thread.currentThread().getName() + &quot;\t 我是构造方法SingletonDemo&quot;);    }    public static SingletonDemo getInstance() {        if(instance == null) {            // a 双重检查加锁多线程情况下会出现某个线程虽然这里已经为空，但是另外一个线程已经执行到d处            synchronized (SingletonDemo.class) //b            {                //不加volitale关键字的话有可能会出现尚未完全初始化就获取到的情况。原因是内存模型允许无序写入                if(instance == null) {                    // d 此时才开始初始化                    instance = new SingletonDemo();                }            }        }        return instance;    }    public static void main(String[] args) {//        // 这里的 == 是比较内存地址//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());//        System.out.println(SingletonDemo.getInstance() == SingletonDemo.getInstance());        for (int i = 0; i &lt; 10; i++) {            new Thread(() -&gt; {                SingletonDemo.getInstance();            }, String.valueOf(i)).start();        }    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、Volatile和JMM内存模型的可见性&quot;&gt;&lt;a href=&quot;#01、Volatile和JMM内存模型的可见性&quot; class=&quot;headerlink&quot; title=&quot;01、Volatile和JMM内存模型的可见性&quot;&gt;&lt;/a&gt;01、Volatile和JMM内存
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>02CAS底层原理</title>
    <link href="http://lsqstudy.top/post/7e7a8188.html"/>
    <id>http://lsqstudy.top/post/7e7a8188.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T13:51:28.252Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、CAS你知道吗？"><a href="#01、CAS你知道吗？" class="headerlink" title="01、CAS你知道吗？"></a>01、CAS你知道吗？</h1><h2 id="1-1、概念"><a href="#1-1、概念" class="headerlink" title="1.1、概念"></a>1.1、概念</h2><p>CAS的全称是Compare-And-Swap，它是CPU并发原语</p><p>它的功能是判断内存某个位置的值是否为预期值，如果是则更改为新的值，这个过程是原子的</p><p>CAS并发原语体现在Java语言中就是<code>sun.misc.Unsafe</code>类的各个方法。调用UnSafe类中的CAS方法，JVM会帮我们实现出CAS汇编指令，这是一种完全依赖于硬件的功能，通过它实现了原子操作，再次强调，由于CAS是一种系统原语，原语属于操作系统用于范畴，是由若干条指令组成，用于完成某个功能的一个过程，并且原语的执行必须是连续的，在执行过程中不允许被中断，也就是说CAS是一条CPU的原子指令，不会造成所谓的数据不一致的问题，也就是说CAS是线程安全的。</p><h2 id="1-2、代码使用"><a href="#1-2、代码使用" class="headerlink" title="1.2、代码使用"></a>1.2、代码使用</h2><p>首先调用AtomicInteger创建了一个实例， 并初始化为5</p><pre><code class="java">        // 创建一个原子类        AtomicInteger atomicInteger = new AtomicInteger(5);</code></pre><p>然后调用CAS方法，企图更新成2019，这里有两个参数，一个是5，表示期望值，第二个就是我们要更新的值</p><pre><code class="java">atomicInteger.compareAndSet(5, 2019)</code></pre><p>然后再次使用了一个方法，同样将值改成1024</p><pre><code class="java">atomicInteger.compareAndSet(5, 1024)</code></pre><p>完整代码如下：</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.atomic.AtomicInteger;/** * @Description: 比较并交换：compareAndSet * @Author: LSQ * Date: 2020/7/15 19:50 */public class CASDemo {    public static void main(String[] args) {        // 创建一个原子类        AtomicInteger atomicInteger = new AtomicInteger(5);        /**         * 一个是期望值，一个是更新值，但期望值和原来的值相同时，才能够更改         * 假设三秒前，我拿的是5，也就是expect为5，然后我需要更新成 2019         */        System.out.println(atomicInteger.compareAndSet(5, 2019) + &quot;\t current data: &quot; + atomicInteger.get());        System.out.println(atomicInteger.compareAndSet(5, 1024) + &quot;\t current data: &quot; + atomicInteger.get());    }}</code></pre><p>上面代码的执行结果为</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715195203.png" style="zoom:80%;" /><p>这是因为我们执行第一个的时候，期望值和原本值是满足的，因此修改成功，但是第二次后，主内存的值已经修改成了2019，不满足期望值，因此返回了false，本次写入失败</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715195237.png" style="zoom:80%;" /><p>这个就类似于SVN或者Git的版本号，如果没有人更改过，就能够正常提交，否者需要先将代码pull下来，合并代码后，然后提交</p><h2 id="1-3、CAS底层原理"><a href="#1-3、CAS底层原理" class="headerlink" title="1.3、CAS底层原理"></a>1.3、CAS底层原理</h2><p>首先我们先看看 atomicInteger.getAndIncrement()方法的源码</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715195451.png" alt=""></p><p>从这里能够看到，底层又调用了一个unsafe类的getAndAddInt方法</p><h3 id="1-3-1、unsafe类"><a href="#1-3-1、unsafe类" class="headerlink" title="1.3.1、unsafe类"></a>1.3.1、unsafe类</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215035.png" alt=""></p><p>Unsafe是CAS的核心类，由于Java方法无法直接访问底层系统，需要通过本地（Native）方法来访问，Unsafe相当于一个后门，基于该类可以直接操作特定的内存数据。Unsafe类存在<code>sun.misc</code>包中，其内部方法操作可以像C的指针一样直接操作内存，因为Java中的CAS操作的执行依赖于Unsafe类的方法。</p><p><code>注意Unsafe类的所有方法都是native修饰的，也就是说unsafe类中的方法都直接调用操作系统底层资源执行相应的任务</code></p><blockquote><p>为什么Atomic修饰的包装类，能够保证原子性，依靠的就是底层的unsafe类</p></blockquote><h3 id="1-3-2、变量valueOffset"><a href="#1-3-2、变量valueOffset" class="headerlink" title="1.3.2、变量valueOffset"></a>1.3.2、变量valueOffset</h3><p>表示该变量值在内存中的偏移地址，因为Unsafe就是根据内存偏移地址获取数据的。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215043.png" alt=""></p><p>从这里我们能够看到，通过valueOffset，直接通过内存地址，获取到值，然后进行加1的操作</p><h3 id="1-3-3、变量value用volatile修饰"><a href="#1-3-3、变量value用volatile修饰" class="headerlink" title="1.3.3、变量value用volatile修饰"></a>1.3.3、变量value用volatile修饰</h3><p>保证了多线程之间的内存可见性</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215051.png" alt=""></p><p>var5：就是我们从主内存中拷贝到工作内存中的值</p><p>那么操作的时候，需要比较工作内存中的值，和主内存中的值进行比较</p><p>假设执行 compareAndSwapInt返回false，那么就一直执行 while方法，直到期望的值和真实值一样</p><ul><li>val1：AtomicInteger对象本身</li><li>var2：该对象值得引用地址</li><li>var4：需要变动的数量</li><li>var5：用var1和var2找到的内存中的真实值<ul><li>用该对象当前的值与var5比较</li><li>如果相同，更新var5 + var4 并返回true</li><li>如果不同，继续取值然后再比较，直到更新完成</li></ul></li></ul><p>这里没有用synchronized，而用CAS，这样提高了并发性，也能够实现一致性，是因为每个线程进来后，进入的do while循环，然后不断的获取内存中的值，判断是否为最新，然后在进行更新操作。</p><p>假设线程A和线程B同时执行getAndInt操作（分别跑在不同的CPU上）</p><ol><li>AtomicInteger里面的value原始值为3，即主内存中AtomicInteger的 value 为3，根据JMM模型，线程A和线程B各自持有一份价值为3的副本，分别存储在各自的工作内存</li><li>线程A通过getIntVolatile(var1 , var2) 拿到value值3，这是线程A被挂起（该线程失去CPU执行权）</li><li>线程B也通过getIntVolatile(var1, var2)方法获取到value值也是3，此时刚好线程B没有被挂起，并执行了compareAndSwapInt方法，比较内存的值也是3，成功修改内存值为4，线程B打完收工，一切OK</li><li>这是线程A恢复，执行CAS方法，比较发现自己手里的数字3和主内存中的数字4不一致，说明该值已经被其它线程抢先一步修改过了，那么A线程本次修改失败，只能够重新读取后在来一遍了，也就是在执行do while</li><li>线程A重新获取value值，因为变量value被volatile修饰，所以其它线程对它的修改，线程A总能够看到，线程A继续执行compareAndSwapInt进行比较替换，直到成功。</li></ol><p>Unsafe类 + CAS思想： 也就是自旋，自我旋转</p><h2 id="1-4、底层汇编"><a href="#1-4、底层汇编" class="headerlink" title="1.4、底层汇编"></a>1.4、底层汇编</h2><p>Unsafe类中的compareAndSwapInt是一个本地方法，该方法的实现位于unsafe.cpp中</p><ul><li>先想办法拿到变量value在内存中的地址</li><li>通过Atomic::cmpxchg实现比较替换，其中参数X是即将更新的值，参数e是原内存的值</li></ul><h2 id="1-5、CAS缺点"><a href="#1-5、CAS缺点" class="headerlink" title="1.5、CAS缺点"></a>1.5、CAS缺点</h2><p>CAS不加锁，保证一次性，但是需要多次比较</p><ul><li>循环时间长，开销大（因为执行的是do while，如果比较不成功一直在循环，最差的情况，就是某个线程一直取到的值和预期值都不一样，这样就会无限循环）</li><li>只能保证一个共享变量的原子操作<ul><li>当对一个共享变量执行操作时，我们可以通过循环CAS的方式来保证原子操作</li><li>但是对于多个共享变量操作时，循环CAS就无法保证操作的原子性，这个时候只能用锁来保证原子性</li></ul></li><li>引出来ABA问题？</li></ul><h2 id="1-6、ABA问题"><a href="#1-6、ABA问题" class="headerlink" title="1.6、ABA问题"></a>1.6、ABA问题</h2><p>下面讲解</p><h2 id="1-7、总结"><a href="#1-7、总结" class="headerlink" title="1.7、总结"></a>1.7、总结</h2><h3 id="1-7-1、CAS"><a href="#1-7-1、CAS" class="headerlink" title="1.7.1、CAS"></a>1.7.1、CAS</h3><p>CAS是compareAndSwap，比较当前工作内存中的值和主物理内存中的值，如果相同则执行规定操作，否者继续比较直到主内存和工作内存的值一致为止</p><h3 id="1-7-2、CAS应用"><a href="#1-7-2、CAS应用" class="headerlink" title="1.7.2、CAS应用"></a>1.7.2、CAS应用</h3><p>CAS有3个操作数，内存值V，旧的预期值A，要修改的更新值B。当且仅当预期值A和内存值V相同时，将内存值V修改为B，否者什么都不做</p><h1 id="02、原子类AtomicInteger的ABA问题"><a href="#02、原子类AtomicInteger的ABA问题" class="headerlink" title="02、原子类AtomicInteger的ABA问题"></a>02、原子类AtomicInteger的ABA问题</h1><h2 id="2-1、连环套路"><a href="#2-1、连环套路" class="headerlink" title="2.1、连环套路"></a>2.1、连环套路</h2><p>从AtomicInteger引出下面的问题</p><p>CAS -&gt; Unsafe -&gt; CAS底层思想 -&gt; ABA -&gt; 原子引用更新 -&gt; 如何规避ABA问题</p><h2 id="2-2、ABA问题是什么"><a href="#2-2、ABA问题是什么" class="headerlink" title="2.2、ABA问题是什么"></a>2.2、ABA问题是什么</h2><p>狸猫换太子</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215104.png" style="zoom:80%;" /><p>假设现在有两个线程，分别是T1 和 T2，然后T1执行某个操作的时间为10秒，T2执行某个时间的操作是2秒，<br>最开始AB两个线程，分别从主内存中获取A值，<br>但是因为B的执行速度更快，他先把A的值改成B，然后再修改成A，然后执行完毕，<br>T1线程在10秒后，执行完毕，判断内存中的值为A，并且和自己预期的值一样，它就认为没有人更改了主内存中的值，就快乐的修改成B，<br>但是实际上 可能中间经历了 ABCDEFA 这个变换，也就是中间的值经历了狸猫换太子。</p><p>所以ABA问题就是，在进行获取主内存值的时候，该内存值在我们写入主内存的时候，已经被修改了N次，<br>但是最终又改成原来的值了</p><h2 id="2-3、CAS导致ABA问题"><a href="#2-3、CAS导致ABA问题" class="headerlink" title="2.3、CAS导致ABA问题"></a>2.3、CAS导致ABA问题</h2><p>CAS算法实现了一个重要的前提，需要取出内存中某时刻的数据，并在当下时刻比较并替换，那么这个时间差会导致数据的变化。</p><p>CAS只管开头和结尾，也就是头和尾是一样，那就修改成功，中间的这个过程，可能会被人修改过。</p><p>ABA问题就像一个人被拖下车打了一顿，其后又被送回到车上。</p><h2 id="2-4、原子引用"><a href="#2-4、原子引用" class="headerlink" title="2.4、原子引用"></a>2.4、原子引用</h2><p>原子引用其实和原子包装类是差不多的概念，就是将一个java类，用原子引用类进行包装起来，那么这个类就具备了原子性</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.atomic.AtomicReference;/** * @Description: 原子引用 * @Author: LSQ * Date: 2020/7/15 20:57 */class User {    String userName;    int age;    public User(String userName, int age) {        this.userName = userName;        this.age = age;    }    public String getUserName() {        return userName;    }    public void setUserName(String userName) {        this.userName = userName;    }    public int getAge() {        return age;    }    public void setAge(int age) {        this.age = age;    }    @Override    public String toString() {        return &quot;User{&quot; +                &quot;userName=&#39;&quot; + userName + &#39;\&#39;&#39; +                &quot;, age=&quot; + age +                &#39;}&#39;;    }}public class AtomicReferenceDemo {    public static void main(String[] args) {        User z3 = new User(&quot;z3&quot;, 22);        User l4 = new User(&quot;l4&quot;, 25);        // 创建原子引用包装类        AtomicReference&lt;User&gt; atomicReference = new AtomicReference&lt;&gt;();        // 现在主物理内存的共享变量，为z3        atomicReference.set(z3);        // 比较并交换，如果现在主物理内存的值为z3，那么交换成l4        System.out.println(atomicReference.compareAndSet(z3, l4) + &quot;\t &quot; + atomicReference.get().toString());        // 比较并交换，现在主物理内存的值是l4了，但是预期为z3，因此交换失败        System.out.println(atomicReference.compareAndSet(z3, l4) + &quot;\t &quot; + atomicReference.get().toString());    }}</code></pre><h3 id="2-4-1、基于原子引用的ABA问题"><a href="#2-4-1、基于原子引用的ABA问题" class="headerlink" title="2.4.1、基于原子引用的ABA问题"></a>2.4.1、基于原子引用的ABA问题</h3><p>我们首先创建了两个线程，然后T1线程，执行一次ABA的操作，T2线程在一秒后修改主内存的值</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.TimeUnit;import java.util.concurrent.atomic.AtomicReference;/** * @Description:ABA问题的解决，AtomicStampedReference * @Author: LSQ * Date: 2020/7/15 21:00 */public class ABADemo {    /**     * 普通的原子引用包装类     */    static AtomicReference&lt;Integer&gt; atomicReference = new AtomicReference&lt;&gt;(100);    public static void main(String[] args) {        new Thread(() -&gt; {            // 把100 改成 101 然后在改成100，也就是ABA            atomicReference.compareAndSet(100, 101);            atomicReference.compareAndSet(101, 100);        }, &quot;t1&quot;).start();        new Thread(() -&gt; {            try {                // 睡眠一秒，保证t1线程，完成了ABA操作                TimeUnit.SECONDS.sleep(1);            } catch (InterruptedException e) {                e.printStackTrace();            }            // 把100 改成 101 然后在改成100，也就是ABA            System.out.println(atomicReference.compareAndSet(100, 2019) + &quot;\t&quot; + atomicReference.get());        }, &quot;t2&quot;).start();    }}</code></pre><p>我们发现，它能够成功的修改，这就是ABA问题</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215116.png" style="zoom:80%;" /><h2 id="2-5、解决ABA问题"><a href="#2-5、解决ABA问题" class="headerlink" title="2.5、解决ABA问题"></a>2.5、解决ABA问题</h2><p>新增修改版本号，类似于时间戳的概念</p><pre><code>T1：  100 1                 2019 2T2：  100 1     101 2       100  3</code></pre><p>如果T1修改的时候，版本号为2，落后于现在的版本号3，所以要重新获取最新值，这里就提出了一个使用时间戳版本号，来解决ABA问题的思路</p><h2 id="2-6、AtomicStampedReference"><a href="#2-6、AtomicStampedReference" class="headerlink" title="2.6、AtomicStampedReference"></a>2.6、AtomicStampedReference</h2><p>时间戳原子引用，来这里应用于版本号的更新，也就是每次更新的时候，需要比较期望值和当前值，以及期望版本号和当前版本号</p><pre><code class="java">package com.lsq.juc.CAS;import java.util.concurrent.TimeUnit;import java.util.concurrent.atomic.AtomicReference;import java.util.concurrent.atomic.AtomicStampedReference;/** * @Description:ABA问题的解决，AtomicStampedReference * @Author: LSQ * Date: 2020/7/15 21:00 */public class ABADemo {    /**     * 普通的原子引用包装类     */    static AtomicReference&lt;Integer&gt; atomicReference = new AtomicReference&lt;&gt;(100);    // 传递两个值，一个是初始值，一个是初始版本号    static AtomicStampedReference&lt;Integer&gt; atomicStampedReference = new AtomicStampedReference&lt;&gt;(100, 1);    public static void main(String[] args) {        System.out.println(&quot;============以下是ABA问题的产生==========&quot;);        new Thread(() -&gt; {            // 把100 改成 101 然后在改成100，也就是ABA            atomicReference.compareAndSet(100, 101);            atomicReference.compareAndSet(101, 100);        }, &quot;t1&quot;).start();        new Thread(() -&gt; {            // 睡眠一秒，保证t1线程，完成了ABA操作          try {TimeUnit.SECONDS.sleep(1);} catch (InterruptedException e) { e.printStackTrace();}            // 把100 改成 101 然后在改成100，也就是ABA            System.out.println(atomicReference.compareAndSet(100, 2019) + &quot;\t&quot; + atomicReference.get());        }, &quot;t2&quot;).start();        try {TimeUnit.SECONDS.sleep(2);} catch (InterruptedException e) { e.printStackTrace();}        System.out.println(&quot;============以下是ABA问题的解决==========&quot;);        new Thread(() -&gt; {            // 获取版本号            int stamp = atomicStampedReference.getStamp();            System.out.println(Thread.currentThread().getName() + &quot;\t 第一次版本号&quot; + stamp);            // 暂停t3一秒钟          try {TimeUnit.SECONDS.sleep(1);} catch (InterruptedException e) { e.printStackTrace();}            // 传入4个值，期望值，更新值，期望版本号，更新版本号            atomicStampedReference.compareAndSet(100, 101, atomicStampedReference.getStamp(), atomicStampedReference.getStamp() + 1);            System.out.println(Thread.currentThread().getName() + &quot;\t 第二次版本号&quot; + atomicStampedReference.getStamp());            atomicStampedReference.compareAndSet(101, 100, atomicStampedReference.getStamp(), atomicStampedReference.getStamp() + 1);            System.out.println(Thread.currentThread().getName() + &quot;\t 第三次版本号&quot; + atomicStampedReference.getStamp());        }, &quot;t3&quot;).start();        new Thread(() -&gt; {            // 获取版本号            int stamp = atomicStampedReference.getStamp();            System.out.println(Thread.currentThread().getName() + &quot;\t 第一次版本号&quot; + stamp);            // 暂停t4 3秒钟，保证t3线程也进行一次ABA问题           try {TimeUnit.SECONDS.sleep(3);} catch (InterruptedException e) { e.printStackTrace();}            boolean result = atomicStampedReference.compareAndSet(100, 2019, stamp, stamp + 1);            System.out.println(Thread.currentThread().getName() + &quot;\t 修改成功否：&quot; + result + &quot;\t 当前最新实际版本号：&quot; + atomicStampedReference.getStamp());            System.out.println(Thread.currentThread().getName() + &quot;\t 当前实际最新值&quot; + atomicStampedReference.getReference());        }, &quot;t4&quot;).start();    }}</code></pre><p>运行结果为：</p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715215123.png" style="zoom:80%;" /><p>我们能够发现，线程t3，在进行ABA操作后，版本号变更成了3，而线程t4在进行操作的时候，就出现操作失败了，因为版本号和当初拿到的不一样</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、CAS你知道吗？&quot;&gt;&lt;a href=&quot;#01、CAS你知道吗？&quot; class=&quot;headerlink&quot; title=&quot;01、CAS你知道吗？&quot;&gt;&lt;/a&gt;01、CAS你知道吗？&lt;/h1&gt;&lt;h2 id=&quot;1-1、概念&quot;&gt;&lt;a href=&quot;#1-1、概念&quot; cla
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>03ArrayList为什么线程不安全</title>
    <link href="http://lsqstudy.top/post/32574176.html"/>
    <id>http://lsqstudy.top/post/32574176.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T14:27:01.627Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、Collection线程不安全的举例"><a href="#01、Collection线程不安全的举例" class="headerlink" title="01、Collection线程不安全的举例"></a>01、Collection线程不安全的举例</h1><h2 id="1-1、前言"><a href="#1-1、前言" class="headerlink" title="1.1、前言"></a>1.1、前言</h2><p>1、当我们执行下面语句的时候，底层进行了什么操作</p><pre><code class="java">new ArrayList&lt;Integer&gt;();</code></pre><p>底层创建了一个空的数组，伴随着初始值为10</p><p>当执行add方法后，如果超过了10，那么会进行扩容，扩容的大小为原值的一半，也就是5个，使用下列方法扩容</p><pre><code class="java">Arrays.copyOf(elementData, netCapacity)</code></pre><h2 id="1-2、单线程环境下"><a href="#1-2、单线程环境下" class="headerlink" title="1.2、单线程环境下"></a>1.2、单线程环境下</h2><p>单线程环境的ArrayList是不会有问题的</p><pre><code class="java">public class ArrayListNotSafeDemo {    public static void main(String[] args) {        List&lt;String&gt; list = new ArrayList&lt;&gt;();        list.add(&quot;a&quot;);        list.add(&quot;b&quot;);        list.add(&quot;c&quot;);        for(String element : list) {            System.out.println(element);        }    }}</code></pre><h2 id="1-3、多线程环境"><a href="#1-3、多线程环境" class="headerlink" title="1.3、多线程环境"></a>1.3、多线程环境</h2><p>为什么ArrayList是线程不安全的？因为在进行写操作的时候，方法上为了保证并发性，是没有添加synchronized修饰，所以并发写的时候，就会出现问题</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221328.png" alt=""></p><p>当我们同时启动30个线程去操作List的时候</p><pre><code class="java">/** * 集合类线程不安全举例 * @author: 陌溪 * @create: 2020-03-12-20:15 */public class ArrayListNotSafeDemo {    public static void main(String[] args) {        List&lt;String&gt; list = new ArrayList&lt;&gt;();        for (int i = 0; i &lt; 30; i++) {            new Thread(() -&gt; {                list.add(UUID.randomUUID().toString().substring(0, 8));                System.out.println(list);            }, String.valueOf(i)).start();        }    }}</code></pre><p>这个时候出现了错误，也就是<code>java.util.ConcurrentModificationException</code></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221324.png" alt=""></p><p>这个异常是 并发修改的异常</p><h2 id="1-4、解决方案"><a href="#1-4、解决方案" class="headerlink" title="1.4、解决方案"></a>1.4、解决方案</h2><h3 id="1-4-1、方案一：Vector【不推荐】"><a href="#1-4-1、方案一：Vector【不推荐】" class="headerlink" title="1.4.1、方案一：Vector【不推荐】"></a>1.4.1、方案一：Vector【不推荐】</h3><p>不用ArrayList这种不安全的List实现类，而采用Vector，线程安全的</p><p>关于Vector如何实现线程安全的，而是在方法上加了锁，即synchronized</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221320.png" alt=""></p><p>这样就每次只能够一个线程进行操作，所以不会出现线程不安全的问题，但是因为加锁了，导致并发性基于下降</p><h3 id="1-4-2、方案二：Collections-synchronized-【不推荐】"><a href="#1-4-2、方案二：Collections-synchronized-【不推荐】" class="headerlink" title="1.4.2、方案二：Collections.synchronized()【不推荐】"></a>1.4.2、方案二：Collections.synchronized()【不推荐】</h3><pre><code class="java">List&lt;String&gt; list = Collections.synchronizedList(new ArrayList&lt;&gt;());</code></pre><p>采用Collections集合工具类，在ArrayList外面包装一层 同步 机制</p><h3 id="1-4-3、方案三：JUC的方法-写时复制-【推荐】"><a href="#1-4-3、方案三：JUC的方法-写时复制-【推荐】" class="headerlink" title="1.4.3、方案三：JUC的方法[写时复制]【推荐】"></a>1.4.3、方案三：JUC的方法[写时复制]【推荐】</h3><blockquote><p><code>CopyOnWriteArrayList</code>：写时复制，主要是一种读写分离的思想</p></blockquote><p>写时复制，CopyOnWrite容器即写时复制的容器，往一个容器中添加元素的时候，不直接往当前容器Object[]添加，而是先将Object[]进行copy，复制出一个新的容器object[] newElements，然后新的容器Object[] newElements里添加原始，添加元素完后，在将原容器的引用指向新的容器 setArray(newElements)；这样做的好处是可以对copyOnWrite容器进行并发的度，而不需要加锁，因为当前容器不需要添加任何元素。所以CopyOnWrite容器也是一种读写分离的思想，读和写不同的容器。</p><p>就是写的时候，把ArrayList扩容一个出来，然后把值填写上去，在通知其他的线程，ArrayList的引用指向扩容后的</p><blockquote><p>查看底层add方法源码</p></blockquote><pre><code class="java">    public boolean add(E e) {        final ReentrantLock lock = this.lock;        lock.lock();        try {            Object[] elements = getArray();            int len = elements.length;            Object[] newElements = Arrays.copyOf(elements, len + 1);            newElements[len] = e;            setArray(newElements);            return true;        } finally {            lock.unlock();        }    }</code></pre><blockquote><p>首先需要加锁</p></blockquote><pre><code class="java">final ReentrantLock lock = this.lock;lock.lock();</code></pre><blockquote><p>然后在末尾扩容一个单位</p></blockquote><pre><code class="java">Object[] elements = getArray();int len = elements.length;Object[] newElements = Arrays.copyOf(elements, len + 1);</code></pre><blockquote><p>然后在把扩容后的空间，填写上需要add的内容</p></blockquote><pre><code class="java">newElements[len] = e;</code></pre><p>最后把内容set到Array中</p><h2 id="1-5、HashSet线程不安全"><a href="#1-5、HashSet线程不安全" class="headerlink" title="1.5、HashSet线程不安全"></a>1.5、HashSet线程不安全</h2><h3 id="1-5-1、CopyOnWriteArraySet"><a href="#1-5-1、CopyOnWriteArraySet" class="headerlink" title="1.5.1、CopyOnWriteArraySet"></a>1.5.1、CopyOnWriteArraySet</h3><p>底层还是使用CopyOnWriteArrayList进行实例化</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221315.png" alt=""></p><h3 id="1-5-2、HashSet底层结构"><a href="#1-5-2、HashSet底层结构" class="headerlink" title="1.5.2、HashSet底层结构"></a>1.5.2、HashSet底层结构</h3><p>同理HashSet的底层结构就是HashMap</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/02/20200715221311.png" alt=""></p><blockquote><p>但是为什么我调用 HashSet.add()的方法，只需要传递一个元素，而HashMap是需要传递key-value键值对？</p></blockquote><p>首先我们查看hashSet的add方法</p><pre><code class="java">public boolean add(E e) {    return map.put(e, PRESENT)==null;}</code></pre><p>我们能发现但我们调用add的时候，存储一个值进入map中，只是作为key进行存储，而value存储的是一个Object类型的常量，也就是说HashSet只关心key，而不关心value</p><h2 id="1-6、HashMap线程不安全"><a href="#1-6、HashMap线程不安全" class="headerlink" title="1.6、HashMap线程不安全"></a>1.6、HashMap线程不安全</h2><p>同理HashMap在多线程环境下，也是不安全的</p><pre><code class="java">public static void main(String[] args) {    Map&lt;String, String&gt; map = new HashMap&lt;&gt;();    for (int i = 0; i &lt; 30; i++) {        new Thread(() -&gt; {            map.put(Thread.currentThread().getName(), UUID.randomUUID().toString().substring(0, 8));            System.out.println(map);        }, String.valueOf(i)).start();    }}</code></pre><h3 id="1-6-1、解决方法"><a href="#1-6-1、解决方法" class="headerlink" title="1.6.1、解决方法"></a>1.6.1、解决方法</h3><p>1、使用<code>Collections.synchronizedMap(new HashMap&lt;&gt;())</code>;【不推荐】</p><p>2、使用<code>ConcurrentHashMap</code>【推荐】</p><pre><code class="java">Map&lt;String, String&gt; map = new ConcurrentHashMap&lt;&gt;();</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、Collection线程不安全的举例&quot;&gt;&lt;a href=&quot;#01、Collection线程不安全的举例&quot; class=&quot;headerlink&quot; title=&quot;01、Collection线程不安全的举例&quot;&gt;&lt;/a&gt;01、Collection线程不安全的举例&lt;/
      
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>docs/interview/juc/copy</title>
    <link href="http://lsqstudy.top/post/33f9d78b.html"/>
    <id>http://lsqstudy.top/post/33f9d78b.html</id>
    <published>2020-07-13T16:00:00.000Z</published>
    <updated>2020-07-15T02:22:41.613Z</updated>
    
    <summary type="html">
    
    </summary>
    
    
      <category term="面试" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/%E9%9D%A2%E8%AF%95/JUC/"/>
    
    
      <category term="面试" scheme="http://lsqstudy.top/tags/%E9%9D%A2%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>01Java多线程</title>
    <link href="http://lsqstudy.top/post/801aec6f.html"/>
    <id>http://lsqstudy.top/post/801aec6f.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.665Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、多线程"><a href="#01、多线程" class="headerlink" title="01、多线程"></a>01、多线程</h1><h2 id="1-1、进程和线程"><a href="#1-1、进程和线程" class="headerlink" title="1.1、进程和线程"></a>1.1、进程和线程</h2><blockquote><p>1、进程</p></blockquote><p>进程是一个具有一定独立功能的程序关于某个数据集合的一次运行活动。它是操作系统动态执行的基本单元，在传统的操作系统中，进程既是基本的分配单元，也是基本的执行单元。</p><blockquote><p>2、线程</p></blockquote><p><strong>通常在一个进程中可以包含若干个线程，当然一个进程中至少有一个线程，不然没有存在的意义</strong>。线程可以利用进程所拥有的资源，在引入线程的操作系统中，通常都是把进程作为分配资源的基本单位，而把线程作为独立运行和独立调度的基本单位，由于线程比进程更小，基本上不拥有系统资源，故对它的调度所付出的开销就会小得多，能更高效的提高系统多个程序间并发执行的程度。</p><blockquote><p>3、其他说明</p></blockquote><p><strong>进程与线程最主要的区别：它们是操作系统管理资源的不同方式的体现。</strong><br>准确来说进程与线程属于衍生关系。<br>进程是操作系统执行程序的一次过程,在这个过程中可能会产生多个线程。</p><blockquote><p>大四的时候写论文，用word写论文，同时用QQ音乐放音乐，同时用QQ聊天，多个进程。</p><p>word如没有保存，停电关机，再通电后打开word可以恢复之前未保存的文档，word也会检查你的拼写，两个线程：容灾备份，语法检查</p></blockquote><p><strong>由于系统在线程之间的切换比在进程之间的切换更高效率，所以线程也被成为轻量级进程。</strong></p><h2 id="1-2、并发和并行"><a href="#1-2、并发和并行" class="headerlink" title="1.2、并发和并行"></a>1.2、并发和并行</h2><ol><li><p>并发: 多个线程任务被一个cpu轮流执行。注意，这里并不是只允许一个cpu执行多任务，多个cpu执行也是可以的。<strong>并发强调的是计算机应用程序有处理多个任务的能力。</strong></p></li><li><p>并行:多个线程被多个cpu同时执行。这里也并不是只允许多个cpu处理多任务，一个cpu也是可以的，只要cpu能在同一时刻处理多任务。<strong>并行强调的是计算机应用程序拥有同时处理多任务的能力。</strong></p></li></ol><h2 id="1-3、多线程的利弊"><a href="#1-3、多线程的利弊" class="headerlink" title="1.3、多线程的利弊"></a>1.3、多线程的利弊</h2><ul><li><p>利:</p><ul><li><p>线程可以比作轻量级的进程，cpu在线程之间的切换比在进程之间的切换，耗费的资源要少的多。</p></li><li><p>现在是多核cpu时代，意味着多个线程可以被多个cpu同时运行(并行)，如果可以利用好多线程，那么可以编写出高并发的程序。</p></li></ul></li><li><p>弊:</p><ul><li>虽然线程带来的好处很多，但是并发编程并不容易，如果控制不好线程，那么就可能造成死锁，资源闲置，内存泄露等问题。</li></ul></li></ul><h2 id="1-4、什么是上下文切换"><a href="#1-4、什么是上下文切换" class="headerlink" title="1.4、什么是上下文切换?"></a>1.4、什么是上下文切换?</h2><p>cpu是采用时间片的轮转制度，在多个线程之间来回切换运行的。<br>当cpu切换到另一个线程的时候，它会先保存当前线程执行的状态，<br>以便在下次切换回来执行时，可以重新加载状态，继续运行。<br><strong>从保存线程的状态再到重新加载回线程的状态的这个过程就叫做上下文切换。</strong></p><h2 id="1-5、线程的优先级"><a href="#1-5、线程的优先级" class="headerlink" title="1.5、线程的优先级"></a>1.5、线程的优先级</h2><p>在Java中可以通过Thread类的setPriority方法来设置线程的优先级，<br>虽然可以通过这样的方式来设置线程的优先级，但是线程执行的先后顺序并不依赖与线程的优先级。<br>换句话说就是，<strong>线程的优先级不保证线程执行的顺序。</strong></p><h2 id="1-6、线程的几种状态"><a href="#1-6、线程的几种状态" class="headerlink" title="1.6、线程的几种状态"></a>1.6、线程的几种状态</h2><p>见:jdk Thread类源码中的state枚举类</p><pre><code class="text">  NEW,RUNNABLE,BLOCKED,WAITING,TIMED_WAITING,TERMINATED</code></pre><h2 id="1-7、sleep方法和wait方法的区别"><a href="#1-7、sleep方法和wait方法的区别" class="headerlink" title="1.7、sleep方法和wait方法的区别"></a>1.7、sleep方法和wait方法的区别</h2><ol><li><p>sleep方法是Thread类的方法；而wait方法是Object类的方法。</p></li><li><p>sleep方法会<strong>使当前线程让出cpu的调度资源</strong>，从而让其他线程有获得被执行的机会，</p></li></ol><p><strong>但是并不会让当前线程释放锁对象。</strong><br>而wait方法是<strong>让当前线程释放锁并进入wait状态，</strong><br>不参与获取锁的争夺，从而让其他等待资源的线程有机会获取锁，<br>只有当其他线程调用notify或notifyAll方法是，被wait的线程才能重新与其他线程一起争夺资源。</p><h2 id="1-8、stop-suspend-resume等方法为什么会被遗弃"><a href="#1-8、stop-suspend-resume等方法为什么会被遗弃" class="headerlink" title="1.8、stop,suspend,resume等方法为什么会被遗弃"></a>1.8、stop,suspend,resume等方法为什么会被遗弃</h2><ul><li><p>stop: stop方法被弃用很好理解，因为stop方法是强行终止线程的执行，<br>不管线程的run方法是否执行完，资源是否释放完，它都会终止线程的运行，并释放锁。<br>显然，这在设计上就不合理。  </p></li><li><p>suspend和resume: suspend方法用于阻塞一个线程,但并不释放锁，<br>而resume方法的作用只是为了恢复被suspend的线程。<br>假设A，B线程都争抢同一把锁，A线程成功的获得了锁，<br>然后被suspend阻塞了，却并没有释放锁，它需要其他线程来唤醒，<br>但此时B线程需要获得这把锁才能唤醒A，所以此时就陷入了死锁。</p></li></ul><h2 id="1-9、interrupt-interrupted-isInterrupted方法区别"><a href="#1-9、interrupt-interrupted-isInterrupted方法区别" class="headerlink" title="1.9、interrupt,interrupted,isInterrupted方法区别"></a>1.9、interrupt,interrupted,isInterrupted方法区别</h2><ul><li><p>interrupt: 这个方法并不是中断当前线程，而是给当前线程设置一个中断状态。</p></li><li><p>isInterrupted: 当线程调用interrupt方法后，线程就有了一个中断状态，<br>而使用isInterrupted方法就可以检测到线程的中断状态。</p></li><li><p>interrupted: 这个方法用于清除interrupt方法设置的中断状态。<br>如果一个线程之前调用了interrupt方法设置了中断状态，<br>那么interrupted方法就可以清除这个中断状态。          </p></li></ul><h2 id="1-10、join方法"><a href="#1-10、join方法" class="headerlink" title="1.10、join方法"></a>1.10、join方法</h2><p>join方法的作用是让指定线程加入到当前线程中执行。</p><p>假如在main方法里面创建一个线程A执行，并调用A的join方法，<br>那么当前线程就是main，指定的A线程就会在main之前执行，<br>等A执行完后，才会继续执行main。</p><pre><code class="java">public static void main(String[] args) throws Exception{    Thread a = new Thread(()-&gt;                          {                              try                              {                                  TimeUnit.SECONDS.sleep(1);                              }catch (Exception e){}                              System.out.println(&quot;thread join&quot;);                          });    a.start();    //a会在main线程之前执行    a.join();    System.out.println(&quot;main&quot;);}</code></pre><p><strong>join方法的底层是wait方法，调用A线程(子线程)的join方法实际上是让main线程wait，<br>等A线程执行完后，才能继续执行后面的代码。</strong></p><h2 id="1-11、yield方法"><a href="#1-11、yield方法" class="headerlink" title="1.11、yield方法"></a>1.11、yield方法</h2><p>yield属于Thread的静态方法，<br>它的作用是让当前线程让出cpu调度资源。</p><p>yield方法其实就和线程的优先级一样，你虽然指定了，<br>但是最后的结果不由得你说了算，<br><strong>即使调用了yield方法，最后仍然可能是这个线程先执行，<br>只不过说别的线程可能先执行的机会稍大一些。</strong></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、多线程&quot;&gt;&lt;a href=&quot;#01、多线程&quot; class=&quot;headerlink&quot; title=&quot;01、多线程&quot;&gt;&lt;/a&gt;01、多线程&lt;/h1&gt;&lt;h2 id=&quot;1-1、进程和线程&quot;&gt;&lt;a href=&quot;#1-1、进程和线程&quot; class=&quot;headerlink
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>05线程间通信</title>
    <link href="http://lsqstudy.top/post/a69e1457.html"/>
    <id>http://lsqstudy.top/post/a69e1457.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:36:38.315Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、线程间通信"><a href="#01、线程间通信" class="headerlink" title="01、线程间通信"></a>01、线程间通信</h1><h2 id="1-1、面试题：两个线程打印"><a href="#1-1、面试题：两个线程打印" class="headerlink" title="1.1、面试题：两个线程打印"></a>1.1、面试题：两个线程打印</h2><p>两个线程，一个线程打印0，另一个打印1,要求用线程间通信</p><p>线程间通信：1、生产者+消费者2、通知等待唤醒机制</p><h2 id="1-2、synchronized实现"><a href="#1-2、synchronized实现" class="headerlink" title="1.2、synchronized实现"></a>1.2、synchronized实现</h2><h3 id="1-2-1、代码"><a href="#1-2-1、代码" class="headerlink" title="1.2.1、代码"></a>1.2.1、代码</h3><pre><code class="java">import java.util.concurrent.locks.Condition;import java.util.concurrent.locks.Lock;import java.util.concurrent.locks.ReentrantLock;class ShareDataOne//资源类{  private int number = 0;//初始值为零的一个变量  public synchronized void increment() throws InterruptedException   {     //1判断     if(number !=0 ) {//防止虚假唤醒用while       this.wait();     }     //2干活     ++number;     System.out.println(Thread.currentThread().getName()+&quot;\t&quot;+number);     //3通知     this.notifyAll();  }  public synchronized void decrement() throws InterruptedException   {     // 1判断     if (number == 0) {//防止虚假唤醒用while       this.wait();     }     // 2干活     --number;     System.out.println(Thread.currentThread().getName() + &quot;\t&quot; + number);     // 3通知     this.notifyAll();  }}/** *  * @Description: *现在两个线程， * 可以操作初始值为零的一个变量， * 实现一个线程对该变量加1，一个线程对该变量减1， * 交替，来10轮。  * 1 多线程变成模板（套路）-----上 *     1.1  线程    操作    资源类   *     1.2  高内聚  低耦合 * 2 多线程变成模板（套路）-----下 *     2.1  判断 *     2.2  干活 *     2.3  通知 */public class NotifyWaitDemoOne{  public static void main(String[] args)  {     ShareDataOne sd = new ShareDataOne();     new Thread(() -&gt; {       for (int i = 1; i &lt; 10; i++) {          try {            sd.increment();          } catch (InterruptedException e) {            // TODO Auto-generated catch block            e.printStackTrace();          }       }     }, &quot;A&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt; 10; i++) {          try {            sd.decrement();          } catch (InterruptedException e) {            // TODO Auto-generated catch block            e.printStackTrace();          }       }     }, &quot;B&quot;).start();  }}</code></pre><h3 id="1-2-2、换成4个线程"><a href="#1-2-2、换成4个线程" class="headerlink" title="1.2.2、换成4个线程"></a>1.2.2、换成4个线程</h3><blockquote><p>换成4个线程会导致错误，虚假唤醒</p></blockquote><p>原因：在java多线程判断时，不能用if，程序出事出在了判断上面，突然有一添加的线程进到if了，突然中断了交出控制权，没有进行验证，而是直接走下去了，加了两次，甚至多次</p><h3 id="1-2-3、解决办法"><a href="#1-2-3、解决办法" class="headerlink" title="1.2.3、解决办法"></a>1.2.3、解决办法</h3><blockquote><p>将<code>if</code>换成<code>while</code></p></blockquote><pre><code class="java">import java.util.concurrent.locks.Condition;import java.util.concurrent.locks.Lock;import java.util.concurrent.locks.ReentrantLock;import org.omg.IOP.Codec;class ShareData//资源类{  private int number = 0;//初始值为零的一个变量  public synchronized void increment() throws InterruptedException   {     //判断     while(number!=0) {       this.wait();     }     //干活     ++number;     System.out.println(Thread.currentThread().getName()+&quot; \t &quot;+number);     //通知     this.notifyAll();;  }  public synchronized void decrement() throws InterruptedException   {     //判断     while(number!=1) {       this.wait();     }     //干活     --number;     System.out.println(Thread.currentThread().getName()+&quot; \t &quot;+number);     //通知     this.notifyAll();  }}/** *  * @Description: *现在两个线程， * 可以操作初始值为零的一个变量， * 实现一个线程对该变量加1，一个线程对该变量减1， * 交替，来10轮。  *  * 笔记：Java里面如何进行工程级别的多线程编写 * 1 多线程变成模板（套路）-----上 *     1.1  线程    操作    资源类   *     1.2  高内聚  低耦合 * 2 多线程变成模板（套路）-----下 *     2.1  判断 *     2.2  干活 *     2.3  通知 */public class NotifyWaitDemo{  public static void main(String[] args)  {     ShareData sd = new ShareData();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.increment();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;A&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.decrement();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;B&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.increment();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;C&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.decrement();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;D&quot;).start();  }}</code></pre><h3 id="1-2-4、原理图"><a href="#1-2-4、原理图" class="headerlink" title="1.2.4、原理图"></a>1.2.4、原理图</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194147.png" alt=""></p><h2 id="1-3、java8新版实现"><a href="#1-3、java8新版实现" class="headerlink" title="1.3、java8新版实现"></a>1.3、java8新版实现</h2><blockquote><p>Condition：查看API，<code>java.util.concurrent</code></p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194150.png" alt=""></p><blockquote><p>代码</p></blockquote><pre><code class="java">import java.util.concurrent.locks.Condition;import java.util.concurrent.locks.Lock;import java.util.concurrent.locks.ReentrantLock;import org.omg.IOP.Codec;class ShareData//资源类{  private int number = 0;//初始值为零的一个变量  private Lock lock = new ReentrantLock();  private Condition condition  = lock.newCondition();   public  void increment() throws InterruptedException   {      lock.lock();         try {          //判断          while(number!=0) {            condition.await();          }          //干活          ++number;          System.out.println(Thread.currentThread().getName()+&quot; \t &quot;+number);          //通知          condition.signalAll();     } catch (Exception e) {       e.printStackTrace();     } finally {       lock.unlock();     }  }  public  void decrement() throws InterruptedException   {      lock.lock();         try {          //判断          while(number!=1) {            condition.await();          }          //干活          --number;          System.out.println(Thread.currentThread().getName()+&quot; \t &quot;+number);          //通知          condition.signalAll();     } catch (Exception e) {       e.printStackTrace();     } finally {       lock.unlock();     }  }  /*public synchronized void increment() throws InterruptedException   {     //判断     while(number!=0) {       this.wait();     }     //干活     ++number;     System.out.println(Thread.currentThread().getName()+&quot; \t &quot;+number);     //通知     this.notifyAll();;  }  public synchronized void decrement() throws InterruptedException   {     //判断     while(number!=1) {       this.wait();     }     //干活     --number;     System.out.println(Thread.currentThread().getName()+&quot; \t &quot;+number);     //通知     this.notifyAll();  }*/}/** *  * @Description: *现在两个线程， * 可以操作初始值为零的一个变量， * 实现一个线程对该变量加1，一个线程对该变量减1， * 交替，来10轮。  * *  * 笔记：Java里面如何进行工程级别的多线程编写 * 1 多线程变成模板（套路）-----上 *     1.1  线程    操作    资源类   *     1.2  高内聚  低耦合 * 2 多线程变成模板（套路）-----下 *     2.1  判断 *     2.2  干活 *     2.3  通知 */public class NotifyWaitDemo{  public static void main(String[] args)  {     ShareData sd = new ShareData();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.increment();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;A&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.decrement();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;B&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.increment();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;C&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;= 10; i++) {          try {            sd.decrement();          } catch (InterruptedException e) {            e.printStackTrace();          }       }     }, &quot;D&quot;).start();  }}</code></pre><h1 id="02、线程间定制化调用通信"><a href="#02、线程间定制化调用通信" class="headerlink" title="02、线程间定制化调用通信"></a>02、线程间定制化调用通信</h1><h2 id="2-1、案例"><a href="#2-1、案例" class="headerlink" title="2.1、案例"></a>2.1、案例</h2><p>多线程之间按顺序调用，实现A-&gt;B-&gt;C</p><p>三个线程启动，要求如下：</p><ul><li>AA打印5次，BB打印10次，CC打印15次</li><li>接着</li><li>AA打印5次，BB打印10次，CC打印15次</li><li>……来10轮  </li></ul><h2 id="2-2、代码"><a href="#2-2、代码" class="headerlink" title="2.2、代码"></a>2.2、代码</h2><pre><code class="java">package com.atguigu.thread;import java.util.concurrent.locks.Condition;import java.util.concurrent.locks.Lock;import java.util.concurrent.locks.ReentrantLock;class ShareResource{  private int number = 1;//1:A 2:B 3:C   private Lock lock = new ReentrantLock();  private Condition c1 = lock.newCondition();  private Condition c2 = lock.newCondition();  private Condition c3 = lock.newCondition();  public void print5(int totalLoopNumber)  {     lock.lock();     try      {       //1 判断       while(number != 1)       {          //A 就要停止          c1.await();       }       //2 干活       for (int i = 1; i &lt;=5; i++)        {          System.out.println(Thread.currentThread().getName()+&quot;\t&quot;+i+&quot;\t totalLoopNumber: &quot;+totalLoopNumber);       }       //3 通知       number = 2;       c2.signal();     } catch (Exception e) {       e.printStackTrace();     } finally {       lock.unlock();     }  }  public void print10(int totalLoopNumber)  {     lock.lock();     try      {       //1 判断       while(number != 2)       {          //A 就要停止          c2.await();       }       //2 干活       for (int i = 1; i &lt;=10; i++)        {          System.out.println(Thread.currentThread().getName()+&quot;\t&quot;+i+&quot;\t totalLoopNumber: &quot;+totalLoopNumber);       }       //3 通知       number = 3;       c3.signal();     } catch (Exception e) {       e.printStackTrace();     } finally {       lock.unlock();     }  }    public void print15(int totalLoopNumber)  {     lock.lock();     try      {       //1 判断       while(number != 3)       {          //A 就要停止          c3.await();       }       //2 干活       for (int i = 1; i &lt;=15; i++)        {          System.out.println(Thread.currentThread().getName()+&quot;\t&quot;+i+&quot;\t totalLoopNumber: &quot;+totalLoopNumber);       }       //3 通知       number = 1;       c1.signal();     } catch (Exception e) {       e.printStackTrace();     } finally {       lock.unlock();     }  }  }/** *  * @Description:  * 多线程之间按顺序调用，实现A-&gt;B-&gt;C * 三个线程启动，要求如下： *  * AA打印5次，BB打印10次，CC打印15次 * 接着 * AA打印5次，BB打印10次，CC打印15次 * ......来10轮   * */public class ThreadOrderAccess{  public static void main(String[] args)  {     ShareResource sr = new ShareResource();     new Thread(() -&gt; {       for (int i = 1; i &lt;=10; i++)        {          sr.print5(i);       }     }, &quot;AA&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;=10; i++)        {          sr.print10(i);       }     }, &quot;BB&quot;).start();     new Thread(() -&gt; {       for (int i = 1; i &lt;=10; i++)        {          sr.print15(i);       }     }, &quot;CC&quot;).start();     }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、线程间通信&quot;&gt;&lt;a href=&quot;#01、线程间通信&quot; class=&quot;headerlink&quot; title=&quot;01、线程间通信&quot;&gt;&lt;/a&gt;01、线程间通信&lt;/h1&gt;&lt;h2 id=&quot;1-1、面试题：两个线程打印&quot;&gt;&lt;a href=&quot;#1-1、面试题：两个线程打印&quot;
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>04Java8之lambda表达式和流式计算复习</title>
    <link href="http://lsqstudy.top/post/4ce22d9a.html"/>
    <id>http://lsqstudy.top/post/4ce22d9a.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.668Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、lambda表达式"><a href="#01、lambda表达式" class="headerlink" title="01、lambda表达式"></a>01、lambda表达式</h1><h2 id="1-1、什么是Lambda"><a href="#1-1、什么是Lambda" class="headerlink" title="1.1、什么是Lambda"></a>1.1、什么是Lambda</h2><p>Lambda 是一个匿名函数，可以写出更简洁、更灵活的代码。作为一种更紧凑的代码风格，使Java的语言表达能力得到了提升。</p><p>Lambda 表达式在Java 8中引入了一个新的语法元素和操作符。</p><ul><li>左侧：指定了 Lambda 表达式需要的所有参数</li><li>中间操作符为 “-&gt;” ， 该操作符被称为 Lambda操作符或剪头操作符</li><li>右侧：指定了 Lambda 体，即 Lambda 表达式要执行的功能</li><li></li></ul><blockquote><p>口决：拷贝小括号（），写死右箭头-&gt;，落地大括号{…}</p></blockquote><h2 id="1-2、使用要求"><a href="#1-2、使用要求" class="headerlink" title="1.2、使用要求"></a>1.2、使用要求</h2><ul><li>需要给接口加个注解：<code>@FunctionalInterface</code></li><li>必须是函数式接口，必须只有一个方法，如有两个方法，立刻报错。</li><li>如果接口只有一个方法java默认它为函数式接口。</li></ul><pre><code class="java">Foo foo = () -&gt; {System.out.println(&quot;****hello lambda&quot;);};</code></pre><h2 id="1-3、接口里是否能有实现方法？"><a href="#1-3、接口里是否能有实现方法？" class="headerlink" title="1.3、接口里是否能有实现方法？"></a>1.3、接口里是否能有实现方法？</h2><h3 id="1-3-1、default方法"><a href="#1-3-1、default方法" class="headerlink" title="1.3.1、default方法"></a>1.3.1、default方法</h3><p>接口里在java8后容许有接口的实现，default方法默认实现，接口里default方法可以有多个</p><pre><code class="java">default int div(int x,int y) {  return x/y; }</code></pre><h3 id="1-3-2、静态方法实现"><a href="#1-3-2、静态方法实现" class="headerlink" title="1.3.2、静态方法实现"></a>1.3.2、静态方法实现</h3><p> 静态方法实现：接口新增 ，可以有多个。</p><p>注意静态的叫类方法，不能用对像foo去调吗、要改成类名Foo</p><pre><code class="java">public static int sub(int x,int y){  return x-y;}</code></pre><h2 id="1-3、示例代码"><a href="#1-3、示例代码" class="headerlink" title="1.3、示例代码"></a>1.3、示例代码</h2><pre><code class="java">@FunctionalInterfaceinterface Foo{// public void sayHello() ;// public void say886() ; public int add(int x,int y); default int div(int x,int y) {   return x/y; } public static int sub(int x,int y) {   return x-y; }}/** *  * @Description: Lambda Express-----&gt; 函数式编程 * 1 拷贝小括号(形参列表)，写死右箭头 -&gt;，落地大括号 {方法实现} * 2 有且只有一个public方法@FunctionalInterface注解增强定义 * 3 default方法默认实现 * 4 静态方法实现 */public class LambdaDemo{ public static void main(String[] args) {//  Foo foo = new Foo() {//   @Override//   public void sayHello() {//     System.out.println(&quot;Hello!!&quot;);//   }////   @Override//   public void say886() {//     // TODO Auto-generated method stub//     //   }//  };//  foo.sayHello();//  System.out.println(&quot;============&quot;);//  foo = ()-&gt;{System.out.println(&quot;Hello!! lambda !!&quot;);};//  foo.sayHello(); Foo foo = (x,y)-&gt;{   System.out.println(&quot;Hello!! lambda !!&quot;);   return x+y;   };   int result = foo.add(3,5);   System.out.println(&quot;******result=&quot;+result);   System.out.println(&quot;******result div=&quot;+foo.div(10, 2));   System.out.println(&quot;******result sub=&quot;+Foo.sub(10, 2)); }} </code></pre><h1 id="02、流式计算"><a href="#02、流式计算" class="headerlink" title="02、流式计算"></a>02、流式计算</h1><h2 id="2-1、函数式接口"><a href="#2-1、函数式接口" class="headerlink" title="2.1、函数式接口"></a>2.1、函数式接口</h2><h3 id="2-1-1、java-util-function"><a href="#2-1-1、java-util-function" class="headerlink" title="2.1.1、java.util.function"></a>2.1.1、<code>java.util.function</code></h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194127.png" alt=""></p><h3 id="2-1-2、java内置核心四大函数式接口"><a href="#2-1-2、java内置核心四大函数式接口" class="headerlink" title="2.1.2、java内置核心四大函数式接口"></a>2.1.2、java内置核心四大函数式接口</h3><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194131.png" alt=""></p><h3 id="2-1-3、实例"><a href="#2-1-3、实例" class="headerlink" title="2.1.3、实例"></a>2.1.3、实例</h3><pre><code class="java">//R apply(T t);函数型接口，一个参数，一个返回值Function&lt;String,Integer&gt; function = t -&gt;{return t.length();};System.out.println(function.apply(&quot;abcd&quot;));//boolean test(T t);断定型接口，一个参数，返回booleanPredicate&lt;String&gt; predicate = t-&gt;{return t.startsWith(&quot;a&quot;);};System.out.println(predicate.test(&quot;a&quot;));// void accept(T t);消费型接口，一个参数，没有返回值Consumer&lt;String&gt; consumer = t-&gt;{    System.out.println(t);};consumer.accept(&quot;javaXXXX&quot;);//T get(); 供给型接口，无参数，有返回值Supplier&lt;String&gt; supplier =()-&gt;{return UUID.randomUUID().toString();};System.out.println(supplier.get());</code></pre><h2 id="2-2、Stream流"><a href="#2-2、Stream流" class="headerlink" title="2.2、Stream流"></a>2.2、Stream流</h2><h3 id="2-2-1、stream流是什么？"><a href="#2-2-1、stream流是什么？" class="headerlink" title="2.2.1、stream流是什么？"></a>2.2.1、stream流是什么？</h3><p>是数据渠道，用于操作数据源（集合、数组等）所生成的元素序列。</p><blockquote><p>一句话：集合讲的是数据，流讲的是计算！</p></blockquote><h3 id="2-2-2、stream流特点"><a href="#2-2-2、stream流特点" class="headerlink" title="2.2.2、stream流特点"></a>2.2.2、stream流特点</h3><pre><code>1. Stream 自己不会存储元素 2. Stream 不会改变源对象。相反，他们会返回一个持有结果的新Stream。 3. Stream 操作是延迟执行的。这意味着他们会等到需要结果的时候才执行。</code></pre><h3 id="2-2-3、怎么使用？"><a href="#2-2-3、怎么使用？" class="headerlink" title="2.2.3、怎么使用？"></a>2.2.3、怎么使用？</h3><blockquote><p>1、阶段</p></blockquote><ul><li>创建一个Stream：一个数据源（数组、集合）</li><li>中间操作：一个中间操作，处理数据源数据</li><li>终止操作：一个终止操作，执行中间操作链，产生结果</li></ul><blockquote><p>2、源头=&gt;中间流水线=&gt;结果</p></blockquote><pre><code class="java">import lombok.AllArgsConstructor;import lombok.Data;import lombok.NoArgsConstructor;import java.util.Arrays;import java.util.List;import java.util.UUID;import java.util.function.*;@Data@NoArgsConstructor@AllArgsConstructorclass User{    private Integer id;    private String  userName;    private int     age;}/** * @create 2019-02-26 22:24 * * 题目：请按照给出数据，找出同时满足 *      偶数ID且年龄大于24且用户名转为大写且用户名字母倒排序 *      最后只输出一个用户名字 */public class StreamDemo{    public static void main(String[] args)    {        User u1 = new User(11,&quot;a&quot;,23);        User u2 = new User(12,&quot;b&quot;,24);        User u3 = new User(13,&quot;c&quot;,22);        User u4 = new User(14,&quot;d&quot;,28);        User u5 = new User(16,&quot;e&quot;,26);        List&lt;User&gt; list = Arrays.asList(u1,u2,u3,u4,u5);        list.stream().filter(p -&gt; {            return p.getId() % 2 == 0;        }).filter(p -&gt; {            return p.getAge() &gt; 24;        }).map(f -&gt; {            return f.getUserName().toUpperCase();        }).sorted((o1, o2) -&gt; {            return o2.compareTo(o1);        }).limit(1).forEach(System.out::println);        //    R apply(T t);        Function&lt;String,Integer&gt; function = t -&gt; {return t.length();};        System.out.println(function.apply(&quot;abc&quot;));        // boolean test(T t);        Predicate&lt;String&gt; predicate = t -&gt; {return t.startsWith(&quot;a&quot;);};        System.out.println(predicate.test(&quot;a&quot;));        //void accept(T t);        Consumer&lt;String&gt; consumer = t -&gt; {System.out.println(t);};        consumer.accept(&quot;java1018&quot;);        //    T get();        Supplier&lt;String&gt; supplier =  () -&gt; {return UUID.randomUUID().toString();};        System.out.println(supplier.get());;    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、lambda表达式&quot;&gt;&lt;a href=&quot;#01、lambda表达式&quot; class=&quot;headerlink&quot; title=&quot;01、lambda表达式&quot;&gt;&lt;/a&gt;01、lambda表达式&lt;/h1&gt;&lt;h2 id=&quot;1-1、什么是Lambda&quot;&gt;&lt;a href=&quot;#
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>01Java常见基础知识点</title>
    <link href="http://lsqstudy.top/post/2535cae0.html"/>
    <id>http://lsqstudy.top/post/2535cae0.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.662Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、面向对象和面向过程的区别"><a href="#01、面向对象和面向过程的区别" class="headerlink" title="01、面向对象和面向过程的区别"></a>01、面向对象和面向过程的区别</h1><p>首先面向过程和面向对象的语言没有具体的性能高下之分,要依据每种语言的设计来做参考.<br>个人认为面向过程与面向对象的最大区别在于: <strong>面向过程的语言是结构化的,面向对象的语言是模块化的。</strong><br><strong>模块化的代码比结构化的代码更易于维护,复用与扩展。</strong></p><h1 id="02、OracleJdk与OpenJdk的区别"><a href="#02、OracleJdk与OpenJdk的区别" class="headerlink" title="02、OracleJdk与OpenJdk的区别"></a>02、OracleJdk与OpenJdk的区别</h1><p>OpenJdk是基于Sum捐赠的HotSpot的源代码开发的,是开源的。<br>OracleJdk是Oracle对Jdk的商业化版本,由Oracle发布并维护。<br>因此OracleJdk比OpenJdk更可靠(不过随着版本的迭代，二者之间的差异正在减少)。</p><h1 id="03、Java与C-的异同"><a href="#03、Java与C-的异同" class="headerlink" title="03、Java与C++的异同"></a>03、Java与C++的异同</h1><ul><li><p>Java和C++都是基于面向对象思想的语言。</p></li><li><p>Java不提供指针来访问内存。C++允许指针访问内存。</p></li><li><p>垃圾回收机制。Java无需开发者手动释放内存,因为Java有垃圾回收机制自动回收内存;<br>C++则需要开发者手动释放内存。因此Java在内存管理上相对C++更安全。</p></li><li><p>Java不支持多继承，而C++支持。</p></li></ul><h1 id="04、JVM、JDK和JRE的区别"><a href="#04、JVM、JDK和JRE的区别" class="headerlink" title="04、JVM、JDK和JRE的区别"></a>04、JVM、JDK和JRE的区别</h1><ul><li><p>JVM: JVM(java virtual machine)是java虚拟机</p></li><li><p>JRE: JRE(java runtime environment)是java运行时环境</p></li><li><p>JDK: JDK(java development kit)是java开发工具包,不仅包含了jre和jvm,还提供了javac编译器和javadoc等其他开发所需的工具</p></li></ul><h1 id="05、Java语言的特点"><a href="#05、Java语言的特点" class="headerlink" title="05、Java语言的特点"></a>05、Java语言的特点</h1><ul><li><p>面向对象</p></li><li><p>平台无关性,也就是跨平台(依靠JVM)</p></li><li><p>垃圾回收机制(GC)</p></li><li><p>支持多线程</p></li><li><p>支持便捷的网络编程</p></li><li><p>编译与解释(JIT)</p></li><li><p>安全(个人认为所有语言写出来的代码的安全性是开发者决定的,<br>而不是语言本身决定的,语言能决定的只是提供方便或不便的安全的API)</p></li></ul><h1 id="06、面向对象的特征"><a href="#06、面向对象的特征" class="headerlink" title="06、面向对象的特征"></a>06、面向对象的特征</h1><p>面向对象三大特征:<strong>封装,继承,多态</strong>。</p><ul><li><strong>封装</strong>: 封装是隐藏对象属性和实现细节,只对外提供可访问或修改的接口的技术。</li></ul><p><strong>封装的目的是为了简化编程和增加程序的安全性,使得使用者无需了解对象的具体实现细节。</strong></p><ul><li><strong>继承</strong>: 继承是 在已存在的类上定义新的类的技术。<br>在Java中,已存在的类被称为基类(父类),新的类叫做派生类(子类).子类拥有父类的所有属性,方法。<br>但是子类对于父类中私有的方法或属性只是拥有,并不能访问和使用。</li></ul><p><strong>继承的目的主要是为了代码的复用.</strong></p><ul><li><strong>多态:</strong> 多态指的是相同类型的对象,调用其相同的方法,参数也相同,但是它的表现形式也就是结果不同。</li></ul><p><strong>多态的目的是为了程序的可扩展性和维护性。</strong><br>在Java中可以使用继承与接口2大特性实现多态。</p><h1 id="07、重载和重写的区别"><a href="#07、重载和重写的区别" class="headerlink" title="07、重载和重写的区别"></a>07、重载和重写的区别</h1><ul><li><p>重载: 重载是描述一个类中有多个方法名相同的方法，但是它们的参数,类型,返回值,参数的顺序可能不同，表现形式也就不同。</p></li><li><p>重写: 重写是描述子类对父类的某个方法的逻辑进行了重新编写,但重写的只是方法的内容,<br>方法名,参数,类型,顺序,返回值都是不变的。</p></li></ul><h1 id="08、接口与抽象类的区别"><a href="#08、接口与抽象类的区别" class="headerlink" title="08、接口与抽象类的区别"></a>08、接口与抽象类的区别</h1><ul><li><p>接口需要被实现,而抽象类是需要被继承的。</p></li><li><p>接口里的方法都是公共抽象的，而抽象类既允许抽象也允许非抽象的方法(在jdk8中,接口被允许定义default方法,jdk9中还允许定义private私有方法)。</p></li><li><p>一个类允许实现多个接口,但只允许继承一个抽象父类。</p></li><li><p>接口是对类的规范,规范的是行为能力。而抽象类是对类的抽象,抽象的是逻辑。</p></li></ul><h1 id="09、Object类的方法有哪些"><a href="#09、Object类的方法有哪些" class="headerlink" title="09、Object类的方法有哪些?"></a>09、Object类的方法有哪些?</h1><ol><li>getClass</li><li>equals</li><li>hashCode</li><li>toString</li><li>wait</li><li>wait(long): 让当前Thread进入TIMED_WATING状态</li><li>wait(long,int):让当前Thread进入TIMED_WATING状态</li><li>notify</li><li>nofifyAll</li><li>clone</li><li>finalize</li></ol><h1 id="10、静态属性方法和成员属性方法区别"><a href="#10、静态属性方法和成员属性方法区别" class="headerlink" title="10、静态属性方法和成员属性方法区别"></a>10、静态属性方法和成员属性方法区别</h1><p><strong>静态属性和方法属于类Class,而成员属性和方法属于实例化的对象。</strong></p><p>静态方法只能使用静态方法和静态属性,不能使用成员属性和方法,<br>因为静态属性和方法在对象还没被实例化的时候就存在了。</p><p><strong>简单理解就是不允许一个已存在的事物使用一个不存在的事物。</strong></p><h1 id="11、子类属性与父类属性初始化顺序"><a href="#11、子类属性与父类属性初始化顺序" class="headerlink" title="11、子类属性与父类属性初始化顺序"></a>11、子类属性与父类属性初始化顺序</h1><ol><li>无论如何,静态数据首先加载,所以先初始化父类静态变量和静态初始化块(静态变量和静态初始化块按源码编写的顺序执行，<br>普通初始化块和普通成员变量也是如此),再初始化子类静态变量和静态初始化块。</li><li>普通初始化块和普通成员变量优先于构造方法,所以接下来加载父类的普通初始化块和普通成员变量,再调用父类构造方法。</li><li>调用子类普通代码块，普通成员变量和构造方法。</li></ol><h1 id="12、自动拆箱和装箱"><a href="#12、自动拆箱和装箱" class="headerlink" title="12、自动拆箱和装箱"></a>12、自动拆箱和装箱</h1><p>自动拆箱和装箱实际上是Java编译器的一个语法糖。</p><p>自动装箱是指: <strong>将基本数据类型转为对应的包装类对象的过程。</strong></p><p>自动拆箱是指: <strong>将包装类转为对应的基本数据类型。</strong></p><p><strong>自动装箱实际上是调用了包装类对象的valueof方法</strong>,如: Integer.valueof(1)</p><p><strong>自动拆箱实际上是调用了包装类的xxxValue方法</strong>,如: Integer.intValue()</p><p><strong>在自动装箱的时候,如果包装类允许缓存并且值在缓存的范围内,那么装箱生成的对象会被缓存到常量池中。</strong></p><p><strong>Integer,Byte,Short,Long,Character包装类型具有缓存池,<br>而其他三种:Float,Double,Boolean不具有缓存池。</strong></p><p>包装类的缓存池缓存的范围基本都为: -128 - 127之间，<br>除了Character的缓存范围为 0 - 127。</p><h1 id="13、String为什么不可变"><a href="#13、String为什么不可变" class="headerlink" title="13、String为什么不可变?"></a>13、String为什么不可变?</h1><p>String是Java中最常使用的类没有之一,如果String是可变的,那么会发生非常多数不清的问题。<br>如ip地址,人名,邮箱非常多的敏感数据。<br>如果String是可变的,就会发生安全问题,<br>且字符串常量池也就无从谈起了。</p><p>String是不可变的,那么它本质上也是线程安全的。<br><strong>不可变类的缺点就是每个不同的值需要创建一个对象</strong></p><p><strong>String 是用final修饰的，保证了String类不能被扩展。<br>String内部的字段是用final修饰的(jdk版本11,String由byte[]实现)，<br>并且没有对外提供修改字段的方法。这也是为什么String不可变的原理。</strong></p><h1 id="14、final关键字的作用"><a href="#14、final关键字的作用" class="headerlink" title="14、final关键字的作用"></a>14、final关键字的作用</h1><ul><li><p>被final修饰的类，不能被继承，并且这个类所有的成员方法都为final，不能被重写。</p></li><li><p>被final修饰的属性变量，不能被修改。如果该变量是基本数据类型的，那么其值在初始化后不能被修改。<br>如果该变量是引用类型的，那么该引用不能再指向其他对象。</p></li><li><p>被final修饰的方法不能被子类重写。</p></li></ul><h1 id="15、StringBuilder和StringBuffer区别"><a href="#15、StringBuilder和StringBuffer区别" class="headerlink" title="15、StringBuilder和StringBuffer区别"></a>15、StringBuilder和StringBuffer区别</h1><p><strong>StringBuilder和StringBuffer都是可变的字符串,但是StringBuilder是线程不安全的。</strong></p><p>StringBuffer是安全的,因此单线程情况下考虑使用StringBuilder,多线程情况下考虑使用StringBuffer。</p><p><strong>他们之间的关系就好比HashMap和HashTable的关系。</strong></p><h1 id="16、equals知识点"><a href="#16、equals知识点" class="headerlink" title="16、equals知识点"></a>16、equals知识点</h1><ul><li><p><strong>== 和 equals区别</strong>:</p><ul><li>==比较的是对象的内存地址,equals比较的是对象的值。<br>因此在Java中比较2个对象的值是否相等使用equals,判断2个对象是否是一个对象,使用==。</li></ul></li><li><p>hashCode方法返回的真是对象内存地址吗? 这个已在对象内存布局部分有讲解，此处就不重复写了。</p></li><li><p>equals方法重写要求</p><ul><li><p>自反性: x.equals(x) == true 永远成立</p></li><li><p>非空性: x.equals(null) == false 永远成立</p></li><li><p>对称性: 如果 x.equals(y) == true , 那 y.equals(x)== true  </p></li><li><p>传递性: 如果 x.equals(y) == true,并且 y.equals(z) == true,那么一定满足x.equals(z) == true</p></li><li><p>一致性: 如果x.equals(y) == true , 那么只要x和y的值不变,那么x.equals(y) == true 永远成立</p></li></ul></li><li><p>为什么重写equals方法一定要重写hashcode方法?</p><p>在普通环境下(不涉及hash表),equals方法和hashcode方法一毛钱关系没有的,<br>此时重写equals但不重写hashcode是没有关系的。<br>但<strong>当使用map,set这些散列表时,<br>它们会根据对象的hashcode来计算对象在散列表中的位置的。</strong><br>试想下,如果2个对象的值相等,但是由于它们是2个对象,hashcode却不相等。<br>那么即使放入map,set(map)仍会存在重复数据。</p></li></ul><h1 id="17、深拷贝与浅拷贝"><a href="#17、深拷贝与浅拷贝" class="headerlink" title="17、深拷贝与浅拷贝"></a>17、深拷贝与浅拷贝</h1><p><strong>深拷贝: 拷贝所有的内容,除了基本数据类型的变量复制一份,连引用类型的变量也复制一份。</strong></p><p><strong>浅拷贝: 复制基本数据类型的变量,对于引用类型的变量,直接返回这个引用本身。</strong></p><h1 id="18、IO流分类"><a href="#18、IO流分类" class="headerlink" title="18、IO流分类"></a>18、IO流分类</h1><ol><li>按照流的流向,分为:输入流和输入流。</li><li>按照操作单元,分为:字节流和字符流。</li></ol><h1 id="19、使用字节流还是字符流"><a href="#19、使用字节流还是字符流" class="headerlink" title="19、使用字节流还是字符流?"></a>19、使用字节流还是字符流?</h1><p>考虑通用性,应该使用字节流。<br>如果只是文本文件的操作,可以使用字符流。</p><h1 id="20、BigDecimal"><a href="#20、BigDecimal" class="headerlink" title="20、BigDecimal"></a>20、BigDecimal</h1><p>BigDecimal是Java中表示大浮点数的类型。</p><p>在Java中,如果遇到浮点数的判断,可以使用BigDecimal来做计算,<br>因为如果使用普通数据类型很可能会发生精度丢失的情况,这个时候的结果可能会出乎意料之外.</p><h1 id="21、Java异常体系结构"><a href="#21、Java异常体系结构" class="headerlink" title="21、Java异常体系结构"></a>21、Java异常体系结构</h1><p>在Java中,异常分为 Exception和Error,这2个类都继承自Throwable。</p><ul><li><p>Exception: Exception异常是程序本身可以处理的。Exception 分为运行时异常(RuntimeException)和<br>非运行时异常(CheckedException)。</p><ul><li><p>RuntimeException: RuntimeException(运行时异常)是在程序运行时可能会发生的异常,如NullPointException,<br>这类异常往往是不可预料的,编译器也不会要求你手动try catch或throws。</p></li><li><p>CheckedException: CheckedException(非运行时异常)是RuntimeException以外的异常,如IOException，<br>这类异常要求必须显示的try catch或throws ， 如果不处理,那么编译就不会通过。</p></li></ul></li><li><p>Error: Error错误是程序无法处理的,表示程序或JVM出现了很严重的，无法解决的问题。</p></li></ul><h1 id="22、Comparable和Comparator"><a href="#22、Comparable和Comparator" class="headerlink" title="22、Comparable和Comparator"></a>22、Comparable和Comparator</h1><ul><li><p>Comparable: 自然排序接口。实现了它的类意味着就支持排序。</p></li><li><p>Comparator: 外部比较器。无需让需要排序的对象实现排序逻辑，而是根据Comparator定义的逻辑来排序。<br>Comparator相较于Comparable更加的灵活。</p></li></ul><h1 id="23、什么是泛型-什么是类型擦除"><a href="#23、什么是泛型-什么是类型擦除" class="headerlink" title="23、什么是泛型,什么是类型擦除?"></a>23、什么是泛型,什么是类型擦除?</h1><p>Java泛型(Generics) 是JDK5中引入的一个新特性，泛型提供了编译时类型安全检测机制，<br>该机制允许程序员在编译时检测到非法的类型。<br>泛型的本质是参数化类型，也就是说所操作的数据类型被指定为一个参数。</p><p>Java中的<strong>泛型</strong>是<strong>伪泛型</strong>，在Java编译期间，所有的泛型信息都会被擦除，这就是通常所说的类型擦除。</p><pre><code class="text">List&lt;Integer&gt; list = new ArrayList&lt;&gt;();list.add(12);//这里直接添加会报错list.add(&quot;a&quot;);Class&lt;? extends List&gt; clazz = list.getClass();Method add = clazz.getDeclaredMethod(&quot;add&quot;, Object.class);//但是通过反射添加，是可以的add.invoke(list, &quot;kl&quot;);System.out.println(list)</code></pre><h1 id="24、泛型通配符"><a href="#24、泛型通配符" class="headerlink" title="24、泛型通配符"></a>24、泛型通配符</h1><p><strong>常用的 T，E，K，V，?</strong></p><ul><li>？ 表示不确定的 java 类型</li><li>T (type) 表示具体的一个java类型</li><li>K V (key value) 分别代表java键值中的Key Value</li><li>E (element) 代表Element</li></ul><h1 id="25、-无界通配符"><a href="#25、-无界通配符" class="headerlink" title="25、? 无界通配符"></a>25、? 无界通配符</h1><p>一个抽象父类Animal和子类Dog，现在需要一个动物列表，我的第一反应是这样的：</p><pre><code class="text">List&lt;Animal&gt; listAnimals</code></pre><p>但是老板的想法却是这样的：</p><pre><code class="text">List&lt;? extends Animal&gt; listAnimals</code></pre><p>通配符在声明局部变量时是没有什么意义的，但是当你为一个方法设置声明一个参数时，它是非常重要的。</p><pre><code class="text">   public static void main(String[] args) {         List&lt;Dog&gt; dogs = new ArrayList&lt;&gt;();         countLegs(dogs);         /*编译器会会报错，显示类型不匹配*/         //countLegs1(dogs);     }     static int countLegs(List&lt;? extends Animal&gt; animals){         int reVal = 0;         for (Animal animal : animals) {             reVal+= animal.getLegs();         }         return reVal;     }     static int countLegs1(List&lt;Animal&gt; animals){         int reVal = 0;         for (Animal animal : animals) {             reVal+= animal.getLegs();         }         return reVal;     }</code></pre><p>所以，对于不确定或者不关心实际要操作的类型，可以使用无限制通配符(<strong>&lt;?&gt;</strong>)，表示可以持有任意类型。像countLegs方法，规定了参数传入的上界，但是并不关心具体类型是什么，对于传入的参数，只要是<code>Animal</code>的子类，就都可以支持，而countLegs1方法不行。</p><p>为什么countLegs1方法就不行呢？<code>Dog</code>不是<code>Animal</code>的子类吗，根据多态的角度来讲，理论上应该是可以的，但是，在泛型的继承体系中，<code>Dog</code>并不算<code>Animal</code>的一个子类。</p><p>假设我们有以下代码：</p><p>animals的泛型是父类对象<code>Animal</code>，dogs的泛型是子类对象<code>Dog</code>，那么dogs转换animals能成功吗，我们知道子类对象转父类对象是可以的，但是子类泛型转父类泛型能成功吗，我们假设能成功。</p><pre><code class="text">  public static void main(String[] args) {         List&lt;Animal&gt; animals = new ArrayList&lt;Animal&gt;();         List&lt;Dog&gt; dogs = new ArrayList&lt;Dog&gt;();         animals = dogs;     }</code></pre><p>那么animals就指向了一个泛型为<code>Dog</code>的集合容器，但是现在这个集合容器的泛型是<code>Animal</code>，看上去可以将另一个子类<code>Cat</code>加进容器中。</p><pre><code class="text"> public static void main(String[] args) {        List&lt;Animal&gt; animals = new ArrayList&lt;Animal&gt;();        List&lt;Dog&gt; dogs = new ArrayList&lt;Dog&gt;();        animals = dogs;        animals.add(new Cat(4));    }</code></pre><p>这段代码看上去好像没什么问题，但是仔细想想。现在animals指向的是一个泛型为Dog的容器，现在又将Cat放进容器中，就相当于在一堆狗里面放进一只猫，这就矛盾了。所以子类泛型<strong>不能</strong>转换为父类泛型，反过来也是一样。</p><h1 id="26、上界通配符-lt-extend-E-gt"><a href="#26、上界通配符-lt-extend-E-gt" class="headerlink" title="26、上界通配符 &lt;? extend E&gt;"></a>26、上界通配符 &lt;? extend E&gt;</h1><p>上界：用extend关键字指定，表示所指定的类型只能是某个类的子类或者这个类本身</p><pre><code class="text"> static &lt;K extends Comparable,E extends Serializable&gt; K test(K arg1,E arg2){        K result = arg1;        arg1.compareTo(arg2);        return result;    }</code></pre><pre><code class="text"> // 表示既要实现Comparable接口，又要实现Serializable接口static &lt;K extends Comparable &amp; Serializable&gt; K test(K arg1) {        K result = arg1;        return result;    }</code></pre><p>有多个限定类型用”&amp;”隔开，有多个类型变量用”,”逗号隔开</p><h1 id="27、下界通配符-lt-super-E-gt"><a href="#27、下界通配符-lt-super-E-gt" class="headerlink" title="27、下界通配符 &lt;? super E&gt;"></a>27、下界通配符 &lt;? super E&gt;</h1><p>下界：用super关键字指定，表示所指定的类型只能是这个类本身或者某个类的父类，直至Object</p><pre><code class="text">static &lt;T&gt; void  test1(List&lt;? super  T&gt; dst ,List&lt;T&gt; list){        for (T t : list) {            dst.add(t);        }    }</code></pre><h1 id="28、-和T的区别"><a href="#28、-和T的区别" class="headerlink" title="28、?和T的区别"></a>28、?和T的区别</h1><p>? 表示不确定的类型，常用于泛型方法的调用代码和形参，不能用于定义泛型类、泛型方法和泛型变量。</p><p>T 表示一个具体的类型，常用于泛型类和泛型方法的定义，可以定义泛型变量。</p><p><strong>利用T来保证泛型的一致性。</strong></p><pre><code class="text">// 通过 T 来 确保 泛型参数的一致性,通常我们需要先声明public &lt;T extends Number&gt; voidtest(List&lt;T&gt; dest, List&lt;T&gt; src)//通配符是 不确定的，所以这个方法不能保证两个 List 具有相同的元素类型，不需要提前声明public voidtest(List&lt;? extends Number&gt; dest, List&lt;? extends Number&gt; src)</code></pre><p><strong>类型参数可以多重限定而通配符不行。</strong></p><pre><code class="text"> static &lt;T extends Comparable &amp; Serializable&gt; T test(T arg1) {        T result = arg1;        return result;    }</code></pre><p><strong>?可以使用下界限定，但T不行。</strong></p><p>类型参数 T 只具有 一种 类型限定方式：</p><pre><code class="text">T extends A</code></pre><p>但是通配符 ? 可以进行 两种限定：</p><pre><code class="text">? extends A? super A</code></pre><h1 id="29、为什么要慎用-Arrays-asList"><a href="#29、为什么要慎用-Arrays-asList" class="headerlink" title="29、为什么要慎用 Arrays.asList()?"></a>29、为什么要慎用 Arrays.asList()?</h1><p><strong>因为Arrays.asList这个方法返回的根本就不是我们期盼的ArrayList,<br>而是Arrays类内部实现的ArrayList,这个内部类只支持访问和set操作,<br>并不支持remove,add,clear等修改操作。</strong></p><h1 id="30、Java中引用的类型"><a href="#30、Java中引用的类型" class="headerlink" title="30、Java中引用的类型"></a>30、Java中引用的类型</h1><p>Java中引用类型总共有四种: <strong>强引用，软引用，弱引用，虚引用</strong>。</p><ul><li><p><strong>强引用(Strong Reference)</strong>: Java程序中绝大部分都是强引用，一般使用new关键字创建的对象就是强引用。<br>只要强引用存在，强引用的对象就不会被回收，除非不可达(参考jvm部分)</p></li><li><p>软引用(Soft Reference): 软引用一般不会被回收，但是当堆内存不够的时候，<br>比如几乎快要发生OOM的时候，就会回收掉软引用对象。</p></li><li><p>弱引用(Weak Reference): 只要垃圾回收开始，就会回收掉弱引用的对象。</p></li><li><p>虚引用(Phantom Reference,又称幽灵引用): 和其他几种引用不同，虚引用不决定对象的生命周期，<br>它在任何时候都可能被回收掉。</p></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、面向对象和面向过程的区别&quot;&gt;&lt;a href=&quot;#01、面向对象和面向过程的区别&quot; class=&quot;headerlink&quot; title=&quot;01、面向对象和面向过程的区别&quot;&gt;&lt;/a&gt;01、面向对象和面向过程的区别&lt;/h1&gt;&lt;p&gt;首先面向过程和面向对象的语言没有具体
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="Java基础" scheme="http://lsqstudy.top/categories/Java/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java基础" scheme="http://lsqstudy.top/tags/Java%E5%9F%BA%E7%A1%80/"/>
    
  </entry>
  
  <entry>
    <title>07集合类是不安全的</title>
    <link href="http://lsqstudy.top/post/6d98264e.html"/>
    <id>http://lsqstudy.top/post/6d98264e.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:35:06.172Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、证明集合不安全"><a href="#01、证明集合不安全" class="headerlink" title="01、证明集合不安全"></a>01、证明集合不安全</h1><h2 id="1-1、线程不安全错误"><a href="#1-1、线程不安全错误" class="headerlink" title="1.1、线程不安全错误"></a>1.1、线程不安全错误</h2><p>异常：<code>java.util.ConcurrentModificationException</code></p><p>ArrayList在迭代的时候，如果同时对其进行修改就会抛出<code>java.util.ConcurrentModificationException</code>异常<br>并发修改异常</p><h2 id="1-2、原理"><a href="#1-2、原理" class="headerlink" title="1.2、原理"></a>1.2、原理</h2><blockquote><p>1、代码</p></blockquote><pre><code class="java">List&lt;String&gt; list = new ArrayList&lt;&gt;();for (int i = 0; i &lt;30 ; i++) {            new Thread(()-&gt;{                list.add(UUID.randomUUID().toString().substring(0,8));                System.out.println(list);            },String.valueOf(i)).start();        }</code></pre><blockquote><p>2、看ArrayList的源码</p></blockquote><p>没有<code>synchronized</code>线程不安全</p><pre><code class="java">public boolean add(E e) {    ensureCapacityInternal(size + 1);  // Increments modCount!!    elementData[size++] = e;    return true;}</code></pre><h1 id="02、解决方案"><a href="#02、解决方案" class="headerlink" title="02、解决方案"></a>02、解决方案</h1><h2 id="2-1、Vector"><a href="#2-1、Vector" class="headerlink" title="2.1、Vector"></a>2.1、Vector</h2><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194204.png" alt=""></p><blockquote><p>1、用法</p></blockquote><pre><code class="java">List&lt;String&gt; list = new Vector&lt;&gt;();</code></pre><blockquote><p>2、看Vector的源码</p></blockquote><p>有synchronized线程安全</p><pre><code class="java">public synchronized boolean add(E e) {    modCount++;    ensureCapacityHelper(elementCount + 1);    elementData[elementCount++] = e;    return true;}</code></pre><h2 id="2-2、Collections"><a href="#2-2、Collections" class="headerlink" title="2.2、Collections"></a>2.2、Collections</h2><blockquote><p>1、用法</p></blockquote><pre><code class="java">List&lt;String&gt; list = Collections.synchronizedList(new ArrayList&lt;&gt;());</code></pre><blockquote><p>2、说明</p></blockquote><p><code>Collections</code>提供了方法<code>synchronizedList</code>保证list是同步线程安全的</p><p>那HashMap，HashSet是线程安全的吗？也不是所以有同样的线程安全方法</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194207.png" alt=""></p><h2 id="2-3、【重点】写时复制"><a href="#2-3、【重点】写时复制" class="headerlink" title="2.3、【重点】写时复制"></a>2.3、【重点】写时复制</h2><blockquote><p>1、用法</p></blockquote><pre><code class="java">List&lt;String&gt; list = new CopyOnWriteArrayList&lt;&gt;();</code></pre><blockquote><p>2、说明</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194211.png" alt=""></p><h1 id="03、【重点】写时复制"><a href="#03、【重点】写时复制" class="headerlink" title="03、【重点】写时复制"></a>03、【重点】写时复制</h1><p>不加锁性能提升出错误，加锁数据一致性能下降</p><h2 id="3-1、CopyOnWriteArrayList定义"><a href="#3-1、CopyOnWriteArrayList定义" class="headerlink" title="3.1、CopyOnWriteArrayList定义"></a>3.1、<code>CopyOnWriteArrayList</code>定义</h2><p>CopyOnWriteArrayList是arraylist的一种线程安全变体，其中所有可变操作（add、set等）都是通过生成底层数组的新副本来实现的。</p><h2 id="3-2、举例：名单签到"><a href="#3-2、举例：名单签到" class="headerlink" title="3.2、举例：名单签到"></a>3.2、举例：名单签到</h2><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194214.png" alt=""></p><h2 id="3-3、CopyOnWrite理论"><a href="#3-3、CopyOnWrite理论" class="headerlink" title="3.3、CopyOnWrite理论"></a>3.3、CopyOnWrite理论</h2><p>CopyOnWrite容器即写时复制的容器。往一个容器添加元素的时候，不直接往当前容器Object[]添加，<br>而是先将当前容器Object[]进行Copy，复制出一个新的容器Object[] newElements，然后向新的容器Object[] newElements里添加元素。<br>添加元素后，再将原容器的引用指向新的容器setArray(newElements)。<br>这样做的好处是可以对CopyOnWrite容器进行并发的读，而不需要加锁，因为当前容器不会添加任何元素。<br>所以CopyOnWrite容器也是一种读写分离的思想，读和写不同的容器。</p><blockquote><p>源码：</p></blockquote><pre><code class="java">/** * Appends the specified element to the end of this list. * * @param e element to be appended to this list * @return {@code true} (as specified by {@link Collection#add}) */public boolean add(E e) {    final ReentrantLock lock = this.lock;    lock.lock();    try {        Object[] elements = getArray();        int len = elements.length;        Object[] newElements = Arrays.copyOf(elements, len + 1);//这里复制了        newElements[len] = e;        setArray(newElements);        return true;    } finally {        lock.unlock();    }}</code></pre><h2 id="3-4、扩展类比"><a href="#3-4、扩展类比" class="headerlink" title="3.4、扩展类比"></a>3.4、扩展类比</h2><h3 id="3-4-1、HashSet"><a href="#3-4-1、HashSet" class="headerlink" title="3.4.1、HashSet"></a>3.4.1、HashSet</h3><pre><code class="java">Set&lt;String&gt; set = new HashSet&lt;&gt;();//线程不安全Set&lt;String&gt; set = new CopyOnWriteArraySet&lt;&gt;();//线程安全</code></pre><blockquote><p>HashSet底层数据结构是什么？</p><p>HashMap  ?</p><p>但HashSet的add是放一个值，而HashMap是放K、V键值对</p></blockquote><p><strong>答案：</strong>HashMap  ，key是我们传进去的值，value是Object的实例变量</p><p><strong>源码：</strong></p><pre><code class="java">public HashSet() {    map = new HashMap&lt;&gt;();}private static final Object PRESENT = new Object();public boolean add(E e) {    return map.put(e, PRESENT)==null;}</code></pre><h3 id="2-4-2、HashMap"><a href="#2-4-2、HashMap" class="headerlink" title="2.4.2、HashMap"></a>2.4.2、HashMap</h3><pre><code class="java">Map&lt;String,String&gt; map = new HashMap&lt;&gt;();//线程不安全Map&lt;String,String&gt; map = new ConcurrentHashMap&lt;&gt;();//线程安全</code></pre><h2 id="3-5、代码"><a href="#3-5、代码" class="headerlink" title="3.5、代码"></a>3.5、代码</h2><pre><code class="java">import java.util.*;import java.util.concurrent.ConcurrentHashMap;import java.util.concurrent.CopyOnWriteArrayList;import java.util.concurrent.CopyOnWriteArraySet;public class NotSafeDemo {    public static void main(String[] args) {        Map&lt;String,String&gt; map = new ConcurrentHashMap&lt;&gt;();        for (int i = 0; i &lt;30 ; i++) {            new Thread(()-&gt;{                map.put(Thread.currentThread().getName(),UUID.randomUUID().toString().substring(0,8));                System.out.println(map);            },String.valueOf(i)).start();        }    }/** * 请举例说明集合类是不安全的 */    private static void setNoSafe() {        Set&lt;String&gt; set = new CopyOnWriteArraySet&lt;&gt;();        for (int i = 0; i &lt;30 ; i++) {            new Thread(()-&gt;{                set.add(UUID.randomUUID().toString().substring(0,8));                System.out.println(set);            },String.valueOf(i)).start();        }    }    private static void listNoSafe() {        //        List&lt;String&gt; list = Arrays.asList(&quot;a&quot;,&quot;b&quot;,&quot;c&quot;);        //        list.forEach(System.out::println);        //写时复制        List&lt;String&gt; list = new CopyOnWriteArrayList&lt;&gt;();        // new CopyOnWriteArrayList&lt;&gt;();        //Collections.synchronizedList(new ArrayList&lt;&gt;());        //new Vector&lt;&gt;();//new ArrayList&lt;&gt;();        for (int i = 0; i &lt;30 ; i++) {                    new Thread(()-&gt;{                        list.add(UUID.randomUUID().toString().substring(0,8));                        System.out.println(list);                    },String.valueOf(i)).start();                }    }}    /**     * 写时复制     CopyOnWrite容器即写时复制的容器。往一个容器添加元素的时候，不直接往当前容器Object[]添加，     而是先将当前容器Object[]进行Copy，复制出一个新的容器Object[] newElements，然后向新的容器Object[] newElements里添加元素。     添加元素后，再将原容器的引用指向新的容器setArray(newElements)。     这样做的好处是可以对CopyOnWrite容器进行并发的读，而不需要加锁，因为当前容器不会添加任何元素。     所以CopyOnWrite容器也是一种读写分离的思想，读和写不同的容器。    public boolean add(E e) {        final ReentrantLock lock = this.lock;        lock.lock();        try {            Object[] elements = getArray();            int len = elements.length;            Object[] newElements = Arrays.copyOf(elements, len + 1);            newElements[len] = e;            setArray(newElements);            return true;        } finally {            lock.unlock();        }    }     */</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、证明集合不安全&quot;&gt;&lt;a href=&quot;#01、证明集合不安全&quot; class=&quot;headerlink&quot; title=&quot;01、证明集合不安全&quot;&gt;&lt;/a&gt;01、证明集合不安全&lt;/h1&gt;&lt;h2 id=&quot;1-1、线程不安全错误&quot;&gt;&lt;a href=&quot;#1-1、线程不安全错
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>02Lock接口</title>
    <link href="http://lsqstudy.top/post/27e06f6c.html"/>
    <id>http://lsqstudy.top/post/27e06f6c.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.666Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、复习Synchronized"><a href="#01、复习Synchronized" class="headerlink" title="01、复习Synchronized"></a>01、复习Synchronized</h1><h2 id="1-1、多线程编程模板上"><a href="#1-1、多线程编程模板上" class="headerlink" title="1.1、多线程编程模板上"></a>1.1、多线程编程模板上</h2><p>1、线程 操作 资源类</p><p>2、高内聚低耦合</p><blockquote><p>例子：卖票程序<code>SaleTicket.java</code></p></blockquote><h1 id="02、Lock"><a href="#02、Lock" class="headerlink" title="02、Lock"></a>02、Lock</h1><h2 id="1-1、是什么？"><a href="#1-1、是什么？" class="headerlink" title="1.1、是什么？"></a>1.1、是什么？</h2><p>锁实现提供了比使用同步方法和语句可以获得的更广泛的锁操作。它们允许更灵活的结构，可能具有非常不同的属性，并且可能支持多个关联的条件对象。</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713193957.png" alt=""></p><h2 id="1-2、ReentrantLock可重入锁"><a href="#1-2、ReentrantLock可重入锁" class="headerlink" title="1.2、ReentrantLock可重入锁"></a>1.2、ReentrantLock可重入锁</h2><blockquote><p>如何使用</p></blockquote><pre><code class="java">class X {   private final ReentrantLock lock = new ReentrantLock();   // ...   public void m() {     lock.lock();  // block until condition holds     try {       // ... method body     } finally {       lock.unlock()     }   } }</code></pre><blockquote><p>synchronized与Lock的区别 </p></blockquote><ol><li>首先synchronized是java内置关键字，在jvm层面，Lock是个java类；</li><li>synchronized无法判断是否获取锁的状态，Lock可以判断是否获取到锁；</li><li>synchronized会自动释放锁，Lock手工释放锁(a 线程执行完同步代码会释放锁 ；b 线程执行过程中发生异常会释放锁)，Lock需在finally中手工释放锁（unlock()方法释放锁），否则容易造成线程死锁；</li><li>用synchronized关键字的两个线程1和线程2，如果当前线程1获得锁，线程2线程等待。如果线程1阻塞，线程2则会一直等待下去，而Lock锁就不一定会等待下去，如果尝试获取不到锁，线程可以不用一直等待就结束了；</li><li>synchronized的锁可重入、不可中断、非公平，而Lock锁可重入、可判断、可公平（两者皆可）</li><li>Lock锁适合大量同步的代码的同步问题，synchronized锁适合代码少量的同步问题。</li></ol>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、复习Synchronized&quot;&gt;&lt;a href=&quot;#01、复习Synchronized&quot; class=&quot;headerlink&quot; title=&quot;01、复习Synchronized&quot;&gt;&lt;/a&gt;01、复习Synchronized&lt;/h1&gt;&lt;h2 id=&quot;1-1、多
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>03获得线程的4种方式.md</title>
    <link href="http://lsqstudy.top/post/b3ad3f7c.html"/>
    <id>http://lsqstudy.top/post/b3ad3f7c.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.667Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、继承Thread类"><a href="#01、继承Thread类" class="headerlink" title="01、继承Thread类"></a>01、继承Thread类</h1><blockquote><p>不建议使用：java是单继承，资源宝贵，要用接口方式</p></blockquote><pre><code class="java">public class SaleTicket extends Thread...... SaleTicket t1 = new SaleTicket();t1.start();......</code></pre><h1 id="02、实现Runnable接口"><a href="#02、实现Runnable接口" class="headerlink" title="02、实现Runnable接口"></a>02、实现Runnable接口</h1><blockquote><p>1、不建议：这种方法会新增类，有更新更好的方法</p></blockquote><pre><code class="java">class MyThread implements Runnable//新建类实现runnable接口new Thread(new MyThread,...)</code></pre><blockquote><p>2、 这种方法不需要创建新的类，可以new接口</p></blockquote><pre><code class="java">new Thread(new Runnable() {    @Override    public void run() {    }   }, &quot;your thread name&quot;).start();</code></pre><blockquote><p>3、lambda表达式：  这种方法代码更简洁精炼</p></blockquote><pre><code class="java">new Thread(() -&gt; { }, &quot;your thread name&quot;).start();</code></pre><h1 id="03、实现Callable接口"><a href="#03、实现Callable接口" class="headerlink" title="03、实现Callable接口"></a>03、实现Callable接口</h1><h2 id="3-1、是什么"><a href="#3-1、是什么" class="headerlink" title="3.1、是什么"></a>3.1、是什么</h2><blockquote><p>这是一个函数式接口，因此可以用作lambda表达式或方法引用的赋值对象。</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194006.png" alt=""></p><h2 id="3-2、与runnable对比"><a href="#3-2、与runnable对比" class="headerlink" title="3.2、与runnable对比"></a>3.2、与runnable对比</h2><ol><li>是否有返回值</li><li>是否抛异常</li><li>落地方法不一样，一个是run，一个是call</li></ol><pre><code class="java">//创建新类MyThread实现runnable接口class MyThread implements Runnable{ @Override public void run() { }}//新类MyThread2实现callable接口class MyThread2 implements Callable&lt;Integer&gt;{ @Override public Integer call() throws Exception {  return 200; } }</code></pre><h2 id="3-3、怎么用"><a href="#3-3、怎么用" class="headerlink" title="3.3、怎么用"></a>3.3、怎么用</h2><blockquote><p>1、直接替换runnable是否可行：不可行，因为：thread类的构造方法根本没有Callable</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194017.png" alt=""></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194027.png" alt=""></p><blockquote><p>解决方法：找中间类</p></blockquote><pre><code class="java">FutureTask&lt;Integer&gt; ft = new FutureTask&lt;Integer&gt;(new MyThread());new Thread(ft, &quot;AA&quot;).start();</code></pre><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194032.png" alt=""></p><blockquote><p>3、运行成功后如何获得返回值？答案：<code>ft.get();</code></p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194037.png" alt=""></p><h2 id="3-4、FutureTask"><a href="#3-4、FutureTask" class="headerlink" title="3.4、FutureTask"></a>3.4、FutureTask</h2><h3 id="3-4-1、是什么？"><a href="#3-4-1、是什么？" class="headerlink" title="3.4.1、是什么？"></a>3.4.1、是什么？</h3><p>未来的任务，用它就干一件事，异步调用<br>main方法就像一个冰糖葫芦，一个个方法由main串起来。<br>但解决不了一个问题：正常调用挂起堵塞问题</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194040.png" alt=""></p><blockquote><p>例子</p></blockquote><p>（1）老师上着课，口渴了，去买水不合适，讲课线程继续，我可以单起个线程找班长帮忙买水，<br>水买回来了放桌上，我需要的时候再去get。<br>（2）4个同学，A算1+20,B算21+30,C算31*到40,D算41+50，是不是C的计算量有点大啊，<br>FutureTask单起个线程给C计算，我先汇总ABD，最后等C计算完了再汇总C，拿到最终结果<br>（3）高考：会做的先做，不会的放在后面做</p><h3 id="3-4-2、原理"><a href="#3-4-2、原理" class="headerlink" title="3.4.2、原理"></a>3.4.2、原理</h3><p>在主线程中需要执行比较耗时的操作时，但又不想阻塞主线程时，可以把这些作业交给Future对象在后台完成，<br>当主线程将来需要时，就可以通过Future对象获得后台作业的计算结果或者执行状态。</p><p>一般FutureTask多用于耗时的计算，主线程可以在完成自己的任务后，再去获取结果。</p><p>仅在计算完成时才能检索结果；如果计算尚未完成，则阻塞 get 方法。一旦计算完成，<br>就不能再重新开始或取消计算。get方法而获取结果只有在计算完成时获取，否则会一直阻塞直到任务转入完成状态，<br>然后会返回结果或者抛出异常。 </p><p>只计算一次</p><blockquote><p>注意：get方法放到最后</p></blockquote><h3 id="3-4-3、代码"><a href="#3-4-3、代码" class="headerlink" title="3.4.3、代码"></a>3.4.3、代码</h3><pre><code class="java">import java.util.concurrent.Callable;import java.util.concurrent.FutureTask;import java.util.concurrent.TimeUnit;class MyThread implements Runnable{    @Override    public void run() {    }}class MyThread2 implements Callable&lt;Integer&gt;{    @Override    public Integer call() throws Exception {        System.out.println(Thread.currentThread().getName()+&quot;come in callable&quot;);        return 200;    }}public class CallableDemo {    public static void main(String[] args) throws Exception {        //FutureTask&lt;Integer&gt; futureTask = new FutureTask(new MyThread2());        FutureTask&lt;Integer&gt; futureTask = new FutureTask(()-&gt;{            System.out.println(Thread.currentThread().getName()+&quot;  come in callable&quot;);            TimeUnit.SECONDS.sleep(4);            return 1024;        });        FutureTask&lt;Integer&gt; futureTask2 = new FutureTask(()-&gt;{            System.out.println(Thread.currentThread().getName()+&quot;  come in callable&quot;);            TimeUnit.SECONDS.sleep(4);            return 2048;        });        new Thread(futureTask,&quot;zhang3&quot;).start();        new Thread(futureTask2,&quot;li4&quot;).start();        //System.out.println(futureTask.get());        //System.out.println(futureTask2.get());        //1、一般放在程序后面，直接获取结果        //2、只会计算结果一次        while(!futureTask.isDone()){            System.out.println(&quot;***wait&quot;);        }        System.out.println(futureTask.get());        System.out.println(Thread.currentThread().getName()+&quot; come over&quot;);    }}/** *  * 在主线程中需要执行比较耗时的操作时，但又不想阻塞主线程时，可以把这些作业交给Future对象在后台完成，当主线程将来需要时，就可以通过Future对象获得后台作业的计算结果或者执行状态。一般FutureTask多用于耗时的计算，主线程可以在完成自己的任务后，再去获取结果。仅在计算完成时才能检索结果；如果计算尚未完成，则阻塞 get 方法。一旦计算完成，就不能再重新开始或取消计算。get方法而获取结果只有在计算完成时获取，否则会一直阻塞直到任务转入完成状态，然后会返回结果或者抛出异常。 只计算一次get方法放到最后 */</code></pre><h1 id="04、ThreadPool线程池"><a href="#04、ThreadPool线程池" class="headerlink" title="04、ThreadPool线程池"></a>04、ThreadPool线程池</h1><h2 id="4-1、为什么用线程池"><a href="#4-1、为什么用线程池" class="headerlink" title="4.1、为什么用线程池"></a>4.1、为什么用线程池</h2><blockquote><p>1、例子</p></blockquote><p>10年前单核CPU电脑，假的多线程，像马戏团小丑玩多个球，CPU需要来回切换。<br>现在是多核电脑，多个线程各自跑在独立的CPU上，不用切换效率高。</p><blockquote><p>2、线程池的优势</p></blockquote><p>线程池做的工作只要是控制运行的线程数量，处理过程中将任务放入队列，然后在线程创建后启动这些任务，如果线程数量超过了最大数量，超出数量的线程排队等候，等其他线程执行完毕，再从队列中取出任务来执行。</p><blockquote><p>3、主要特点为：线程复用;控制最大并发数;管理线程。</p></blockquote><ul><li>降低资源消耗。通过重复利用已创建的线程降低线程创建和销毁造成的销耗。</li><li>提高响应速度。当任务到达时，任务可以不需要等待线程创建就能立即执行。</li><li>提高线程的可管理性。线程是稀缺资源，如果无限制的创建，不仅会销耗系统资源，还会降低系统的稳定性，使用线程池可以进行统一的分配，调优和监控。</li></ul><h2 id="4-2、线程池如何使用"><a href="#4-2、线程池如何使用" class="headerlink" title="4.2、线程池如何使用"></a>4.2、线程池如何使用</h2><h3 id="4-2-1、架构说明"><a href="#4-2-1、架构说明" class="headerlink" title="4.2.1、架构说明"></a>4.2.1、架构说明</h3><p>Java中的线程池是通过Executor框架实现的，该框架中用到了Executor，Executors，ExecutorService，ThreadPoolExecutor这几个类</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194047.png" alt=""></p><h3 id="4-2-2、代码实现"><a href="#4-2-2、代码实现" class="headerlink" title="4.2.2、代码实现"></a>4.2.2、代码实现</h3><p>代码：</p><pre><code class="java">import java.util.Arrays;import java.util.List;import java.util.concurrent.Executor;import java.util.concurrent.ExecutorService;import java.util.concurrent.Executors;/** * 线程池 * Arrays * Collections * Executors */public class MyThreadPoolDemo {    public static void main(String[] args) {        //List list = new ArrayList();        //List list = Arrays.asList(&quot;a&quot;,&quot;b&quot;);        //固定数的线程池，一池五线程//       ExecutorService threadPool =  Executors.newFixedThreadPool(5); //一个银行网点，5个受理业务的窗口//       ExecutorService threadPool =  Executors.newSingleThreadExecutor(); //一个银行网点，1个受理业务的窗口       ExecutorService threadPool =  Executors.newCachedThreadPool(); //一个银行网点，可扩展受理业务的窗口        //10个顾客请求        try {            for (int i = 1; i &lt;=10; i++) {                threadPool.execute(()-&gt;{                    System.out.println(Thread.currentThread().getName()+&quot;\t 办理业务&quot;);                });            }        } catch (Exception e) {            e.printStackTrace();        } finally {            threadPool.shutdown();        }    }}</code></pre><blockquote><p>1、<code>Executors.newFixedThreadPool(int)</code></p></blockquote><p>执行长期任务性能好，创建一个线程池，一池有N个固定的线程，有固定线程数的线程</p><p>说明：</p><ul><li><code>newFixedThreadPool</code>创建的线程池的</li><li><code>orePoolSize</code>和<code>maximumPoolSize</code>值是相等的</li><li>它使用的是<code>LinkedBlockingQueue</code></li></ul><p>源码：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194051.png" alt=""></p><blockquote><p>2、<code>Executors.newSingleThreadExecutor()</code></p></blockquote><p>一个任务一个任务的执行，一池一线程</p><p>说明：</p><ul><li><code>newSingleThreadExecutor</code> 创建的线程池</li><li><code>corePoolSize和maximumPoolSize</code>值都是1</li><li>它使用的是<code>LinkedBlockingQueue</code></li></ul><p>源码：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194054.png" alt=""></p><blockquote><p><code>Executors.newCachedThreadPool()</code></p></blockquote><p>执行很多短期异步任务，线程池根据需要创建新线程，但在先前构建的线程可用时将重用它们。可扩容，遇强则强</p><p>说明：</p><ul><li><code>newCachedThreadPool</code>创建的线程池</li><li>将<code>corePoolSize</code>设置为0，将<code>maximumPoolSize</code>设置为<code>Integer.MAX_VALUE</code>，</li><li>它使用的是<code>SynchronousQueue</code>，也就是说来了任务就创建线程运行，当线程空闲超过60秒，就销毁线程</li></ul><p>源码：</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194058.png" alt="image-20200713165658778"></p><h2 id="4-3、线程池7大参数重要参数"><a href="#4-3、线程池7大参数重要参数" class="headerlink" title="4.3、线程池7大参数重要参数"></a>4.3、线程池7大参数重要参数</h2><blockquote><p>1、源码</p></blockquote><pre><code class="java">public ThreadPoolExecutor(int corePoolSize,                          int maximumPoolSize,                          long keepAliveTime,                          TimeUnit unit,                          BlockingQueue&lt;Runnable&gt; workQueue,                          ThreadFactory threadFactory,                          RejectedExecutionHandler handler) {    if (corePoolSize &lt; 0 ||        maximumPoolSize &lt;= 0 ||        maximumPoolSize &lt; corePoolSize ||        keepAliveTime &lt; 0)        throw new IllegalArgumentException();    if (workQueue == null || threadFactory == null || handler == null)        throw new NullPointerException();    this.corePoolSize = corePoolSize;    this.maximumPoolSize = maximumPoolSize;    this.workQueue = workQueue;    this.keepAliveTime = unit.toNanos(keepAliveTime);    this.threadFactory = threadFactory;    this.handler = handler;}</code></pre><blockquote><p>2、参数说明</p></blockquote><ol><li><code>corePoolSize</code>：线程池中的常驻核心线程数</li><li><code>maximumPoolSize</code>：线程池中能够容纳同时执行的最大线程数，此值必须大于等于1</li><li><code>keepAliveTime</code>：多余的空闲线程的存活时间，当前池中线程数量超过corePoolSize时，当空闲时间达到keepAliveTime时，多余线程会被销毁直到只剩下corePoolSize个线程为止</li><li><code>unit</code>：keepAliveTime的单位 </li><li><code>workQueue</code>：任务队列，被提交但尚未被执行的任务</li><li><code>threadFactory</code>：表示生成线程池中工作线程的线程工厂，用于创建线程，一般默认的即可</li><li><code>handler</code>：拒绝策略，表示当队列满了，并且工作线程大于等于线程池的最大线程数（maximumPoolSize）时如何来拒绝请求执行的runnable的策略</li></ol><h2 id="4-4、【重点】线程池底层工作原理"><a href="#4-4、【重点】线程池底层工作原理" class="headerlink" title="4.4、【重点】线程池底层工作原理"></a>4.4、【重点】线程池底层工作原理</h2><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194102.png" alt=""></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194105.png" alt=""></p><ol><li>在创建了线程池后，开始等待请求。</li><li>当调用execute()方法添加一个请求任务时，线程池会做出如下判断：<ul><li>如果正在运行的线程数量小于corePoolSize，那么马上创建线程运行这个任务；</li><li>如果正在运行的线程数量大于或等于corePoolSize，那么将这个任务放入队列；</li><li>如果这个时候队列满了且正在运行的线程数量还小于maximumPoolSize，那么还是要创建非核心线程立刻运行这个任务；</li><li>如果队列满了且正在运行的线程数量大于或等于maximumPoolSize，那么线程池会启动饱和拒绝策略来执行。</li></ul></li><li>当一个线程完成任务时，它会从队列中取下一个任务来执行。</li><li>当一个线程无事可做超过一定的时间（keepAliveTime）时，线程会判断： 如果当前运行的线程数大于corePoolSize，那么这个线程就被停掉。所以线程池的所有任务完成后，它最终会收缩到corePoolSize的大小。</li></ol><h2 id="4-5、线程池用哪个？生产中如设置合理参数"><a href="#4-5、线程池用哪个？生产中如设置合理参数" class="headerlink" title="4.5、线程池用哪个？生产中如设置合理参数"></a>4.5、线程池用哪个？生产中如设置合理参数</h2><h3 id="4-5-1、线程池的拒绝策略"><a href="#4-5-1、线程池的拒绝策略" class="headerlink" title="4.5.1、线程池的拒绝策略"></a>4.5.1、线程池的拒绝策略</h3><blockquote><p>1、是什么</p></blockquote><p>等待队列已经排满了，再也塞不下新任务了。同时，线程池中的max线程也达到了，无法继续为新任务服务。这个是时候我们就需要拒绝策略机制合理的处理这个问题。</p><blockquote><p>2、JDK内置的拒绝策略</p></blockquote><p>下面内置拒绝策略均实现了RejectedExecutionHandle接口</p><ol><li><code>AbortPolicy</code>(默认)：直接抛出RejectedExecutionException异常阻止系统正常运行</li><li><code>CallerRunsPolicy</code>：“调用者运行”一种调节机制，该策略既不会抛弃任务，也不会抛出异常，而是将某些任务回退到调用者，从而降低新任务的流量。</li><li><code>DiscardOldestPolicy</code>：抛弃队列中等待最久的任务，然后把当前任务加人队列中尝试再次提交当前任务。</li><li><code>DiscardPolicy</code>：该策略默默地丢弃无法处理的任务，不予任何处理也不抛出异常。如果允许任务丢失，这是最好的一种策略。</li></ol><h3 id="4-5-2、超级大坑"><a href="#4-5-2、超级大坑" class="headerlink" title="4.5.2、超级大坑"></a>4.5.2、超级大坑</h3><blockquote><p>1、在工作中单一的/固定数的/可变的三种创建线程池的方法哪个用的多？</p></blockquote><p>答案是一个都不用，我们工作中只能使用自定义的</p><blockquote><p>2、Executors中JDK已经给你提供了，为什么不用？</p></blockquote><p>略</p><blockquote><p>3、在工作中如何使用线程池？是否自定义过线程池？</p></blockquote><p>代码：</p><pre><code class="java">import java.util.Arrays;import java.util.List;import java.util.concurrent.*;/** * 线程池 * Arrays * Collections * Executors */public class MyThreadPoolDemo {    public static void main(String[] args) {        ExecutorService threadPool = new ThreadPoolExecutor(                2,                5,                2L,                TimeUnit.SECONDS,                new ArrayBlockingQueue&lt;Runnable&gt;(3),                Executors.defaultThreadFactory(),                //new ThreadPoolExecutor.AbortPolicy()                //new ThreadPoolExecutor.CallerRunsPolicy()                //new ThreadPoolExecutor.DiscardOldestPolicy()                new ThreadPoolExecutor.DiscardOldestPolicy()        );        //10个顾客请求        try {            for (int i = 1; i &lt;= 10; i++) {                threadPool.execute(() -&gt; {                    System.out.println(Thread.currentThread().getName() + &quot;\t 办理业务&quot;);                });            }        } catch (Exception e) {            e.printStackTrace();        } finally {            threadPool.shutdown();        }    }    private static void threadPool() {        //List list = new ArrayList();        //List list = Arrays.asList(&quot;a&quot;,&quot;b&quot;);        //固定数的线程池，一池五线程//       ExecutorService threadPool =  Executors.newFixedThreadPool(5); //一个银行网点，5个受理业务的窗口//       ExecutorService threadPool =  Executors.newSingleThreadExecutor(); //一个银行网点，1个受理业务的窗口        ExecutorService threadPool = Executors.newCachedThreadPool(); //一个银行网点，可扩展受理业务的窗口        //10个顾客请求        try {            for (int i = 1; i &lt;= 10; i++) {                threadPool.execute(() -&gt; {                    System.out.println(Thread.currentThread().getName() + &quot;\t 办理业务&quot;);                });            }        } catch (Exception e) {            e.printStackTrace();        } finally {            threadPool.shutdown();        }    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、继承Thread类&quot;&gt;&lt;a href=&quot;#01、继承Thread类&quot; class=&quot;headerlink&quot; title=&quot;01、继承Thread类&quot;&gt;&lt;/a&gt;01、继承Thread类&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;不建议使用：java是单继承，资源
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>09ReentrantReadWriteLock 读写锁</title>
    <link href="http://lsqstudy.top/post/5adedb8b.html"/>
    <id>http://lsqstudy.top/post/5adedb8b.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.671Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、案例：缓存"><a href="#01、案例：缓存" class="headerlink" title="01、案例：缓存"></a>01、案例：缓存</h1><h2 id="1-1、有问题的代码"><a href="#1-1、有问题的代码" class="headerlink" title="1.1、有问题的代码"></a>1.1、有问题的代码</h2><pre><code class="java">import java.util.HashMap;import java.util.Map;import java.util.concurrent.TimeUnit;class MyCache{    private volatile Map&lt;String,Object&gt; map = new HashMap&lt;&gt;();    public void put(String key,Object value){        System.out.println(Thread.currentThread().getName()+&quot;\t &quot;+key);        //        try {TimeUnit.MILLISECONDS.sleep(300);} catch (InterruptedException e) {e.printStackTrace(); }        map.put(key,value);        System.out.println(Thread.currentThread().getName()+&quot;\t &quot;+key);    }    public Object get(String key){        Object result = null;        System.out.println(Thread.currentThread().getName()+&quot;\t &quot;+key);        try {TimeUnit.MILLISECONDS.sleep(300);} catch (InterruptedException e) {e.printStackTrace(); }        result = map.get(key);        System.out.println(Thread.currentThread().getName()+&quot;\t &quot;+result);        return result;    }}public class ReadWriteLockDemo {    public static void main(String[] args) {        MyCache myCache = new MyCache();        for (int i = 1; i &lt;= 5; i++) {            final int num = i;            new Thread(()-&gt;{                myCache.put(num+&quot;&quot;,num+&quot;&quot;);            },String.valueOf(i)).start();        }        for (int i = 1; i &lt;= 5; i++) {            final int num = i;            new Thread(()-&gt;{                myCache.get(num+&quot;&quot;);            },String.valueOf(i)).start();        }    }}</code></pre><h2 id="1-2、改进后的代码："><a href="#1-2、改进后的代码：" class="headerlink" title="1.2、改进后的代码："></a>1.2、改进后的代码：</h2><pre><code class="java">import java.util.HashMap;import java.util.Map;import java.util.concurrent.TimeUnit;import java.util.concurrent.locks.ReadWriteLock;import java.util.concurrent.locks.ReentrantReadWriteLock;class MyCache {    private volatile Map&lt;String, Object&gt; map = new HashMap&lt;&gt;();    private ReadWriteLock rwLock = new ReentrantReadWriteLock();    public void put(String key, Object value) {        rwLock.writeLock().lock();        try {            System.out.println(Thread.currentThread().getName() + &quot;\t 正在写&quot; + key);            //暂停一会儿线程            try {                TimeUnit.MILLISECONDS.sleep(300);            } catch (InterruptedException e) {                e.printStackTrace();            }            map.put(key, value);            System.out.println(Thread.currentThread().getName() + &quot;\t 写完了&quot; + key);            System.out.println();        } catch (Exception e) {            e.printStackTrace();        } finally {            rwLock.writeLock().unlock();        }    }    public Object get(String key) {        rwLock.readLock().lock();        Object result = null;        try {            System.out.println(Thread.currentThread().getName() + &quot;\t 正在读&quot; + key);            try {                TimeUnit.MILLISECONDS.sleep(300);            } catch (InterruptedException e) {                e.printStackTrace();            }            result = map.get(key);            System.out.println(Thread.currentThread().getName() + &quot;\t 读完了&quot; + result);        } catch (Exception e) {            e.printStackTrace();        } finally {            rwLock.readLock().unlock();        }        return result;    }}public class ReadWriteLockDemo {    public static void main(String[] args) {        MyCache myCache = new MyCache();        for (int i = 1; i &lt;= 5; i++) {            final int num = i;            new Thread(() -&gt; {                myCache.put(num + &quot;&quot;, num + &quot;&quot;);            }, String.valueOf(i)).start();        }        for (int i = 1; i &lt;= 5; i++) {            final int num = i;            new Thread(() -&gt; {                myCache.get(num + &quot;&quot;);            }, String.valueOf(i)).start();        }    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、案例：缓存&quot;&gt;&lt;a href=&quot;#01、案例：缓存&quot; class=&quot;headerlink&quot; title=&quot;01、案例：缓存&quot;&gt;&lt;/a&gt;01、案例：缓存&lt;/h1&gt;&lt;h2 id=&quot;1-1、有问题的代码&quot;&gt;&lt;a href=&quot;#1-1、有问题的代码&quot; class=&quot;
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>08JUC强大的辅助类</title>
    <link href="http://lsqstudy.top/post/d8320594.html"/>
    <id>http://lsqstudy.top/post/d8320594.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.670Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、AQS-AbstractQueuedSynchronizer"><a href="#01、AQS-AbstractQueuedSynchronizer" class="headerlink" title="01、AQS(AbstractQueuedSynchronizer)"></a>01、AQS(AbstractQueuedSynchronizer)</h1><p>AQS是Doug Lea大师为JDK编写的一套基于API层面的抽象队列同步器.<br>AbstractQueuedSynchronizer,抽象队列同步器.<br>Lock,CountDownLatch等等这些并发工具都是基于AQS来实现的。</p><h2 id="1-2、AQS的两种共享资源的访问方式"><a href="#1-2、AQS的两种共享资源的访问方式" class="headerlink" title="1.2、AQS的两种共享资源的访问方式"></a>1.2、AQS的两种共享资源的访问方式</h2><blockquote><p>AQS定义了两种共享资源方式.</p></blockquote><ol><li>独占式(Exclusive): <strong>同一时间只有一个线程可以访问共享资源,也就是独占锁。</strong><br>如:Synchronized,ReentrantLock。<br><strong>对于独占式锁的实现,在AQS中对应tryAcquire获取锁和tryRelease释放锁。</strong></li><li>共享式(Share): <strong>同一时间允许多个线程同时访问共享资源,也就是共享锁。</strong><br>CountDownLatch,Semaphore,ReentrantReadWriteLock的ReadLock都是共享锁。<br><strong>对于共享式锁的实现,在AQS中对应tryAcquireShare获取锁和tryReleaseShare释放锁。</strong></li></ol><h1 id="02、CountDownLatch：减少计数"><a href="#02、CountDownLatch：减少计数" class="headerlink" title="02、CountDownLatch：减少计数"></a>02、<code>CountDownLatch</code>：减少计数</h1><h2 id="2-1、CountDownLatch原理"><a href="#2-1、CountDownLatch原理" class="headerlink" title="2.1、CountDownLatch原理"></a>2.1、CountDownLatch原理</h2><p>CountDownLatch主要有两个方法，当一个或多个线程调用await方法时，这些线程会阻塞。其它线程调用countDown方法会将计数器减1(调用countDown方法的线程不会阻塞)，当计数器的值变为0时，因await方法阻塞的线程会被唤醒，继续执行。</p><h2 id="2-2、示例代码"><a href="#2-2、示例代码" class="headerlink" title="2.2、示例代码"></a>2.2、示例代码</h2><pre><code class="java">import java.util.concurrent.CountDownLatch;/** *  * @Description: *  *让一些线程阻塞直到另一些线程完成一系列操作后才被唤醒。 *  * CountDownLatch主要有两个方法，当一个或多个线程调用await方法时，这些线程会阻塞。 * 其它线程调用countDown方法会将计数器减1(调用countDown方法的线程不会阻塞)， * 当计数器的值变为0时，因await方法阻塞的线程会被唤醒，继续执行。 *  * 解释：6个同学陆续离开教室后值班同学才可以关门。 *  * main主线程必须要等前面6个线程完成全部工作后，自己才能开干  */public class CountDownLatchDemo{   public static void main(String[] args) throws InterruptedException   {         CountDownLatch countDownLatch = new CountDownLatch(6);       for (int i = 1; i &lt;=6; i++) //6个上自习的同学，各自离开教室的时间不一致       {          new Thread(() -&gt; {              System.out.println(Thread.currentThread().getName()+&quot;\t 号同学离开教室&quot;);              countDownLatch.countDown();          }, String.valueOf(i)).start();       }       countDownLatch.await();       System.out.println(Thread.currentThread().getName()+&quot;\t****** 班长关门走人，main线程是班长&quot;);   }}</code></pre><h1 id="03、CyclicBarrier：循环栅栏"><a href="#03、CyclicBarrier：循环栅栏" class="headerlink" title="03、CyclicBarrier：循环栅栏"></a>03、<code>CyclicBarrier</code>：循环栅栏</h1><h2 id="3-1、CyclicBarrier原理"><a href="#3-1、CyclicBarrier原理" class="headerlink" title="3.1、CyclicBarrier原理"></a>3.1、CyclicBarrier原理</h2><p>CyclicBarrier的字面意思是可循环（Cyclic）使用的屏障（Barrier）。它要做的事情是让一组线程到达一个屏障（也可以叫同步点）时被阻塞，直到最后一个线程到达屏障时，屏障才会开门，所有被屏障拦截的线程才会继续干活。线程进入屏障通过CyclicBarrier的await()方法。</p><h2 id="3-2、示例代码"><a href="#3-2、示例代码" class="headerlink" title="3.2、示例代码"></a>3.2、示例代码</h2><pre><code class="java">import java.util.concurrent.BrokenBarrierException;import java.util.concurrent.CyclicBarrier;/** *  * * CyclicBarrier * 的字面意思是可循环（Cyclic）使用的屏障（Barrier）。它要做的事情是， * 让一组线程到达一个屏障（也可以叫同步点）时被阻塞， * 直到最后一个线程到达屏障时，屏障才会开门，所有 * 被屏障拦截的线程才会继续干活。 * 线程进入屏障通过CyclicBarrier的await()方法。 *  * 集齐7颗龙珠就可以召唤神龙 */public class CyclicBarrierDemo{  private static final int NUMBER = 7;  public static void main(String[] args)  {     //CyclicBarrier(int parties, Runnable barrierAction)      CyclicBarrier cyclicBarrier = new CyclicBarrier(NUMBER, ()-&gt;{System.out.println(&quot;*****集齐7颗龙珠就可以召唤神龙&quot;);}) ;     for (int i = 1; i &lt;= 7; i++) {       new Thread(() -&gt; {          try {            System.out.println(Thread.currentThread().getName()+&quot;\t 星龙珠被收集 &quot;);            cyclicBarrier.await();          } catch (InterruptedException | BrokenBarrierException e) {            // TODO Auto-generated catch block            e.printStackTrace();          }       }, String.valueOf(i)).start();     }  }}</code></pre><h1 id="04、Semaphore：信号灯"><a href="#04、Semaphore：信号灯" class="headerlink" title="04、Semaphore：信号灯"></a>04、<code>Semaphore</code>：信号灯</h1><h2 id="4-1、原理"><a href="#4-1、原理" class="headerlink" title="4.1、原理"></a>4.1、原理</h2><blockquote><p>在信号量上我们定义两种操作：</p></blockquote><ul><li><code>acquire</code>（获取）： 当一个线程调用acquire操作时，它要么通过成功获取信号量（信号量减1），要么一直等下去，直到有线程释放信号量，或超时。</li><li><code>release</code>（释放）：实际上会将信号量的值加1，然后唤醒等待的线程。</li></ul><blockquote><p>信号量主要用于两个目的</p></blockquote><ul><li>一个是用于多个共享资源的互斥使用</li><li>另一个用于并发线程数的控制。</li></ul><h2 id="4-2、示例代码"><a href="#4-2、示例代码" class="headerlink" title="4.2、示例代码"></a>4.2、示例代码</h2><pre><code class="java">import java.util.Random;import java.util.concurrent.Semaphore;import java.util.concurrent.TimeUnit;/** *  * @Description: TODO(这里用一句话描述这个类的作用)   *  * 在信号量上我们定义两种操作： * acquire（获取） 当一个线程调用acquire操作时，它要么通过成功获取信号量（信号量减1）， *             要么一直等下去，直到有线程释放信号量，或超时。 * release（释放）实际上会将信号量的值加1，然后唤醒等待的线程。 *  * 信号量主要用于两个目的，一个是用于多个共享资源的互斥使用，另一个用于并发线程数的控制。 */public class SemaphoreDemo{  public static void main(String[] args)  {     Semaphore semaphore = new Semaphore(3);//模拟3个停车位     for (int i = 1; i &lt;=6; i++) //模拟6部汽车     {       new Thread(() -&gt; {          try           {            semaphore.acquire();            System.out.println(Thread.currentThread().getName()+&quot;\t 抢到了车位&quot;);            TimeUnit.SECONDS.sleep(new Random().nextInt(5));            System.out.println(Thread.currentThread().getName()+&quot;\t------- 离开&quot;);          } catch (InterruptedException e) {            e.printStackTrace();          }finally {            semaphore.release();          }       }, String.valueOf(i)).start();     }  }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、AQS-AbstractQueuedSynchronizer&quot;&gt;&lt;a href=&quot;#01、AQS-AbstractQueuedSynchronizer&quot; class=&quot;headerlink&quot; title=&quot;01、AQS(AbstractQueuedSynch
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>06多线程锁</title>
    <link href="http://lsqstudy.top/post/d596a47.html"/>
    <id>http://lsqstudy.top/post/d596a47.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.669Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、锁的8个问题"><a href="#01、锁的8个问题" class="headerlink" title="01、锁的8个问题"></a>01、锁的8个问题</h1><ol><li>标准访问，先打印短信还是邮件</li><li>停4秒在短信方法内，先打印短信还是邮件</li><li>普通的hello方法，是先打短信还是hello</li><li>现在有两部手机，先打印短信还是邮件</li><li>两个静态同步方法，1部手机，先打印短信还是邮件</li><li>两个静态同步方法，2部手机，先打印短信还是邮件</li><li>1个静态同步方法，1个普通同步方法，1部手机，先打印短信还是邮件</li><li>1个静态同步方法，1个普通同步方法，2部手机，先打印短信还是邮件</li></ol><h1 id="02、8锁分析"><a href="#02、8锁分析" class="headerlink" title="02、8锁分析"></a>02、8锁分析</h1><p>A 一个对象里面如果有多个synchronized方法，某一个时刻内，只要一个线程去调用其中的一个synchronized方法了，<br>其它的线程都只能等待，换句话说，某一个时刻内，只能有唯一一个线程去访问这些synchronized方法<br>锁的是当前对象this，被锁定后，其它的线程都不能进入到当前对象的其它的synchronized方法</p><p>加个普通方法后发现和同步锁无关<br>换成两个对象后，不是同一把锁了，情况立刻变化。</p><p>synchronized实现同步的基础：Java中的每一个对象都可以作为锁。<br>具体表现为以下3种形式。<br>对于普通同步方法，锁是当前实例对象。<br>对于静态同步方法，锁是当前类的Class对象。<br>对于同步方法块，锁是Synchonized括号里配置的对象</p><p>当一个线程试图访问同步代码块时，它首先必须得到锁，退出或抛出异常时必须释放锁。</p><p>也就是说如果一个实例对象的非静态同步方法获取锁后，该实例对象的其他非静态同步方法必须等待获取锁的方法释放锁后才能获取锁，<br>可是别的实例对象的非静态同步方法因为跟该实例对象的非静态同步方法用的是不同的锁，<br>所以毋须等待该实例对象已获取锁的非静态同步方法释放锁就可以获取他们自己的锁。</p><p>所有的静态同步方法用的也是同一把锁——类对象本身，<br>这两把锁是两个不同的对象，所以静态同步方法与非静态同步方法之间是不会有竞态条件的。<br>但是一旦一个静态同步方法获取锁后，其他的静态同步方法都必须等待该方法释放锁后才能获取锁，<br>而不管是同一个实例对象的静态同步方法之间，<br>还是不同的实例对象的静态同步方法之间，只要它们同一个类的实例对象！</p><h1 id="03、代码"><a href="#03、代码" class="headerlink" title="03、代码"></a>03、代码</h1><pre><code class="java">import java.util.concurrent.TimeUnit;class Phone{ public  synchronized void sendSMS() throws Exception {   System.out.println(&quot;------sendSMS&quot;); } public synchronized void sendEmail() throws Exception {   System.out.println(&quot;------sendEmail&quot;); } public void getHello()  {   System.out.println(&quot;------getHello&quot;); }}/** *  * @Description: 8锁 *  1 标准访问，先打印短信还是邮件 2 停4秒在短信方法内，先打印短信还是邮件 3 新增普通的hello方法，是先打短信还是hello 4 现在有两部手机，先打印短信还是邮件 5 两个静态同步方法，1部手机，先打印短信还是邮件 6 两个静态同步方法，2部手机，先打印短信还是邮件 7 1个静态同步方法,1个普通同步方法，1部手机，先打印短信还是邮件 8 1个静态同步方法,1个普通同步方法，2部手机，先打印短信还是邮件 * --------------------------------- *  */public class Lock_8{ public static void main(String[] args) throws Exception {   Phone phone = new Phone();   Phone phone2 = new Phone();   new Thread(() -&gt; {    try {     phone.sendSMS();    } catch (Exception e) {     e.printStackTrace();    }   }, &quot;AA&quot;).start();   Thread.sleep(100);   new Thread(() -&gt; {    try {     phone.sendEmail();     //phone.getHello();     //phone2.sendEmail();    } catch (Exception e) {     e.printStackTrace();    }   }, &quot;BB&quot;).start(); }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、锁的8个问题&quot;&gt;&lt;a href=&quot;#01、锁的8个问题&quot; class=&quot;headerlink&quot; title=&quot;01、锁的8个问题&quot;&gt;&lt;/a&gt;01、锁的8个问题&lt;/h1&gt;&lt;ol&gt;
&lt;li&gt;标准访问，先打印短信还是邮件&lt;/li&gt;
&lt;li&gt;停4秒在短信方法内，先打
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>12异步回调</title>
    <link href="http://lsqstudy.top/post/a2c489.html"/>
    <id>http://lsqstudy.top/post/a2c489.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.677Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、原理"><a href="#01、原理" class="headerlink" title="01、原理"></a>01、原理</h1><p>同步、异步、异步回调</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194316.png" alt=""></p><h1 id="02、示例代码"><a href="#02、示例代码" class="headerlink" title="02、示例代码"></a>02、示例代码</h1><pre><code class="java">import java.util.concurrent.CompletableFuture;public class CompletableFutureDemo {    public static void main(String[] args) throws Exception {        //同步，异步，异步回调        //同步//        CompletableFuture&lt;Void&gt; completableFuture1 = CompletableFuture.runAsync(()-&gt;{//            System.out.println(Thread.currentThread().getName()+&quot;\t completableFuture1&quot;);//        });//        completableFuture1.get();        //异步回调        CompletableFuture&lt;Integer&gt; completableFuture2 = CompletableFuture.supplyAsync(()-&gt;{            System.out.println(Thread.currentThread().getName()+&quot;\t completableFuture2&quot;);            int i = 10/0;            return 1024;        });        completableFuture2.whenComplete((t,u)-&gt;{            System.out.println(&quot;-------t=&quot;+t);            System.out.println(&quot;-------u=&quot;+u);        }).exceptionally(f-&gt;{            System.out.println(&quot;-----exception:&quot;+f.getMessage());            return 444;        }).get();    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、原理&quot;&gt;&lt;a href=&quot;#01、原理&quot; class=&quot;headerlink&quot; title=&quot;01、原理&quot;&gt;&lt;/a&gt;01、原理&lt;/h1&gt;&lt;p&gt;同步、异步、异步回调&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://gitee.com/lsqpic/BlogP
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>10BlockingQueueDemo 阻塞队列</title>
    <link href="http://lsqstudy.top/post/15068b6.html"/>
    <id>http://lsqstudy.top/post/15068b6.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.675Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、栈与队列"><a href="#01、栈与队列" class="headerlink" title="01、栈与队列"></a>01、栈与队列</h1><ul><li>栈：先进后出，后进先出</li><li>队列：先进先出</li></ul><h1 id="02、阻塞队列"><a href="#02、阻塞队列" class="headerlink" title="02、阻塞队列"></a>02、阻塞队列</h1><p>阻塞：必须要阻塞/不得不阻塞 </p><blockquote><p>线程1往阻塞队列里添加元素，线程2从阻塞队列里移除元素</p></blockquote><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194244.png" alt=""></p><p>当队列是<strong>空时</strong>，从队列中<strong>获取元素</strong>的操作将会被阻塞<br>当队列是<strong>满时</strong>，从队列中<strong>添加元素</strong>的操作将会被阻塞</p><h1 id="03、阻塞队列的用处"><a href="#03、阻塞队列的用处" class="headerlink" title="03、阻塞队列的用处"></a>03、阻塞队列的用处</h1><blockquote><p>1、在多线程领域</p></blockquote><p>所谓阻塞，在某些情况下会<strong>挂起</strong>线程（即阻塞），一旦条件满足，被挂起的线程又会自动<strong>被唤起</strong></p><blockquote><p>2、为什么需要BlockingQueue</p></blockquote><p>好处是：我们不需要关心什么时候需要阻塞线程，什么时候需要唤醒线程，因为这一切BlockingQueue都给你一手包办了。</p><p>在concurrent包发布以前，在多线程环境下，我们每个程序员都必须去自己控制这些细节，尤其还要兼顾效率和线程安全，而这会给我们的程序带来不小的复杂度。</p><h1 id="04、架构梳理、种类分析"><a href="#04、架构梳理、种类分析" class="headerlink" title="04、架构梳理、种类分析"></a>04、架构梳理、种类分析</h1><h2 id="4-1、架构介绍"><a href="#4-1、架构介绍" class="headerlink" title="4.1、架构介绍"></a>4.1、架构介绍</h2><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194247.png" alt=""></p><h2 id="4-2、种类分析"><a href="#4-2、种类分析" class="headerlink" title="4.2、种类分析"></a>4.2、种类分析</h2><ol><li><code>ArrayBlockingQueue</code>【重点】：由数组结构组成的有界阻塞队列。</li><li><code>LinkedBlockingQueue</code>【重点】：由链表结构组成的有界（但大小默认值为integer.MAX_VALUE）阻塞队列。</li><li><code>PriorityBlockingQueue</code>：支持优先级排序的无界阻塞队列。</li><li><code>DelayQueue</code>：使用优先级队列实现的延迟无界阻塞队列。</li><li><code>SynchronousQueue</code>【重点】：不存储元素的阻塞队列，也即单个元素的队列。</li><li><code>LinkedTransferQueue</code>：由链表组成的无界阻塞队列。</li><li><code>LinkedBlockingDeque</code>：由链表组成的双向阻塞队列。</li></ol><h1 id="05、BlockingQueue核心方法"><a href="#05、BlockingQueue核心方法" class="headerlink" title="05、BlockingQueue核心方法"></a>05、BlockingQueue核心方法</h1><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194250.png" alt=""></p><blockquote><p>1、抛出异常</p></blockquote><ul><li>当阻塞队列满时，再往队列里<code>add</code>插入元素会抛<code>IllegalStateException:Queue full</code></li><li>当阻塞队列空时，再往队列里<code>remove</code>移除元素会抛<code>NoSuchElementException</code></li></ul><blockquote><p>2、特殊值</p></blockquote><ul><li>插入方法，成功<code>ture</code>失败<code>false</code></li><li>移除方法，成功返回出队列的元素，队列里没有就返回<code>null</code></li></ul><blockquote><p>3、一直阻塞</p></blockquote><ul><li>当阻塞队列满时，生产者线程继续往队列里put元素，队列会一直阻塞生产者线程直到put数据or响应中断退出</li><li>当阻塞队列空时，消费者线程试图从队列里take元素，队列会一直阻塞消费者线程直到队列可用</li></ul><blockquote><p>4、超时退出</p></blockquote><p>当阻塞队列满时，队列会阻塞生产者线程一定时间，超过限时后生产者线程会退出</p><h1 id="06、示例代码"><a href="#06、示例代码" class="headerlink" title="06、示例代码"></a>06、示例代码</h1><pre><code class="java">import java.util.ArrayList;import java.util.List;import java.util.concurrent.ArrayBlockingQueue;import java.util.concurrent.BlockingQueue;import java.util.concurrent.TimeUnit;/** * 阻塞队列 */public class BlockingQueueDemo {    public static void main(String[] args) throws InterruptedException {//        List list = new ArrayList();        BlockingQueue&lt;String&gt; blockingQueue = new ArrayBlockingQueue&lt;&gt;(3);        //第一组//        System.out.println(blockingQueue.add(&quot;a&quot;));//        System.out.println(blockingQueue.add(&quot;b&quot;));//        System.out.println(blockingQueue.add(&quot;c&quot;));//        System.out.println(blockingQueue.element());        //System.out.println(blockingQueue.add(&quot;x&quot;));//        System.out.println(blockingQueue.remove());//        System.out.println(blockingQueue.remove());//        System.out.println(blockingQueue.remove());//        System.out.println(blockingQueue.remove());//    第二组//        System.out.println(blockingQueue.offer(&quot;a&quot;));//        System.out.println(blockingQueue.offer(&quot;b&quot;));//        System.out.println(blockingQueue.offer(&quot;c&quot;));//        System.out.println(blockingQueue.offer(&quot;x&quot;));//        System.out.println(blockingQueue.poll());//        System.out.println(blockingQueue.poll());//        System.out.println(blockingQueue.poll());//        System.out.println(blockingQueue.poll());//    第三组        //         blockingQueue.put(&quot;a&quot;);//         blockingQueue.put(&quot;b&quot;);//         blockingQueue.put(&quot;c&quot;);//         //blockingQueue.put(&quot;x&quot;);//        System.out.println(blockingQueue.take());//        System.out.println(blockingQueue.take());//        System.out.println(blockingQueue.take());//        System.out.println(blockingQueue.take());//    第四组                System.out.println(blockingQueue.offer(&quot;a&quot;));        System.out.println(blockingQueue.offer(&quot;b&quot;));        System.out.println(blockingQueue.offer(&quot;c&quot;));        System.out.println(blockingQueue.offer(&quot;a&quot;,3L, TimeUnit.SECONDS));    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、栈与队列&quot;&gt;&lt;a href=&quot;#01、栈与队列&quot; class=&quot;headerlink&quot; title=&quot;01、栈与队列&quot;&gt;&lt;/a&gt;01、栈与队列&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;栈：先进后出，后进先出&lt;/li&gt;
&lt;li&gt;队列：先进先出&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
  <entry>
    <title>11分支合并框架</title>
    <link href="http://lsqstudy.top/post/49c55a91.html"/>
    <id>http://lsqstudy.top/post/49c55a91.html</id>
    <published>2020-07-12T16:00:00.000Z</published>
    <updated>2020-07-14T13:28:37.676Z</updated>
    
    <content type="html"><![CDATA[<h1 id="01、原理"><a href="#01、原理" class="headerlink" title="01、原理"></a>01、原理</h1><ul><li><code>Fork</code>：把一个复杂任务进行分拆，大事化小</li><li><code>Join</code>：把分拆任务的结果进行合并</li></ul><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194311.png" alt=""></p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194308.png" alt=""></p><h1 id="02、相关类"><a href="#02、相关类" class="headerlink" title="02、相关类"></a>02、相关类</h1><h2 id="2-1、ForkJoinPool"><a href="#2-1、ForkJoinPool" class="headerlink" title="2.1、ForkJoinPool"></a>2.1、ForkJoinPool</h2><p>分支合并池    类比=&gt;   线程池</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194304.png" alt=""></p><h2 id="2-2、ForkJoinTask"><a href="#2-2、ForkJoinTask" class="headerlink" title="2.2、ForkJoinTask"></a>2.2、ForkJoinTask</h2><p>ForkJoinTask    类比=&gt;   FutureTask</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194301.png" alt=""></p><h2 id="2-3、RecursiveTask"><a href="#2-3、RecursiveTask" class="headerlink" title="2.3、RecursiveTask"></a>2.3、RecursiveTask</h2><p>递归任务：继承后可以实现递归(自己调自己)调用的任务</p><p><img src="https://gitee.com/lsqpic/BlogPicBed-1/raw/master/img/2020/06/01/20200713194258.png" alt=""></p><blockquote><p>源码：</p></blockquote><pre><code class="java">class Fibonacci extends RecursiveTask&lt;Integer&gt; {   final int n;   Fibonacci(int n) { this.n = n; }   Integer compute() {     if (n &lt;= 1)       return n;     Fibonacci f1 = new Fibonacci(n - 1);     f1.fork();     Fibonacci f2 = new Fibonacci(n - 2);     return f2.compute() + f1.join();   } }</code></pre><h1 id="03、示例代码"><a href="#03、示例代码" class="headerlink" title="03、示例代码"></a>03、示例代码</h1><pre><code class="java">import java.util.concurrent.ExecutionException;import java.util.concurrent.ForkJoinPool;import java.util.concurrent.ForkJoinTask;import java.util.concurrent.RecursiveTask;class MyTask extends RecursiveTask&lt;Integer&gt;{    private static final Integer ADJUST_VALUE = 10;    private int begin;    private int end;    private int result;    public MyTask(int begin, int end) {        this.begin = begin;        this.end = end;    }    @Override    protected Integer compute() {        if((end - begin)&lt;=ADJUST_VALUE){           for(int i =begin;i &lt;= end;i++){                result = result + i;           }        }else{            int middle = (begin + end)/2;            MyTask task01 = new MyTask(begin,middle);            MyTask task02 = new MyTask(middle+1,end);            task01.fork();            task02.fork();            result =  task01.join() + task02.join();        }        return result;    }}/** * 分支合并例子 * ForkJoinPool * ForkJoinTask * RecursiveTask */public class ForkJoinDemo {    public static void main(String[] args) throws ExecutionException, InterruptedException {        MyTask myTask = new MyTask(0,100);        ForkJoinPool forkJoinPool = new ForkJoinPool();        ForkJoinTask&lt;Integer&gt; forkJoinTask = forkJoinPool.submit(myTask);        System.out.println(forkJoinTask.get());        forkJoinPool.shutdown();    }}</code></pre>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;01、原理&quot;&gt;&lt;a href=&quot;#01、原理&quot; class=&quot;headerlink&quot; title=&quot;01、原理&quot;&gt;&lt;/a&gt;01、原理&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Fork&lt;/code&gt;：把一个复杂任务进行分拆，大事化小&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Jo
      
    
    </summary>
    
    
      <category term="Java" scheme="http://lsqstudy.top/categories/Java/"/>
    
      <category term="JUC" scheme="http://lsqstudy.top/categories/Java/JUC/"/>
    
    
      <category term="JUC" scheme="http://lsqstudy.top/tags/JUC/"/>
    
  </entry>
  
</feed>
